# Archived OMX Session Handoff

This document is retained only to preserve the name of a former session handoff.
It is not a recovery entrypoint and contains no executable workflow.

For every new AgentMo session, start with:

1. `AGENTS.md`
2. `docs/SUPERPOWERS_WORKFLOW.md`
3. `docs/CURRENT_STATUS.md`
4. `release/README.md` and the relevant dated release record

Use current source and tests to verify behavior. Historical project facts,
evidence summaries, and non-certification boundaries are retained in `release/`;
the removed legacy planning workspace is not an operational source.
