# 2026.08.14 — Discover Agent Idea Candidate proposal contract

## 背景

2026.08.12 的权威架构明确 Discover 应从证据提炼 Agent Idea 并交给人类确认。
第一个代码垂直切片需要让提议内容可校验、可发现、可精确绑定证据，同时不能让
普通调用方在同一内容文件中自声明人类批准或进入 Plan 的权限。

## 范围

- 新增 durable subject `agent-idea-candidate` 与 schema identity
  `agentmo.agent-idea-candidate.v1`。
- 新增 closed JSON Schema、生产 validator、validator-valid minimal template、artifact
  registry/admission 接线与 exact Discovery DB companion binding。
- 新增只读 CLI `agentmo agent-idea-candidate-report`，支持 bounded human/JSON
  diagnostics、evidence-kind/trust composition 与不足警告。
- 将生产模块纳入 npm files、Builder release/tarball inventory、`npm run check` syntax
  closure 与 Node 20 owned syntax batch。
- 新增 Stage 2 回归，证明 design-plan、user-need 与 approval 路径不把 Candidate
  接受为权威。

## 架构决策

- 内容与授权分离。Candidate 不包含 `humanDecision`、approval state、enter-Plan
  permission、build/runtime authority 或 organization-authentication claims。
- `source.discoveryDb` 精确记录 identity、subject 与 digest；CLI 同时 exact-admit
  Candidate 和唯一 Discovery DB companion。
- 每个 `evidenceIds` 项必须在 exact admitted DB 中解析为唯一 fact。缺失、重复或
  歧义 ID fail closed；schema 不要求某个领域专属 fact kind。
- `extraction_field` 只能是 planning lead；报告必须警告它不证明 user need、value、
  capability、domain quality 或 Plan readiness。
- 固定 `certificationBoundary` 仅允许 `proposalOnly:true`，其余需求、价值、能力、
  质量、Plan/production readiness 与 enter-Plan/build/runtime authority 均为 false。
- 未来 Decision Artifact 必须独立设计并 exact-bind Candidate digest；本轮不实现。

## 代码与文档入口

- `src/agent-idea-candidate.js`
- `src/artifact-contract.js`
- `src/artifact-registry.js`
- `src/artifact-admission.js`
- `src/artifact-subjects.js`
- `src/cli.js`
- `docs/superpowers/specs/2026-08-14-agent-idea-candidate-contract-design.md`
- `docs/superpowers/plans/2026-08-14-agent-idea-candidate-contract.md`
- `README.md`
- `docs/STAGE_CONTRACTS.md`
- `docs/MVP_RUNBOOK.md`

## 验证证据

已完成的聚焦验证：

```text
node --test test/agent-idea-candidate.test.js test/artifact-contract.test.js test/artifact-subjects.test.js
# 18 pass, 0 fail

node --test test/agent-idea-candidate.test.js test/artifact-contract.test.js test/artifact-admission.test.js test/artifact-subjects.test.js test/artifact-surface-coverage.test.js test/node20-core-runner.test.js
# 66 pass, 0 fail, 1 skip

node --test test/design-plan.test.js
# 16 pass, 0 fail

node --test test/design-plan.test.js test/user-need.test.js test/blueprint-draft.test.js test/stage-contracts.test.js test/discovery-source-workspace.test.js
# 68 pass, 0 fail
```

独立 review 后补强：无 exact DB context 的公开 report API 现在 fail closed；未知或
超长 fact `kind` / `trustLevel` 只进入固定 `other` / `unknown` 分类并使用 Map 计数，
不回显值或受原型键污染；Stage 2 approval 回归提供真实 companion admissions，CLI
同时拒绝 Candidate 作为额外 design-plan digest subject。公共 schema 通过
`x-agentmo-byte-sorted-unique:true` 明示 `evidenceIds` 的 UTF-8 byte-order 约束。

第二轮 independent review remediation 进一步关闭以下边界：

- exact-byte admission 在 `JSON.parse` 前中央拒绝所有对象深度的重复 member name，
  包括 escaped-equivalent key；原有 duplicate identity/provenance 固定 reason 保持兼容。
- Candidate 与被引用 Discovery DB 的未配对 Unicode surrogate 在 parse/UTF-8 排序前
  fail closed；公共 Schema 与 production validator 统一以 Unicode code point 计算长度，
  并共同拒绝 whitespace-only、NUL 与无效 surrogate 字符串。
- Candidate validator/report 最多返回 32 条固定、value-blind issues；超出 schema
  上限的数组不再逐项诊断。shape-invalid identity/ideaId 不进入 summary，且只有完整
  验证通过的 Candidate 才产生 evidence composition 或 extraction warning。
- 重复 `--discovery-db` 无论同值或异值都在路径解析/读取前固定拒绝，不产生 partial
  artifact 或文件写入。
- Stage 2 回归将 Candidate 分别替换为 user-need、discovery-approval 与 decision-ledger
  current-head 输入，并提供其余 authentic companions；三项分别到达真实 loader 的
  `AGENTMO_UNSUPPORTED_ARTIFACT`、`AGENTMO_UNSUPPORTED_ARTIFACT` 与
  `AGENTMO_DECISION_LEDGER_LINEAGE_INVALID`。额外 Candidate digest 仍单独命中
  `AGENTMO_ARTIFACT_DIGEST_UNKNOWN_SUBJECT`。

本轮可完成的验证结果：

```text
node --test test/agent-idea-candidate.test.js test/artifact-admission.test.js test/artifact-contract.test.js test/artifact-subjects.test.js test/artifact-surface-coverage.test.js test/node20-core-runner.test.js
# 73 pass, 0 fail, 1 skip；exit 0

node --test test/design-plan.test.js test/user-need.test.js test/blueprint-draft.test.js test/stage-contracts.test.js test/discovery-source-workspace.test.js test/agent-idea-candidate.test.js
# 85 pass, 0 fail；exit 0

node --test test/artifact-migration.test.js test/control-snapshot.test.js
# 64 pass, 0 fail；exit 0

node --check src/agent-idea-candidate.js
node --check src/artifact-admission.js
node --check src/artifact-contract.js
node --check src/artifact-registry.js
node --check src/cli.js
# 五项均 exit 0
```

Node 20 post-publication consumer 仍 fail closed，而不是被测试绕过：当前 owned manifest
要求 syntax 41/41、command-set digest
`4917e7651518888a67133f36c59e5c7c9fd9f0c5da4f2fd72e4f1acb780d0dc9`；历史
[`2026.07.13-node20-core-receipt.json`](./evidence/2026.07.13-node20-core-receipt.json)
保持原字节与 SHA-256
`c06631d9ccb43ebb2b5cbf85a4f20cccc65421148d051cdb238fc96a1f1559bf`，只绑定 syntax
40/40 和旧 command-set digest。仓库与本 Session 获准的临时根中没有 canonical
Node 20.20.2 executable、受信 archive 与 exact checksum manifest，因此不能运行
可信 producer，也没有创建或发布新的 receipt。精确 consumer 结果为 5 pass、1 fail、
exit 1，失败码 `AGENTMO_NODE20_LANE_RECEIPT_INVALID`。历史 receipt 未覆盖、未改写，
也未以 caller/env marker 替代 repo-owned trust anchor。

完整 `npm run check` 共发生两次尝试，聚合状态为 **PARTIAL / aggregate not green**：

1. 第一次尝试完成 syntax closure 后，在 `check:test:main` 的 single-flight gate 命中
   `AGENTMO_CHECK_ALREADY_RUNNING` 并退出 1。后续确认该 worktree 的精确临时锁为零字节、
   无 `lsof` 持有者且没有对应活跃检查进程；该次不记为 pass。
2. 只删除上述精确 stale temporary lock 后，第二次也是最后一次尝试进入主测试。Candidate、
   artifact admission/contract/surface 等前置套件通过，但随后出现既有 Builder/Codex/
   OpenClaw lifecycle 与 Darwin platform failures。命令运行 `01:02:28` 后仍停留在同一
   `test/builder-marketplace-projection-transaction.test.js` 子进程，该子进程已运行
   `01:00:07` 且长期无新 TAP。按总控规定的 60 分钟硬上限，向本 Session 启动的
   `check-single-flight` 发送一次 `SIGTERM`；转发/finally 后顶层 `npm run check`
   以 exit 1 结束并明确输出 `Interrupted while running`。未产生完整 TAP aggregate，
   因此该次状态为 **TIMEOUT / INTERRUPTED**，不能记为 pass。退出后精确临时锁不存在，
   worktree 保持 clean。

第二次尝试观察到的第一项真实失败是 Codex host additive-only contract 的
`deactivation preserves project bytes, host registrations, owner evidence, and consumer evidence`；
后续还观察到 prior-attempt continuation、Builder installed-hook、append-only lifecycle、
marketplace projection、packed Builder 与 OpenClaw platform 等既有范围失败。本轮不重跑
完整检查；后续 full-check 策略由总控另行决定。

第三轮 independent review remediation 收窄并补齐以下代码边界：

- migration reason contract 现在承认中央 `duplicate_member`，使 planner、plan validator、
  non-applicable receipt、CLI preview/apply 自洽且 value-blind；普通与 escaped-equivalent
  重复 member 均 fail closed，不创建 partial output。
- Candidate validator/report/formatter 在公共边界只快照 own enumerable data properties；
  accessor、Proxy、稀疏/覆写数组与异常对象固定拒绝且不触发 getter。该轮 formatter 的
  cross-field normalization 限制 diagnostics 总量并隐藏未知值，但第四轮 review 证明普通
  JSON 仍能伪造满足这些字段检查的 pass report，因此这一边界当时尚未完全闭合。
- 未配对 surrogate 的 admission policy 恢复到授权的窄范围：Candidate 与 exact referenced
  Discovery DB 继续 scalar-only；其他既有 artifact family 保持 writer→validator→admission
  round-trip。中央 duplicate-member 拒绝仍覆盖所有 artifact family。

第三轮修复后的聚焦门禁：

```text
node --test test/agent-idea-candidate.test.js test/artifact-migration.test.js test/user-need.test.js
# 82 pass, 0 fail；exit 0

node --test test/agent-idea-candidate.test.js test/artifact-admission.test.js test/artifact-contract.test.js test/artifact-subjects.test.js test/artifact-surface-coverage.test.js
# 65 pass, 0 fail, 1 skip；exit 0

node --test test/artifact-migration.test.js test/migration-filesystem.test.js test/control-snapshot.test.js
# 72 pass, 0 fail；exit 0

node --test test/design-plan.test.js test/user-need.test.js test/blueprint-draft.test.js test/stage-contracts.test.js test/discovery-source-workspace.test.js test/agent-idea-candidate.test.js
# 88 pass, 0 fail；exit 0

node --test --test-name-pattern='keeps duplicate and escaped-surrogate migration preview/apply behavior self-consistent' test/cli.test.js
# 1 pass, 0 fail；exit 0
```

第四轮只修复剩余 Candidate public API 边界；Migration 与 Unicode scope 保持第三轮已关闭
状态且未改动：

- formatter 不再尝试验证普通 report JSON。`buildAgentIdeaCandidateReport` 将原始返回对象
  绑定到 module-private `WeakMap` render state；只有该 exact object 能使用 builder 完成
  Candidate+DB validation 后保存的状态。JSON round-trip、clone、reparse、synthetic
  identity、错误 composition、revoked Proxy 与其他非品牌输入统一输出固定 fail；品牌不进入
  JSON、artifact 或公开字段。CLI JSON shape 不变，human path 明确格式化 builder 原对象。
- public snapshot 对 Candidate 与 DB context 分别实施 aggregate string/code-unit budget；
  Candidate budget 由公开 Schema 各字段的最大 code-point 数及 UTF-16 最坏 2× 上界推导，
  并保留 canonical keys/provenance 余量。字段 validation 在 `trim`、surrogate scan 与 UTF-8
  ordering 前先检查安全 code-unit 上限，以 bounded index loop 计数 code points，不再展开
  `[...value]`。
- 所有来自 public Candidate/context 的数组在 snapshot 时必须是 dense own data indices；
  后续 validation、evidence resolution、composition 计数与 byte-order 检查均使用 index-based
  traversal，不调用可被继承污染的 `entries`、`some`、`every`、`map` 或 `forEach`。

第四轮聚焦证据：

```text
node --test test/agent-idea-candidate.test.js
# 20 pass, 0 fail；exit 0

node --test test/agent-idea-candidate.test.js test/artifact-admission.test.js test/artifact-contract.test.js test/artifact-subjects.test.js test/artifact-surface-coverage.test.js
# 68 pass, 0 fail, 1 skip；exit 0

node --test test/design-plan.test.js test/user-need.test.js test/blueprint-draft.test.js test/stage-contracts.test.js test/discovery-source-workspace.test.js test/agent-idea-candidate.test.js
# 91 pass, 0 fail；exit 0

node --check src/agent-idea-candidate.js
node --check src/cli.js
node --check test/agent-idea-candidate.test.js
# 三项均 exit 0
```

第四轮没有重跑完整 `npm run check`，也没有修改 runtime/Node 20 evidence；aggregate 状态仍为
**PARTIAL / aggregate not green**。

第五轮 review 只关闭同进程原生 prototype mutation 对 Candidate 私有状态与 formatter 的
影响；第四轮的品牌语义、string budgets、外部 dense-own arrays，以及已关闭的 Migration /
Unicode scope 均保持不变：

- 模块初始化时捕获并绑定 `WeakMap.prototype.get` / `set` intrinsics。品牌 lookup 与登记不再
  通过运行时可写 prototype 动态派发；伪 get state、throwing get、observing/replacing set
  均不能伪造、泄露或取得 private map。内部异常统一退化为固定、value-blind、bounded fail。
- evidence-kind / trust composition 使用 own data descriptor 写入内部数组，低位
  `Array.prototype` accessor 不再截获元素。formatter 通过 own-index append 与有界手工
  newline composition 生成文本，不再调用继承的 `push` 或 `join`；任何内部异常返回同一固定
  failure 文本。

第五轮聚焦证据：

```text
node --test test/agent-idea-candidate.test.js
# 22 pass, 0 fail；exit 0

node --test test/agent-idea-candidate.test.js test/artifact-admission.test.js test/artifact-contract.test.js test/artifact-subjects.test.js test/artifact-surface-coverage.test.js
# 70 pass, 0 fail, 1 skip；exit 0

node --test test/design-plan.test.js test/user-need.test.js test/blueprint-draft.test.js test/stage-contracts.test.js test/discovery-source-workspace.test.js test/agent-idea-candidate.test.js
# 93 pass, 0 fail；exit 0

node --check src/agent-idea-candidate.js
node --check test/agent-idea-candidate.test.js
# 两项均 exit 0
```

第五轮没有运行完整 `npm run check` 或 runtime consumer，也没有修改 Node 20 evidence；
Node 20 blocker 继续 OPEN，aggregate 继续为 **PARTIAL / aggregate not green**。

本轮不修改 Node 20 evidence；blocker 保持 OPEN：owned manifest syntax 41，而历史 receipt
只证明 syntax 40，consumer 仍应为 5 pass / 1 fail、exit 1，失败码
`AGENTMO_NODE20_LANE_RECEIPT_INVALID`。

开工前的较小基线集合已独立复现 4 个无关失败：一个 OpenClaw safe-fs 期望码漂移，
以及三个 `docs/CURRENT_STATUS.md` / archived `docs/OMX_SESSION_MIGRATION.md` command-doc
期待不一致。本变更后的同一 CLI/command-doc 集合仍为 39 pass、4 fail，失败项相同。
全量 lane 后续更多 Builder/OpenClaw 失败位于本变更未修改的生命周期/平台范围；因
开工前未运行同一全量 lane，不能把这些额外项声称为已证明的基线，仅记录为当前
环境的无关阻塞证据。

## 2026-08-15 Node20 evidence closure

Node20 trusted producer 使用 repo-owned trust anchor 与已独立核验的官方 Node.js
`v20.20.2` arm64 发行输入运行。attempt 1 在 command-set digest
`9bc48f13d8f8a160c8da869fb57d3dd398e56ab115ba9d655ec66fc2abbdab51`
下因长期 stale 的 Stage TAP expected count 以
`AGENTMO_NODE20_LANE_TAP_COUNT_MISMATCH`、exit 1 fail closed；该次没有生成临时
receipt，也没有发布公共 receipt。Stage direct diagnostic 随后确认真实结果为
3 pass、2 skip、0 fail、5 total；额外 skip 是自 `ec2f10d` 已存在但不匹配固定
Stage 1/2/3 handoff pattern 的 Phase 4 non-transitive test，不是运行失败。

修正 manifest authority 后，attempt 2 运行以下 exact producer command 并 exit 0：

```text
npm run check:core:node20 -- --node-bin "$NODE20_BIN" --archive "$NODE20_ARCHIVE" --checksums "$NODE20_CHECKSUMS" --expected-version 20.20.2 --expected-arch arm64 --receipt "$NODE20_RECEIPT"
```

临时 receipt 先通过 `readNode20Receipt`，再逐字节发布为
[`2026.08.14-node20-core-receipt.json`](./evidence/2026.08.14-node20-core-receipt.json)；
临时与公共 bytes 的 `cmp` exit 0，SHA-256 均为
`64fd5deba66e05c94c176934a5472ecdebc15a85ac63d943257d1bc0480be538`，
`observedAt=2026-08-15T10:43:35.437Z`。历史 2026.07.13 receipt 保持原字节，
SHA-256 仍为
`c06631d9ccb43ebb2b5cbf85a4f20cccc65421148d051cdb238fc96a1f1559bf`。

```text
receipt-sha256=64fd5deba66e05c94c176934a5472ecdebc15a85ac63d943257d1bc0480be538
command-set-sha256=455e7d36ab8eb2334e0854977063637cc79bc9b9734fd3c3df2bfa6ea86894e2
checksums-manifest-sha256=c6f74825d6ddf350ef06600c67fec6ea2f7996cf438a78c3cb2a89b29d4320ed
Node 20 main batch PASS — 62/63；syntax PASS — 42/42；Stage 1/2/3 handoff subset PASS — 3/3，另有 2 skip；0 fail。
```

完整 evidenceRef：

```text
receipt=release/evidence/2026.08.14-node20-core-receipt.json; receipt-sha256=64fd5deba66e05c94c176934a5472ecdebc15a85ac63d943257d1bc0480be538; command-set-sha256=455e7d36ab8eb2334e0854977063637cc79bc9b9734fd3c3df2bfa6ea86894e2; archive-sha256=466e05f3477c20dfb723054dfebffe55bc74660ee77f612166fca121dacb65b6; checksums-manifest-sha256=c6f74825d6ddf350ef06600c67fec6ea2f7996cf438a78c3cb2a89b29d4320ed; archive-member-sha256=38de4fc456c0c439bac48c727d378f749abb4e31f4116703bb1ee9a746fccbb6; executable-sha256=38de4fc456c0c439bac48c727d378f749abb4e31f4116703bb1ee9a746fccbb6; batches=syntax:42/0/0/42|core-contracts:62/1/0/63|stage-contracts:3/2/0/5
```

post-publication consumers 使用以下命令验证 exact receipt、compatibility matrix、
release record/index、manifest 隔离、历史 env marker 不可替代 receipt，以及 value-blind
和非认证边界：

```text
node --test test/runtime-evidence-consumers.test.js
# 6 pass, 0 fail；exit 0
```

[Runtime compatibility matrix](../docs/RUNTIME_COMPATIBILITY.md) 保留以下历史
current-host/boundary facts；它们本轮未重跑，也不冒充 attempt 2 证据：current host `v24.18.0` arm64，`npm run check`，以及
`current-host full suite PASS — 448/448 across 47 suites`。完整历史 boundary command 为：

```text
node --test test/runtime-compatibility.test.js test/runtime-compatibility-seams.test.js test/runtime-evidence-consumers.test.js test/cli.test.js test/live-smoke-script.test.js test/command-docs.test.js test/node20-core-runner.test.js test/node20-core-lane.test.js test/artifact-surface-coverage.test.js && bash -n scripts/node20-core-lane.sh scripts/openclaw-live-smoke.sh
```

公开 runtime flag 仍是 collision-free `--runtime-env-file` option reaches AgentMo through real Node and shebang launchers。Node20 receipt 只证明该固定 manifest 的有界机制执行；`target-executed` | provider-backed OpenClaw live execution | official target range | `untested`。本证据 does not certify domain quality, approve production, or certify wider OpenClaw compatibility。

完整 `npm run check` 在本次 closure 后尚未运行；aggregate 状态继续为
**PARTIAL / full check pending**，且没有创建 tag、GitHub Release、npm publication 或
deployment。

## 2026-08-15 aggregate infrastructure repairs

Node20 evidence closure 后的第一次完整 `npm run check` 已由总控中止并记录为
**HUNG / INTERRUPTED，exit 130**，不是 pass。抽样在 current-host Node 24 与受信的官方
Node 20.20.2 上复现了同一 raw-descriptor deadlock：bootstrap verifier 的 fd 4 与 packaged
hook loader 的 fd 3 使用预取式 `ReadStream`；超过 24 MiB 上限后抛错时仍留有 in-flight
read，而 peer 保持打开，因此 destroy 等待不会结束。

修复后的 raw-fd reader 严格串行读取，每次最多 64 KiB，并在读取到 24 MiB + 1 byte 时
立即 fail closed；它不关闭 retained descriptor，也不依赖 peer EOF。对抗 canary 在写端保持
打开的情况下，以同一个 3 秒 deadline 覆盖 MAX+1 写入、拒绝与 child close，并用
`fstatSync` 证明 descriptor 仍有效。精确直测结果为：current-host Node 24 `1 pass / 0 fail`
（exit 0），官方 Node 20.20.2 `1 pass / 0 fail`（exit 0）；两者均为 child 自然 exit 0、
signal null、`descriptorRetained: true`。

除 receipt-owned `artifact-admission` 保持 receipt 生成时的 Linux predicate 外，同一次收口
将其余 OpenClaw native filesystem 测试限定为 Linux/x64。Darwin 上 package
inspect/determinism/OpenClaw package portable fixtures 为 `10 pass / 0 fail`（exit 0）；
target descriptor/admission/install-plan/Phase 3 组合为 `13 pass / 12 skip / 0 fail`
（exit 0）；install evidence 为 `0 pass / 5 skip / 0 fail`，install transaction 为
`13 pass / 8 skip / 0 fail`，组合为 `13 pass / 13 skip / 0 fail`（exit 0）。transaction
比原审计列出的五个 native cases 多三个精确 skip，因为三个 recovery cases 也会真实打开
native safe-fs session，不能在 Darwin 伪装为 portable evidence。artifact admission
为 `21 pass / 1 skip / 0 fail`（exit 0），runtime evidence consumers 为 `6 pass / 0 fail`
（exit 0），command docs 为 `10 pass / 0 fail`（exit 0）。

一次 Builder packed/package-produce/safe-fs 组合聚焦在约 8 分钟后有界中止，exit 1；中止前
safe-fs 的 Darwin negative/retained-fd cases 通过、native suites 诚实 skip，但 packed runtime
inventory 仍报告既有 expected-count drift（actual 116 / expected 106），随后同文件再次长期
无新 TAP。该组合不记为 pass，也不据此修改生产 safe-fs、Node20 manifest、digest 或 receipt。

后续有界诊断确认 `106 total / 101 runtime` 是 commit `40b1331` 时代的旧测试常量；生产
inventory 没有误收 helper 或 scanner。此后合法增加了 Candidate CLI/contract 与 POC runtime
共十个 `src/` assets，模块当前精确结果为 `116 total / 111 runtime`。测试现逐项约束这十个
source paths 均以 `runtime` kind 出现，而不是只追随总数。指定 exact name-pattern：
`admits one deterministic fixed runtime inventory and its complete packed import closure` 的结果为
`1 pass / 0 skip / 0 fail`，exit 0。生产 inventory、防护、package allowlist、Node20 manifest、
digest 与 receipt 均未因该测试合同修正而改变。

完整检查随后触达 `builder-shipped-test-control` 的 shipped-vs-dev authority 漂移：旧测试递归
扫描整个仓库 `scripts/`，因本地 `check-single-flight.js` 的固定 main-lane 编排而报告
`scripts/check-single-flight.js:AGENTMO_TEST`（RED：`0 pass / 1 fail`，exit 1）。修正后，测试
只从生产导出的 `BUILDER_NPM_TARBALL_INVENTORY` 取得成员，再限定真实发布 source 前缀
`bin/`、`plugin/`、`scripts/`、`src/` 与既有 source extensions；它明确要求发布 inventory
包含 `scripts/verify-codex-uat-candidate.js` 且不包含 dev-only
`scripts/check-single-flight.js`。所有真实 shipped executable/source bytes 继续逐字接受原 marker
禁令。GREEN 为 `1 pass / 0 skip / 0 fail`，exit 0；编排器、生产 inventory、package allowlist、
marker 集与 `package.json` 均未修改。

宿主环境的 full-check attempt 2 随后把首个真实失败定位到
`test/builder-codex-host.test.js` 的 escaped stdout sibling 计时；总控以 Ctrl-C 结束该次
aggregate 后出现的 cancelled tests 不计为失败。两个用例原先从 `observeCodexHost()` 启动即
计算 4 秒 settle 与 `<2.5s` elapsed，但 fake direct parent 在退出前仍需启动第二个 Node
grandchild、写 ready/pid 并轮询，因此前置 fixture 开销被错误记为 “after direct parent exits”。
production 的 finish→shutdown→stream-destroy→500ms fallback 未改。

测试现在先启动 observation promise，再有界等待 fake parent 通过自身 `process.on("exit")`
同步写出的 test-only parent-exit marker；`startedAt` 与 settle deadline 均从该 marker 后开始。
fixture 尚未写 marker 时的 RED 为 `0 pass / 2 fail`、exit 1，两个失败均为固定 marker timeout。
加入 command/app-server 两个 parent lifecycle marker 后，两条 exact pattern 分别为 `1/1 pass`
（exit 0）；合并 pattern 连续三轮均为 `2 pass / 0 fail`（每轮 exit 0）。4 秒 settle、
`<2.5s` post-exit assertion、value-blind canary、escaped grandchild alive proof 与 finally cleanup
均保持不变。后续独立复审确认同一 fixture 的 parent-exit 前置阶段曾接近 4.99 秒，因此仅把
marker 出现前的等待从 4 秒提高到 10 秒；marker 后的 4 秒 settle 与 `<2.5s` 性能边界不变。
当前两条 exact pattern 合并验证为 `2 pass / 0 fail`、exit 0；`post-exit-json` case 和生产
timeout 未修改。

full-check attempt 2 暴露的另一个测试同步问题是 Builder Doctor 的 same-byte inode swap race：
旧测试通过 `utimes`、`atimeNs` 变化和 10 秒轮询推断 retained read 已发生，既依赖文件系统
计时语义，也不能证明 swap 位于真实 read 与 production binding check 之间。RED 移除这些
推断后，parent 等待新的 `read-boundary`，而旧 helper 不发送该消息，精确结果为
`0 pass / 1 fail`、exit 1。

GREEN helper 在动态导入 doctor 前，以 test-only `FileHandle` prototype wrapper 截获精确目标
inode 的首次真实 read；只有 `originalRead` 返回 `bytesRead > 0` 且 retained dev/ino 匹配后，
才向 parent 发送 portable relativePath 与十进制 identity，并等待两次 rename、byte/inode
检查完成后的 `swap-complete` ACK。finally 恢复原 prototype descriptor。read-boundary fail-fast
deadline 从 2 秒调整为 15 秒，post-swap terminal 从 10 秒调整为 30 秒，ready 仍为 2 秒，
整项测试以 60 秒为有界上限；读取事实仍只由 IPC 与 retained identity 证明。

cleanup helper 逐项尝试 child close/kill、replacement restore 与 original restore，捕获首个
cleanup error 但不跳过后续步骤；主体与 cleanup 同时失败时，AggregateError 以主体错误为
cause。轻量 canary 同时模拟 close 与第一 restore 失败，并证明最后 restore 仍执行。authority
链的一层或多层 revalidation 最终使整体 diagnosis 为 `inconsistent`、receipt 为 `corrupt`、
authority-bearing install marker 为 `unsafe`；本记录不把结果归因于某一个具体 check。同字节
replacement 的机械内容状态不被误写成 authority 证明。本轮 canary + race 聚焦为
`2 pass / 0 fail`、exit 0，其中精确 race 为 `1/1 pass`。新增 canary 前完整
`test/builder-doctor.test.js` 曾为 `18 pass / 0 fail`、exit 0；本轮按范围约束没有重跑新增
canary 后的完整文件。production doctor/API/env 与 Node20 receipt 均未修改。

完整 `npm run check` 本轮没有重跑，aggregate 仍为 **PARTIAL / full check pending**。

full-check attempt 3 随后在
`test/builder-marketplace-projection-transaction.test.js` 的
`retained final authority rejects a pathname substitution` 永久等待，并由总控人工中止；该次记录为
**HUNG / INTERRUPTED**，不是产品功能失败。现场 inode/临时目录证据显示 child 仍持有原始
`file.txt`，`file.txt.retained` 尚不存在，因此不能把这次实况归因于已证明的 `proceed` 消息丢失。
可确认的问题是：首阶段等待或随后断言异常时测试没有 `finally`，会遗留等待 `proceed` 的 child；
此外旧协议在 child 的 retained→proceed 与 parent 的 proceed→asserted 两个方向都存在
listener-after-send 的 lost-message 窗口。

测试协议现改为双方先注册再发送：child 在公布 retained 前建立 proceed listener，并只接受精确
`{proceed:true}`；parent 在 child 可能发送消息前建立单一 retained/asserted 路由，并在发送
proceed 前持有 asserted promise。retained、asserted、自然 exit、单项总时限与 SIGTERM→SIGKILL
cleanup 均有界；主体失败后仍独立尝试 child 回收、路由拆除和临时目录删除。旧协议加上有界回收
后的 RED 为 `0 pass / 1 fail`、exit 1，约 10.09 秒，固定停在 retained timeout；修正后的同一
exact pattern 为 `1 pass / 0 fail`、exit 0，约 10.02 秒。pathname substitution、retained
authority、symlink/digest 的生产语义和断言没有放宽。

attempt 3 被中止时仍有 CPU/子进程进展的 packed-install worker，以及 Ctrl-C 后产生的
cancelled/interrupted tests，均不记为新 finding。完整 `npm run check` 仍待后续明确运行，
aggregate 继续为 **PARTIAL / full check pending**。

第二轮独立复审又确认了两个 test-protocol 边界，但不改变上述现场因果：旧 router 没有托管
ChildProcess `error`，只等待可能永不出现的 `exit`，且 proceed send 没有 callback；同时它只按
`status` resolve，未知、畸形、重复和越序消息会沉默等待或被接受。轻量 EventEmitter canary 的
RED 为 `0 pass / 1 fail`、exit 1，约 0.16 秒；asserted-before-retained 被旧 router 忽略，最终
只得到 canary timeout。

修正后的 test-only protocol 在 spawn 后第一同步步骤统一注册 `message`、`error`、`exit`、`close`，以单一
retained→asserted 状态机 buffer 合法即时消息，并让越序、unknown/malformed、duplicate、child
error 与 send callback error 共享同一个固定 protocol failure。protocol stage/send/terminal
deferred 只 resolve tagged result；cleanup 专用的 raw close signal 不进入验收，避免未 await
waiter 的早期 unhandled rejection。asserted waiter 在 callback-confirmed proceed send 前建立。
cleanup 后四个 listener 均为零。最终 canary 为 `1 pass / 0 fail`、exit 0，
约 0.07 秒；真实 pathname-substitution exact pattern 为 `1 pass / 0 fail`、exit 0，约 9.13 秒，
并继续断言 child `code=0`、`signal=null`。完整 check 未在此轮运行，状态仍是
**PARTIAL / full check pending**。

第三轮复审进一步覆盖了三个同一协议内的竞态。旧 `sendProceed` 的 mutable deferred 会被第二次
调用覆盖，使首次 waiter 永久 pending；旧 terminal result 可能在较晚 duplicate/error 后仍以裸
`{code,signal}` 通过；真实测试还会先创建 rejecting asserted promise，再单独等待 send，从而在
send failure 时留下无人持有的 rejection。新增 canary 的 RED 精确命中首次 duplicate-send waiter
timeout：`0 pass / 1 fail`、exit 1，约 0.16 秒。

最终 test-only router 将 proceed send 固定为 single-use，并在任何 local deferred 创建前检查
invocation/state；callback 与 throw 只解析本次 local，重复调用使既有和新增 send waiter 接收同一
固定 failure。最终 terminal 验收是 tagged health result，cleanup 另用不参与验收的 raw close
signal；asserted 成功后出现的延迟 duplicate 或 child error 会使随后 exit/close 验收失败。真实
路径同时等待 tagged send/asserted signals，再统一解包，因此 callback failure 从创建起就没有裸
rejecting asserted promise。最终合并 canary + pathname-substitution exact pattern 为
`2 pass / 0 fail`、exit 0，约 9.09 秒；其中 canary 约 0.003 秒、真实 child 约 9.03 秒。
完整 check 仍未运行，aggregate 继续为 **PARTIAL / full check pending**。

后续唯一一次完整 check 的 main lane 为 `1040 tests / 987 pass / 53 skip / 0 fail`，
packed-hook-chain 为 `1 pass / 0 fail`；packed-behavior 正常退出但为
`8 tests / 6 pass / 2 fail`、exit 1。两个失败都发生在成功调用 Builder behavior-eval 的公开
CLI 包装处，并固定呈现 `AGENTMO_CLI_REQUEST_REJECTED`。对保留隔离项目直接调用相同打包模块
后，内部根因为 `prepareAuthenticatedHookHarness` 写入
`examples/ai-frontier-poc.seed.json` 时收到 `ENOENT`：release asset loop 会在 containment
检查后递归创建 0700 parent，metadata loop 则直接 `writeFile`。当前 metadata inventory 新含两个
嵌套 `examples/*.json`，因此不是 parser、进程挂起或公开错误边界漂移。

最小修复只在 metadata containment 检查之后、`writeFile` 之前加入与 asset loop 相同的递归
0700 parent materialization；metadata inventory、examples、公开错误和安全边界均未改变。
syntax 与 pre-focused `git diff --check` 均 exit 0。合并两个失败 name-pattern 的首次聚焦执行已
自然结束，但执行工具没有保留可读取的 TAP/exit，因此不能记为 pass；一次误启动的重复执行被
有界中止并明确作废（exit 1、`cancelled 1`），也不作为验证证据。总控接管的唯一权威 focused
session 随后以 `2 tests / 2 pass / 0 fail / 0 cancelled / 0 skipped`、exit 0 完成，
`duration_ms=527259.375375`；其中 fixed-scenario 为 `182443.09825ms`，immutable-successor 为
`248834.379125ms`。该结果只关闭两条 packed-behavior 回归；完整 check 尚未重跑，aggregate
仍为 **PARTIAL / full check pending**，不能写为 full green。

下一次 host full attempt 的 main lane 仍为 `1040 tests / 987 pass / 53 skip / 0 fail`；
packed-hook-chain 在真实 authenticated hook 成功链的 PostCompact 首次执行处返回 code 1、stderr
为空，packed-behavior 因链式停止没有运行，aggregate exit 1。journal 已完整提交到
seq 5；测试的 35 秒外层 deadline 先到期约 4–5 秒后，detached launcher 才继续提交 seq 6
`post-compact/hook-prepared` 与 seq 7 `hook-finalized`。同一失败树的 PostCompact replay 随后
以 exit 0、约 6.438 秒完成 noop；成功树同阶段约为 6.771 秒与 7.786 秒。fixture bytes、claims、
lock 与 journal 完整性均未显示产品 admission 漂移。该证据表明 test-only 35 秒 watchdog 反向
短于产品 hook 的 60 秒 child timeout（另有 1 秒 settlement grace），而不是授权放宽产品 timeout。

收口将真实 authenticated packed-hook 成功链的外层 watchdog 固定为命名的 90 秒，仅覆盖这类
成功合同；unknown event、三个 hostile cwd 与 missing receipt 明确使用 35 秒 fast-rejection
options，其他原有快速拒绝与攻击 probes 的 2/15/35 秒边界保持不变。`runNode` 的失败诊断只报告
固定 termination 枚举、code、signal、killed、elapsedMs 与 stdout/stderr byte counts，不输出
input、路径或原始 stdout/stderr，也不 retry；任意非-killed、非整数 exit-code 的 launch error
均固定分类为 `launch-failed`。产品 child timeout 为 60 秒，settlement grace 为 1 秒，behavior
与 packed 外层均为 90 秒，因此留给前置 admission 的精确余量是 29 秒，不写成 30 秒。

另一个真实生产风险是：外层收到 SIGTERM/SIGINT 后，detached launcher group 可能继续提交。
`runAdjacentLauncher` 现在在 detached child spawn 前安装分别绑定原始 SIGTERM/SIGINT 的临时
handlers；若 signal 赢在 child 赋值前则先记录，spawn 返回后立即执行同一取消路径。取消会清理
timer/listeners、终止真实 detached group、销毁 pipes，再向自身 re-raise 同一原始 signal；若
re-raise 失败则固定 exit 1。success、nonzero exit、timeout、同步/异步 spawn failure、close 与
cancellation 的 settlement 都移除 listeners。产品 60 秒 timeout、admission、digest gate 与
no-retry 边界均未放宽。

EventEmitter canary 证明 spawn 内即时 signal 不丢失、pipes/group cleanup 被调用且所有 listeners
归零，但不单独冒充真实 OS group 证据。两个隔离 wrapper 分别向实际 launcher ready 后发送
SIGTERM 与 SIGINT：wrapper 对外保留原 signal，唯一真实 detached group member 消亡，850 ms
观察窗内 late-effect marker 未出现，而 signal 前已写入的 commit marker 保留。该证据只证明
ready 后的取消阻止尚未发生的 late effect；不证明回滚已经赢得竞态的 durable commit。
commit-wins 仍由 immutable journal 与 idempotent replay reconciliation 处理。

最终有效 RED 为 `0 pass / 3 fail`、exit 1，`duration_ms=215.318542`：timeout split 缺失，
SIGTERM/SIGINT 均被折叠为 code 1 / signal null。GREEN supervisor 为 `7 pass / 0 fail`、exit 0，
`duration_ms=3558.593583`。固定 `runAdjacentLauncher` function digest 更新为
`sha256:894e98d0747e23186caabb72f0a755089b303da7ffe4f07cd984f11ec22ed0c2`；精确 hook I/O
inventory 与更严格的 pre-spawn handler order authority 只同步实际字节/行号。package closure
为 `1 pass / 0 fail`、exit 0，artifact surface exact items 为 `2 pass / 0 fail`、exit 0。
此前 `duration_ms=163173.239958` 的 focused 1/1 绑定上一版 hook bytes；本轮 production bytes
变化后的唯一新 focused lane 为 `1 pass / 0 fail / 0 cancelled / 0 skipped`、exit 0，
`duration_ms=162579.380833`，且没有 retry。完整 full rerun 仍待执行，aggregate 继续为
**PARTIAL / full check pending**。

最新一次权威 full check 的 main lane 自然结束为
`1044 tests / 986 pass / 5 fail / 53 skip / 0 cancelled`、exit 1，约 28.4 分钟；
syntax 已通过，但 aggregate 的 `&&` 在 main 失败后没有运行 packed-hook-chain 或
packed-behavior。五项 finding 均定位到 test fixture、测试预算或开发编排，没有发现生产
authority、admission 或 runtime 语义回归：

1. short-lived escaped command fixture 原用 25 ms 猜测 direct parent 已退出，高负载时 late
   JSON 可能先于 supervisor 的 direct-child exit callback。test-only grandchild 现在持有 direct
   parent PID，在 2 秒 fail-closed deadline 内轮询到 `ESRCH`，写入 parent-reaped marker，再经短
   settlement grace 尝试 late JSON。observation 在 marker 前启动，`<2.5s` 结算只从已确认 reap
   后开始；availability、registration、两个 sourceMatch 与 value-blind 断言保持。新增 marker
   的 RED 为 `0/1`、exit 1；GREEN exact 为 `1/1`、exit 0、`duration_ms=1688.676`。首次完整
   host 聚焦因仍从 observation 启动计时而为 `20/21`；修正计时边界后完整文件为
   `21 pass / 0 fail`、exit 0、`duration_ms=131430.771583`。
2. 86-member projection 已完整发布 86 members 与 16 authority records，但并发 main 的墙钟为
   95.811 秒，超过仍应保留的 `<90_000ms` 性能门禁。只有这个精确 test 在
   `AGENTMO_TEST_LANE=main` 时 skip；默认 direct 仍执行。新增
   `check:test:durable-fs` 使用 exact name pattern 与 `--test-concurrency=1`，aggregate 顺序为
   main → durable-fs → packed-hook-chain → packed-behavior。隔离性能结果为 `1/1`、exit 0，
   test `33648.307792ms`（总 `duration_ms=33704.286291`），没有提高 90 秒阈值。
3. retained pathname-swap test 的外层 60 秒与 retained 20 秒预算短于最多三个顺序 15 秒
   POSIX effects 的合法内层预算。只把 test 总上限改为 90 秒、retained phase 改为 60 秒；
   asserted 10 秒、exit 5 秒与 cleanup 2+2 秒保持。exact 为 `1/1`、exit 0，
   `duration_ms=9018.87325`；生产 timeout、IPC 状态机与 authority 断言未改。
4. external producer crash test 原先看见 scratch 即保存路径，30 秒 deadline 后即使 baseline
   尚未成为 regular file 也会 SIGKILL。测试现在只有在 baseline regular file 且 successor
   `ENOENT` 的 exact phase-ready 后才允许语义性 kill；所有路径用 bounded finally terminate/reap，
   成功时清理临时目录，失败时保留取证。原 30 秒 synchronization safety deadline 无需放宽，
   它不是产品 SLA。phase-ready 实现的前三次样本均在 ready 后 kill 并仅保留 baseline：
   `20481ms`、`20290ms`、`20295ms`，每次 `1/1`、exit 0。cleanup 收入显式 finally 后，绑定
   最终测试字节的连续三次 ready elapsed 为 `20308ms`、`20230ms`、`20153ms`，每次仍为
   `1/1`、exit 0。相邻正常 release publication 为 `1/1`、exit 0，
   `duration_ms=47320.704583`。
5. `test/helpers/hook-supervisor-signal-child.js` 原无条件执行 `main()`，会被 Node 默认 test
   discovery 当作测试文件并因缺少 argv 失败。helper 现在只在精确 test-only sentinel
   `--agentmo-hook-supervisor-signal-child` 下运行；无 sentinel 为 zero-effect exit 0，parent 明确
   传入 sentinel。canary RED 为 `0/1`、exit 1；GREEN 为 `1/1`、exit 0。该阶段包含真实
   SIGTERM/SIGINT 的 supervisor 集曾为 `8 pass / 0 fail`、exit 0，
   `duration_ms=3578.08425`；后续增加 PGID 边界 canary 后的最终宿主结果见下文。helper 仍是
   non-shipped test asset。

main-skip canary 对两个重型用例为 `0 pass / 2 skip / 0 fail`、exit 0；默认 direct 与新的
isolated lane 继续实际运行它们。command-docs 精确绑定新 script 字节及 aggregate 顺序。本轮没有
运行完整 `npm run check`、packed-hook 或 packed-behavior；full rerun 仍 pending，aggregate
继续为 **PARTIAL / full check pending**。

后续独立复审收紧了两项纯测试/编排合同。第一，旧 command-docs 仅以 `indexOf` 比较 lane 的
相对位置，不能拒绝 `;`、`|| true`、重复或插入 lane。mutation canary 的 RED 为 `0/1`、
exit 1，固定表现为应拒绝的 mutation 没有抛错。最终门禁按 exact ` && ` 分割 aggregate，
提取所有 `npm run check:test:*` segments，并要求它们恰好一次、连续占据 terminal chain：
main → durable-fs → packed-hook-chain → packed-behavior；不固定前置 syntax commands。
GREEN canary 为 `1/1`、exit 0，`duration_ms=168.800542`。`package.json` 的正确 fail-fast
语义与字节未因本轮复审修改。

第二，external producer test helper 原先只监听 `exit`，因此 spawn `error` 会成为未托管异常，
也没有以 `close` 证明 pipes/child 已收口。lifecycle canary 的 RED 为 `0/1`、exit 1，精确命中
synthetic spawn error。test-only tracker 现在统一记录 tagged error/exit/close、kill purpose 与
single-use kill-attempt 状态：spawn 前 error 会快速完成固定 launch-failed terminal，spawn 后 error
则固定为 lifecycle-failed；正常、nonzero 与 signal 终止均等待 close，error cleanup 也有界等待
close 且不向已记录 terminal 的 child 发信号；phase-ready
在返回前同时复核 tracker 与 ChildProcess terminal state。
语义性 crash injection 与 finally cleanup 分开登记，已发送 kill 时 cleanup 只等待 close，已确认
close 后不再 kill。early exit、early signal、spawn error、cleanup kill 与 crash-injection/cleanup
竞态 canary 为 `1/1`、exit 0，最终 `duration_ms=77.61`。真实 producer-crash exact 保持原 30 秒
safety deadline、baseline-only 与 final out-directory `ENOENT` 断言，ready elapsed `20476ms`，
`1/1`、exit 0，`duration_ms=20565.668791`。本轮仍未运行 full、packed、durable-fs aggregate
或 86-member performance test；full 状态继续 pending。

再后续的 test-only 复审补齐了三个边界；C/D 曾在受限宿主失败，修复后已由真实宿主聚焦验证。
release producer lifecycle
canary 先以 `0/1`、exit 1 精确证明 exit-before-close 时旧 `sendKill()` 仍会发信号；修复后
`sendKill()` 在内部同时检查 error/exit/close 与 ChildProcess terminal fields，并在调用
`child.kill()` 前原子预留 single-use attempt。exit/signal-before-close、kill 返回 false、kill
抛错和 post-spawn error 均由同一个 canary 覆盖，GREEN 为 `1/1`、exit 0、
`duration_ms=67.783333`。

short-lived escaped-command fixture 的 deadline 改为 10 秒 leaf ESRCH、15 秒 outer marker、
4 秒 post-marker settlement 与 30 秒 whole-test，并在 fixture 恢复前有界 drain observation；
层级/drain canary 为 `2/2`、exit 0。受限宿主首次 exact 在 15 秒 outer precondition 截止前
未观察到 parent-reaped marker，结果 `0/1`、exit 1。根因是外层 `FAKE_CODEX_SOURCE` 模板生成
nested child source 时留下未注入的 deadline free identifier，direct parent 因此在 spawn 前
抛错；改为 outer-template literal 注入后，真实宿主 exact 为 `1/1 pass`、exit 0，case 约
`1607ms`、总 `duration_ms=1688.676`。`src/builder-codex-host.js` 没有因此修改。

真实 signal fixture 的 cleanup ownership 已改为负 PGID：异常路径先 SIGTERM wrapper，再按需
升级整个 detached group；确认 group `ESRCH` 后立即释放 ownership，之后不再探测历史 PID/PGID。
轻量 timeout cleanup canary 为 `1/1`、exit 0。受限宿主曾得到 `7 pass / 2 fail`、exit 1；
两条真实 SIGTERM/SIGINT case 均在负 PGID probe 得到 `EPERM`。只读复审确认 Darwin/XNU 在
detached group 已被 SIGKILL、但成员短暂仅剩 zombie 时可能对 `kill(-pgid, 0)` 返回 `EPERM`。
test-only helper 现在把 `EPERM` 仅视为状态未定并继续在原 deadline 内轮询，只有 `ESRCH` 判定
group 消亡，持续 `EPERM` 仍 fail closed；没有修改 production。最终真实宿主 supervisor 为
`10 pass / 0 fail / 0 cancelled / 0 skipped`、exit 0，`duration_ms=3585.502333`。helper 无
sentinel 的 Node test 为 `1/1`、exit 0。完整 full、packed 与 durable lanes 均未重跑，aggregate 仍为
**PARTIAL / full check pending**。

## 最终 aggregate closure（2026-08-16）

本轮唯一最终 `npm run check` 自然 exit 0；precheck、syntax 与四段 fail-fast chain 均完整
执行。`check:test:main` 为 `1051 tests / 107 suites / 996 pass / 0 fail / 0 cancelled /
55 skipped`，`duration_ms=1652197.926875`。`check:test:durable-fs` 为
`2 tests / 2 suites / 2 pass / 0 fail / 0 cancelled / 0 skipped`，
`duration_ms=54916.28675`；其中 86-member 为 `34057.646667ms`，producer crash 为
`20706.853958ms`。`check:test:packed-hook-chain` 为 `1/1 pass`、exit 0，
`duration_ms=162926.913166`。`check:test:packed-behavior` 为 `8/8 pass / 0 fail /
0 cancelled / 0 skipped`、exit 0，`duration_ms=1440888.651583`。full 中 supervisor
仍为 `10/10 pass`，main 中 short-lived host exact 也通过。

这是唯一记为最终 GREEN 的 aggregate rerun；此前 timeout、人工中止、cancelled 或 PARTIAL
attempts 只保留为历史时间线，不算通过。本结果证明有界机制与当前测试集合通过，不证明领域
质量、production readiness 或更广泛 OpenClaw 兼容。当前改动仍未 commit、push、merge、PR、
tag、GitHub Release 或 npm publication。

## 非认证边界

- Candidate 是 proposal，不是 human decision、approval 或 Plan input。
- Candidate 与其 report 不证明用户需求、价值、Agent 能力、领域质量、Plan readiness、
  production readiness、部署或运行时成功。
- exact digest 与 evidence resolution 只证明有界机制绑定，不证明被引用事实为真。
- 本变更不提供 Decision Artifact，不修改 Plan/Produce 权限，不实现自动 Idea 生成，
  不绑定 OpenClaw，也不依赖外部开发工作流或插件。

## 后续风险

- 独立 Decision Artifact 的身份、签发者/authority、Candidate digest exact-binding、
  revision/rejection 语义与进入 Plan 的消费边界仍需单独设计和审批。
- 自动提炼 Idea 的质量评估与人类交互流程尚未实现。
- 当前报告的 composition 是机械统计，不能升级为语义或质量认证。

本记录不声明 push、merge、PR、tag、GitHub Release、npm publication、runtime
installation 或 deployment。
