# 2026.08.10 — 开发协作从 GSD 迁移到 Superpowers

## 背景与范围

AgentMo 将开发协作入口收敛为 Superpowers。此次记录只迁移项目恢复和证据导航；不改动
产品代码、POC runtime、默认 OpenClaw 配置、schedule、delivery 或生产状态，也没有
创建 tag、GitHub Release、npm publication 或 deployment。已创建本地迁移 commit，
但尚未 push。

## 已沉淀的长期事实

- `docs/SUPERPOWERS_WORKFLOW.md` 定义唯一协作方式、新 session 最小阅读顺序、当前
  POC、人工授权边界和 release-first 历史追溯。
- `docs/CURRENT_STATUS.md`、`release/PROJECT_HISTORY.md` 与
  `release/2026.08.06.md` 共同保留当前三阶段目标、白领 Research DB POC 状态、
  Phase 4 Linux gate 与未获授权的 runtime/production 边界。
- Phase 4 的 40-member Agent Package fixture 与 D-42 archive 已以相同 SHA-256 复制到
  `test/fixtures/phase4-agent-package/` 和
  `test/fixtures/phase4-agent-package.d42`，使测试不再依赖旧规划工作区。D-42 archive
  SHA-256 为
  `7726d7b635a972403c598bf53eeb9c44a75c57ffd5c4a573470a066a798b955f`。

## 经确认的清理

在用户明确确认后，已删除仓库 `.planning/`（包括其可重建 research cache）和已盘点的
本机 GSD core、skills、hooks、agents、安装状态与 GSD 辅助脚本；同时移除了 Codex
全局配置中 34 个 GSD agent block。已发布的日期 release、Git history、tag 和 GitHub
Release 不在删除范围。历史 release 保留其当时文件路径与证据表述，不为迁移重写。

## 验证与边界

迁移仅涉及文档、测试 fixture 路径与已确认的 GSD 清理。实际验证记录为：

- `node --test test/artifact-admission.test.js test/artifact-surface-coverage.test.js
  test/canonical-identity.test.js`：47 pass、0 fail、1 skip（exit 0）；其中 artifact
  surface 的负向断言同时保留 `.planning/` 与 `.agentmo/`。
- `git diff --check`：exit 0。
- 完整 `npm run check`：未记录为 PASS。先前的单飞主测试在其 npm 父进程消失后仍由
  孤儿化的测试子树持有 lock，已超过 20 分钟且没有可取得的退出码；在该活锁释放前，
  本次不能开始新的完整检查。

这些验证不运行真实 OpenClaw lifecycle、schedule、delivery、domain 或 production
操作，也不改变任何 AgentMo 产品或认证语义。完整检查的真实 exit 0 仍是本地迁移
commit 的未闭合验证边界。
