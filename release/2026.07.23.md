# 2026.07.23 — Phase 02 Builder hardening closure

## Status

The Phase 02 local release gate is complete: the aggregate repository check passed and a fresh independent final review found zero Critical and zero Warning findings. This is not a real Codex UAT, host-support certification, domain-quality result, production-readiness result, deployment approval, tag, package publication, or GitHub Release.

## Final release-gate addendum

- Closed a real UAT append-capability disclosure: hostile same-realm code that wrapped Object.freeze could previously capture the private token and use the generic journal to append raw UAT bytes.
- Kept the token lexical in src/builder-codex-uat.js, then stripped it at the generic journal boundary before normalization or any frozen record is created.
- Added a packed fresh-child regression that hooks Object.freeze before import and through a legal UAT start, recursively searches every captured object graph, and proves no captured value can append either a raw UAT genesis or a generic successor.
- Preserved the exact I/O inventory after the authority-transport refactor.

## Scope

- Replaced persistent/pathname-loaded hook snapshots with a retained-byte, pipe-fed runtime graph after the trusted host hook seed starts; nested POSIX effects execute verified in-memory child code rather than re-importing a mutable runtime pathname.
- Added bounded hook supervision: normal and failure paths retire the direct process group, destroy local pipes on timeout, and fail closed if a same-group descendant remains.
- Strengthened append-only recovery with mandatory selected-write evidence, prefix-only exact resume, retained inode identity checks, project-root witness binding, and explicit resume-required install recovery.
- Reworked Codex marketplace projection publication into bounded contiguous batches of at most 16 members. Each batch has durable intent and observation, per-member retained/no-follow checks, and final full replay; legacy per-member projection events remain readable.
- Bound projection scanning and file reads, reject foreign/deep/oversized physical trees before admission, and retain member identity through batch observation.

## Key paths and frozen hashes

| Path | SHA-256 |
| --- | --- |
| `src/builder-append-only-authority.js` | `1d95a56ec13e0c96c1fbf6681dcfb879b7e03334471f94d73d6887b640bba11a` |
| `src/builder-codex-host.js` | `be72040a61b094b57481721b131ffbe04ab8f44bef4790f55cdf38c82cbd0dd0` |
| `src/builder-install.js` | `14273f83bc5f24cc1b1cd50698ce0ea4b0edea3467bd9edb83b9d4ba03b654ee` |
| `src/builder-posix-effect.js` | `fe14d8a18b7f4af9c2434f1f30234c32dd12387b10b27ce048063152de8f0102` |
| `src/builder-package.js` | `41b63cb8860136a38e83e39c18a9ac34bcf50e058e5b319d7ad4bae586cf1319` |
| `src/builder-codex-uat.js` | `14b2cddfe00a2878fcfe69bdc6f7f4ef2481bd4fc02a7cfede109ebf6ae454de` |
| `src/builder-immutable-journal.js` | `25658e1c7570cc011bbe45bc519cf6090b937d62059d291581cccc143d38dcb0` |

## Validation evidence

| Command or gate | Result |
| --- | --- |
| `node --test test/builder-append-only-authority.test.js` | 22 pass, 1 platform skip |
| `node --test test/builder-install-security.test.js` | 20/20 pass |
| `node --test test/builder-marketplace-projection-transaction.test.js` | 16/16 pass; 86-member projection completed in 36.8 s |
| activated-receipt focused gate | 1/1 pass |
| `node --test test/builder-package-security.test.js` | 38/38 pass |
| `node --test test/artifact-surface-coverage.test.js` | 17/17 pass |
| independent post-fix security review | Critical 0, Warning 0 |
| `node --test test/builder-codex-uat.test.js` | 27/27 pass |
| `node --test test/builder-immutable-journal-v1.test.js` | 4/4 pass |
| final hostile freeze-hook regression | 1/1 pass; raw genesis and successor rejected |
| final aggregate `npm run check` | exit 0; 760 pass, 0 fail, 1 skip |
| fresh final release review | Critical 0, Warning 0; [02-FINAL-RELEASE-REVIEW.md](../.planning/phases/02-codex-builder/02-FINAL-RELEASE-REVIEW.md) |
| `git diff --check` | pass |

## Boundaries and remaining risk

- The initial `agentmo-hook.js` seed is executed by the host before JavaScript can authenticate it. The host must protect that seed; AgentMo authenticates the runtime graph only after the trusted seed begins.
- A project-local witness prevents scoped authority reset while retained project evidence remains. Complete removal of all local evidence, including that witness, cannot be distinguished from a virgin project without an external authority.
- All evidence above proves bounded mechanisms only. It does not prove a real authenticated Codex session, Codex trust, domain quality, Agent Package quality, production readiness, deployment approval, or wider OpenClaw compatibility.
- The final capability review covers a hostile Object.freeze wrapper that still invokes the original freeze. It does not claim resistance to arbitrary same-realm primordial replacement, debugger access, same-user source modification, or cryptographic module origin.
- No `.env` content was read and no live UAT, network publication, tag, package publication, deployment, commit, or push occurred.
