# 2026.07.30

## Plan 04-08 fresh-process lifecycle authority CLI

AgentMo now exposes archive-only, create-only lifecycle authority production
without adding any install/apply/runtime effect:

- receipt registration remains earlier than the five new durable descriptors:
  verified absent genesis, install plan, ordinary approval, sensitive-action
  decision, and exact-conflict approval;
- `openclaw-install-genesis` binds one exact read-only probe plus an exact
  canonical request and publishes only a verified absent-genesis authority;
- `openclaw-install-preview` accepts one externally digest-bound D-42 archive,
  one exact probe, and exactly one action-specific basis: absent genesis for
  install, current receipt for upgrade/uninstall, or current plus selected
  predecessor receipt/archive for rollback;
- every receipt is admitted from caller-selected bytes under
  `openclaw-install-receipt` with its caller-supplied external SHA-256;
- `openclaw-install-approve` exact-admits one install plan and publishes
  ordinary, each sensitive-action, and the complete exact-conflict authority
  to independent absent files.

Fresh-process tests cover all four lifecycle bases, second-process exact
admission of every output family, stale external digests, missing and
substituted bases, and internal D-42 member/manifest/inventory attacks after
the attacker recomputes the outer digest. The required Wave 8 gate passed
41 tests with zero failures. The bounded adjacent artifact-contract,
command-subject, and CLI gate passed 39 tests with zero failures. Syntax checks
and `git diff --check` passed.

This release evidence proves bounded exact admission, archive revalidation,
proposal generation, and independent approval publication only. It adds no
receipt writer, transaction journal, apply/install/upgrade/rollback/uninstall
effect, target mutation, OpenClaw process, activation, plugin or MCP load,
credential route, runtime evidence, domain-quality claim, Birth/Delivery
claim, production-readiness claim, publication, or deployment.

## Plan 04-09 receipt-last lifecycle transaction mechanism

AgentMo now exposes one `applyOpenClawInstallPlan` mutation seam, a create-only
`agentmo.openclaw-install-receipt.v1` writer, conservative recovery, and the
`openclaw-install-apply` CLI contract:

- apply accepts only explicit file plus external SHA-256 pairs for the target
  carrier admission, deterministic D-42 archive, probe, install plan, ordinary
  approval, each sensitive decision, conditional conflict approval, and the
  lifecycle-specific absent-genesis or predecessor authorities;
- the deterministic archive is retained no-follow and revalidated across the
  outer digest, manifest digest, canonical inventory digest, complete member
  set, path, type, mode, length, member digest, and retained identity before
  the private journal;
- transaction operations re-observe current path, parent identity, digest, and
  exact conflict set before effects, then publish a complete receipt only after
  post-observation;
- recovery initially required four cached facts before cleanup. Plan 04-13
  below supersedes that behavior: a reopened published pathname is always
  preserved and itemized instead of being deleted;
- credential setup remains value-blind and can cross only an injected official
  OpenClaw `shell:false` seam with bounded result metadata. No MCP route exists.

All effect-bearing tests use disposable `mkdtemp()` project roots and fake
OpenClaw seams. No real OpenClaw state, HOME, credential, schedule, plugin,
process, or runtime lifecycle was touched. The exact Wave 9 gate passed 50
tests with zero failures after conservative recovery re-observation and
immutable predecessor-sequence handling were added.

This is bounded Phase 4 mechanism evidence only. It does not prove a live
OpenClaw install, upgrade, rollback, uninstall, credential login, activation,
runtime execution, domain quality, Birth, Delivery, production readiness, or
wider OpenClaw compatibility. Those live proofs remain absent and Phase 5-owned.

## Plan 04-12 trustworthy fresh reprobe gap closure

AgentMo now closes the first reviewed Phase 4 root gap:

- OpenClaw probe validation requires authentic external companion admissions
  for the blueprint, build contract, plan approval, target carrier admission,
  target descriptor, and package manifest; internally consistent probe bytes
  cannot authenticate their own source bindings.
- Probe observations execute only a verified private copy of the OpenClaw
  executable from a private empty cwd with fixed argv, `shell:false`, and a
  synthetic non-secret environment. Source executable or target drift fails
  closed.
- Public apply has no caller adapter or old-probe fallback. It performs a fresh
  production reprobe before authority consumption, journal creation, credential
  routing, or target mutation and compares the complete approved fingerprint.
- Genesis and preview admit the canonical package manifest from the already
  validated D-42 capture rather than rereading a mutable package pathname.
- Public help and `docs/MVP_RUNBOOK.md` expose the complete exact companion
  tuple required by probe, genesis, and lifecycle preview.

Focused package admission/produce/inspect tests passed 37/37; OpenClaw
probe/install-plan/install-transaction/contract tests passed 35/35; public
contract, maintained-command, and static I/O inventory tests passed 30/30.
`git diff --check` passed.

## Plan 04-13 retained-dirfd filesystem kernel

AgentMo now routes its OpenClaw target observation, create-only publication,
private journal, and recovery inspection through one repository-owned native
retained-dirfd kernel:

- Linux publication uses `renameat2(..., RENAME_NOREPLACE)` and Darwin uses
  `renameatx_np(..., RENAME_EXCL)`; there is no plain-rename, PATH lookup,
  package-lifecycle compilation, downloaded binary, prebuilt binary, or
  JavaScript mutation fallback.
- `openclaw-fs-kernel-build` compiles the packed auditable C source with fixed
  `/usr/bin/cc`, fixed arguments, and a closed environment, then publishes an
  absent-only helper and durable closed build receipt.
- `openclaw-install-apply` requires the explicit helper path, receipt path, and
  caller-supplied external receipt digest. It revalidates source, compiler,
  arguments, environment descriptor, binary digest, and file identities before
  opening the transaction journal.
- Ancestor swaps, final symlinks, same-path replacement, unsupported platform,
  protocol drift, and recovery replacement fail closed. Reopened published
  objects are preserved and itemized rather than deleted from a cached
  pathname.

The focused OpenClaw, CLI, contract, documentation, and exact I/O-inventory
gate passed 113/113. `npm pack --dry-run --json`, using an isolated temporary
cache because the user npm cache is not writable, confirmed that the package
contains `native/openclaw-fs-kernel.c` and `src/openclaw-safe-fs.js` and no
compiled helper. No real OpenClaw state, HOME, credential, schedule, network,
activation, runtime, domain, Birth, Delivery, or production surface was used.

The final affected gate, including runtime spawn inventory, passed 121/121.
Builder package trust boundaries passed 21/21. The one required aggregate
`npm run check` completed with 901 pass, 3 fail, and 1 skip out of 905 tests:
two failures were stale exact inventories for the two new packed runtime
members and two new safe-fs spawn sites; both passed their 2/2 exact replay
after correction. The third was the known load-sensitive packed behavior
scenario; its isolated replay passed 1/1 in about 195 seconds. The roughly
26-minute aggregate was deliberately not repeated; Plan 04-19 owns the final
canonical full gate after all gap waves merge.

The complete repository run produced an explicit bounded non-green result:
893 tests, 891 pass, 1 fail, and 1 skip. The single failure was an existing
packed Builder behavior test whose timer included full receipt/projection
admission before the hostile PATH-shadow process started. The test now measures
the unchanged eight-second safety budget from the actual hostile process start;
its exact isolated replay passed 1/1 without changing production timeouts. A
second full run was intentionally not started; Plan 04-19 owns the canonical
final full gate.

All effects remained inside disposable fixture roots or fake official seams.
No `.env` contents, real credentials, user HOME, real OpenClaw installation,
activation, runtime, schedule, MCP, domain, Birth, Delivery, or production
evidence was used or created.

## Plans 04-14 and 04-17 durable authority and atomic publication

Phase 4 now closes two additional reviewed root gaps:

- ordinary, sensitive, and conflict approvals consume independent durable
  per-nonce final markers before any private journal or target effect;
- the native retained-dirfd kernel creates the final marker name exclusively
  before writing canonical bytes, so zero, partial, stale, or unknown markers
  remain permanently fail-closed rather than releasing authority after a
  crash;
- only a complete exact-attempt marker set plus its byte-exact private journal
  may resume; caller-provided `usedNonces` state is no longer accepted;
- credential actions use a closed official OpenClaw `secrets apply` or
  `models auth login` grammar and re-use the complete sensitive-decision,
  plan, action, time, nonce, and verified-executable validation;
- package directories, D-42 archives, target descriptors, and target
  admissions are built completely under private sibling names, fsynced, and
  published by one native no-replace rename that atomically consumes the
  source name;
- every public publisher requires the exact helper path, durable receipt path,
  and caller-supplied external receipt digest. There is no auto-build, PATH
  lookup, hard-link, JavaScript mutation, or pathname-cleanup fallback;
- prepublication private objects and postpublication final objects are
  separately itemized and preserved for recovery. Nested recipe paths retain
  their canonical suffix and same-basename resources do not collide.

The executor gates passed 44/44 for durable authority, 41/41 for atomic
publisher behavior, 17/17 for exact I/O inventory, 40/40 for package
downstream behavior, 29/29 for probe/install behavior, and 1/1 for public CLI
helper requirements. Independent orchestration replay passed 93/93 for the
combined core, 1/1 for the public CLI helper gate, and 44/44 for adjacent
command-docs/package/probe/install contracts: 138/138 independent tests.
Native C warnings-as-errors, JavaScript syntax, source assertions, and
`git diff --check` passed.

The final full `npm run check` is intentionally reserved for Plan 04-19 after
the remaining lifecycle, strict-receipt, and packed-journey gap plans merge.
All current effects remain inside disposable fixture/private roots. This is
bounded mechanism evidence only and does not certify a real OpenClaw install,
credential login, activation, runtime, domain quality, Birth, Delivery,
production readiness, or wider compatibility.

## Plan 04-15 observed genesis and four lifecycle execution

AgentMo now executes a real bounded lifecycle chain inside one isolated target:

- absent genesis authority derives its path set from the install operations
  and records two retained safe-fs absence observations plus exact parent
  identities; callers cannot submit `checkedPaths`, `verifiedAbsent`, or an
  absence digest as truth;
- preview re-observes the same paths before approval authority is used, so a
  newly appearing path or parent-identity drift fails closed;
- the public genesis route requires the isolated target root and exact admitted
  helper, receipt, and external receipt digest tuple;
- a new closed official action dispatcher accepts only exact OpenClaw config
  patch or the Plan 14 credential routes. Config mutation runs one dry-run,
  proves the target stayed unchanged, then runs actual and verifies the exact
  result digest;
- install, upgrade, rollback, and uninstall now form one real predecessor-bound
  transaction/receipt chain. Rollback selects the exact install predecessor and
  uninstall reaches lineage sequence 3;
- immutable generation writes use exact D-42 `sourcePath` bytes and mode.
  Unknown config fields, unknown assets, and modified inactive generations are
  preserved.

The focused lifecycle/approval/credential/contract gate passed 45/45. Adjacent
artifact-contract, subject, probe, and safe-fs tests passed 32/32. Exact I/O
inventory passed 17/17 and packed runtime closure passed 3/3. The orchestrator
independently repeated the same 97 unique tests with zero failures; JavaScript
syntax and `git diff --check` passed.

All execution used a fake exact OpenClaw executable and disposable fixture
roots. External action results are returned by the transaction but are not yet
embedded into the strict receipt theorem; Plan 04-16 owns that closure. No live
OpenClaw compatibility, credential login, activation, schedule, MCP, domain,
Birth, Delivery, or production claim is made.

## Plan 04-16 strict receipt authority and result closure

OpenClaw install receipts are now admitted as an exact, companion-backed
authority/result ledger rather than a self-reported outcome:

- `complete` requires one unique successful result for every exact managed
  operation and sensitive/external action, exact consumption of ordinary,
  conflict, and per-action approval nonces, exact post-state, and empty
  preservation/recovery sets;
- failed, unsupported, preserved, ambiguous, recovery-required, missing,
  duplicate, substituted, or reordered evidence cannot be represented as a
  valid complete receipt;
- repeatable companions are registry-scoped only to ordered sensitive-action
  decisions for install receipts; arrays do not become a generic companion
  escape hatch;
- current and rollback predecessor receipts require a recursively authentic
  companion graph through plan, approvals, private journal, probe, package
  manifest, target/carrier admission, blueprint, build contract, plan
  approval, and target descriptor.

Independent review found and removed an interim structural predecessor
fallback that would have accepted forged historical receipt bytes with a
caller-recomputed digest. Missing or substituted predecessor companions now
fail before target effects.

Executor gates passed 67/67 focused tests, 35/35 adjacent tests, and 17/17
exact I/O inventory tests. Independent orchestration replay passed the same
67/67 focused and 35/35 adjacent gates; `git diff --check` passed.

All tests used disposable roots and value-blind fake seams. The final full
`npm run check` remains intentionally reserved for Plan 04-19. This evidence
does not certify real OpenClaw installation, activation, runtime, credentials,
domain quality, Birth, Delivery, or production readiness.

## Plan 04-18 extracted distribution closure

The actual npm tarball now completes one isolated public CLI journey through:

- explicit source build and admission of the retained-dirfd helper;
- package produce and offline archive inspect;
- fresh exact-target probe and observed genesis;
- lifecycle preview plus ordinary, sensitive, and conflict approval;
- install, upgrade, explicit rollback, and uninstall apply;
- recursive strict receipt re-admission from extracted runtime modules.

The published source/check inventory gained five missing security-module
syntax checks. Exact inventory tests reject lifecycle hooks, compiled helpers,
helper receipts, authority/nonce state, `.env`, raw transcripts, and other
private state. `npm pack --dry-run --json` exited zero with 99 entries:
README plus 98 release assets, including only the native C source rather than a
host-built helper.

Packed negative coverage includes fresh-probe target drift, durable
three-family authority replay, credential command confusion, helper receipt
drift, symlinked ancestors, false-complete receipts, caller-certified genesis,
postpublication replacement, and nested recipe preservation. The exact
rejection codes are:

- `AGENTMO_OPENCLAW_PROBE_TARGET_DRIFT`
- `AGENTMO_OPENCLAW_AUTHORITY_RECOVERY_REQUIRED`
- `AGENTMO_OPENCLAW_FS_ADMISSION_REJECTED`

Inventory tests passed 30/30. The extracted public CLI journey passed 1/1. The
one-time combined packed/focused gate passed 55/55 in about 16 minutes 20
seconds. Independent orchestration repeated inventory 30/30 and the final
CLI-remediated journey 1/1; syntax, JSON parsing, package dry-run, and
`git diff --check` passed.

No checkout runtime fallback, network, `.env`, credential, real HOME/OpenClaw
state, activation, schedule, MCP, domain, Birth, Delivery, or production
surface was used. The final full `npm run check` remains reserved for Plan
04-19.

## Plan 04-19 operator contract reconciliation and final gate record

The operator contract now matches the 04-12..18 implementation:

- D-32..D-42, all six reviewed root gaps, CR-01..10, and WR-01..03 are
  cross-mapped to exact Produce/Inspect, fresh companion-backed probe,
  observed genesis, three approval families, durable per-authority/per-nonce
  markers, four lifecycle, strict receipt, atomic publication, preservation,
  recovery, and unsupported-route behavior.
- The retained-dirfd helper is built explicitly before current publishers with
  `openclaw-fs-kernel-build --binary-out ... --receipt-out ...`. Target
  descriptor, target admission, package production, genesis, preview, and
  apply all require the exact helper, durable receipt, and caller-supplied
  external receipt digest. Apply additionally requires a bounded attempt ID
  and private mode-`0700` authority-state root.
- The npm tarball is source-only for this mechanism: it includes
  `native/openclaw-fs-kernel.c` and the JavaScript facade, with no compiled
  helper, helper receipt, authority/nonce state, download, or npm lifecycle
  compilation.
- Linux/Darwin publication uses the native no-replace syscall, with
  temp-complete source-consuming publication. Unknown, modified, externally
  owned, ambiguous, private prepublication, and reopened public objects are
  preserved and itemized rather than deleted by pathname.
- A zero, partial, stale, or unknown durable authority marker remains
  permanently fail-closed. Only one complete per-family marker set plus its
  byte-exact private journal may resume the same exact attempt.
- A complete receipt requires exact one-to-one managed/external results,
  three-family nonce consumption, exact post-state, and empty
  preservation/recovery sets. Historical current/rollback receipts require the
  full recursive plan/approval/journal/probe/package/target/source companion
  graph; structural receipt bytes plus a recomputed top digest are insufficient.
- Linux lifecycle closure exposed that flat CLI companion flags could describe
  an install receipt but not an arbitrary predecessor chain. Preview/apply now
  accept one canonical bounded/no-follow companion request bundle plus its
  external SHA-256, reject bundle/flat ambiguity, and re-open the recursive
  receipt theorem against the exact OpenClaw target/helper/root authority. The
  bundle is request-only and is not installed or persisted in the package.
  Its file bindings are portable paths resolved relative to the bundle parent;
  host-absolute, drive-qualified, backslash, empty-segment, dot, and traversal
  references fail before companion evidence intake.

Two disposable explicit helper builds exited zero. The first receipt digest was
`sha256:701bffcebb95eca9bb335cbf69b80281ac5056976572ec49038a0f2f331b1300`;
the second used an explicitly named disposable root so its bounded receipt
facts could be extracted without retaining any host path:

| Field | Digest |
| --- | --- |
| repository C source | `sha256:d4f71d2939bd6ee1b5c9ac876f7a79c5a87a276ef20d1fd7c6ebc2097a347407` |
| fixed compiler bytes | `sha256:179301dcb41ea78accc3fa0048a7e6f6710d891945a751a34addd622020c1818` |
| compiler version | `sha256:6ca8b80e26676957c32023076caf6cd49985be35709f975ed2ac53f1733f20db` |
| toolchain fingerprint | `sha256:e1e620a5233bbd619639cf8a001471840444c5bc5c4d4ccab7c0dc63add63408` |
| fixed argv JSON bytes | `sha256:a70686afe706d07de8e75d7271e895df99d858358196ce262c8cabac7a590824` |
| closed environment descriptor | `sha256:bcec370131fefa2c6c342ff60b24dcffbe4805f5061603570ebb796805aa68ad` |
| built helper bytes | `sha256:aed0ecaefbcee986b00c6ca445a191a65f68b01f3fa6e2a21fa699bff03e6a4b` |
| durable receipt bytes | `sha256:488bead47da20ae1282643714923010e5a375a1667434772a23b41aabbd93fd3` |

### Final gate status

Commands ran against the final merged implementation state without retaining
raw stdout/stderr:

| Gate | Exit | Bounded result | Duration |
| --- | ---: | --- | ---: |
| `node --test test/command-docs.test.js test/stage-contracts.test.js` | 1 | 14 pass, 1 fail; new receipt-contract invocation was not a registered maintained-shell durable subject | 2.22 s |
| same focused docs command after the first docs correction | 1 | 14 pass, 1 fail; zero-input helper build was still inside a maintained shell fence | 2.15 s |
| same focused docs command after final docs correction | 0 | 15 pass, 0 fail | 2.08 s |
| Phase 4 focused 25-file package/OpenClaw/CLI/packed/contracts suite | 1 | 244 pass, 1 fail; stale spawn inventory omitted the existing official-action runner | 996.80 s |
| isolated affected replay after the inventory correction | 0 | runtime seams 8/8; artifact surface 18/18 | bounded independent replay |
| initial final-state `npm run check` attempt | 1 | 938 tests: 936 pass, 1 fail, 1 skip | 1354.79 s |
| exact Phase 3 journey after its helper-tuple correction | 0 | 1/1 pass; adjacent target-descriptor and safe-fs 22/22 pass | bounded independent replay |
| corrected-source successor `npm run check` | 1 | 938 tests: 936 pass, 1 fail, 1 skip; corrected Phase 3 journey and runtime seams passed, load-sensitive immutable-successor hook returned code 1 | 1302.68 s |
| exact immutable-successor behavior isolation | 0 | named scenario passed with the same production timeout and bytes | about 178 s |
| first lane-structured `npm run check` | 1 | main lane: 938 tests, 935 pass, 1 fail, 2 skip; immutable-successor was isolated, while a second packed-hook chain exposed a shorter 15-second test-harness timeout | 3295.92 s |
| exact packed-hook chain after test-harness correction | 0 | named scenario passed 1/1 without changing the production 30-second fail-closed deadline | 457.18 s |
| final lane-structured `npm run check` | 0 | main lane: 938 tests, 935 pass, 0 fail, 3 skip; packed-hook chain: 1/1 pass; immutable-successor: 1/1 pass | main 1294.23 s; isolated lanes 113.35 s and 230.70 s |
| final `npm pack --dry-run --json --cache <new-isolated-cache>` | 0 | 99 entries; 520841-byte archive projection; 2716139 bytes unpacked; native C source present; no compiled helper | 0.34 s |
| final `git diff --check` | 0 | no whitespace/error diagnostics | 0.01 s |

The initial full-gate failure was diagnosed at the Phase 3 fresh-process journey:
its `openclaw-target-describe` test invocation still omitted the Plan 17
mandatory helper path, receipt path, and external receipt digest, so production
correctly failed closed. The journey was corrected to build the helper/receipt
in an external disposable root and pass the exact tuple; its isolated replay
and adjacent gates passed.

A single corrected-source successor aggregate was then run because the source
state had changed. It preserved a non-green result: the corrected Phase 3
journey and runtime seam passed, while the immutable-successor Builder behavior
scenario's installed `SessionStart` hook exited 1 under the aggregate's
concurrent load. The exact named scenario passed in isolation without changing
the production 30-second fail-closed child deadline.

The approved test-lane correction then kept that production deadline unchanged
and moved the immutable-successor scenario out of the concurrent main lane. A
new full run exposed the same aggregate-pressure class in a second packed-hook
chain: its test helper killed the outer process after 15 seconds, before the
product's 30-second deadline plus settlement grace could decide the result.
That test-only outer boundary was raised to 35 seconds and the complete
packed-hook scenario passed 1/1 in isolation. Both resource-sensitive scenarios
now run as explicit isolated lanes after the main suite.

The final lane-structured `npm run check` exited zero. The main lane passed all
935 executed tests with three explicit skips: the two isolated scenarios and
the existing platform-dependent skip. The packed-hook and immutable-successor
lanes then each passed their exact named scenario. This is one composed
regression gate, not three independent certification claims.

The final dry-run package shasum was
`9b1bfb645294130c7b877f82d4416776f69047e2`.

### Status and remaining risk

This Plan 04-19 full gate is **green but noncanonical**. The production
fail-closed deadline remains unchanged, the resource-sensitive scenarios are
executed exactly once in explicit isolated lanes, and the package/diff gates
exit zero. The noncanonical pre-verification audit and canonical
reviewer/verifier have not run; therefore Phase 4 is not yet canonically passed.

All successful evidence remains bounded fixture/native/packed mechanism
evidence. It is not `declared-ready` or `live-success`, and none of those
categories transitively proves real OpenClaw install/upgrade/rollback/uninstall,
credential login, plugin activation, runtime/restart, schedule, memory/RAG,
domain quality, Birth, Delivery, production readiness, deployment approval, or
wider OpenClaw compatibility. No real target, user HOME, credential, `.env`,
network, provider payload, transcript, or raw process output entered this
record.
