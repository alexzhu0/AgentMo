# 2026.07.21 — Transaction-bound publication and uninstall recovery

## Summary

Phase 02 Plan 24 review iteration 2 closes six Critical publication/recovery races and one hostile-test coverage warning across the private continuation authority, immutable journal, Codex host state, install/recovery transaction, and packed UAT continuation.

## Mechanism changes

- Private continuation and prior-preflight authorities now separate prepared bytes from committed authority with deterministic commit markers. Loaders ignore uncommitted finals, and publishers return the exact successor they committed.
- Immutable journal links are irreversible once observable. A live writer can no longer remove a predecessor after another process has recovered or advanced it.
- Codex host owner/ledger and install publication effects retain exact inode anchors across final filesystem effects. Effect-adjacent hostile seams prove that swapped competitors remain recoverable.
- Install recovery authority is staged, write-all, file-synced, published absent-only, and parent-directory-synced before managed mutation. Admission requires current-user ownership, safe parent modes, and exact `0600` authority mode.
- UAT continuation persists an exact `uninstall-armed` record before uninstall. A fresh packed CLI process can verify post-uninstall absence and preserved evidence, then idempotently append the uninstall observation and reach `candidate-ready`.

## Evidence

- Focused private-authority, immutable-journal, host, install-security, and packed continuation regressions passed.
- Exact repository I/O surface inventory passed with all new reads, writes, links, renames, syncs, and retained-handle operations classified.
- `npm run check` — 699/699 tests passed across 72 suites.
- `git diff --check` — passed.

## Boundaries and remaining risk

These results prove bounded deterministic mechanism behavior under synthetic local races and fresh-process restart. They do not certify domain quality, real Codex session behavior, production readiness, deployment approval, or wider OpenClaw compatibility. No real UAT, network, GitHub, private locator, retained real attempt, or `.env` access was performed.
