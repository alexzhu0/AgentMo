# 2026.08.03 — Phase 4 exact-candidate Linux and full regression gates

## Status

The earlier exact source candidate
`7c902af59b5705de5ca31e83561adeaaeeed130f` passed the bounded native
supervisor boundary on Ubuntu 24.04 with Node 20.20.2. GitHub Actions run
`30781382363`, job `91586558379`, completed successfully in 29 seconds.

The dependent full-project regression in the same run was cancelled after its
`npm run check` step exceeded the 60-minute job timeout. It is inconclusive,
not green: the workflow's package and diff steps were skipped. No local
aggregate is substituted for that exact-candidate result. At that point, the
noncanonical pre-verification audit and canonical post-gate reviewer/verifier
had not run. Phase 4 remained pending review and was not self-certified by that
evidence. A later exact successor closes the infrastructure gap below.

The current source candidate
`d943bc02d62bc78b743400d5d1eef36dc2704525` completed GitHub Actions run
`30799041760` with all eight jobs successful: one Node 20 native containment
job, six stable Node 24 main-test shards, and one Node 24 syntax, packed
behavior, package-inventory, and diff job. This is the terminal green
full-project mechanism gate required before the noncanonical pre-verification
audit. It is not a canonical Phase 4 verdict.

## Exact commands and subjects

The successful job checked out the exact candidate and ran the repository-owned
workflow `.github/workflows/phase4-linux-supervisor.yml` with these bounded
commands:

```text
cc native/openclaw-process-supervisor.c -std=c11 -O2 -Wall -Wextra -Werror -fsyntax-only
cc native/openclaw-fs-kernel.c -std=c11 -O2 -Wall -Wextra -Werror -o <private-runner-temp>/agentmo-openclaw-fs-kernel
timeout --signal=KILL 60s node --test test/openclaw-credential-handoff.test.js
timeout --signal=KILL 60s node --test --test-name-pattern=<bounded-non-bootstrap-set> test/openclaw-process-supervisor.test.js
timeout --signal=KILL 20s node --test --test-name-pattern='direct pidfd admission fails' test/openclaw-process-supervisor.test.js
timeout --signal=KILL 20s node --test --test-name-pattern='bootstrap clock admission fails' test/openclaw-process-supervisor.test.js
```

The workflow compiled the current native supervisor with warnings as errors,
compiled the retained-dirfd filesystem kernel, and exercised the Linux
containment, capacity, supervisor-signal, bootstrap-failure, candidate-identity,
and credential-handoff regressions. It inspected the source policy for native
and x32 syscall-number handling; it did not execute an x32 filter evaluator or
x32 runtime case. All listed steps exited zero.

## Bounded evidence

| Evidence | Value |
| --- | --- |
| source commit | `7c902af59b5705de5ca31e83561adeaaeeed130f` |
| workflow run | `30781382363` |
| native job | `91586558379` |
| host / Node | Ubuntu 24.04 / Node 20.20.2 |
| native job status | success, 29 seconds |
| full-project successor | cancelled at 1h00m16s; `npm run check` timed out; package/diff steps skipped; inconclusive |
| canonical review/verifier | not run; no pass claim |

The superseding terminal gate is:

| Evidence | Value |
| --- | --- |
| source commit | `d943bc02d62bc78b743400d5d1eef36dc2704525` |
| workflow run | `30799041760` |
| workflow status | success; 8/8 jobs successful |
| native supervisor boundary | success; 1m12s; Ubuntu 24.04 / Node 20 |
| syntax, packed behavior, and publication | success; 35m30s; Ubuntu 24.04 / Node 24 |
| main regression shard 0 | success; 20m28s; Node 24 |
| main regression shards 1..5 | success; all terminal green; Node 24 |
| final local package projection | 103 entries; 557,325 bytes packed; 2,931,549 bytes unpacked; shasum `c39db26d86f1ce0b897af8fb72a765409413b145` |
| canonical review/verifier | not run; no pass claim |

Only command identities, commit/job identifiers, bounded status, and timing are
recorded. No raw stdout/stderr, environment values, credential values, provider
payloads, transcripts, user HOME state, or credential-bearing OpenClaw state
are retained here.

Current exact source and workflow digests are:

| Subject | SHA-256 |
| --- | --- |
| native process supervisor source | `sha256:add052a0ff125f2e6905589ca365a4d663e63ac587f5a5603183d7312f3d79a9` |
| retained-dirfd filesystem kernel source | `sha256:eb3c2d787393dbf8ec652c4674e75ec056629e1d2fedea80e8903423b854914b` |
| Linux workflow bytes | `sha256:35d676710308456a16235c26e4da62eff048c05594a5c4c017d248b824a0c7ee` |
| package manifest bytes | `sha256:874fe3d17aac67fed941832dcb959c9316e504debe5d799fdebe72bd47b7270e` |

## Post-documentation gates

The documentation successor `c93cfbc` changes no implementation, native
source, workflow, test, or package manifest bytes. The required bounded local
post-documentation checks are:

| Gate | Exit | Bounded result |
| --- | ---: | --- |
| `node --test test/command-docs.test.js test/stage-contracts.test.js` | 0 | 15 pass, 0 fail |
| `npm pack --dry-run --json --cache <new-isolated-cache>` | 0 | 103 entries; 556,456 bytes packed; 2,925,702 bytes unpacked; shasum `8d93bae82a9829306656eaa6f0239a0eecc30758` |
| `git diff --check` | 0 | no whitespace/error diagnostics |

The dry-run inventory contains both native C sources and the JavaScript
facades. It contains no compiled helper, helper receipt, runtime evidence
instance, authority state, credential value, or npm lifecycle compiler hook.
These checks validate maintained documentation, distribution projection, and
whitespace only; they did not convert the cancelled earlier run into a pass.
The later success is separately bound to exact commit `d943bc02...` and run
`30799041760`.

## Certification boundary and remaining risk

This result proves bounded Linux execution of the repository's current native
containment and credential regression mechanism for one exact candidate. It
does not prove a live OpenClaw install, upgrade, rollback, uninstall, plugin
activation, agent run, schedule, restart, memory/RAG behavior, credential login,
domain quality, `live-success`, Birth, Delivery, production readiness,
deployment approval, or wider OpenClaw compatibility.

The fresh advisory audit has now run and found three Critical findings and one
Warning: compiler-output provenance is not retained through helper admission,
verified supervisor and OpenClaw bytes are later executed through replaceable
pathnames, persistent post-bootstrap clock failure can prevent termination and
proof completion, and x32 coverage had been overstated. Phase 4 therefore
remains blocked. See
`.planning/phases/04-package/04-PRE-VERIFICATION-AUDIT.md` for the bounded,
non-self-certifying report.

Remaining mandatory work is:

- remediate and independently re-audit all three Critical findings and the
  Warning;
- create `04-19-SUMMARY.md` only after Plan 04-19's no-open-Critical/Warning
  condition is met;
- run the execute-phase-owned canonical review and verifier only after that
  summary exists.

No GitHub Release, npm publication, tag, deployment, or real target mutation is
created by this record.

## Post-audit remediation candidate

The source successor adds an x86_64 filter-level regression that installs the
same native seccomp program, proves a syscall number carrying
`__X32_SYSCALL_BIT` returns `ENOSYS`, and then proves the corresponding native
syscall number remains allowed. The Linux workflow gives this regression its
own bounded command rather than describing token inspection as runtime
coverage. This paragraph records candidate mechanism only: no pass, closure,
or certification claim applies until an exact-source Linux run is terminal
green and the fresh audit is repeated.
