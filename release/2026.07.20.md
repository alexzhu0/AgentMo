# AgentMo 2026.07.20 — Codex Builder verifier boundary and fail-closed UAT outcome

## 背景

Phase 2 Plan 02-17 先补齐 packed successor verifier 与 exact decision boundary，再允许创建唯一的实际 UAT attempt。目标是让 baseline/setup、activation、normal-trust Codex lifecycle、distinct upgrade、prestarted uninstall continuation、candidate-ready 和人类决定形成一条不可替换的 evidence chain；任何较早失败都必须以首个 bounded terminal 结束。

本次 plan execution 已得到合法 bounded outcome，但 UAT 未通过，Phase 2 仍为 `gaps_found` / incomplete。

## 范围与实现

- `scripts/build-builder-uat-releases.js` 只向 absent output root 构建不同语义版本的 baseline/successor tarball，并通过 production inventory、manifest、static import closure、fresh extraction、syntax 与 bounded identity gates。
- `scripts/verify-codex-uat-candidate.js` 作为 exact packed asset，只暴露 read-only `preview` 与一次性的 `decide approve|reject`；它绑定 package/version/release/tarball/verifier、完整 journal、candidate-ready、candidate basis、scenario count 与所有 false certification flags。
- `src/builder-codex-uat.js` 的预期脚本路径会绑定 verifier 重新检查后的 exact preview；但最终代码审查确认模块仍导出了可绕过该路径的 raw append 与 inspect/decide authority，因此该边界尚未达到发布准入要求。
- `src/builder-package.js` 与 `package.json` 将 verifier 纳入固定 release inventory、syntax check 与 packed closure。
- `src/builder-codex-uat-continuation.js` 只接受唯一 official verifier path `scripts/verify-codex-uat-candidate.js`；旧路径、其他文件、nested path 与 prefix path 均 fail closed。Continuation 不执行 verifier。

## Actual release identities

| Release | Version | Release SHA-256 | Tarball SHA-256 | Verifier SHA-256 |
| --- | --- | --- | --- | --- |
| Baseline | `0.1.0-uat.17.1` | `sha256:04f700671552a27cd24561f433ff0bc12e527a0ec6fef3e026033c78e4337105` | `sha256:ab2c27521575d57ac11e32d27f5071114f65d30c6e9f892d685b1c1b27345563` | `sha256:e73b9c195363c521d423f0702d2dc7d0be66933b26d6494b834bc821dd4662f2` |
| Successor | `0.1.0-uat.17.2` | `sha256:43fe7a96619f83563e48e34b82edb45b10327f4b575b029aca441d6ce0ecee97` | `sha256:dd6aeabdf92c9af1fba3f5ae7e22486b4854295b26ed42f722a75723661150be` | `sha256:e73b9c195363c521d423f0702d2dc7d0be66933b26d6494b834bc821dd4662f2` |

两份 tarball 分别 fresh extract 后，production package loader、release digest、tarball digest、manifest official verifier path 和 manifest-bound verifier bytes 全部 exact match。没有复用或提升 Task 1 的临时 fixture bytes。

## UAT bounded outcome

唯一实际 attempt 在创建前通过全部 Task 1 gates。Journal 成功记录 `attempt-started`，baseline setup preview 产生并绑定 apply digest `sha256:48388698a454f21e5e77aa2058fb47c7386c80f00fba9bf6764ef279374642c7`。单次 setup apply 随后以 `AGENTMO_BUILDER_INSTALL_HOST_ROLLBACK_FAILED` fail closed。

Read-only diagnosis 只确认 bounded state：projection inconsistent、receipt missing、project marker/agent 为 unowned-present、marketplace/plugin missing、activation unverified。随后唯一 journal 追加首个且唯一的 `failure` terminal；head 为 `sha256:5a82e22d54bb8a52f1515d54e03d0e0668efdc083637b426d5280b38ebeb8d5f`。Task 4 使用 packed loader 独立复核两条唯一链、terminal kind/code/head 与完整 journal 文件集合的前后摘要，确认 `appended: false`。

未记录 `setup-applied`、`activation-applied`、Codex process start、normal trust/auth、SessionStart、任何 lifecycle scenario、candidate、verifier preview、人类 approve/reject 或 live success。Attempt 未重试、未替换，也未创建第二 attempt。

## 架构决策

- **采用：** 先完成 verifier/package gates，再创建任何实际 attempt/tarball。
- **采用：** 同一 package name 下使用不同 baseline/successor semantic versions，并绑定不同 release/tarball digests。
- **采用：** candidate-ready 之后才允许 freshly extracted successor verifier preview；本次未到 candidate-ready，因此 verifier 不运行、decision 不追加。
- **采用：** 首个 pre-candidate failure 终止唯一 chain；不得把 partial setup 当作 activation 或重试依据。
- **拒绝：** 旧 verifier path、双路径兼容、checkout verifier、continuation 内执行 verifier、same-version successor、第二 attempt、人工代填观察结果。

## 可复现命令与验证证据

Actual release producer 的公开形状：

```text
node scripts/build-builder-uat-releases.js --out <new-ignored-root>/releases --baseline-version <version> --successor-version <distinct-version> --json
```

Repository verification：

| Command | Result |
| --- | --- |
| `node --test test/builder-codex-uat.test.js test/builder-packed-install.test.js` | 25/25 pass |
| `node --test test/artifact-surface-coverage.test.js` | 14/14 pass |
| Phase 02-11 through 02-17 focused Builder test set | 206/206 pass before actual attempt creation |
| `npm run check` | 658/658 pass, 70 suites |
| `git diff --check` | pass |

Task 4 完成后再次运行 focused、full 与 diff gates；最终结果记录在 `.planning/phases/02-codex-builder/02-17-SUMMARY.md`。本记录不包含 private path、raw stdout/stderr、transcript、payload、环境值或凭据。

## 非认证边界

以下全部保持 false 或 unproven：

- cryptographic Codex origin；
- real Codex session / normal-trust lifecycle behavior；
- activation 与 host behavior；
- domain quality 与 Agent Package quality；
- package certification、production readiness 与 deployment approval；
- wider Codex compatibility 与任何 wider OpenClaw compatibility。

Task 1 的 mechanism tests 与 exact package correspondence 不能提升上述证据等级。合法 failure terminal 证明 fail-closed control path 生效，不证明目标行为成功。

## 剩余风险与后续

- setup host transaction 在 receipt publication 前进入 rollback failure，留下需人工审查的 inconsistent project projection；不得自动修复或把它用作新 attempt 的基础。
- 最终 39-file standard-depth review 记录 8 个 Critical blockers 与 1 个 Warning：UAT/verifier-decision authority 可绕过、setup 失败可遗留 receiptless partial projection、project/shared-host publication 与 CAS 存在 pathname replacement 风险、immutable journal cleanup/commit recovery 不安全。详见 `.planning/phases/02-codex-builder/02-REVIEW.md`。
- Goal-backward verification 只确认 1/5 roadmap truths、41/73 plan truths 与 3/8 requirements；详见 `.planning/phases/02-codex-builder/02-VERIFICATION.md`。
- 必须先修复并复审上述 Critical blockers，再显式批准一个全新的 isolated attempt；本次 chain 不可继续。
- Phase 2 保持 `gaps_found`，不得调用 phase completion。
- 后续 attempt 仍必须 activation-applied 先于 Codex process、trust/auth 与 SessionStart，并在 candidate-ready 后才允许 standalone verifier 和 human decision。

## Commit 状态

按执行约束未进行 stage、commit 或 push；commits: none。
