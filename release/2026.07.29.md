# 2026.07.29 — Phase 4 deterministic package and ownership-safe lifecycle mechanism

## Status

Phase 4 now closes the bounded deterministic Agent Package and separately
authorized OpenClaw lifecycle mechanism from fail-closed carrier admission
through receipt-last preservation evidence. The initial Plan 04-01 package gate
correctly rejected four abstract hooks without an approved bundled owner; the
Phase 3 remediation then selected an exact native-plugin recipe and exact target
descriptor before any package bytes were generated. The resulting canonical
directory is build authority, while the deterministic D-42 archive is the only
probe/preview/approval/apply transport. All effect-bearing tests remained in
disposable fixtures or fake official seams. This is not a live install, runtime,
domain, Birth, Delivery, wider-compatibility, or production success claim.

## Scope

- Added `agentmo.package-manifest.v1` validation with exact fields, source bindings, target compatibility, capability ownership, permissions, evidence references, risks, and non-certification fields.
- Added D-42 canonical member closure over portable regular-file paths, fixed modes, byte lengths, per-member SHA-256, and a canonical inventory digest.
- Added capability-ledger validation that rejects declaration-only executable capabilities, unindexed members, lifecycle promotion, and speculative MCP for the current v1 resource graph.
- Added deterministic carrier selection:

  - workspace content for portable declarative resources;
  - skills for domain instructions;
  - native plugin only for exact Phase 3-reapproved typed-hook recipe bytes;
  - no MCP without an approved external executable need.

- Added a stable `AGENTMO_PACKAGE_HOOK_OWNER_UNAPPROVED` blocker for the current OpenClaw `2026.6.11@29d018f0` contract.
- A future passing successor must bind exact UTF-8 recipe contents, portable paths, regular-file modes, byte lengths, per-file digests, canonical recipe digest, owner, all four event mappings, version range, permission, timeout, failure semantics, and unsupported behavior. No pre-existing implementation path is accepted.

## Validation evidence

| Gate | Result |
| --- | --- |
| RED command | expected failure: missing `src/package-contract.js` and `src/package-carriers.js`; underlying `node --test` exit 1, plan `!` wrapper exit 0 |
| Plan 04-01 focused gate | 10 pass, 0 fail; exit 0 |
| Phase 3/4 adjacent focused gate | 16 pass, 0 fail; exit 0 |
| new module/test syntax checks | pass; exit 0 |
| `git diff --check` plus untracked-file no-index whitespace checks | pass; exit 0 |
| `npm run check` | not green: explicitly interrupted after the long packed matrix stalled; 792 pass, 1 fail, 2 cancelled, 1 skip |

The full aggregate failure was outside the Plan 04-01 files: `test/codex-builder-behavior.test.js` reported the pre-existing load-sensitive PATH-shadow timeout assertion. The packed suite was interrupted and therefore cancelled/pending; neither outcome is presented as green.

## Non-certification boundary

- No package bytes were generated or installed.
- No real or user-level OpenClaw state, configuration, plugin, credential, process, or sibling repository was read or changed.
- No `.env`, secret value, raw provider payload, transcript, or unredacted runtime output was read or retained.
- Carrier consistency and manifest validation prove bounded deterministic mechanism behavior only.
- No commit, stage, push, tag, npm publication, GitHub Release, or deployment occurred.

## Remaining risk

Phase 4 remains blocked from package generation until Phase 3 reapproves a complete package-local native-plugin recipe and exact hook mappings, followed by the separate target/carrier admission planned for 04-02.

## Plan 04-02 bounded authority update

Plan 04-02 now defines, but does not self-approve, the missing authority chain:

- `agentmo.build-contract.v1` can embed one raw-byte-admitted `agentmo.native-plugin-recipe.v1` with sorted portable paths, fixed regular-file modes, exact normalized UTF-8 content, recomputed member SHA-256 values, four exact OpenClaw event mappings, permissions, timeout/failure semantics, and a canonical recipe digest.
- The existing exact plan-approval route becomes stale for every recipe/content/mapping mutation because it binds the successor build-contract raw bytes.
- `agentmo.openclaw-target-carrier-admission.v1` binds exact blueprint, recipe-bearing build contract, plan approval, target executable/root bytes, target revision, and recipe digest. Its writer is create-only and its fresh-process admission requires the exact three companion artifacts.
- `openclaw-target-admit` accepts no plugin implementation path or bytes and exposes no MCP route or activation authority.
- A complete human-inspectable recipe candidate is stored at `.planning/phases/04-package/04-02-native-plugin-recipe.json`; it is construction intent only and no file named by that recipe has been materialized.

Focused verification passed 20/20, and the adjacent artifact/subject/command-contract gate passed 31/31. The installed `OpenClaw 2026.7.1-2 (0790d9f)` remains rejected under the existing `2026.6.11@29d018f0` authority. No actual recipe-bearing Phase 3 build contract, successor plan approval, or real target/carrier admission has been selected, so Phase 4 remains paused at the blocking human checkpoint.

## Approved upstream architecture remediation

The operator rejected the minimal hard-code-current-version alternative and approved a data-bound exact target authority:

- `agentmo.openclaw-target-descriptor.v1` is derived from retained, no-follow, read-only observations of the executable, first-party `package.json`, and first-party `dist/build-info.json` bytes. It records exact raw digests, retained identity bases, target-root member closure, full/display revision, Node range, provenance authority, and false certification boundaries without persisting an absolute target path.
- `build-contract`, the native-plugin recipe validator, carrier selection, target/carrier admission, artifact registry/contracts/subjects, public CLI, and packed surface are target-neutral in source. A different target requires new exact descriptor and approval bytes rather than code edits.
- The build contract embeds and raw-digest-binds the exact descriptor. Recipe `versionRange` binds the descriptor's observed version and display revision. Target/carrier admission re-observes all three first-party members and rejects descriptor, plan approval, executable, package, build-info, target-root, or recipe drift.
- Legacy descriptor-less build contracts remain structurally inspectable but cannot authorize the current target or be silently upgraded.
- The current descriptor candidate is `.planning/phases/04-package/04-02-openclaw-target-descriptor.json`; the recipe candidate was rebound to `2026.7.1-2@0790d9f`. Neither candidate is a package, install, activation, runtime, domain, or production approval.
- The old exact Phase 3 source bytes are not present in durable repository evidence. Current repository examples have different raw digests from the human-approved Phase 3 manifest/database pair. AgentMo therefore refuses to reconstruct the old chain from summaries or silently issue a new discovery/Plan approval. The next legal step is an exact human decision over a newly regenerated source pair before downstream build-contract, plan-approval, and target/carrier artifacts can be published.

Exact remediation evidence:

- target descriptor raw digest: `sha256:0abad669ae3cac7b6219737a728df21799eeec6ac7946e8fd38285f9e4322bee`
- target descriptor authority digest: `sha256:db260e3f2a4af92f3e0fc3518a0f1552b0c8b6d5ebe19d127b27f3a1ae01cf06`
- observed target: `2026.7.1-2@0790d9f`, full revision `0790d9f593ad30c940ed93b5872a8cf6d6f3cf8c`
- observed Node range: `>=22.22.3 <23 || >=24.15.0 <25 || >=25.9.0`
- recipe raw digest: `sha256:9cade4863c259d5d94aa2bb964bbceb622d1a010a7e34fe4b91a8216b4f05c3a`
- canonical recipe digest: `sha256:5bd43924237a304222f60a9788a8f0130819ba0208f567ebfc798b0735205402`
- recovery record raw digest: `sha256:15ac710c650d48c552cfa92fdbcf2f1c806339856d6b0f3f32d6cd0953805f8c`
- discovery approval preview raw digest: `sha256:74f094322476557a6313a3b8d9c16c189d2271b7b036784b32f0f016123c3779`
- discovery approval preview decision digest: `sha256:23e072e8d58a3628ef14d32e1166b4bd22ae3ae5bd3704f38038741218f20fa0`
- combined focused and adjacent test gate: 114 pass, 0 fail; exit 0
- packed Builder exact import-closure gate: 1 pass, 0 fail; exit 0

No source authority, recipe, plan, carrier, package, install, activation, runtime, domain, or production approval was inferred from this remediation. The exact current discovery pair still requires a separate human decision.

## Plan 04-03 deterministic package materialization

The exact human-selected Plan 04-02 authority now produces the first complete
canonical Agent Package directory and D-42 transport archive:

- Canonical directory: `.planning/phases/04-package/04-03-agent-package`
- D-42 archive: `.planning/phases/04-package/04-03-agent-package.d42`
- Archive SHA-256: `sha256:7726d7b635a972403c598bf53eeb9c44a75c57ffd5c4a573470a066a798b955f`
- Manifest SHA-256: `sha256:af98b46e5d5a6e46db7c7b020fea51115bae0829d943583ce9d756ce1d1c45`
- Canonical inventory SHA-256: `sha256:d6be393fc176c9f28811e9e8771fae7cff5efb81a824697a6300ae80466c32a5`
- Member closure: 40 regular files with portable paths, fixed modes, exact byte
  lengths, and per-member SHA-256 values.

`package-produce` re-admits the complete source chain and the exact
`openclaw-target-carrier-admission` bytes before staging any output. Native
plugin files are materialized solely from the admitted inline recipe, and the
OpenClaw projection contains workspace documents, a workspace skill,
tool/policy bindings, memory and evaluation resources, proposal-only schedule
and credential setup files, and no MCP surface.

The archive is reconstructed from the committed directory and is the sole
later preview/approval/apply transport. Its retained reader verifies the
external archive digest, manifest digest, canonical inventory digest, complete
member set, file type, mode, size, content digest, canonical JSON encoding, and
no-follow regular-file identity.

Verification passed the 30-test Wave 3 focused and adjacent gate, fresh-process
CLI production, module syntax checks, exact output closure inspection, and
`git diff --check`. The full aggregate suite was intentionally not run because
the approved wave calls for bounded Phase 3/4 gates.

This evidence proves deterministic package mechanism execution only.
Installation, user-level modification, plugin activation, credential writing,
schedule execution, OpenClaw runtime, domain quality, Birth, production
readiness, publication, and deployment remain false and unauthorized.

## Plan 04-04 offline package inspection and public manifest admission

The exact Plan 04-03 Agent Package now has one read-only, value-blind inspection
path for both its canonical build directory and its D-42 transport archive:

- `package-inspect` produces one deeply frozen sanitized candidate for human and
  JSON output. Both renderings preserve the same fields and values.
- Directory inspection remains build-authority verification; it recomputes the
  canonical archive closure but does not make the directory a downstream
  transport.
- Archive inspection first binds the caller-supplied external digest, then
  verifies the canonical manifest, inventory, all 40 members, modes, sizes,
  content digests, member ordering, and retained regular-file identities.
- The public `package-manifest` contract and artifact registry entry use the
  production manifest validator. Admission remains exact-subject,
  exact-raw-digest, duplicate-identity rejecting, and forged-result rejecting.
- Secret-bearing, authentication/session, database, transcript, provider
  payload, and raw output surfaces are rejected before an inspection result is
  returned.

Exact closure verified:

- Archive SHA-256: `sha256:7726d7b635a972403c598bf53eeb9c44a75c57ffd5c4a573470a066a798b955f`
- Manifest SHA-256: `sha256:af98b46e5d5d5a6e46db7c7b020fea51115bae0829d943583ce9d756ce1d1c45`
- Inventory SHA-256: `sha256:d6be393fc176c9f28811e9e8771fae7cff5efb81a824697a6300ae80466c32a5`
- Member count: 40

Bounded verification passed the 36-test Wave 4 package/contract gate, the
56-test adjacent CLI/artifact/Stage gate, syntax checks, `git diff --check`,
fresh-process human/JSON equivalence, exact public manifest admission, and 11
archive mutation cases rejected before effect. The full aggregate suite was
intentionally not run under this wave's bounded-test constraint.

No OpenClaw process, installation, activation, user-level state, credential,
schedule, runtime, or delivery path was invoked or changed. Package closure
verification does not certify installation, runtime behavior, domain quality,
Birth, delivery, production readiness, publication, or deployment.

## Plan 04-05 read-only OpenClaw capability fingerprint

AgentMo now exposes one durable `agentmo.openclaw-probe.v1` capability
fingerprint before any installation planning:

- The probe first re-admits an exact D-42 archive, its manifest, inventory, and
  complete member closure, then re-admits the exact target/carrier authority
  and target descriptor.
- Target executable, package metadata, and build information are observed with
  bounded retained no-follow reads. Any exact-byte drift fails before child
  process observation.
- Three fixed read-only CLI observations run through `process.execPath` with
  fixed argv, `shell:false`, a disposable synthetic HOME/state/config/workspace,
  an explicit non-secret environment, bounded output, and no inherited
  environment.
- Raw stdout, stderr, temporary paths, and operator paths are not retained.
  Normalized observation facts bind CLI/JSON, workspace/skill, policy/config,
  permission, package-conflict, target, runtime, and archive surfaces into one
  stable fingerprint.
- The public `openclaw-probe` route writes only an absent output path. The
  artifact contract, registry, and durable command-subject map all reject
  partial or stale probe evidence.

Selected downstream authorities remain:

- D-42 archive SHA-256: `sha256:7726d7b635a972403c598bf53eeb9c44a75c57ffd5c4a573470a066a798b955f`
- Target/carrier admission SHA-256: `sha256:5549707121dc1753bfe00909c9dc26d59668de1408d7894b0ecb17340ebe2bf6`
- Target descriptor SHA-256: `sha256:0abad669ae3cac7b6219737a728df21799eeec6ac7946e8fd38285f9e4322bee`

Bounded verification passed the exact 13-test Wave 5 gate, the 53-test adjacent
artifact/subject/CLI gate, module syntax checks, and `git diff --check`. No
live or user OpenClaw target was invoked; all subprocess evidence used
disposable synthetic test targets and synthetic HOME isolation.

The fingerprint proves bounded compatibility observation only. It does not
certify installation, plugin load, MCP connection, credential use, agent or
schedule invocation, runtime behavior, domain quality, Birth, Delivery,
production readiness, publication, or deployment.

## Plan 04-06 pure lifecycle and approval authorities

AgentMo now models OpenClaw install lifecycle review without publishing an
artifact or touching a target:

- `install`, `upgrade`, explicit `rollback`, and `uninstall` plans each have
  one closed predecessor basis: verified absent genesis, current receipt, or
  current plus selected predecessor receipt/archive as appropriate.
- Every plan, ordinary approval, per-sensitive-action decision, and exact
  conflict-set approval independently repeats the same D-42 archive SHA-256,
  internal manifest digest, canonical inventory digest, and complete sorted
  member inventory.
- Member authorities bind portable relative path, regular-file type, fixed
  mode, byte length, and SHA-256. Package roots, standalone manifests, caller
  success flags, partial inventories, and drifted members fail closed.
- The one-screen review is a frozen semantic model shared by human and JSON
  renderers. Ordinary managed-write authority cannot grant sensitive-action,
  conflict, or broader-scope authority.
- Sensitive actions bind exact executable, argv, cwd, scope, target, timeout,
  and environment-name set. Conflict approval binds the whole sorted
  path/current digest/desired digest/action set.

The bounded Wave 6 gate passed 15 tests with zero failures; syntax checks for
the two models and three focused test files passed, and `git diff --check`
passed. This wave added no writer, durable registry subject, CLI route,
filesystem mutation, process execution, credential route, or OpenClaw target
effect.

These pure authorities prove only deterministic proposal and exact approval
semantics. They do not prove publication, receipt durability, installation,
rollback execution, uninstall execution, plugin load, credential use, runtime
behavior, domain quality, Birth, Delivery, production readiness, publication,
or deployment.

## Plan 04-07 create-only authorities and receipt admission prerequisite

The Wave 6 lifecycle models now have create-only durable publication without
adding a lifecycle command or touching an OpenClaw target:

- absent-genesis, install-plan, ordinary approval, per-sensitive-action
  decision, and exact-conflict approval writers accept only builder-issued,
  validator-valid, persistability-audited candidates;
- each writer uses exclusive absent-output publication, re-reads final bytes,
  rejects post-validation byte drift, and returns only the caller-selected path
  plus an external SHA-256;
- one-screen decision publication validates the complete ordinary, sensitive,
  and conflict cardinality, candidate set, unique destinations, and whole-set
  absence before creating its first file;
- `agentmo.openclaw-install-receipt.v1` now has a closed validator, public
  contract/template, exact subject, and durable registry descriptor;
- complete and incomplete receipt candidates bind lifecycle action, exact
  target, full D-42 archive/member closure, absent-genesis or exact predecessor
  receipts, immutable lineage, per-path ownership observations, preserved
  assets, recovery needs, and false runtime/domain/Birth/Delivery/production
  claims.

The final bounded gate passed 44 tests with zero failures, including the
convergence-required `test/phase4-contracts.test.js`; syntax checks and
`git diff --check` also passed.

This wave adds no receipt writer, transaction journal, receipt-last
publication, lifecycle CLI, target mutation, OpenClaw execution, activation,
or installation. Receipt admission and authority publication prove bounded
mechanism evidence only; they do not certify a completed install, OpenClaw
compatibility beyond the separately admitted probe, runtime behavior, domain
quality, Birth, Delivery, production readiness, publication, or deployment.

## Plans 04-08 and 04-09 fresh-process authority and receipt-last lifecycle

Plan 04-08 registered the install receipt before five lifecycle authority
descriptors and exposed create-only fresh-process routes:

- `openclaw-install-genesis` publishes verified absence from one exact probe
  plus exact request;
- `openclaw-install-preview` revalidates the externally digest-bound D-42
  archive and selects exactly one lifecycle basis;
- `openclaw-install-approve` publishes one ordinary approval, one exact
  decision for each sensitive action, and one authority for the entire exact
  conflict set.

The four lifecycle bases are closed:

| Lifecycle | Exact predecessor/genesis basis |
| --- | --- |
| install | verified absent genesis |
| upgrade | current receipt |
| explicit rollback | current receipt plus selected predecessor receipt and selected predecessor D-42 archive |
| uninstall | current receipt |

Every plan and authority repeats the external archive digest, manifest digest,
canonical inventory digest, and complete member path/type/mode/length/digest
closure. Package roots, manifest-only transport, missing digest mates, basis
substitution, stale external digests, and internally modified archives with a
recomputed outer digest all fail before successor publication.

Plan 04-09 added the sole `applyOpenClawInstallPlan` mutation seam and public
`openclaw-install-apply` contract. It exact-admits the target/carrier admission,
archive, probe, plan, ordinary approval, every sensitive decision, conditional
conflict approval, and action-specific predecessor/genesis files from explicit
paths plus caller-supplied SHA-256 values. It retains the archive no-follow and
revalidates the external digest, manifest, inventory, full member closure, and
regular-file identity before a private journal or target effect.

Apply then immediately re-observes target, parent, ownership, current digest,
and conflict state, performs only exact approved writes, post-observes each
path, and publishes a create-only complete or incomplete receipt last. A plan,
approval, journal, or partial observation cannot self-certify success.

Recovery is preserve-first. Automatic cleanup removes an asset only when it was
created by this attempt, still carries the exact owner-marker digest, retains
the authenticated file identity, and still equals the approved desired digest.
Unknown, modified, external, replaced, or ambiguous assets remain in place and
are recorded in an incomplete receipt with recovery needs. Explicit rollback is
an operator-selected lifecycle action and is not automatic failure recovery.

Credential values remain outside AgentMo artifacts and argv evidence. The
mechanism carries only value-blind presence and bounded result metadata across
an explicitly approved official OpenClaw `shell:false` seam. No raw output,
auth/session state, credential-bearing OpenClaw state, secret value, or MCP
route is accepted or persisted.

Plan 04-08 verification passed 41/41 in its required gate and 39/39 in the
bounded adjacent artifact/subject/CLI gate. Plan 04-09 passed 50/50 in its
required authority/transaction gate and 15/15 in the bounded adjacent
subject/documentation gate. Every effect-bearing case used disposable
`mkdtemp()` roots, copied fixture authority bytes, or a fake official seam; no
operator OpenClaw state or credential was used.

`release/2026.07.30.md` remains the later incremental record for these two
waves. This 2026.07.29 record is the canonical bounded Phase 4 phase narrative;
the later record is retained and is not rewritten as historical Phase 3
evidence.

## Plan 04-10 packed distribution and non-transitive evidence

The formal Builder inventory and npm tarball now include the complete Phase 4
runtime module closure. Packed imports and commands resolve only from freshly
extracted tarball bytes, with no source-checkout fallback. The packed grammar
exposes archive path plus external archive SHA-256 for preview/apply and no
package-root or manifest-only install transport.

The declared support-triage regression composes exact-approved inputs through
package production, offline archive inspection, and a fixture-only exact target
probe. It confirms no MCP carrier, plugin load, agent invocation, schedule
trigger, credential use, runtime, domain, Birth, Delivery, or production claim.
Package, inspection, probe, and receipt facts have empty implication sets.

Recorded packed evidence before the final Phase 4 rerun was 23/24 in one full
run plus 1/1 for the isolated post-fix authenticated pathname-swap case,
yielding 24/24 current cases under the Wave 10 bounded rerun instruction.
Artifact-surface coverage passed 17/17; the Phase/Stage focused gate passed
14/14; the Builder inventory contained 94 assets and npm dry-run contained 95
entries.

## Exact Phase 4 evidence identities

| Evidence | Repository path | SHA-256/status |
| --- | --- | --- |
| canonical package directory | `.planning/phases/04-package/04-03-agent-package/` | deterministic build authority; 40 members |
| D-42 archive | `.planning/phases/04-package/04-03-agent-package.d42` | `sha256:7726d7b635a972403c598bf53eeb9c44a75c57ffd5c4a573470a066a798b955f` |
| package manifest | `.planning/phases/04-package/04-03-agent-package/agentmo.package.json` | `sha256:af98b46e5d5d5a6e46db7c7b020fea51115bae0829d943583ce9d756ce1d1c45` |
| canonical inventory | manifest `inventoryDigest` | `sha256:d6be393fc176c9f28811e9e8771fae7cff5efb81a824697a6300ae80466c32a5` |
| target descriptor | `.planning/phases/04-package/04-02-openclaw-target-descriptor.json` | `sha256:0abad669ae3cac7b6219737a728df21799eeec6ac7946e8fd38285f9e4322bee` |
| target/carrier admission | `.planning/phases/04-package/04-02-openclaw-target-carrier-admission-candidate.json` | `sha256:5549707121dc1753bfe00909c9dc26d59668de1408d7894b0ecb17340ebe2bf6` |
| recipe-bearing build contract | `.planning/phases/04-package/04-02-build-contract.json` | `sha256:0a5472dd44e7c7c03b92ced9ccbf15ddec55fc668a5c8a4d6b203629ac14d05b` |
| exact plan approval | `.planning/phases/04-package/04-02-plan-approval.json` | `sha256:42f31fbe1654d9d9b2d5a5331a516e1ab0e79d319a2faf4bd882013695cf53cd` |

The package binds exact target `2026.7.1-2@0790d9f`. A mismatched target is
never valid under the old approval and must return to descriptor, contract,
approval, and target/carrier admission.

## Phase 4 non-certification and remaining risk

- No real OpenClaw install, upgrade, explicit rollback, uninstall, plugin load,
  MCP connection, credential login, agent invocation, or schedule execution was
  performed.
- No live runtime, restart, memory/RAG, domain evaluation, `live-success`,
  Birth, Delivery, production-readiness, deployment, publication, or wider
  compatibility evidence exists for Phase 4. These remain Phase 5-owned.
- The exact probe proves bounded read-only compatibility observation only.
- A complete receipt proves bounded lifecycle mechanism execution only; an
  incomplete receipt records preservation/failure and cannot be upgraded by
  cleanup or self-report.
- Credential handling remains value-blind; no `.env`, credential value, raw
  provider payload, transcript, credential-bearing state, or unredacted
  stdout/stderr was read or promoted into this record.
- All repository changes remain local and uncommitted. No tag, push, npm
  publication, GitHub Release, or deployment is implied.

## Plan 04-11 operator contract and final gate closure

The maintained stage contract, runbook, runtime notes, Birth boundary, public
README, MVP ledger, and release index now expose the same bounded Phase 4
mechanism:

- the canonical package directory is deterministic build authority;
- the externally SHA-256-bound D-42 archive is the only downstream
  probe/preview/approval/apply transport;
- every downstream authority revalidates the internal manifest digest,
  canonical inventory digest, and all 40 path/type/mode/length/member-digest
  facts;
- install uses verified absent genesis, upgrade and uninstall use the exact
  current receipt, and explicit rollback additionally selects one predecessor
  receipt and D-42 archive;
- ordinary managed-write approval, each sensitive-action decision, and the
  entire conflict-set approval remain independent authority families;
- receipt publication is last, automatic recovery is preserve-first, and
  credential values remain inside the official OpenClaw seam with no MCP route.

Final validation used no live provider, real OpenClaw lifecycle, credential,
agent, schedule, runtime, domain, Birth, Delivery, or production execution:

| Gate | Actual result |
| --- | --- |
| maintained command/stage/Phase 4 docs | initial 23/24 exposed one documentation-fence classification error; the exact isolated command-doc gate passed 10/10 after the Phase 4 examples were retained as exact non-shell command text |
| public status/release contract gate | 19/19 pass; exit 0 |
| complete Phase 4 focused gate | 68/68 pass; exit 0; 12.333 seconds |
| packed Builder plus artifact-surface gate | 41/41 pass; exit 0; 912.829 seconds |
| Stage 2/3 and support-triage contract gate | 67/67 pass; exit 0; 9.799 seconds |
| first full `npm run check` | 882 pass, 2 fail, 1 skip; one PATH-shadow bound was load-sensitive and one exact production-spawn inventory omitted `src/openclaw-probe.js` |
| isolated failure closure | PATH-shadow 1/1 pass without timeout changes; exact inventory case 1/1 pass after adding only the missing row; adjacent runtime/probe gate 18/18 pass |
| final full `npm run check` | 884 pass, 0 fail, 0 cancelled, 1 skip; exit 0; 1297.955 seconds |
| final whitespace gate | `git diff --check` pass; exit 0 |

The separate packed gate exceeded a 360-second reference bound but completed
normally with no failure, cancellation, or timeout. The final full run likewise
completed with an explicit green TAP result. This timing is remaining
load-sensitive test cost, not evidence of live OpenClaw compatibility.

The historical Phase 3 record `release/2026.07.28.md` remained byte-identical
at SHA-256
`37ed9dfc07601b0dc7e6afb39a24a048530704232b236065adf198867f6a6073`.
`release/2026.07.30.md` remains a distinct later incremental Phase 4 record.
