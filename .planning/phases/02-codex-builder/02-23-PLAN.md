---
phase: 02-codex-builder
plan: "23"
type: execute
wave: 22
depends_on: ["02-22"]
files_modified:
  - src/builder-codex-uat-private-authority.js
  - scripts/preflight-codex-uat-prior-attempt.js
  - test/builder-codex-uat-prior-attempt.test.js
  - test/helpers/io-surface-inventory.js
  - test/artifact-surface-coverage.test.js
autonomous: true
gap_closure: true
requirements: [BLDR-04, BLDR-05, BLDR-07]
must_haves:
  truths:
    - "A repository operator can prove the preserved failed attempt is the exact known two-entry terminal without mutating it or disclosing its locator."
    - "The preflight retains the known journal directory and both no-follow file handles across production journal load, install diagnosis, and the second load, then compares exact identity and raw bytes before/after."
    - "Missing stdin, drift, extra/missing entries, unsafe ancestors, foreign ownership, writable modes, symlinks, wrong terminal/code/head, or any append indicator exits non-zero before mutation."
    - "The operator command publishes one value-blind `agentmo.codex-uat-prior-preflight.v1` receipt at the fixed repository-relative private authority root, and 02-23's focused gate exactly inventories every new script I/O while proving the diagnostic remains outside the package boundary."
  artifacts:
    - path: src/builder-codex-uat-private-authority.js
      provides: Repository-private fixed-root bootstrap, exact prior-receipt admission, and durable continuation state authority
    - path: scripts/preflight-codex-uat-prior-attempt.js
      provides: Repository-only CLI importing the production private-authority module for retained-handle preflight and later exact admission/transition commands
    - path: test/builder-codex-uat-prior-attempt.test.js
      provides: Synthetic positive and hostile preflight matrix
    - path: test/helpers/io-surface-inventory.js
      provides: Exact allowlist ownership for every preflight script/src I/O surface
    - path: test/artifact-surface-coverage.test.js
      provides: Exact diagnostic coverage plus negative package/public-boundary assertions
  key_links:
    - from: stdin/in-process locator bundle
      to: loadCodexUatAttemptJournal and diagnoseBuilderInstall
      via: retained safe-directory plus exact two-file no-follow handles held through both calls
    - from: before/after captures
      to: value-blind preflight evidence
      via: exact schema validation over identities, raw SHA-256, entry set/count, terminal/head, diagnosis digests, and appended=false
    - from: scripts/preflight-codex-uat-prior-attempt.js
      to: src/builder-codex-uat-private-authority.js
      via: direct production imports for bootstrapPrivateAuthorityRoot, prior-receipt publish/load/admit, and continuation publish/load/transition; no duplicate script-local validator
    - from: scripts/preflight-codex-uat-prior-attempt.js
      to: .omx/codex-uat/phase-02-final-retry/prior-preflight.receipt.json
      via: bootstrap only after retained-handle preflight PASS, then absent-only O_EXCL mode-0600 publication and exact production reload beneath an admitted mode-0700 root
    - from: src/builder-codex-uat-private-authority.js and scripts/preflight-codex-uat-prior-attempt.js
      to: test/helpers/io-surface-inventory.js and test/artifact-surface-coverage.test.js
      via: exact discovered-versus-allowlisted I/O equality and explicit negative package/help/plugin/manifest/runtime-closure assertions
---

<objective>
交付实际可运行但不接触真实 locator 的 prior-attempt retained-handle preflight 工具，为后续单次人工提供 locator 的只读 gate 建立机器权威（D-25、D-28、D-29、D-30、D-31）。

Purpose: 取代 Markdown regex 与 pathname-only load，证明旧两条 failure chain 在 diagnosis 前后保持 exact 不变。
Output: Repository-only stdin CLI/module、closed value-blind evidence schema 与 synthetic hostile tests。
</objective>

## Phase Goal

**As a** developer installing AgentMo Builder in Codex, **I want to** install and run the packaged Builder on a clean Codex host and recover its workflow across pause, compaction, restart, upgrade, and uninstall with provable asset ownership, **so that** I can use and audit AgentMo Builder safely without depending on the AgentMo source repository.

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-codex-builder/02-CONTEXT.md
@.planning/phases/02-codex-builder/02-17-SUMMARY.md
@.planning/phases/02-codex-builder/02-VERIFICATION.md
@.planning/phases/02-codex-builder/02-22-SUMMARY.md
@src/builder-codex-uat.js
@src/builder-immutable-journal.js
@src/builder-doctor.js
@src/builder-codex-uat-private-authority.js
@package.json

<interfaces>
- Known terminal head: `sha256:5a82e22d54bb8a52f1515d54e03d0e0668efdc083637b426d5280b38ebeb8d5f`.
- Known terminal: kind `failure`, code `AGENTMO_BUILDER_INSTALL_HOST_ROLLBACK_FAILED`, exactly two entries.
- Locator transport is a bounded JSON object supplied only through the command's local terminal stdin or the in-process function parameter. It directly names the project root, base journal, and one successor entry; it has no Codex response/prompt, argv, environment, stdout/stderr, log, summary, or receipt representation.
- `src/builder-codex-uat-private-authority.js` is the sole production authority. Export `bootstrapPrivateAuthorityRoot`, `publishPriorPreflightReceipt`, `loadPriorPreflightReceipt`, `admitPriorPreflightReceipt`, `publishCodexUatContinuation`, `loadCodexUatContinuation`, and `transitionCodexUatContinuation`; `scripts/preflight-codex-uat-prior-attempt.js` imports these exports and exposes fixed-target `run-prior-preflight`, `admit-prior-preflight-receipt`, `init-continuation`, `admit-continuation`, and `transition-continuation` subcommands. The module/script remain repository-private and are not Builder package/public CLI surfaces.
- The private authority root is fixed as `.omx/codex-uat/phase-02-final-retry/`. `bootstrapPrivateAuthorityRoot` may run only after the locator's complete retained-handle preflight has passed. It walks the anchored repository-relative ancestor chain without discovery and creates the absent fixed root as current uid, non-symlink, mode `0700`, then fsyncs the created directory and retained parent; an existing root is usable only after exact uid/mode/no-symlink/stable-identity admission. Unsafe, replaced, or ambiguous ancestors/root fail closed. Invalid locator/preflight input creates neither root nor receipt.
- The receipt schema is `agentmo.codex-uat-prior-preflight.v1`. `publishPriorPreflightReceipt` uses absent-only `O_EXCL`, mode `0600`, file fsync plus retained-parent fsync; `loadPriorPreflightReceipt` opens no-follow and retains a stable handle; `admitPriorPreflightReceipt` exact-reloads and validates root `0700`, file `0600`, current uid, closed schema, tool/schema digest, exactly two base/successor identities and raw digests, exact entry-set digest/count `2`, known failure kind/code/head, before/after equality fields, and `appended:false`. It contains no locator, filename supplied by the locator, machine-absolute path, raw journal bytes, transcript, payload, environment value, credential, or stdout/stderr body.
- The fixed continuation record is `continuation.json` with closed schema `agentmo.codex-uat-continuation.v1`. Initial `publishCodexUatContinuation` is `O_EXCL`/mode `0600` beneath the exact-admitted `0700` root. `loadCodexUatContinuation` performs no-follow stable-handle exact reload. `transitionCodexUatContinuation` requires the admitted predecessor/head and either CAS publication or one immutable transition-journal successor; enforce the closed legal matrix covering `awaiting-local-invocation`, terminal states, and `candidate-ready`, and reject wrong/stale head, replacement, partial/duplicate write, unsafe parent, or illegal transition. Operational paths must be fixed-root-relative; machine-absolute paths and secrets/raw evidence are schema-invalid.
- `loadCodexUatAttemptJournal` remains the semantic journal loader and `diagnoseBuilderInstall` remains the read-only project diagnosis. The new tool surrounds both with stronger retained identity/raw-byte evidence; it does not add mutation authority.
- This is a repository repair/UAT operator tool, not a packed Builder product surface. Do not add package `files`, public AgentMo CLI routing, plugin assets, or packed closure unless execution proves a public packed consumer is required; record that exclusion explicitly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify the retained preflight and hostile matrix</name>
  <files>test/builder-codex-uat-prior-attempt.test.js</files>
  <read_first>AGENTS.md, .planning/phases/02-codex-builder/02-17-SUMMARY.md, .planning/phases/02-codex-builder/02-VERIFICATION.md, src/builder-codex-uat.js, src/builder-immutable-journal.js, src/builder-doctor.js, package.json</read_first>
  <behavior>
    - A synthetic exact two-entry failure fixture first proves root/receipt absence, completes retained-handle preflight, bootstraps the anchored fixed root, and publishes one schema-valid `agentmo.codex-uat-prior-preflight.v1` receipt with entryCount 2, exact known head/code, exact before/after identities and raw SHA-256, equal entry-set digest, equal loader/diagnosis digests, appended false, and exact tool/schema digests.
    - Empty/missing stdin, argv/env locator attempts, malformed or extra locator keys, missing base/successor, wrong directory, extra journal entry, raw-byte drift, inode replacement, unsafe ancestor, symlink, foreign uid, group/world-writable mode, wrong terminal/code/head, and diagnosis/loader drift all exit non-zero and write no file.
    - Invalid locator or any preflight failure leaves both fixed root and receipt absent; anchored absent-only root creation sets current uid/mode 0700 and fsyncs root/parent, while an existing root is accepted only on exact uid/mode/no-symlink/stable identity. Unsafe ancestors/root, replacement, pre-existing receipt, partial publication, or receipt schema/digest/entry mismatch fails closed without overwriting, truncating, scanning, or guessing.
    - Production publish/load/admit coverage proves a retained no-follow mode-0600 receipt handle rejects wrong uid/mode/schema/tool/schema digest, either entry identity/raw digest, entry-set/count, terminal/head, before/after equality, and appended value; a fresh child-process `admit-prior-preflight-receipt` reaches the same result as the in-process production loader.
    - Production continuation init/load/transition coverage proves O_EXCL initial publication, root/file modes, closed schema, fixed-root-relative paths, predecessor/head CAS or immutable successor semantics, and the legal awaiting-local-invocation/terminal/candidate-ready matrix; fresh child-process admission plus wrong/stale head, replacement, unsafe ancestor, partial/duplicate write, illegal transition, secret field, and absolute-path cases fail closed.
    - Captured stdout/stderr never contains locator strings, raw journal bytes, project paths, credentials, payloads, transcripts, or environment values; failure codes are bounded.
    - The script is absent from package `files`, AgentMo public help/dispatch, plugin assets, and packed release manifests.
  </behavior>
  <action>Write the synthetic tests first. Build an exact two-entry journal fixture plus project diagnosis fixture entirely under disposable private repositories; never use or search for the retained real attempt. Import every authority function from `src/builder-codex-uat-private-authority.js`, exercise the retained preflight in-process, and spawn `scripts/preflight-codex-uat-prior-attempt.js` in fresh child processes for local-stdin transport and each exact admission/transition command. Assert `bootstrapPrivateAuthorityRoot` cannot run until the complete retained-handle locator preflight returns PASS; invalid locator and every preflight rejection leave root/receipt absent. Cover anchored absent-only root creation and fsync, existing-root exact admission, closed receipt publish/load/admit, continuation initial publish/load and every legal CAS/immutable transition. Include wrong/stale heads, replacement between checks, unsafe ancestors, foreign uid, wrong modes, symlinks, partial/duplicate publication, schema/tool digest drift, receipt entry identity/raw digest/entry-set/count drift, illegal continuation transitions, secret keys, and machine-absolute operational paths. Model foreign uid and unsafe mode through the same pure metadata-policy function used by production; no test-only bypass may mint a passing authority. Snapshot fixture paths before every rejection and assert bounded output contains no secret or absolute path.</action>
  <acceptance_criteria>The suite fails because the retained preflight/receipt contract does not yet exist, and every required locator, authority-root, publication, schema, and drift negative case has a zero-unowned-write assertion plus value-blind output assertion.</acceptance_criteria>
  <verify><automated>node --test test/builder-codex-uat-prior-attempt.test.js</automated></verify>
  <done>The exact machine-checkable contract and all drift/unsafe-input failures are captured before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the single-process no-follow preflight</name>
  <files>src/builder-codex-uat-private-authority.js, scripts/preflight-codex-uat-prior-attempt.js, test/builder-codex-uat-prior-attempt.test.js</files>
  <read_first>test/builder-codex-uat-prior-attempt.test.js, src/builder-codex-uat.js, src/builder-immutable-journal.js, src/builder-doctor.js, src/persistability.js, test/helpers/io-surface-inventory.js, test/artifact-surface-coverage.test.js, package.json</read_first>
  <action>Implement the dependency-free production authority in `src/builder-codex-uat-private-authority.js` and keep the CLI as a thin direct importer; do not duplicate exact loaders in the script. The `run-prior-preflight` command reads the bounded locator only from local stdin or an in-process Buffer/object, rejects locator-bearing argv/environment/logs, then clears the locator reference after retaining the explicitly supplied safe ancestor chain, journal directory, base, and successor no-follow handles. Validate exact two-file entry set, canonical identities, stable dev/ino/uid/mode/nlink/size/timestamps, raw SHA-256, both `loadCodexUatAttemptJournal` calls around read-only `diagnoseBuilderInstall`, known count/terminal/code/head, and before/after equality while handles remain open. Only after all those checks return PASS call `bootstrapPrivateAuthorityRoot`, then `publishPriorPreflightReceipt` and `admitPriorPreflightReceipt`; invalid input must leave fixed root/receipt absent. Implement the exact root/receipt semantics and all continuation exports specified in `<interfaces>`, including O_EXCL/fsync/modes/current uid/no-follow stable handles, closed fields, fixed-root-relative paths, predecessor/head compare-and-swap or immutable transition journal, and legal state matrix. Expose fixed-target `admit-prior-preflight-receipt`, `init-continuation`, `admit-continuation`, and `transition-continuation` commands that call those same production exports and emit only bounded status/head/digest/error codes. Reject unsafe, stale, replacement, partial, duplicate, wrong-schema/digest/state, secret-bearing, or absolute-path inputs without alternate target or repair. Keep module and CLI repository-private per D-25/D-30.</action>
  <acceptance_criteria>The positive fixture passes only with the same retained handles across both loader calls/diagnosis and one exact absent-only receipt; every unsafe/drift/authority/publication case exits non-zero without altering the old journal or a pre-existing receipt; receipt bytes validate exactly and contain no locator/machine-absolute/raw material; package/public CLI surfaces remain unchanged.</acceptance_criteria>
  <verify><automated>node --test test/builder-codex-uat-prior-attempt.test.js &amp;&amp; git diff --check</automated></verify>
  <done>The actual old-attempt gate now has a runnable retained-handle implementation, but no real locator or UAT state has been read.</done>
</task>

<task type="auto">
  <name>Task 3: Close preflight I/O inventory and artifact boundaries in the introducing plan</name>
  <files>test/helpers/io-surface-inventory.js, test/artifact-surface-coverage.test.js</files>
  <read_first>AGENTS.md, src/builder-codex-uat-private-authority.js, scripts/preflight-codex-uat-prior-attempt.js, src/builder-codex-uat.js, src/builder-immutable-journal.js, src/builder-doctor.js, package.json, test/helpers/io-surface-inventory.js, test/artifact-surface-coverage.test.js, test/builder-codex-uat-prior-attempt.test.js</read_first>
  <action>Inventory every filesystem/process surface added by both `src/builder-codex-uat-private-authority.js` and `scripts/preflight-codex-uat-prior-attempt.js` at exact discovered IDs in `IO_SURFACE_ALLOWLIST`, owner `phase-02-plan-23`: local-stdin locator intake; retained journal reads; anchored root bootstrap/admission/sync; receipt publish/load/admit; continuation initial publish/load/CAS-or-immutable transition/admit; and bounded stdout/stderr. Extend artifact coverage so discovered and declared IDs are exactly equal across module, CLI, schemas, subcommands, child-process paths, and all negative/partial/replacement branches, with every row non-pending. Add negative assertions that both repository-private authority files and their private receipt/continuation artifacts are absent from package `files`, AgentMo public CLI/help/dispatch, plugin assets, packed manifest/release assets, and Builder runtime/import closure, while the CLI's direct import of the production authority and required journal/diagnosis readers remain covered. Do not defer first inventory, artifact coverage, or private/public boundary enforcement to 02-24; it may only rerun this already-green contract.</action>
  <acceptance_criteria>The exact script/src I/O set equals its allowlist with zero pending/unclassified rows; durable receipt producer/validator/consumer ownership is explicit; repository package/help/plugin/packed/runtime surfaces reject the diagnostic tool and private receipt; the focused 02-23 gate passes before its summary exists.</acceptance_criteria>
  <verify><automated>node --test test/builder-codex-uat-prior-attempt.test.js test/artifact-surface-coverage.test.js test/builder-package-security.test.js test/builder-packed-install.test.js &amp;&amp; git diff --check</automated></verify>
  <done>02-23 is self-closing for its new I/O, durable receipt, and negative package boundary; 02-24 has only aggregate recheck work.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 (block_on: high)

| Threat | STRIDE | Severity | Disposition | Mitigation / test |
| --- | --- | --- | --- | --- |
| Wrong historical target | Spoofing/Elevation | critical | mitigate | Direct stdin locator plus exact two filenames, known terminal code/head, and no search/guess path. |
| Historical evidence mutation | Tampering/Repudiation | critical | mitigate | Retained directory/two-file handles, stable identity/raw SHA, entry-set and loader/diagnosis equality, appended=false. |
| Unsafe fixed-root bootstrap | Tampering/Elevation | critical | mitigate | Bootstrap is gated on full preflight PASS, anchored absent-only 0700/current-uid creation and fsync; existing roots require exact no-follow identity admission. |
| Stale continuation transition | Spoofing/Tampering | critical | mitigate | One production no-follow loader plus predecessor/head CAS or immutable successor and a closed legal-state matrix. |
| Unsafe ancestor or symlink | Tampering/Elevation | critical | mitigate | Current uid, safe mode, no-follow ancestor/directory/file checks before reads. |
| Locator disclosure | Information disclosure | critical | mitigate | Stdin/in-process only; no argv/env/log/persistence; output schema has no locator/path/raw-byte slots. |
| Self-certified Markdown evidence | Spoofing | high | mitigate | Closed JSON schema plus exact machine checks; no regex-derived approval. |
| Accidental product authority | Elevation | high | mitigate | Repository-only tool excluded from packed files, plugin, and public CLI. |
</threat_model>

<verification>The synthetic suite, exact I/O/artifact coverage, negative package-boundary checks, and diff hygiene pass in 02-23; source inspection confirms no actual locator search and no packed/public exposure. The real old attempt remains untouched.</verification>

<success_criteria>A tested repository-only CLI can exact-admit the known two-entry failure through retained handles and emit only value-blind machine evidence; no actual preflight or new attempt has run.</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

- `src/builder-codex-uat-private-authority.js` with fixed-root bootstrap, closed prior-receipt publish/load/admit, and closed continuation publish/load/transition authority.
- `scripts/preflight-codex-uat-prior-attempt.js` as the repository-private CLI importing that production authority for retained preflight and exact fixed-target admission/transition subcommands.
- `test/builder-codex-uat-prior-attempt.test.js` covering all required drift, ownership, mode, symlink, missing-input, transport, and disclosure failures.
- `test/helpers/io-surface-inventory.js` and `test/artifact-surface-coverage.test.js` with exact introducing-plan I/O ownership and negative package-boundary coverage.
- A fixed private `.omx/codex-uat/phase-02-final-retry/prior-preflight.receipt.json` runtime receipt only when an operator later runs the command; no receipt is created during plan execution tests against the real attempt.
- No packed asset, public AgentMo CLI route, actual-attempt evidence, or certification claim.
</artifacts_produced>

<output>Create `.planning/phases/02-codex-builder/02-23-SUMMARY.md` when done.</output>
