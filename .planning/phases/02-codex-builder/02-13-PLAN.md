---
phase: 02-codex-builder
plan: "13"
type: execute
wave: 13
depends_on: ["02-12"]
files_modified:
  - src/builder-codex-host.js
  - src/builder-install.js
  - src/builder-lifecycle.js
  - src/cli.js
  - test/builder-codex-host.test.js
  - test/builder-install-security.test.js
  - test/builder-lifecycle.test.js
  - test/builder-cli.test.js
autonomous: true
gap_closure: true
requirements: [BLDR-01, BLDR-06]
must_haves:
  truths:
    - "The fixed `agentmo-local` marketplace is registered from one exact user-owned projection independent of every consumer project; project receipts contain consumer references only."
    - "A fresh isolated Codex home registers and observes the stable projection before adding the AgentMo selector, and exact project cwd/source visibility cannot be satisfied by another project or nested decoy."
    - "Removing one project never removes or relocates the user-host marketplace/plugin while any exact consumer remains; zero-consumer removal is a distinct approved owner transaction."
    - "Marketplace rebind/transfer is explicit, and a multi-consumer release migration either moves every admitted consumer plus host projection atomically or fails closed before host mutation."
    - "A post-add failure performs exact authorized compensation or preserves blocking recovery authority; owner/consumer publication never adopts, restores, or deletes a foreign inode."
  artifacts:
    - path: src/builder-codex-host.js
      provides: Stable user-owned marketplace projection, exact host observation/mutation, and identity-safe owner/consumer authority
    - path: src/builder-install.js
      provides: Preview-bound activation transaction and exact post-add compensation
    - path: src/builder-lifecycle.js
      provides: Consumer-only project removal plus explicit transfer, all-consumer migration, and zero-reference owner operations
    - path: src/cli.js
      provides: Closed preview/apply commands for host migration/transfer/removal with no arbitrary Codex command surface
  key_links:
    - from: packed release
      to: stable user-owned marketplace projection
      via: Exact release bytes are projected outside every consumer project before fixed official marketplace registration
    - from: project receipt
      to: user-host consumer ledger
      via: Exact project-scope/release/receipt digest reference only; no project owns the marketplace source
    - from: all-consumer migration preview
      to: apply
      via: One digest binds current owner/ledger, every consumer admission, baseline/successor release, stable projection, and ordered host operations
---

<objective>
Make fresh Codex activation and lifecycle use one stable user-host marketplace owner with exact consumer transactions.

Purpose: Close CR-01, CR-02, CR-03, WR-03, and the host/install portion of WR-05 while incorporating the validated user-host ownership model from Spike 001.
Output: Stable marketplace projection, exact registration/visibility, compensating activation, explicit transfer/all-consumer migration, and consumer-safe removal.
</objective>

## Phase Goal

**As a** developer using AgentMo Builder from multiple projects, **I want to** share one stable Codex host installation without making any project its owner, **so that** project removal, upgrade, transfer, and failure cannot break another consumer.

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-codex-builder/02-CONTEXT.md
@.planning/phases/02-codex-builder/02-RESEARCH.md
@.planning/phases/02-codex-builder/02-VERIFICATION.md
@.planning/phases/02-codex-builder/02-REVIEW.md
@.planning/spikes/001-codex-marketplace-ownership/README.md
@.planning/phases/02-codex-builder/02-12-SUMMARY.md
@src/builder-codex-host.js
@src/builder-install.js
@src/builder-lifecycle.js

<interfaces>
- `resolveBuilderCodexMarketplaceRoot(...)` derives the stable AgentMo-owned `agentmo-local` projection beneath the current user's AgentMo state root, never beneath a consumer project and never from caller-provided HOME/CODEX_HOME overrides inside production APIs.
- `observeCodexHost(...)` returns bounded marketplace, selector, project-cwd, source-root, owner, and consumer observations tied to the admitted host/user scope. Duplicate, warning-bearing, foreign-cwd, nested-source, missing-source, or ambiguous rows fail closed.
- `mutateCodexHost(...)` exposes fixed official marketplace add/remove and plugin add/remove operations. Callers provide admitted closed plans, never arbitrary argv, executable, cwd, or environment.
- `planBuilderHostProjectionMigration(...)` binds baseline/successor release identities, stable projection identity, owner/ledger digests, every consumer's exact receipt/project-scope admission, and all affected project operations. Apply is all-or-none; if any consumer is absent, changed, unavailable, or unapproved, no host or project mutation begins.
- `planBuilderHostProjectionTransfer(...)` explicitly binds an admitted current owner/root and an absent or exact-owned target root under the AgentMo user state boundary. Transfer/rebind never occurs as an implicit side effect of project setup, upgrade, or uninstall.
- Project uninstall consumes one exact project reference. Only a separately approved zero-consumer owner plan may remove the selector and AgentMo-created marketplace registration/projection.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Own and observe one stable user-host marketplace projection</name>
  <files>src/builder-codex-host.js, test/builder-codex-host.test.js</files>
  <read_first>AGENTS.md, .planning/phases/02-codex-builder/02-CONTEXT.md, .planning/phases/02-codex-builder/02-REVIEW.md, .planning/spikes/001-codex-marketplace-ownership/README.md, src/builder-codex-host.js, src/builder-install.js, test/builder-codex-host.test.js</read_first>
  <behavior>
    - The marketplace source is one exact stable user-owned root independent of project A or B; deleting either project does not break host listing for the survivor.
    - Plugin add is rejected until that exact marketplace is registered and re-observed; cached plugin bytes cannot compensate for a missing configured source.
    - A pre-existing exact registration is distinguished from AgentMo-created state; foreign, duplicate, missing-source, warning-bearing, or ambiguous rows fail closed.
    - Skill/hook visibility is accepted only for the exact admitted project cwd and documented exact plugin root; foreign cwd, nested/prefix decoys, and duplicate rows fail.
    - Owner/ledger/stable-projection authority binds dev/ino/nlink/size/digest/uid/gid/mode and cannot adopt a same-byte replacement.
  </behavior>
  <action>Write Spike-001 parity tests and CR-01/CR-03/WR-03/WR-05 host regressions first. Replace the project-local marketplace source assumption with one fixed `agentmo-local` projection under the AgentMo user state root. Register and observe this exact stable source before selector add, and make the fake host reject add/list when registration is absent or its source disappeared. Require exact project cwd and exact canonical plugin root for visibility. Refactor owner/ledger/projection publication and recovery to retain stage/prior handles through commit, verify identity and safe metadata before and after exclusive publication, and operate only on admitted inodes. Keep project paths out of durable value-blind evidence; project consumer identity is its canonical scope digest plus exact receipt/release binding. Implement D-17 through D-19, D-24, D-25, D-28, and D-30.</action>
  <acceptance_criteria>Two projects sharing one isolated host continue to list the plugin after either project disappears; a missing stable source breaks observation despite cache; wrong cwd/root and foreign/duplicate rows cannot satisfy visibility; same-byte/unsafe authority replacements are preserved and rejected.</acceptance_criteria>
  <verify><automated>node --test test/builder-codex-host.test.js</automated></verify>
  <done>The fixed marketplace and selector have one stable user-host owner independent of all projects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply and compensate one exact activation transaction</name>
  <files>src/builder-install.js, src/cli.js, test/builder-install-security.test.js, test/builder-cli.test.js</files>
  <read_first>src/builder-codex-host.js, src/builder-install.js, src/cli.js, test/builder-install-security.test.js, test/builder-cli.test.js</read_first>
  <behavior>
    - Setup preview binds stable marketplace projection/root digest, selector/source/release, project consumer, host scope, prior observation, and fixed operation order; apply requires the same digest.
    - Fixed operation order is projection publication/observation, marketplace add when absent, exact re-observation, plugin add when absent, exact selector/visibility observation, owner/consumer publication, then project receipt last.
    - Failure after marketplace/plugin mutation re-observes exact state while the reservation remains held and either restores the pre-state or retains blocking recovery evidence.
    - Compensation never removes a pre-existing registration/plugin or any resource with another exact consumer.
    - CLI output is value-blind and accepts no caller-defined host command, executable, cwd, environment, or implicit transfer.
  </behavior>
  <action>Add CR-02 injected failures at post-registration, post-add, owner publication, consumer publication, managed-file verification, and receipt-last boundaries. Extend setup preview/apply to the stable projection and fixed host order. Track each submitted host mutation inside the held reservation; on later failure, re-observe selector/source/release/marketplace/owner/ledger and run fixed official compensation only for resources absent before, created by this transaction, and still exact with zero other consumers. Otherwise preserve exact owner/reservation/recovery evidence and block retry/removal. Expose only closed setup and host-owner preview/apply shapes through `src/cli.js`; no implicit rebind or host upgrade is allowed.</action>
  <acceptance_criteria>Every injected post-host-mutation failure restores the exact pre-state or leaves durable blocking recovery authority; project receipt remains last; pre-existing/shared host state is never removed; plan drift fails before mutation.</acceptance_criteria>
  <verify><automated>node --test test/builder-install-security.test.js test/builder-cli.test.js test/builder-codex-host.test.js</automated></verify>
  <done>Fresh activation is one exact compensating user-host transaction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce consumer-safe removal, explicit transfer, and atomic multi-consumer migration</name>
  <files>src/builder-lifecycle.js, src/cli.js, test/builder-lifecycle.test.js, test/builder-cli.test.js</files>
  <read_first>src/builder-lifecycle.js, src/builder-install.js, src/builder-codex-host.js, src/cli.js, test/builder-lifecycle.test.js, test/builder-cli.test.js</read_first>
  <behavior>
    - Project uninstall removes only its exact consumer and project-owned assets; any remaining consumer keeps the marketplace projection and selector unchanged.
    - The final consumer produces a separate zero-reference owner preview; only its exact approved apply removes selector, registration, and owned projection.
    - Rebind/transfer requires an explicit preview binding old/new root identities, owner/ledger, host observations, and all affected consumers; ordinary lifecycle never transfers ownership.
    - A host release change with multiple consumers requires every exact consumer receipt/project scope and an all-consumer plan; missing, stale, modified, or unapproved consumers cause zero mutation.
    - Injected failure during an approved all-consumer migration restores the entire baseline or retains blocking recovery authority; it never leaves mixed release bindings presented as success.
  </behavior>
  <action>Write two-consumer removal, transfer/rebind, partial-authorization, stale-consumer, no-op-release, and mid-migration failure tests before implementation. Keep project uninstall consumer-only. Add closed preview/apply lifecycle operations for zero-consumer owner removal, stable-root transfer, and all-consumer release migration. Bind the exact baseline/successor version and release digest, projection bytes, owner/ledger, host observation, each project scope/receipt/managed-file plan, and fixed operation order into one approval digest. Require distinct versions/releases and all exact consumers before mutation. Use Plan 02-12 retirement and retained recovery to stage all affected project/host bytes; publish the new host projection and consumer states as one reservation-held transaction, or restore/retain the baseline and fail closed. Implement D-18, D-19, D-24, D-26, and D-30 without creating a lifecycle journal.</action>
  <acceptance_criteria>One project cannot remove shared host state; zero-reference removal and transfer require separate exact approvals; multi-consumer upgrade either moves every admitted consumer to one distinct successor release or leaves the exact baseline authoritative with a bounded failure.</acceptance_criteria>
  <verify><automated>node --test test/builder-lifecycle.test.js test/builder-cli.test.js test/builder-codex-host.test.js &amp;&amp; npm run check &amp;&amp; git diff --check</automated></verify>
  <done>User-host ownership, transfer, migration, and project removal are explicit and consumer-safe.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Threat Register

| Threat ID | STRIDE | Component | Severity | Disposition | Mitigation / limitation |
| --- | --- | --- | --- | --- | --- |
| T-02-13-01 | Spoofing | marketplace/selector visibility | critical | mitigate | Stable source, exact canonical cwd/root, unique rows, and warning/error rejection close CR-01/WR-03. |
| T-02-13-02 | Tampering | post-add transaction | critical | mitigate | Reservation-held re-observation plus exact compensation or durable recovery closes CR-02. |
| T-02-13-03 | Tampering | owner/consumer publication | critical | mitigate | Retained stage/prior handles and safe metadata close CR-03/WR-05. |
| T-02-13-04 | Tampering | multi-consumer migration | critical | mitigate | All-consumer exact preview and all-or-none apply prevent split release authority. |
| T-02-13-05 | Elevation of privilege | transfer/remove CLI | high | mitigate | Closed operations, exact approval digests, and no arbitrary host command surface. |
| T-02-13-06 | Information disclosure | host/consumer evidence | high | mitigate | Bounded digests/codes exclude raw host payloads, project paths, credentials, transcripts, and outputs. |
</threat_model>

<finding_coverage>
| Finding / spike result | Task | Negative proof | Disposition |
| --- | --- | --- | --- |
| CR-01 / Spike 001 | Task 1 | Host rejects selector without stable registered source; survivor remains visible after project removal | mitigated |
| CR-02 | Task 2 | Every post-add failure restores exact state or retains blocking authority | mitigated |
| CR-03 | Task 1 | Same-byte stage/retained/canonical swaps cannot publish or restore authority | mitigated |
| WR-03 | Task 1 | Foreign cwd/nested source/duplicate rows cannot satisfy visibility | mitigated |
| WR-05 host/install | Tasks 1-2 | Unexpected uid and writable modes fail before authority use | mitigated |
| Multi-consumer upgrade/transfer | Task 3 | Partial authorization, stale consumer, and mid-apply failure cannot split host/consumer release state | mitigated |
</finding_coverage>

<source_coverage>
| Source | ID | Requirement / decision | Plan coverage | Status |
| --- | --- | --- | --- | --- |
| GOAL | — | Clean packed install and ownership-safe upgrade/uninstall | Tasks 1-3 | COVERED |
| REQ | BLDR-01 | Fresh-host marketplace/plugin activation without checkout dependency | Tasks 1-2 | COVERED |
| REQ | BLDR-06 | Exact-owned lifecycle with foreign/shared preservation | Tasks 2-3 | COVERED |
| RESEARCH | — | Official host operations, one packed product, receipt-last publication | Tasks 1-3 | COVERED |
| CONTEXT | D-17/D-18/D-19/D-24/D-25/D-26/D-28/D-30 | Support boundary, setup, capability, diagnosis, lifecycle, evidence, sole UAT authority | Tasks 1-3 | COVERED |
| SPIKE | 001 | User-host scope, stable source, consumer-only projects, explicit transfer/all-consumer migration | Tasks 1-3 | COVERED |
</source_coverage>

<verification>
- Host, install-security, lifecycle, and CLI suites pass, including two-consumer, transfer, and all-consumer migration matrices.
- Fake and isolated hosts refuse activation without the stable marketplace source and preserve shared state until zero consumers.
- `npm run check` and `git diff --check` pass after Task 3.
</verification>

<success_criteria>
- CR-01, CR-02, CR-03, WR-03, and host/install WR-05 have hostile regressions.
- Fixed `agentmo-local` source survives removal of any consumer project.
- Transfer and multi-consumer upgrade are explicit exact transactions; incomplete authority produces zero mutation or bounded retained recovery, never split success.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

- Stable user-owned `agentmo-local` marketplace projection and exact owner record.
- Exact marketplace/selector/project visibility operations and compensating activation.
- Consumer-only project lifecycle plus explicit transfer, all-consumer migration, and zero-reference owner actions.
- Hostile user-host ownership, race, metadata, compensation, and multi-consumer transaction tests.
- Summary `.planning/phases/02-codex-builder/02-13-SUMMARY.md`.
</artifacts_produced>

<output>
Create `.planning/phases/02-codex-builder/02-13-SUMMARY.md` when done.
</output>
