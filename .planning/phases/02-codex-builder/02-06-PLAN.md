---
phase: 02-codex-builder
plan: "06"
type: execute
wave: 6
depends_on: ["02-05"]
files_modified:
  - src/builder-codex-host.js
  - src/builder-package.js
  - src/builder-install.js
  - src/builder-doctor.js
  - src/cli.js
  - package.json
  - test/builder-codex-host.test.js
  - test/builder-packed-install.test.js
  - test/builder-doctor.test.js
autonomous: true
gap_closure: true
requirements: [BLDR-01, BLDR-05]
must_haves:
  truths:
    - Codex activation uses only fixed official user-host operations after an exact preview explicitly selects hostScope user.
    - One user-scope selector owner record is separate from a sorted project-consumer ledger; project receipts reference both but never gain deletion authority.
    - A new host module is present in the canonical packed runtime inventory, package syntax-check closure and clean tarball import closure in the same change.
    - Doctor distinguishes projected, installed, enabled, skill-visible, hooks-visible, trust and agent projected/visible/unobservable states without repair.
  artifacts:
    - path: src/builder-codex-host.js
      provides: Fixed official Codex observation/mutation adapter and selector owner/consumer model
    - path: src/builder-install.js
      provides: Explicit user-host activation bound to project receipt-last setup
    - path: src/builder-doctor.js
      provides: Read-only normalized host and agent observability diagnosis
    - path: test/builder-packed-install.test.js
      provides: Packed CLI activation and complete import-closure proof
  key_links:
    - from: src/builder-codex-host.js
      to: src/builder-package.js
      via: New runtime module is added to the fixed release inventory
    - from: src/builder-install.js
      to: src/builder-codex-host.js
      via: Exact preview-bound official operation and owner/consumer CAS
    - from: src/builder-doctor.js
      to: src/builder-codex-host.js
      via: Observer-only interface with no mutation capability
---

<objective>
Add official user-host Codex activation, selector ownership/reference accounting and read-only diagnosis on top of the packed local runtime.

Purpose: Turn project projection into scope-honest official activation without direct cache/config/trust writes or shared-selector overclaim.
Output: Packed host adapter, explicit activation flow, owner/consumer evidence and truthful doctor states.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-codex-builder/02-CONTEXT.md
@.planning/phases/02-codex-builder/02-PATTERNS.md
@.planning/phases/02-codex-builder/02-VERIFICATION.md
@.planning/phases/02-codex-builder/02-05-SUMMARY.md
@src/builder-package.js
@src/builder-install.js
@src/builder-doctor.js
@src/cli.js
@package.json

<interfaces>
- builder-codex-host exports separate read-only observeCodexHost and bounded mutateCodexHost functions. Doctor receives only the observer.
- The normalized host selector is fixed by AgentMo release identity and official Codex interface; callers cannot supply argv, cache path, claimed state or raw host output.
- Current official add/remove behavior is user-host scoped. Project preview without explicit hostScope user reports explicit-user-host-scope-required and mutates nothing.
- Consumer ID is a deterministic digest of the exact project scope. The value-blind owner record says created-by-agentmo or preexisting-unowned; the ledger is a canonical sorted set of consumer ID, project-scope digest and release digest.
- Project receipt binds exact owner-record and consumer-ledger digests. It owns only its consumer entry.
- Agent observation has separate projection and visibility fields; absence of an official host query is projected-but-host-unobservable.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the fixed official host adapter and close its packed import surface</name>
  <files>src/builder-codex-host.js, src/builder-package.js, package.json, test/builder-codex-host.test.js, test/builder-packed-install.test.js</files>
  <read_first>src/builder-probe.js, src/builder-package.js, package.json, test/codex-builder-probe.test.js, test/builder-packed-install.test.js</read_first>
  <behavior>
    - Read-only observation uses fixed official command/RPC shapes, shell false, bounded timeout/output and a minimal environment, then exposes only normalized value-blind facts.
    - Mutation accepts only the fixed selector and add/remove operation supplied by admitted AgentMo state; arbitrary argv, host paths and caller success claims are rejected.
    - Owner record and consumer ledger writers use exact prior-digest CAS, absent-output/no-clobber publication and canonical sorted bytes outside Codex-owned cache/config/trust state.
    - The new module appears in the fixed runtime inventory and package.json node --check list; a clean unpacked tarball imports and exercises it with no source checkout.
    - Omitting the module from any one of inventory, syntax closure or tarball closure makes focused tests fail.
  </behavior>
  <action>Write fixed-call, output-redaction, ledger-CAS and missing-packed-module tests first. Create the host adapter with distinct observer/mutator exports and closed normalized states. Store only value-blind AgentMo owner/reference artifacts outside host-owned state. In the same task, add the new production module to the canonical runtime inventory and package.json syntax-check command, and extend the clean packed import-closure assertion. No direct Codex cache/config/trust write is permitted. This implements D-17, D-19, D-24 and D-25.</action>
  <acceptance_criteria>The only Codex host boundary is fixed, bounded and value-blind, and its production module is inseparable from packed and syntax-check closure.</acceptance_criteria>
  <verify>
    <automated>node --test test/builder-codex-host.test.js test/builder-packed-install.test.js</automated>
  </verify>
  <done>Official host observation/mutation and selector owner/consumer primitives exist in the exact packed runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind explicit user-host activation to setup and packed CLI execution</name>
  <files>src/builder-install.js, src/cli.js, test/builder-codex-host.test.js, test/builder-packed-install.test.js</files>
  <read_first>src/builder-install.js, src/builder-codex-host.js, src/cli.js, test/builder-codex-host.test.js, test/builder-packed-install.test.js</read_first>
  <behavior>
    - Default project preview makes no host mutation and reports that explicit user host scope is required.
    - Preview/apply accepts only --host-scope user, binds operation, selector, source/release, owner disposition, prior/desired owner/ledger digests and expected post-observation into planDigest.
    - First AgentMo-created activation writes owner plus one consumer; a preexisting selector is recorded without removal authority; a sibling project adds only a sorted second consumer.
    - Project files and ledger are revalidated before official add; installed/enabled/skill/hooks visibility is re-observed before the project receipt publishes last.
    - Trust pending human is reported honestly. Official failure, stale ledger, forged observation or ambiguous rollback creates no success receipt and no deletion authority.
    - Packed CLI coverage exercises preview and apply, replacing redundant direct builder-cli test edits.
  </behavior>
  <action>Extend setup parsing and plan/apply flow with the single explicit user-host scope. Bind all official operation and owner/reference inputs into the exact preview, then revalidate project bytes and user artifacts at every boundary. Call only the fixed adapter; re-observe installed/enabled/skill/hooks facts and publish the project receipt last. Keep trust as a separate human-owned state and keep agent/domain/production flags false. Exercise the real packed CLI route rather than caller-injected host claims. This implements D-17 through D-20, D-24 and D-28.</action>
  <acceptance_criteria>Only an exact explicitly approved user-scope plan can activate Codex, and each project gains one reference rather than ownership of shared host state.</acceptance_criteria>
  <verify>
    <automated>node --test test/builder-codex-host.test.js test/builder-packed-install.test.js</automated>
  </verify>
  <done>Clean packed setup performs scope-honest official activation with receipt-last project evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Diagnose activation, trust, references and agent observability without mutation</name>
  <files>src/builder-doctor.js, test/builder-doctor.test.js, test/builder-codex-host.test.js</files>
  <read_first>src/builder-doctor.js, src/builder-codex-host.js, src/builder-install.js, test/builder-doctor.test.js, test/builder-codex-host.test.js</read_first>
  <behavior>
    - Doctor reports project projection, user-host installation, enablement, skill visibility, hooks visibility, trust, owner disposition and consumer presence as distinct bounded facts.
    - Agent status distinguishes missing projection, pristine projection, host-visible and projected-but-host-unobservable.
    - Corrupt/missing/stale owner or ledger evidence, selector/source mismatch and observation disagreement are inconsistent, never auto-repaired.
    - Before/after project and user-artifact snapshots plus mutator spies prove zero add/remove, writer, trust or direct host-state changes.
  </behavior>
  <action>Pass doctor only observeCodexHost and stable-read helpers. Normalize agent projection separately from official visibility, including the explicit unobservable state. Validate the exact receipt/marker/owner/consumer bindings produced by setup, discard raw output and paths, and return bounded remediation codes only. Snapshot both project and AgentMo user evidence to prove byte-for-byte read-only behavior. This implements D-17, D-24, D-25 and D-28.</action>
  <acceptance_criteria>Doctor accurately exposes projection, activation, trust and agent observability without possessing or exercising repair authority.</acceptance_criteria>
  <verify>
    <automated>node --test test/builder-doctor.test.js test/builder-codex-host.test.js</automated>
  </verify>
  <done>Host and agent state are visible as separate read-only facts, including projected-but-host-unobservable.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Trust Boundaries

| Boundary | Description |
| --- | --- |
| project setup -> user Codex host | A project command requests a user-scope official mutation. |
| official host output -> normalized evidence | Untrusted process output is reduced to bounded facts. |
| project receipt -> owner/consumer artifacts | Project evidence references shared state without owning deletion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-02-06-01 | Spoofing | host observation | high | mitigate | Fixed official selector/commands, normalized states and exact observation digest. |
| T-02-06-02 | Tampering | owner/consumer evidence | high | mitigate | Canonical bytes, external prior digest, CAS, sorted ledger and post-write admission. |
| T-02-06-03 | Repudiation | user-scope activation | medium | mitigate | Exact plan binds scope, selector, owner disposition and pre/post observation. |
| T-02-06-04 | Information disclosure | host output and user state | high | mitigate | Bounded capture, minimal environment and value-blind reports only. |
| T-02-06-05 | Denial of service | official child process | medium | mitigate | Fixed timeout/output cap and non-terminal failure without success receipt. |
| T-02-06-06 | Elevation of privilege | implicit host mutation/deletion | critical | mitigate | Explicit user scope and separate owner authority; project receipt owns one reference only. |
| T-02-06-SC | Tampering | dependency installation | low | accept | No dependency install; official Codex operation is separately scoped and fixed. |
</threat_model>

<source_coverage>
- GOAL official clean-host activation and truthful diagnosis: Tasks 1-3.
- REQ BLDR-01/BLDR-05: host install/visibility and doctor coverage are complete; destructive lifecycle remains 02-07.
- RESEARCH/PATTERNS official host interface, shared selector ownership, agent observability and no direct host-state writes: Tasks 1-3.
- CONTEXT D-17/D-18/D-19/D-20/D-24/D-25/D-28: cited in actions. D-26 is 02-07; D-21..23/D-27 continue in 02-08..10.
</source_coverage>

<verification>
- node --test test/builder-codex-host.test.js test/builder-packed-install.test.js test/builder-doctor.test.js
- npm pack --dry-run --json
- npm run check
- git diff --check
</verification>

<success_criteria>
- Host activation requires explicit user scope and uses only fixed official operations.
- Owner/consumer evidence prevents project receipts from acquiring selector deletion authority.
- New host code is covered by package inventory, package.json syntax checks and a clean packed import test.
- Doctor reports agent projected/visible/unobservable and remains read-only.
</success_criteria>

<artifacts_produced>
- Packed src/builder-codex-host.js with fixed observer/mutator and owner/consumer primitives.
- Explicit user-host setup plan/apply contract and receipt bindings.
- Read-only activation, trust and agent-observability diagnosis.
- Focused host, packed CLI/import-closure and doctor evidence.
</artifacts_produced>

<output>
Create .planning/phases/02-codex-builder/02-06-SUMMARY.md when done.
</output>
