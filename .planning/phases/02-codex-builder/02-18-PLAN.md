---
phase: 02-codex-builder
plan: "18"
type: execute
wave: 18
depends_on: ["02-17"]
files_modified:
  - src/builder-codex-uat.js
  - src/cli.js
  - scripts/verify-codex-uat-candidate.js
  - test/builder-codex-uat.test.js
  - test/builder-cli.test.js
  - test/builder-packed-install.test.js
  - test/codex-builder-behavior.test.js
  - test/builder-hook-bridge.test.js
autonomous: true
gap_closure: true
requirements: [BLDR-07]
must_haves:
  truths:
    - "直接导入生产模块不能再以原始 details 伪造 setup、activation、trust/auth、scenario 或 human-decision。"
    - "每个非终止 UAT 转换都从对应 exact package/receipt/host/checkpoint/observation admission 派生；人工观察仍明确是非密码学证据。"
    - "只有完成 packed package、tarball、manifest、verifier、自身 bytes、journal 与 candidate 全部复核的单一操作才能追加一次人工决定。"
  artifacts:
    - path: src/builder-codex-uat.js
      provides: Closed UAT transition and verifier-derived decision authority
    - path: scripts/verify-codex-uat-candidate.js
      provides: Sole packed preview/decision entrypoint over the closed verifier operation
    - path: test/builder-codex-uat.test.js
      provides: Direct-import authority-bypass regressions
  key_links:
    - from: src/cli.js
      to: src/builder-codex-uat.js
      via: CLI dispatch invokes only transition-specific operations
    - from: scripts/verify-codex-uat-candidate.js
      to: candidate-ready journal head
      via: one verifier-derived capability/operation with no caller-selected verifier digest
---

<objective>
关闭 CR-01 与 CR-02，使 UAT 历史和 human decision 只能通过 exact evidence admission 与 packed verifier 路径推进（D-17、D-27～D-31）。

Purpose: 防止“合法格式的伪造历史”或直接 import 绕过实际安装、观察与 verifier。
Output: 收窄的 transition API、原子 verifier decision operation、CLI/packed hostile regression。
</objective>

## Phase Goal

**As a** developer installing AgentMo Builder in Codex, **I want to** install and run the packaged Builder on a clean Codex host and recover its workflow across pause, compaction, restart, upgrade, and uninstall with provable asset ownership, **so that** I can use and audit AgentMo Builder safely without depending on the AgentMo source repository.

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-codex-builder/02-CONTEXT.md
@.planning/phases/02-codex-builder/02-REVIEW.md
@.planning/phases/02-codex-builder/02-VERIFICATION.md
@.planning/phases/02-codex-builder/02-17-SUMMARY.md

<interfaces>
- Current unsafe exports are `appendCodexUatAttemptEntry`, `inspectCodexUatCandidateForHumanDecision`, and `decideCodexUatCandidate`; remove them from the public module surface.
- Preserve `loadCodexUatAttemptJournal`, `armCodexUatScenario`, `recordCodexUatScenarioObservation`, candidate leaf-first ordering, and all false certification fields.
- The two-entry terminated actual attempt and both journal-chain files are immutable evidence: tests and implementation must never open them for write, resume them, repair them, delete them, or reinterpret them.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce raw transition and decision bypasses</name>
  <files>test/builder-codex-uat.test.js, test/builder-cli.test.js, test/builder-packed-install.test.js, test/codex-builder-behavior.test.js, test/builder-hook-bridge.test.js</files>
  <read_first>AGENTS.md, .planning/phases/02-codex-builder/02-REVIEW.md, src/builder-codex-uat.js, src/cli.js, scripts/verify-codex-uat-candidate.js, test/builder-codex-uat.test.js, test/builder-cli.test.js, test/builder-packed-install.test.js, test/codex-builder-behavior.test.js, test/builder-hook-bridge.test.js</read_first>
  <behavior>
    - A module namespace import exposes no generic raw appender and no inspect/decide pair that accepts a caller-selected verifier digest.
    - Synthetic raw setup/activation/scenario details cannot reach candidate-ready.
    - A child process importing production modules cannot append human-admission without completing the exact packed self-check.
    - Stale head/candidate, another tarball, tampered verifier, and repeated decision leave journal bytes unchanged.
  </behavior>
  <action>Write failing direct-import and child-process regressions first, using only disposable temporary journals. Replace every complete-history fixture that fabricates setup, activation, trust/auth, scenario, candidate, or decision state through the raw export, including the full behavior fixture in `test/codex-builder-behavior.test.js` and the installed-hook fixture in `test/builder-hook-bridge.test.js`, with evidence-derived closed transition operations. Preserve D-29: the tests prove closed application authority, not cryptographic Codex origin.</action>
  <acceptance_criteria>The new tests fail on the reviewed implementation for CR-01/CR-02, every full-history fixture uses legal transition operations, and rejected attempts are distinguished from successfully appended decisions by byte-for-byte journal snapshots.</acceptance_criteria>
  <verify><automated>node --test test/builder-codex-uat.test.js test/builder-cli.test.js test/builder-packed-install.test.js test/codex-builder-behavior.test.js test/builder-hook-bridge.test.js</automated></verify>
  <done>The two reviewer bypasses have deterministic failing reproductions before production edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace generic append with evidence-derived transitions</name>
  <files>src/builder-codex-uat.js, src/cli.js, test/builder-codex-uat.test.js, test/builder-cli.test.js, test/builder-packed-install.test.js, test/codex-builder-behavior.test.js, test/builder-hook-bridge.test.js</files>
  <read_first>src/builder-codex-uat.js, src/cli.js, src/builder-install.js, src/builder-codex-host.js, src/builder-checkpoint.js, test/builder-codex-uat.test.js, test/builder-cli.test.js, test/builder-packed-install.test.js, test/codex-builder-behavior.test.js, test/builder-hook-bridge.test.js</read_first>
  <action>Make the low-level appender module-private. Add narrowly named operations for attempt genesis, setup admission, activation admission, bounded human trust/auth observation, and failure/interruption; each operation re-loads the exact current head and derives its entry from the relevant loaded package, receipt, host observation, checkpoint, or value-blind operator admission instead of accepting arbitrary entry details. Keep `recordCodexUatScenarioObservation` as the only scenario-success path and preserve fixed order, D-29 non-cryptographic wording, D-30 one-way journal edges, and D-31 failure terminal behavior. Migrate CLI dispatch plus every repository consumer in `src/`, `scripts/`, and `test/`, including the behavior and installed-hook fixtures, to the closed operations; reject unknown or extra authority fields. Run a repository-wide direct-import/use search and record the exact remaining module-private definition and legal transition consumers in the summary so no old public consumer is omitted.</action>
  <acceptance_criteria>No production export accepts arbitrary UAT kind/details; CLI and every full-history fixture perform legal transitions through exact admissions; repository-wide direct-import/use inspection finds no old public consumer; scenario success still requires checkpoint plus observation admissions; failure/interruption remain bounded and cannot resume.</acceptance_criteria>
  <verify><automated>node --test test/builder-codex-uat.test.js test/builder-cli.test.js test/builder-packed-install.test.js test/codex-builder-behavior.test.js test/builder-hook-bridge.test.js</automated></verify>
  <done>CR-01 is closed without weakening D-29 or the single-journal state machine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make packed verification the decision authority</name>
  <files>src/builder-codex-uat.js, scripts/verify-codex-uat-candidate.js, test/builder-codex-uat.test.js, test/builder-packed-install.test.js</files>
  <read_first>src/builder-codex-uat.js, scripts/verify-codex-uat-candidate.js, src/builder-package.js, test/builder-codex-uat.test.js, test/builder-packed-install.test.js</read_first>
  <action>Replace the public inspect/decide pair with one closed verifier operation that itself reads and hashes the exact tarball, freshly extracted package/manifest, fixed verifier asset, complete current journal and candidate, derives verifierDigest from admitted bytes, and appends at most one decision after revalidation. The script becomes a thin bounded CLI over that operation and never supplies an authoritative digest. Record the exact read/write/import delta in the summary for the single canonical inventory reconciliation in 02-23. Preserve D-28 and D-31 false certification flags and value-blind output.</action>
  <acceptance_criteria>Direct import cannot mint decision authority from caller data; only the complete self-check can decide; all hostile cases append nothing; the summary enumerates every changed I/O/import surface for 02-23.</acceptance_criteria>
  <verify><automated>node --test test/builder-codex-uat.test.js test/builder-cli.test.js test/builder-packed-install.test.js test/codex-builder-behavior.test.js test/builder-hook-bridge.test.js &amp;&amp; git diff --check</automated></verify>
  <done>CR-02 is closed and the intended packed verifier route is the only human-decision authority.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 (block_on: high)

| Threat | STRIDE | Severity | Disposition | Mitigation / test |
| --- | --- | --- | --- | --- |
| Authority forgery | Spoofing/Elevation | critical | mitigate | Remove raw exports; direct-import/child-process tests require evidence-derived transition and self-verifying decision. |
| Pathname replacement | Tampering | high | mitigate | No UAT is permitted until 02-21 closes journal final windows and 02-22 closes leaf publication; this plan snapshots rejected journals. |
| Foreign inode deletion | Tampering | high | mitigate | No cleanup authority is added; 02-21 and 02-22 are blocking dependencies before UAT. |
| False-negative durable commit | Repudiation/DoS | high | mitigate | 02-21 must expose explicit committed/recovery status before any new attempt. |
| Symlink or unsafe ancestor | Tampering | high | mitigate | Packed verifier continues no-follow/exact-layout checks; final retained-parent hardening is required in 02-21. |
| Secret/transcript disclosure | Information disclosure | high | mitigate | Schemas and CLI outputs remain value-blind; tests use synthetic digests only and never read `.env`. |
</threat_model>

<verification>Focused UAT/CLI/packed tests pass; no raw authority export remains; the exact inventory delta is handed to 02-23; `git diff --check` passes. This is mechanism evidence only.</verification>

<success_criteria>CR-01 and CR-02 have failing-then-passing hostile regressions, all legal callers use closed operations, and no change touches or resumes the terminated actual attempt.</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

- New public transition-specific UAT operations and one atomic verifier decision operation; exact names are recorded in the summary.
- Removed public raw append/inspect/decide authority.
- No new package asset, no actual UAT artifact, and no domain/production compatibility claim.
</artifacts_produced>

<output>Create `.planning/phases/02-codex-builder/02-18-SUMMARY.md` when done.</output>
