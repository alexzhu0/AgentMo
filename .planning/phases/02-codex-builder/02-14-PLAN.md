---
phase: 02-codex-builder
plan: "14"
type: execute
wave: 14
depends_on: ["02-13"]
files_modified:
  - src/javascript-static-analysis.js
  - src/builder-package.js
  - src/builder-doctor.js
  - package.json
  - test/builder-package-security.test.js
  - test/builder-doctor.test.js
  - test/helpers/io-surface-inventory.js
  - test/artifact-surface-coverage.test.js
autonomous: true
gap_closure: true
requirements: [BLDR-02, BLDR-05]
must_haves:
  truths:
    - "Release closure accounts for every admitted static/dynamic import, CommonJS require, createRequire alias, JSON/CJS dependency, and worker-like local loader; unresolved authority rejects the release."
    - "The I/O inventory analyzes whole JavaScript syntax and rejects an unclassifiable filesystem call rather than omitting it."
    - "Doctor retains and rechecks the canonical project-root-to-final identity chain and reports unsafe on any parent/final swap or unsafe uid/mode."
    - "Doctor diagnoses checkpoint state only through the Wave 11 unique-chain loader, and package closure contains that loader plus every runtime consumer without checkout fallback."
    - "Probe and doctor remain read-only, value-blind, and non-certifying."
  artifacts:
    - path: src/javascript-static-analysis.js
      provides: Shared complete-or-reject loader and filesystem-I/O analysis
    - path: src/builder-package.js
      provides: Complete runtime closure including immutable checkpoint infrastructure
    - path: src/builder-doctor.js
      provides: Retained-chain diagnosis plus read-only unique-checkpoint inspection
    - path: test/artifact-surface-coverage.test.js
      provides: WR-06 hostile syntax/I-O fixtures and exact inventory gate
  key_links:
    - from: src/builder-package.js
      to: src/javascript-static-analysis.js
      via: Every runtime module is completely classified or release admission fails
    - from: src/builder-doctor.js
      to: src/builder-checkpoint.js
      via: Diagnostic-only unique-chain API; no logical checkpoint file parsing
    - from: test/helpers/io-surface-inventory.js
      to: src/javascript-static-analysis.js
      via: Whole-file syntax records replace line regex authority
---

<objective>
Close package, doctor, and I/O evidence gaps while migrating their checkpoint consumers to the immutable authority from Wave 11.

Purpose: Resolve WR-02, WR-04, WR-06, and remaining diagnostic/runtime WR-05 without widening support claims.
Output: Complete-or-reject JavaScript analysis, packed runtime closure, retained-chain read-only doctor, and exact I/O inventory.
</objective>

## Phase Goal

**As a** developer probing or diagnosing an installed Builder, **I want to** trust that every reported dependency, file identity, and checkpoint head belongs to this exact installation, **so that** read-only diagnostics never convert ambiguity into readiness.

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-codex-builder/02-CONTEXT.md
@.planning/phases/02-codex-builder/02-RESEARCH.md
@.planning/phases/02-codex-builder/02-VERIFICATION.md
@.planning/phases/02-codex-builder/02-REVIEW.md
@.planning/phases/02-codex-builder/02-11-SUMMARY.md
@.planning/phases/02-codex-builder/02-13-SUMMARY.md
@src/builder-package.js
@src/builder-doctor.js

<interfaces>
- `analyzeJavaScriptSource(source, options)` classifies loader/I-O-relevant syntax across comments, strings, templates, regexes, multiline calls, aliases, optional chains, and computed literal members; unresolved security-relevant syntax returns one stable rejection.
- Loader records cover import/export-from, literal dynamic import, CommonJS require, `createRequire`, local ESM/CJS/JSON, and worker/fork-like local entry points. Nonliteral targets, eval/Function-created loading, escaping aliases, or unclassified secondary loaders reject package admission.
- `diagnoseBuilderInstall(...)` uses retained ancestry/final handles and the read-only checkpoint diagnostic API from 02-11. It never parses an overwriteable checkpoint pathname or mutates host/project state.
- `BUILDER_RELEASE_ASSET_INVENTORY` includes the immutable journal module, checkpoint/event adapters, analyzer, and every admitted runtime consumer needed after packing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make packed runtime closure and I/O inventory complete-or-reject</name>
  <files>src/javascript-static-analysis.js, src/builder-package.js, package.json, test/builder-package-security.test.js, test/helpers/io-surface-inventory.js, test/artifact-surface-coverage.test.js</files>
  <read_first>AGENTS.md, .planning/phases/02-codex-builder/02-CONTEXT.md, .planning/phases/02-codex-builder/02-REVIEW.md, src/builder-package.js, src/builder-immutable-journal.js, src/builder-checkpoint.js, package.json, test/builder-package-security.test.js, test/helpers/io-surface-inventory.js, test/artifact-surface-coverage.test.js</read_first>
  <behavior>
    - `createRequire(import.meta.url)` plus literal local CJS/JSON includes that exact transitive closure.
    - Nonliteral import/require/createRequire, eval, Function, worker/fork entry expressions, escaping aliases, and unknown secondary loaders reject admission.
    - URL strings, regex literals, comments, templates, multiline calls, aliases, and computed literal fs members cannot hide an I/O surface.
    - Dynamic computed fs members, reassigned aliases, and unclassifiable calls fail coverage rather than disappearing.
    - Packed inventory includes Wave 11 immutable journal/checkpoint/event modules and every consumer without source-checkout fallback.
  </behavior>
  <action>Write WR-04/WR-06 fixtures first, including local CJS/JSON through `createRequire`, dynamic targets, eval/Function, Worker/fork, URL/regex decoys, multiline calls, computed members, and alias reassignment. Add a dependency-free whole-file analyzer that either classifies every security-relevant construct or rejects the source. Replace the ESM-only package collector and line-based I/O scanner with shared records. Recursively admit exact ESM/CJS/JSON dependencies, include `src/builder-immutable-journal.js` and its runtime consumers in package/syntax inventories, and reject path escape, unsupported extension, dynamic authority, or checkout fallback. Implement D-15, D-16, D-18, and D-24.</action>
  <acceptance_criteria>WR-04 fixtures include every exact secondary dependency or reject before packing; WR-06 fixtures appear at exact source locations or produce a stable rejection; a packed release cannot omit the immutable checkpoint runtime or any admitted dependency.</acceptance_criteria>
  <verify><automated>node --test test/builder-package-security.test.js test/artifact-surface-coverage.test.js</automated></verify>
  <done>Every packed loader and I/O surface is exactly closed or rejected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retain doctor path identity and consume the unique checkpoint chain</name>
  <files>src/builder-doctor.js, test/builder-doctor.test.js</files>
  <read_first>src/builder-doctor.js, src/builder-package.js, src/builder-checkpoint.js, src/builder-immutable-journal.js, test/builder-doctor.test.js</read_first>
  <behavior>
    - Doctor retains every canonical ancestor and the final entry, then rechecks all identities before classification.
    - Parent-to-symlink swap, same-byte final swap, swap-back, changed nlink, unexpected uid, or group/world-write mode returns unsafe.
    - Checkpoint status is derived only from one bounded unique chain; fork, gap, orphan, malformed entry, or unsafe metadata is diagnostic failure, never a selected head.
    - Diagnostic evidence contains bounded relative identity/digests but no raw bytes, absolute private paths, output, transcript, payload, or credential.
    - Probe/doctor perform zero writes, repair, trust, activation, lifecycle, checkpoint, or journal mutation.
  </behavior>
  <action>Add WR-02 and diagnostic/runtime WR-05 races first, including seams before final open, during read, before classification, plus fork/gap/orphan checkpoint fixtures. Refactor diagnostic reads to retain the canonical project-root-to-final chain, compare handle/path identity and safe metadata before and after reading, and classify any drift as unsafe. Replace every semantic checkpoint read with the Wave 11 read-only diagnostic loader; report unique head digest/sequence or bounded chain error without minting resume/host authority. Keep doctor strictly read-only and non-certifying per D-24, D-25, D-28, D-29, and D-30.</action>
  <acceptance_criteria>Parent/final swap and unsafe metadata never become pristine/valid; fork/gap/orphan checkpoint state never yields a chosen head; syscall assertions prove zero diagnostic writes or host mutations.</acceptance_criteria>
  <verify><automated>node --test test/builder-doctor.test.js test/builder-package-security.test.js test/artifact-surface-coverage.test.js &amp;&amp; npm run check &amp;&amp; git diff --check</automated></verify>
  <done>Doctor and package consumers share one exact immutable checkpoint/runtime boundary.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Threat Register

| Threat ID | STRIDE | Component | Severity | Disposition | Mitigation / limitation |
| --- | --- | --- | --- | --- | --- |
| T-02-14-01 | Tampering | runtime dependency closure | high | mitigate | Whole-file complete-or-reject analysis closes WR-04. |
| T-02-14-02 | Elevation of privilege | dynamic/secondary loaders | critical | mitigate | Exact literal closure or rejection prevents escaped runtime code. |
| T-02-14-03 | Tampering | I/O inventory | high | mitigate | Syntax-aware records and unresolved-form rejection close WR-06. |
| T-02-14-04 | Elevation of privilege | doctor traversal/metadata | high | mitigate | Retained chain plus uid/mode checks close WR-02/WR-05. |
| T-02-14-05 | Spoofing | checkpoint diagnosis | high | mitigate | Unique-chain loader rejects fork/gap/orphan rather than selecting state. |
| T-02-14-06 | Information disclosure | diagnostic evidence | high | mitigate | Closed bounded fields exclude raw bytes, private paths, outputs, transcripts, and credentials. |
</threat_model>

<finding_coverage>
| Finding | Task | Negative proof | Disposition |
| --- | --- | --- | --- |
| WR-02 | Task 2 | Parent/final swap and swap-back never classify external bytes as valid | mitigated |
| WR-04 | Task 1 | createRequire/CJS/JSON/worker/eval forms are exactly closed or rejected | mitigated |
| WR-05 doctor/runtime | Task 2 | Unexpected uid and writable modes fail diagnosis | mitigated |
| WR-06 | Task 1 | URL/regex/multiline/computed/alias fixtures are inventoried or fail | mitigated |
| Wave 11 consumer migration | Tasks 1-2 | Packed runtime includes immutable modules; doctor uses unique-chain API | mitigated |
</finding_coverage>

<source_coverage>
| Source | ID | Requirement / decision | Plan coverage | Status |
| --- | --- | --- | --- | --- |
| GOAL | — | Accurate clean packed diagnosis and recoverable checkpoint visibility | Tasks 1-2 | COVERED |
| REQ | BLDR-02 | Exact capability/runtime evidence | Tasks 1-2 | COVERED |
| REQ | BLDR-05 | Strictly read-only doctor | Task 2 | COVERED |
| RESEARCH | — | Self-contained release, exact identity, no checkout fallback, read-only diagnosis | Tasks 1-2 | COVERED |
| CONTEXT | D-15/D-16/D-17/D-18/D-24/D-25/D-28/D-29/D-30 | Neutral contract, package, diagnosis, evidence boundaries | Tasks 1-2 | COVERED |
</source_coverage>

<verification>
- Package-security, doctor, and artifact-surface suites pass.
- Static fixtures prove every admitted loader/I-O surface is classified and doctor uses the unique checkpoint loader.
- `npm run check` and `git diff --check` pass after Task 2.
</verification>

<success_criteria>
- WR-02, WR-04, WR-05 diagnostic/runtime, and WR-06 have hostile regressions.
- Packed completeness no longer depends on ESM-only or line-regex assumptions.
- Doctor cannot use swapped paths, unsafe metadata, forked checkpoint state, or raw logical-file parsing as valid evidence.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

- Dependency-free complete-or-reject JavaScript loader/I-O analyzer.
- Complete ESM/CJS/JSON/secondary-loader packed runtime graph including Wave 11 modules.
- Whole-file syntax-aware I/O inventory.
- Retained-chain, metadata-safe, unique-checkpoint read-only doctor.
- Summary `.planning/phases/02-codex-builder/02-14-SUMMARY.md`.
</artifacts_produced>

<output>
Create `.planning/phases/02-codex-builder/02-14-SUMMARY.md` when done.
</output>
