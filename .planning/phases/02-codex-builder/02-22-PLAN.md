---
phase: 02-codex-builder
plan: "22"
type: execute
wave: 21
depends_on: ["02-21"]
files_modified:
  - src/builder-codex-uat.js
  - src/builder-codex-uat-continuation.js
  - src/builder-hook-bridge.js
  - test/builder-codex-uat.test.js
  - test/builder-packed-install.test.js
  - test/builder-hook-bridge.test.js
autonomous: true
gap_closure: true
requirements: [BLDR-04, BLDR-07]
must_haves:
  truths:
    - "Candidate and observation leaves publish only after exact staged bytes and retained safe parent authority are synced."
    - "A short write, sync failure, final-name race, parent replacement, or interruption cannot leave a partial digest-named final leaf that poisons retry."
    - "Installed hook and packed continuation pass retained attempt-directory authority while preserving leaf-to-journal one-way references."
  artifacts:
    - path: src/builder-codex-uat.js
      provides: Anchored staged publication for candidate and observation leaves
    - path: src/builder-codex-uat-continuation.js
      provides: Retained attempt-directory authority across uninstall continuation
    - path: test/builder-packed-install.test.js
      provides: Packed interruption, retry, and parent-replacement regressions
  key_links:
    - from: src/builder-hook-bridge.js
      to: publishCodexUatObservationLeaf
      via: retained no-follow attempt-directory authority rather than caller pathname alone
    - from: src/builder-codex-uat-continuation.js
      to: publishCodexUatCandidate
      via: exact retained parent and absent-only digest publication before candidate-ready
---

<objective>
关闭 WR-01，并将 candidate/observation leaf 与 packed continuation 独立于 journal commit repair 完成（D-27、D-28、D-29、D-30、D-31）。

Purpose: 防止部分 digest-name 文件或被替换目录永久阻塞唯一 UAT chain。
Output: Retained-parent leaf publisher、continuation/hook wiring 与 packed hostile tests。
</objective>

## Phase Goal

**As a** developer installing AgentMo Builder in Codex, **I want to** install and run the packaged Builder on a clean Codex host and recover its workflow across pause, compaction, restart, upgrade, and uninstall with provable asset ownership, **so that** I can use and audit AgentMo Builder safely without depending on the AgentMo source repository.

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-codex-builder/02-CONTEXT.md
@.planning/phases/02-codex-builder/02-REVIEW.md
@.planning/phases/02-codex-builder/02-21-SUMMARY.md
@src/builder-codex-uat.js
@src/builder-codex-uat-continuation.js
@src/builder-hook-bridge.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce partial leaf and retained-parent failures</name>
  <files>test/builder-codex-uat.test.js, test/builder-packed-install.test.js, test/builder-hook-bridge.test.js</files>
  <read_first>AGENTS.md, .planning/phases/02-codex-builder/02-REVIEW.md, src/builder-codex-uat.js, src/builder-codex-uat-continuation.js, src/builder-hook-bridge.js, test/builder-codex-uat.test.js, test/builder-packed-install.test.js, test/builder-hook-bridge.test.js</read_first>
  <behavior>
    - Short write or sync failure leaves no digest-named final candidate/observation.
    - Parent replacement and final-name race preserve competing bytes and append no journal reference.
    - Restart can retry an operation-owned staged failure but cannot adopt a foreign final file.
    - Packed continuation and installed hook reject a stale or replaced retained parent before publication.
  </behavior>
  <action>Add failing seams before final link/rename, after stage sync, and before parent sync for both candidate and observation publication. Cover continuation and installed-hook callers, exact candidate-before-candidate-ready ordering, and D-29/D-31 false claims.</action>
  <acceptance_criteria>The reviewed direct-final-path writer fails interruption/race assertions, and every rejected path proves journal, leaf, and competitor byte sets unchanged.</acceptance_criteria>
  <verify><automated>node --test test/builder-codex-uat.test.js test/builder-packed-install.test.js test/builder-hook-bridge.test.js</automated></verify>
  <done>WR-01 has deterministic negative seams across both leaf types and both packed callers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stage, sync, and publish leaves under retained authority</name>
  <files>src/builder-codex-uat.js, src/builder-codex-uat-continuation.js, src/builder-hook-bridge.js</files>
  <read_first>src/builder-codex-uat.js, src/builder-codex-uat-continuation.js, src/builder-hook-bridge.js, src/builder-immutable-journal.js, test/builder-codex-uat.test.js, test/builder-packed-install.test.js, test/builder-hook-bridge.test.js</read_first>
  <action>Acquire a current-user-owned, non-group/world-writable, no-follow parent and retain its identity through the whole operation. Write and sync a random exclusive stage, absent-only publish that exact open inode to its content-addressed name, sync the retained parent, then retire only exact operation-owned leftovers; ambiguity preserves all competitors and returns a bounded conflict. Pass this authority from installed hook and packed continuation rather than resolving an untrusted pathname again. Preserve D-30 acyclic leaf-to-journal direction and require the legal journal transition to reference the published digest afterward. Record every I/O/import/artifact delta in the summary for 02-23.</action>
  <acceptance_criteria>No failure leaves partial final bytes; exact retries succeed or fail closed on a foreign occupant; parent drift is detected; packed callers cannot widen authority.</acceptance_criteria>
  <verify><automated>node --test test/builder-codex-uat.test.js test/builder-packed-install.test.js test/builder-hook-bridge.test.js &amp;&amp; git diff --check</automated></verify>
  <done>WR-01 is closed and the complete inventory delta is ready for 02-23.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 (block_on: high)

| Threat | STRIDE | Severity | Disposition | Mitigation / test |
| --- | --- | --- | --- | --- |
| Partial final leaf | Tampering/DoS | critical | mitigate | Stage, sync, exact absent-only publication, and retained-parent sync precede authority. |
| Parent replacement | Tampering/Elevation | critical | mitigate | No-follow retained parent identity is checked through final publication. |
| Foreign inode deletion | Tampering | high | mitigate | Only exact operation-owned stages retire; competitor bytes are preserved. |
| Reverse evidence authority | Spoofing | high | mitigate | Leaves remain non-authoritative; only a later journal transition references their exact digest. |
| Evidence disclosure | Information disclosure | high | mitigate | Leaves and diagnostics stay value-blind and contain no path, transcript, payload, output, environment, or credential. |
</threat_model>

<verification>Focused UAT/packed/hook tests and `git diff --check` pass; 02-23 owns final inventory, closure, full-suite, review, and documentation gates.</verification>

<success_criteria>WR-01 is closed, both leaf types survive interruption and competition safely, and no actual UAT artifact is created.</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

- Retained-parent staged publisher for candidate and observation leaves.
- Packed continuation and installed-hook authority wiring.
- No new public certification, actual attempt, or human decision.
</artifacts_produced>

<output>Create `.planning/phases/02-codex-builder/02-22-SUMMARY.md` when done.</output>
