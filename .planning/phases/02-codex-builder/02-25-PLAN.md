---
phase: 02-codex-builder
plan: "25"
type: execute
wave: 24
depends_on: ["02-24"]
files_modified:
  - .omx/codex-uat/phase-02-final-retry/prior-preflight.receipt.json
autonomous: false
gap_closure: true
requirements: [BLDR-01, BLDR-04, BLDR-05, BLDR-06, BLDR-07]
must_haves:
  truths:
    - "The operator runs the 02-23 read-only preflight locally and supplies the raw prior-attempt locator only to that command's stdin; the locator never enters a Codex response, prompt, argv, environment, log, or durable artifact."
    - "A fresh continuation exact-loads the fixed private receipt and revalidates schema, bytes, identities, digests, entry set/count, known failure terminal/head, appended=false, uid/mode/no-symlink, and tool/schema digest before asking for new-attempt authority."
    - "A separate decision checkpoint explicitly approves or rejects exactly one new attempt; missing receipt, EEXIST ambiguity, or any mismatch stops without scanning or guessing."
  artifacts:
    - path: .omx/codex-uat/phase-02-final-retry/prior-preflight.receipt.json
      provides: Fixed-path mode-0600 value-blind `agentmo.codex-uat-prior-preflight.v1` admission receipt
  key_links:
    - from: local terminal stdin
      to: scripts/preflight-codex-uat-prior-attempt.js
      via: raw locator consumed only by the local command and discarded after retained-handle admission
    - from: .omx/codex-uat/phase-02-final-retry/prior-preflight.receipt.json
      to: fresh continuation and new-attempt decision
      via: `admit-prior-preflight-receipt` calling `admitPriorPreflightReceipt` for fixed-path no-follow stable-handle schema/digest/identity/uid/mode revalidation
---

<objective>
Exact-admit the preserved failed attempt through the operator-run read-only command, then collect a separate bounded decision for one new isolated attempt (D-25、D-27～D-31).

Purpose: Replace unsupported checkpoint payload transport with a fixed, private, value-blind receipt that a fresh executor can admit.
Output: One private preflight receipt and one non-secret approve/do-not-run decision; no new attempt.
</objective>

## Phase Goal

**As a** developer installing AgentMo Builder in Codex, **I want to** install and run the packaged Builder on a clean Codex host and recover its workflow across pause, compaction, restart, upgrade, and uninstall with provable asset ownership, **so that** I can use and audit AgentMo Builder safely without depending on the AgentMo source repository.

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-codex-builder/02-CONTEXT.md
@.planning/phases/02-codex-builder/02-24-SUMMARY.md
@.planning/phases/02-codex-builder/02-24-REVIEW.md
@.planning/phases/02-codex-builder/02-VALIDATION.md
@src/builder-codex-uat-private-authority.js
@scripts/preflight-codex-uat-prior-attempt.js

<interfaces>
- Fixed private authority root: `.omx/codex-uat/phase-02-final-retry/`; fixed receipt: `prior-preflight.receipt.json`.
- The operator command is `node scripts/preflight-codex-uat-prior-attempt.js run-prior-preflight`; it imports `bootstrapPrivateAuthorityRoot` from `src/builder-codex-uat-private-authority.js` and may create the fixed root only after the locator retained-handle preflight is completely PASS. Invalid/missing locator or any old-attempt mismatch leaves both root and receipt absent.
- The fresh exact-admission command is `node scripts/preflight-codex-uat-prior-attempt.js admit-prior-preflight-receipt --require-pass`; it calls production `admitPriorPreflightReceipt` against the fixed target and is the only receipt gate. A direct JSON parse or selected-field check cannot authorize Task 3.
- The receipt is absent-only, mode `0600`, under a gitignored/current-user-owned/non-symlink/mode-`0700` root. It contains no locator, filename supplied by the locator, machine-absolute path, raw bytes, transcript, payload, environment value, credential, or raw stdout/stderr.
- Checkpoint responses are only `done`, `approve-new-attempt`, or `do-not-run`. You will NOT be resumed after either checkpoint; the named continuation consumer is a fresh executor and must exact-load durable state before proceeding.
- Missing receipt, `EEXIST` ambiguity, schema/digest/identity mismatch, unsafe authority root, or non-PASS 02-24 gate stops without search, enumeration, inference, repair, retry, or alternate target.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Run the local read-only prior-attempt preflight</name>
  <files>None</files>
  <read_first>AGENTS.md, .planning/phases/02-codex-builder/02-24-REVIEW.md, src/builder-codex-uat-private-authority.js, scripts/preflight-codex-uat-prior-attempt.js</read_first>
  <action>Ask the operator to open a local terminal at the repository root, run `node scripts/preflight-codex-uat-prior-attempt.js run-prior-preflight`, and type the bounded locator JSON only into that command's stdin. The script must first complete the old journal's retained-handle loader/diagnosis/reload checks; only its PASS branch may call production `bootstrapPrivateAuthorityRoot` and `publishPriorPreflightReceipt` to create the anchored fixed root and fixed receipt. Invalid locator or any failed/ambiguous preflight must leave both absent. The command must not place the locator in Codex, argv, environment, shell-expanded text, logs, or files. The operator replies only `done` after a bounded success status and provides no locator/path/output. Pause immediately: this checkpoint performs no command, read, write, validation, diagnosis, search, or UAT mutation. You will NOT be resumed.</action>
  <continuation_consumer>Fresh continuation for Task 2; it ignores all ephemeral state and exact-loads only `.omx/codex-uat/phase-02-final-retry/prior-preflight.receipt.json`.</continuation_consumer>
  <acceptance_criteria>The response is exactly `done`; no locator/private path/raw output is present in the response; the fresh consumer has only the fixed receipt path as authority.</acceptance_criteria>
  <resume_signal>Reply `done` after the local command reports bounded success; paste nothing else.</resume_signal>
  <done>Task 2 receives no raw locator and must independently admit the fixed receipt.</done>
</task>

<task type="auto">
  <name>Task 2: Exact-load the fixed receipt in a fresh continuation</name>
  <files>None</files>
  <read_first>AGENTS.md, .planning/phases/02-codex-builder/02-24-REVIEW.md, .planning/phases/02-codex-builder/02-VALIDATION.md, src/builder-codex-uat-private-authority.js, scripts/preflight-codex-uat-prior-attempt.js</read_first>
  <action>After `done`, run `node scripts/preflight-codex-uat-prior-attempt.js admit-prior-preflight-receipt --require-pass` in the fresh executor. This command must call production `admitPriorPreflightReceipt`, retain a no-follow stable receipt handle, and exact-admit fixed root/receipt parent, current uid, modes `0700`/`0600`, stable identities/bytes, closed schema, tool/schema digest, exactly two base/successor identities and raw digests, entry-set digest/count `2`, known failure kind/code/head, all before/after equalities, and `appended:false`. Do not implement or accept a separate `readFileSync`/JSON selected-field gate. Re-run the 02-24 aggregate, validation, and zero-Critical/no-blocking-high gates only after the production admission command returns bounded PASS. Any absence, `EEXIST` ambiguity, replacement, mismatch, or stale prerequisite stops without scanning, guessing, repair, another target, or new root.</action>
  <acceptance_criteria>One fixed receipt and all prerequisite gates exact-admit; no raw locator is available; failure leaves all new-attempt state absent.</acceptance_criteria>
  <verify><automated>node --test test/builder-codex-uat-prior-attempt.test.js test/artifact-surface-coverage.test.js &amp;&amp; node scripts/preflight-codex-uat-prior-attempt.js admit-prior-preflight-receipt --require-pass</automated></verify>
  <done>The fixed receipt is exact-admitted by a fresh executor and is eligible for the separate authorization decision.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: Authorize exactly one new isolated attempt</name>
  <files>None</files>
  <read_first>.planning/phases/02-codex-builder/02-24-REVIEW.md, .planning/phases/02-codex-builder/02-VALIDATION.md</read_first>
  <action>Present only bounded PASS fields from Task 2 and ask whether to authorize exactly one new attempt. Collect `approve-new-attempt` or `do-not-run`, then pause immediately. This checkpoint performs no command, file operation, path selection, release build, setup, host inspection, Codex invocation, journal append, or documentation update. You will NOT be resumed.</action>
  <continuation_consumer>Fresh completion consumer records the non-secret option in `02-25-SUMMARY.md`; 02-26 Task 1 must exact-load that summary plus the fixed receipt and revalidate both before any effect.</continuation_consumer>
  <decision>Whether 02-26 may create exactly one fresh isolated actual attempt.</decision>
  <options>
    <option id="approve-new-attempt"><name>Approve new attempt</name><pros>Allows one bounded actual retry after all gates.</pros><cons>Permits private host/UAT state and a real Codex invocation.</cons></option>
    <option id="do-not-run"><name>Do not run</name><pros>Leaves new-attempt state absent.</pros><cons>Produces a bounded no-attempt outcome.</cons></option>
  </options>
  <acceptance_criteria>Exactly one non-secret option is collected; no attempt effect occurs in the checkpoint.</acceptance_criteria>
  <resume_signal>Select `approve-new-attempt` or `do-not-run`.</resume_signal>
  <done>02-26 can exact-admit one bounded option from the completed summary; no new attempt has started.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 (block_on: high)

| Threat | STRIDE | Severity | Disposition | Mitigation / test |
| --- | --- | --- | --- | --- |
| Locator disclosure | Information disclosure | critical | mitigate | Locator enters only local command stdin; response is `done`/`abort`; receipt is value-blind. |
| Fresh-continuation confusion | Spoofing/Elevation | critical | mitigate | Fixed-path exact load; no ephemeral checkpoint state is authority. |
| Receipt replacement/reuse | Tampering/Repudiation | critical | mitigate | 0700/0600, uid/no-symlink, O_EXCL, exact identities/digests, and EEXIST hard stop. |
| Unauthorized new attempt | Elevation | critical | mitigate | Separate non-secret decision after receipt and aggregate gate admission. |
| Certification inflation | Spoofing | high | mitigate | Receipt proves bounded preservation only; D-28 through D-31 claims remain false/unproven. |
</threat_model>

<verification>The fresh consumer exact-loads one fixed value-blind receipt and prerequisite gates; only a separate non-secret decision can authorize 02-26.</verification>

<success_criteria>The preserved two-entry failure is exact-admitted without locator transport through Codex, and exactly one bounded decision controls whether a new attempt may exist.</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

- Fixed private `prior-preflight.receipt.json` produced by the operator-run 02-23 command.
- `02-25-SUMMARY.md` records only the bounded approval option and receipt digest/status.
- No new UAT root, tarball, journal, host mutation, Codex session, or certification claim.
</artifacts_produced>

<output>Create `.planning/phases/02-codex-builder/02-25-SUMMARY.md` without locator/private paths/raw output; preserve only the bounded decision and receipt digest/status.</output>
