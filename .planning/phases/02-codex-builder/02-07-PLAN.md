---
phase: 02-codex-builder
plan: "07"
type: execute
wave: 7
depends_on: ["02-06"]
files_modified:
  - src/builder-lifecycle.js
  - src/builder-codex-host.js
  - src/cli.js
  - test/builder-lifecycle.test.js
  - test/builder-codex-host.test.js
  - test/builder-cli.test.js
autonomous: true
gap_closure: true
requirements: [BLDR-01, BLDR-06]
must_haves:
  truths:
    - Upgrade/uninstall derives every project runtime path from the canonical release/receipt inventory and preserves the existing hostile-filesystem controls.
    - Project lifecycle removes only its exact consumer reference and never changes a selector still referenced by a sibling project.
    - Even the last project reference does not authorize selector removal; explicit user-owner action with exact external owner/ledger digests is required.
    - Preexisting, shared, foreign, corrupt, modified or raced state is preserved, and lifecycle never writes Codex cache/config/trust bytes directly.
  artifacts:
    - path: src/builder-lifecycle.js
      provides: Runtime and host-reference-aware exact preview/apply lifecycle
    - path: src/builder-codex-host.js
      provides: Selector owner/consumer CAS and fixed official removal
    - path: test/builder-lifecycle.test.js
      provides: Runtime race, sibling and last-reference refusal matrix
  key_links:
    - from: src/builder-lifecycle.js
      to: src/builder-package.js
      via: Canonical release/receipt inventory is consumed rather than duplicated
    - from: src/builder-lifecycle.js
      to: src/builder-codex-host.js
      via: Separately admitted project receipt, owner record, consumer ledger and observation
    - from: src/cli.js
      to: selector owner removal
      via: Explicit user-scope flags plus one-to-one external digest bindings
---

<objective>
Give the packed runtime and shared user-host selector/reference state the same reversible, ownership-safe upgrade/uninstall semantics as existing Builder assets.

Purpose: Preserve user and sibling assets while making project and user-host lifecycle authority exact and explicit.
Output: Runtime lifecycle parity, sibling-safe reference removal and separately authorized last-reference selector removal.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-codex-builder/02-CONTEXT.md
@.planning/phases/02-codex-builder/02-PATTERNS.md
@.planning/phases/02-codex-builder/02-VERIFICATION.md
@.planning/phases/02-codex-builder/02-06-SUMMARY.md
@src/builder-package.js
@src/builder-install.js
@src/builder-lifecycle.js
@src/builder-codex-host.js
@src/cli.js

<interfaces>
- Every project lifecycle preview and apply requires external builder-install-receipt digest admission and exact planDigest.
- Runtime expected files come from the admitted receipt plus canonical release inventory. No second handwritten runtime list is allowed.
- Existing quarantine directory, retained inode/digest, no-clobber link publication, nlink-one checks, staged-byte sync/digest checks, stale-quarantine refusal and receipt-last postconditions remain mandatory.
- Project uninstall CAS-removes only its deterministic consumer entry. Empty ledger is evidence for a possible owner action, not deletion authority.
- User selector removal requires explicit hostScope user plus removeHostSelector, exact external owner-record and consumer-ledger digests, created-by-agentmo disposition, empty ledger and official post-observed absence.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend three-way lifecycle safety to every packed runtime asset</name>
  <files>src/builder-lifecycle.js, test/builder-lifecycle.test.js</files>
  <read_first>src/builder-package.js, src/builder-install.js, src/builder-lifecycle.js, test/builder-lifecycle.test.js, .planning/phases/02-codex-builder/02-06-SUMMARY.md</read_first>
  <behavior>
    - Upgrade/uninstall derives the exact runtime set from admitted release/receipt data and classifies installed/current/desired digest for each path.
    - Modified, hardlinked, symlinked, late-swapped, recreated, unknown, corrupt or missing runtime paths block terminal success and preserve the proven prior state.
    - Receipt, marker, checkpoint, unknown inventory, link count, retained quarantine identity and staged bytes are revalidated at every existing mutation boundary.
    - Replacement uses staged synchronized bytes and no-clobber publication; uninstall/upgrade publishes or deletes the receipt only after collection-wide postconditions.
    - Stale quarantine blocks subsequent lifecycle and late user files are restored/preserved.
  </behavior>
  <action>Write runtime-specific hardlink, symlink, compare-after-swap, recreated-path, staged-publish and stale-quarantine tests first. Replace fixed expected-path assumptions with the admitted canonical inventory while retaining the existing three-way compare, externally admitted receipt, checkpoint/unknown-state checks, retained quarantine, no-clobber link publication and receipt-last ordering. Do not weaken any previously reviewed safety clause for the larger runtime set. This implements the project-filesystem part of D-19 and D-26.</action>
  <acceptance_criteria>Every launcher/core byte receives the same fail-closed ownership, race and recovery guarantees as existing plugin assets.</acceptance_criteria>
  <verify>
    <automated>node --test test/builder-lifecycle.test.js</automated>
  </verify>
  <done>Runtime upgrade/uninstall is exact, reversible and hostile-filesystem safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove only the current project reference and refuse sibling selector changes</name>
  <files>src/builder-codex-host.js, src/builder-lifecycle.js, test/builder-codex-host.test.js, test/builder-lifecycle.test.js</files>
  <read_first>src/builder-codex-host.js, src/builder-lifecycle.js, test/builder-codex-host.test.js, test/builder-lifecycle.test.js</read_first>
  <behavior>
    - Project A and sibling Project B yield two deterministic consumers; A uninstall removes only A after exact CAS and performs zero official selector removals.
    - A release-changing upgrade that would replace a selector referenced by B is blocked before project/host mutation; same-release idempotence preserves both.
    - Last project uninstall still retains the selector and owner record, returning host-owner-action-required.
    - Foreign/unknown consumer, corrupt/missing ledger, stale digest, recreated entry, host observation mismatch or concurrent CAS preserves all shared state.
    - No code enumerates, writes, renames or deletes Codex cache/config/trust locations.
  </behavior>
  <action>Bind consumer removal as a separate lifecycle operation and re-admit owner/ledger/host observation before and after its CAS. Never infer the full ledger from a project receipt and never couple project receipt deletion to selector deletion. Refuse release replacement while any sibling reference would be invalidated. Assert fixed observer calls and absence of direct host-state filesystem access. This implements D-19, D-24, D-25, D-26 and D-28.</action>
  <acceptance_criteria>A project can upgrade/uninstall only its owned bytes and reference, never a sibling's shared selector state.</acceptance_criteria>
  <verify>
    <automated>node --test test/builder-codex-host.test.js test/builder-lifecycle.test.js</automated>
  </verify>
  <done>Project lifecycle is sibling-safe and leaves selector deletion for separate owner authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gate zero-reference selector removal behind explicit owner authority</name>
  <files>src/cli.js, src/builder-lifecycle.js, test/builder-cli.test.js, test/builder-codex-host.test.js</files>
  <read_first>src/cli.js, src/builder-lifecycle.js, src/builder-codex-host.js, test/builder-cli.test.js, test/builder-codex-host.test.js</read_first>
  <behavior>
    - Selector removal preview/apply requires explicit user scope, explicit remove-host-selector intent and one-to-one owner-record and consumer-ledger digest bindings.
    - Only created-by-agentmo plus an exact empty ledger and matching selector/source/release observation is applicable.
    - Fixed official remove runs only after exact plan admission; post-observed absence precedes owner/ledger terminal cleanup.
    - Preexisting-unowned, stale/forged digest, nonempty ledger, official failure, post-observation mismatch or concurrent recreation preserves selector and evidence.
    - CLI rejects duplicate/missing digest subjects, arbitrary selector/argv/path input and attempts to combine project uninstall authority with user-owner authority implicitly.
  </behavior>
  <action>Add explicit parser and lifecycle routes for the separate user-owner operation with exact plan and external digest admission. Keep project uninstall behavior unchanged by default. Call only the fixed host adapter, observe absence before any owner cleanup and preserve the old owner/ledger on ambiguity. Cover syntax and negative authorization cases without exposing raw host data. This completes D-26 while preserving D-17/D-25/D-28.</action>
  <acceptance_criteria>Only an explicit, exact, zero-reference owner operation may remove an AgentMo-created selector through the official Codex interface.</acceptance_criteria>
  <verify>
    <automated>node --test test/builder-cli.test.js test/builder-codex-host.test.js test/builder-lifecycle.test.js</automated>
  </verify>
  <done>User-host selector deletion is independently authorized, exact-admitted and fail-closed.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Trust Boundaries

| Boundary | Description |
| --- | --- |
| receipt -> project mutation | Mutable project evidence requests destructive file authority. |
| project lifecycle -> shared selector | One project may attempt to alter host state used by siblings. |
| owner/ledger evidence -> official remove | User-scope artifacts request selector deletion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-02-07-01 | Spoofing | owner/reference identity | high | mitigate | Deterministic consumer IDs, separate exact external digests and fixed selector observation. |
| T-02-07-02 | Tampering | runtime/ledger/receipt | high | mitigate | Three-way digest compare, CAS, nlink checks, retained quarantine and receipt-last publication. |
| T-02-07-03 | Repudiation | shared refusal/removal | medium | mitigate | Plan binds consumer set, owner disposition, operation and pre/post digests. |
| T-02-07-04 | Information disclosure | user host state | medium | mitigate | Value-blind owner/reference artifacts and no raw host path/output persistence. |
| T-02-07-05 | Denial of service | stale/shared state | medium | mitigate | Bounded blockers preserve prior state and do not attempt destructive repair. |
| T-02-07-06 | Elevation of privilege | project deleting selector | critical | mitigate | Separate user-owner flags/digests and exact zero-consumer gate. |
| T-02-07-SC | Tampering | dependency installation | low | accept | No dependency install and no direct Codex cache mutation. |
</threat_model>

<source_coverage>
- GOAL reversible runtime/host lifecycle with proven ownership: Tasks 1-3.
- REQ BLDR-01/BLDR-06: runtime parity, sibling safety and explicit owner removal are covered.
- RESEARCH/PATTERNS three-way compare, retained quarantine, official host operations and shared/preexisting ownership: Tasks 1-3.
- CONTEXT D-17/D-19/D-24/D-25/D-26/D-28: cited in actions. Recovery and behavior evidence continue in 02-08..10.
</source_coverage>

<verification>
- node --test test/builder-lifecycle.test.js test/builder-codex-host.test.js test/builder-cli.test.js
- npm run check
- git diff --check
</verification>

<success_criteria>
- Runtime assets preserve every existing compare/revalidation/quarantine/no-clobber control.
- Project lifecycle removes only its exact reference and blocks sibling selector replacement/removal.
- Official selector deletion requires a separate exact zero-reference owner operation.
</success_criteria>

<artifacts_produced>
- Runtime lifecycle parity in src/builder-lifecycle.js.
- Sibling-project/shared-selector refusal and exact consumer CAS.
- Explicit user-owner selector removal route and hostile negative coverage.
</artifacts_produced>

<output>
Create .planning/phases/02-codex-builder/02-07-SUMMARY.md when done.
</output>
