---
phase: 02-codex-builder
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/builder-checkpoint.js
  - src/builder-events.js
  - src/builder-entry.js
  - src/cli.js
  - package.json
  - test/builder-checkpoint.test.js
  - test/builder-hook.test.js
  - test/builder-entry.test.js
autonomous: true
requirements: [BLDR-03, BLDR-04]
must_haves:
  truths:
    - Stable boundaries create exact-digest checkpoints without transcript or secret material.
    - Restart, compaction, and duplicate hook events resolve through one idempotent event ledger.
    - Hooks detect, flush, and announce only; they never approve or advance a stage.
  artifacts: [src/builder-checkpoint.js, src/builder-events.js]
  key_links:
    - src/builder-entry.js admits checkpoints before proposing resume.
---

<objective>
Make the lifecycle durable and replay-safe across pause, compaction, and restart, implementing D-21, D-22, and D-23 with explicit artifacts instead of Codex transcripts.
</objective>

<threat_model>
- Checkpoint injection/tampering: schema validation plus exact-byte digest admission before use.
- Duplicate or reordered hooks: stable event IDs and monotonic cursor make replay a no-op.
- Secret retention: reject secret-shaped fields, raw transcripts, tool bodies, and absolute host details before atomic write.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement checkpoint admission and atomic persistence</name>
  <files>src/builder-checkpoint.js, test/builder-checkpoint.test.js</files>
  <read_first>src/artifact-admission.js, src/persistability.js, src/migration-filesystem.js, src/secret-redaction.js, .planning/phases/02-codex-builder/02-CONTEXT.md</read_first>
  <action>Define checkpoint v1 with workflow/stage identity, artifact paths and exact digests, pending decision, next legal action, receipt digest, capability snapshot, event cursor/dedup keys, and pause reason. Validate and atomically publish only admitted value-blind content.</action>
  <acceptance_criteria>Round-trip and restart tests pass; tampered digest, illegal transition, transcript/tool body, and secret-shaped candidates are rejected before publication.</acceptance_criteria>
  <done>Checkpoint bytes are admitted, digest-bound, safe, and atomically recoverable.</done>
  <verify><automated>node --test test/builder-checkpoint.test.js</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Route hook events and resume through the checkpoint</name>
  <files>src/builder-events.js, src/builder-entry.js, src/cli.js, package.json, test/builder-hook.test.js, test/builder-entry.test.js</files>
  <read_first>src/builder-checkpoint.js, src/builder-entry.js, src/cli.js, .planning/phases/02-codex-builder/02-RESEARCH.md</read_first>
  <action>Handle SessionStart, PreCompact, PostCompact, manual pause, and stable-boundary events through a host-neutral envelope. Persist an event ledger, no-op duplicates, reject cursor rollback, and return proposals only. Wire `builder pause`, `builder resume`, and `builder hook` without automatic approval or stage advance.</action>
  <acceptance_criteria>Duplicate, restart, and compaction tests produce exactly one state transition; explicit entry always recovers without hook delivery; hooks never return an approval or cross-stage mutation.</acceptance_criteria>
  <done>Hook replay and explicit resume converge on one proposal-only durable state.</done>
  <verify><automated>node --test test/builder-hook.test.js test/builder-entry.test.js &amp;&amp; npm run check</automated></verify>
</task>

</tasks>

<verification>
- `node --test test/builder-checkpoint.test.js test/builder-hook.test.js test/builder-entry.test.js`
- `npm run check`
- `git diff --check`
</verification>

<success_criteria>
- BLDR-04 is demonstrated by disk-backed restart/compaction/dedup tests, not in-memory mocks alone.
</success_criteria>

## Artifacts this phase produces

- Durable checkpoint contract, atomic writer, and replay-safe event reducer.
