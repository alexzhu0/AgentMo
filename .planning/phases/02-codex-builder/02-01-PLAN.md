---
phase: 02-codex-builder
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/builders/contract.js
  - src/builders/registry.js
  - src/builders/codex.js
  - src/builder-probe.js
  - src/builder-entry.js
  - src/cli.js
  - package.json
  - test/builder-adapter-contract.test.js
  - test/codex-builder-probe.test.js
  - test/builder-entry.test.js
autonomous: true
requirements: [CORE-05, BLDR-02]
must_haves:
  truths:
    - Builder adapter contract is host-neutral, versioned, and does not equate registration with support.
    - Codex probe reports declared, observed, missing, degraded, and verified evidence without mutating the host.
    - The default entry and direct stage entries share one lifecycle decision engine.
  artifacts:
    - src/builders/contract.js
    - src/builders/codex.js
    - src/builder-probe.js
    - src/builder-entry.js
  key_links:
    - src/cli.js dispatches builder commands into the neutral registry and entry engine.
---

<objective>
Deliver the first working Builder slice: a neutral adapter contract, a real read-only Codex capability probe, and one default AgentMo entry that can start or resume the same Discover -> Plan -> Produce lifecycle. This implements D-15, D-16, D-17, D-20, D-24, and D-28 without claiming support from descriptor presence alone.
</objective>

<threat_model>
- Host command execution: invoke a fixed `codex` binary with fixed argument arrays, no shell interpolation; bound captured output.
- False support claims: keep declared, observed, and verified evidence separate; required capability failure closes the entry.
- Information disclosure: persist no environment values, transcript, or raw unbounded host output.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement the neutral adapter contract and Codex probe</name>
  <files>src/builders/contract.js, src/builders/registry.js, src/builders/codex.js, src/builder-probe.js, test/builder-adapter-contract.test.js, test/codex-builder-probe.test.js</files>
  <read_first>src/targets/registry.js, src/runtime-compatibility.js, src/secret-redaction.js, test/targets.test.js, .planning/phases/02-codex-builder/02-CONTEXT.md</read_first>
  <action>Create a frozen v1 contract with required/optional capabilities, lifecycle events, recovery/dedup surfaces, unsupported/degraded declarations, and evidence levels. Register Codex as the only conformance candidate. Probe `codex --version`, `codex features list`, and bounded help surfaces through injected execution for tests; normalize observations and fail closed when required capabilities are absent. Registration alone must never produce `supported`.</action>
  <acceptance_criteria>Contract validation rejects incomplete/unknown descriptors; probe is deterministic under injected observations; real probing is read-only and returns bounded structured evidence; verified support requires explicit behavior evidence.</acceptance_criteria>
  <done>Neutral contract and read-only Codex probe pass positive and hostile contract tests.</done>
  <verify><automated>node --test test/builder-adapter-contract.test.js test/codex-builder-probe.test.js</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Wire the shared default and direct-stage entry</name>
  <files>src/builder-entry.js, src/cli.js, package.json, test/builder-entry.test.js</files>
  <read_first>src/cli.js, bin/agentmo.js, test/cli.test.js, src/builders/contract.js, src/builder-probe.js</read_first>
  <action>Add `builder probe`, `builder start`, and direct `discover|plan|produce` protocol entry routing. With no checkpoint, default start proposes Discover; with an admitted checkpoint reference it proposes resume and never auto-approves. Direct stages use the same transition validator. Keep existing CLI commands compatible and add new modules to `npm run check`.</action>
  <acceptance_criteria>Default and direct routes return the same schema and legal-transition rules; invalid stage skips and missing required capabilities fail closed; help exposes one primary `$agentmo` experience without an “advanced mode”.</acceptance_criteria>
  <done>One lifecycle engine serves default start/resume and direct stage protocol routes.</done>
  <verify><automated>node --test test/builder-entry.test.js test/cli.test.js &amp;&amp; npm run check</automated></verify>
</task>

</tasks>

<verification>
- `node --test test/builder-adapter-contract.test.js test/codex-builder-probe.test.js test/builder-entry.test.js`
- `npm run check`
- `git diff --check`
</verification>

<success_criteria>
- CORE-05, BLDR-02, and the entry portion of BLDR-03 have executable contract tests.
- No host mutation, trust-store edit, environment read, or support overclaim occurs.
</success_criteria>

## Artifacts this phase produces

- Versioned Builder adapter contract and Codex adapter registry entry.
- Read-only capability probe and shared lifecycle entry engine.
