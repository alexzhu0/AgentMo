---
phase: 02-codex-builder
plan: "21"
type: execute
wave: 20
depends_on: ["02-20"]
files_modified:
  - src/builder-immutable-journal.js
  - src/builder-checkpoint.js
  - src/builder-codex-uat.js
  - src/builder-hook-bridge.js
  - test/builder-checkpoint.test.js
  - test/builder-hook.test.js
  - test/builder-hook-bridge.test.js
  - test/builder-codex-uat.test.js
autonomous: true
gap_closure: true
requirements: [BLDR-04, BLDR-07]
must_haves:
  truths:
    - "A rejected append leaves the prior head loadable, while a durable append returns an explicit committed result whose new head reloads exactly."
    - "Journal stage and lock cleanup cannot delete or adopt a final-window replacement, and an owned cleanup remainder is distinguishable after restart."
    - "Checkpoint, hook, and UAT callers consume the same structured append outcome and never retry an already committed transition."
  artifacts:
    - path: src/builder-immutable-journal.js
      provides: Explicit durable commit/recovery result and identity-safe journal cleanup
    - path: src/builder-checkpoint.js
      provides: Checkpoint caller handling for committed and recovery-required append outcomes
    - path: test/builder-codex-uat.test.js
      provides: UAT caller and restart regressions over the new append contract
  key_links:
    - from: src/builder-immutable-journal.js
      to: src/builder-checkpoint.js
      via: structured rejected-before-commit, committed-clean, or committed-recovery-required result
    - from: src/builder-immutable-journal.js
      to: src/builder-codex-uat.js
      via: exact successor admission consumed without ambiguous retry
---

<objective>
关闭 CR-07 与 CR-08，并迁移 checkpoint、hook、UAT 调用者到唯一明确的 durable append contract（D-21、D-22、D-23、D-27、D-29、D-30、D-31）。

Purpose: 保证唯一恢复权威在 cleanup 竞争、崩溃或 commit 后失败时仍可读取、可判定且不删除 foreign inode。
Output: 明确 commit point、identity-bound cleanup/recovery、全部 append caller 迁移与负向接缝测试。
</objective>

## Phase Goal

**As a** developer installing AgentMo Builder in Codex, **I want to** install and run the packaged Builder on a clean Codex host and recover its workflow across pause, compaction, restart, upgrade, and uninstall with provable asset ownership, **so that** I can use and audit AgentMo Builder safely without depending on the AgentMo source repository.

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-codex-builder/02-CONTEXT.md
@.planning/phases/02-codex-builder/02-REVIEW.md
@.planning/phases/02-codex-builder/02-VERIFICATION.md
@.planning/phases/02-codex-builder/02-20-SUMMARY.md
@src/builder-immutable-journal.js
@src/builder-checkpoint.js

<interfaces>
- `appendImmutableJournalEntry(options)` currently links/syncs the successor before lock cleanup but can throw an indistinguishable conflict afterward.
- `loadImmutableJournal(options)` is the restart authority and must accept an exact operation-owned recovery remainder while continuing to reject foreign lock/stage state.
- `writeBuilderCheckpoint`, installed hook delivery, and the closed UAT transitions must consume one structured append outcome; no caller may infer zero mutation from an exception after the durable commit point.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce journal cleanup and post-commit ambiguity</name>
  <files>test/builder-checkpoint.test.js, test/builder-hook.test.js, test/builder-hook-bridge.test.js, test/builder-codex-uat.test.js</files>
  <read_first>AGENTS.md, .planning/phases/02-codex-builder/02-REVIEW.md, src/builder-immutable-journal.js, src/builder-checkpoint.js, src/builder-hook-bridge.js, src/builder-codex-uat.js, test/builder-checkpoint.test.js, test/builder-hook.test.js, test/builder-hook-bridge.test.js, test/builder-codex-uat.test.js</read_first>
  <behavior>
    - Stage or lock replacement after the final identity validation but before cleanup remains byte-for-byte present and is never unlinked.
    - Pre-commit rejection reloads the old head; post-commit cleanup failure returns committed/recovery-required and reloads the exact new head.
    - Restart distinguishes an exact operation-owned remainder from an attacker file without deleting ambiguity.
    - Checkpoint, hook, and UAT callers never duplicate a committed event after a cleanup warning.
  </behavior>
  <action>Add failing post-validation/pre-effect seams for both stage and lock finalization. Assert exact old/new heads, entry count, inode/byte preservation, restart loadability, and caller no-retry behavior. Use only disposable journals and keep all observations value-blind per D-22 and D-29.</action>
  <acceptance_criteria>The reviewed implementation fails the final cleanup/commit cases, and each test distinguishes zero-mutation rejection from durable commit.</acceptance_criteria>
  <verify><automated>node --test test/builder-checkpoint.test.js test/builder-hook.test.js test/builder-hook-bridge.test.js test/builder-codex-uat.test.js</automated></verify>
  <done>CR-07/CR-08 have deterministic failing reproductions across every current append caller.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement durable append recovery and migrate callers</name>
  <files>src/builder-immutable-journal.js, src/builder-checkpoint.js, src/builder-codex-uat.js, src/builder-hook-bridge.js</files>
  <read_first>src/builder-immutable-journal.js, src/builder-checkpoint.js, src/builder-codex-uat.js, src/builder-hook-bridge.js, test/builder-checkpoint.test.js, test/builder-hook.test.js, test/builder-hook-bridge.test.js, test/builder-codex-uat.test.js</read_first>
  <action>Define the durable commit point only after exact successor publication and retained-parent sync. Return a closed structured outcome for rejected-before-commit, committed-clean, and committed-recovery-required; never report an ordinary zero-write conflict after commit. Bind lock/recovery bytes to operation identity, journal key, predecessor, intended publication, and owned inode identities. Retire only exact operation-owned entries into a private retained directory while handles remain authoritative; preserve all ambiguous competitors. Teach the loader and all current callers to resume from the returned/reloaded exact head without duplicate append. Preserve D-21 through D-23 checkpoint authority and D-30 one-journal semantics. Record every I/O, durable identity, and caller edge in the summary for 02-23.</action>
  <acceptance_criteria>Every return maps to a reloadable old or new head; cleanup ambiguity cannot brick the journal or delete a competitor; all callers handle committed recovery without unsafe retry.</acceptance_criteria>
  <verify><automated>node --test test/builder-checkpoint.test.js test/builder-hook.test.js test/builder-hook-bridge.test.js test/builder-codex-uat.test.js &amp;&amp; git diff --check</automated></verify>
  <done>CR-07/CR-08 are closed and the exact inventory delta is ready for 02-23.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 (block_on: high)

| Threat | STRIDE | Severity | Disposition | Mitigation / test |
| --- | --- | --- | --- | --- |
| Foreign inode deletion | Tampering | critical | mitigate | Post-validation seams plus handle-bound retirement preserve replacements. |
| False-negative durable commit | Repudiation/DoS | critical | mitigate | Explicit commit point and structured committed/recovery result with reload invariant. |
| Replay after ambiguous result | Spoofing/Tampering | high | mitigate | All callers reload the exact returned head and never blindly retry. |
| Unsafe ancestor or symlink | Tampering/Elevation | high | mitigate | Retained current-UID, non-writable, no-follow parent authority remains mandatory. |
| Evidence disclosure | Information disclosure | high | mitigate | Journal/recovery records and tests remain value-blind and never read `.env` or private attempts. |
</threat_model>

<verification>Focused checkpoint/hook/UAT tests and `git diff --check` pass; 02-23 later reconciles the combined exact I/O/artifact inventory and full suite.</verification>

<success_criteria>CR-07/CR-08 are closed, every append result is unambiguous and reloadable, and no actual Codex/UAT state is created.</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

- Structured immutable-journal append outcome with explicit committed/recovery status.
- Identity-bound internal journal recovery/retention evidence.
- Migrated checkpoint, hook, and UAT callers with no unsafe retry.
- No real UAT artifact or certification claim.
</artifacts_produced>

<output>Create `.planning/phases/02-codex-builder/02-21-SUMMARY.md` when done.</output>
