---
phase: 02-codex-builder
plan: "04"
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - src/builder-install.js
  - src/builder-lifecycle.js
  - src/builder-behavior-eval.js
  - src/cli.js
  - package.json
  - test/builder-lifecycle.test.js
  - test/codex-builder-behavior.test.js
  - README.md
  - release/2026.07.15.md
autonomous: true
requirements: [BLDR-06, BLDR-07]
must_haves:
  truths:
    - Upgrade and uninstall mutate only pristine, receipt-owned exact paths and preserve modified/unknown/corrupt assets.
    - Fresh-session behavior evidence covers trigger, non-trigger, pause, compaction/restart recovery, and duplicate no-op.
    - Declared installation, observed visibility, verified behavior, and domain quality remain separate evidence levels.
  artifacts: [src/builder-lifecycle.js, src/builder-behavior-eval.js]
  key_links:
    - Behavior evaluation consumes packed install receipts and fresh Codex observations, never self-certifies from doctor output.
---

<objective>
Close the Builder lifecycle with ownership-safe explicit upgrade/uninstall and fresh-session behavior proof, implementing D-26, D-27, and D-28.
</objective>

<threat_model>
- Destructive ownership ambiguity: three-way compare installed digest, current bytes, and desired digest; corrupt or absent receipts fail closed.
- TOCTOU: re-read current bytes immediately before each approved mutation and invalidate changed plans.
- Evidence spoofing: behavior evidence binds packed release, receipt, host observation, scenario, and result digests; doctor/fixture success cannot upgrade certification.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement exact-plan upgrade and uninstall</name>
  <files>src/builder-install.js, src/builder-lifecycle.js, src/cli.js, package.json, test/builder-lifecycle.test.js</files>
  <read_first>src/builder-install.js, src/migration-filesystem.js, test/migration-filesystem.test.js, .planning/phases/02-codex-builder/02-CONTEXT.md</read_first>
  <action>Produce preview/apply lifecycle plans with exact plan digest and three-way ownership classification. Upgrade or delete only pristine AgentMo-owned assets; preserve and report modified, unknown, shared, or corrupt state. Require explicit user action and revalidate immediately before mutation.</action>
  <acceptance_criteria>Hostile tests prove no prefix deletion, no modified-file overwrite/delete, stale-plan rejection, and idempotent pristine upgrade/uninstall.</acceptance_criteria>
  <done>Lifecycle apply mutates only revalidated pristine receipt-owned paths.</done>
  <verify><automated>node --test test/builder-lifecycle.test.js</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Run fresh-session behavior evaluation and record the release boundary</name>
  <files>src/builder-behavior-eval.js, src/cli.js, package.json, test/codex-builder-behavior.test.js, README.md, release/2026.07.15.md</files>
  <read_first>src/builder-entry.js, src/builder-events.js, src/builder-doctor.js, docs/MVP_RUNBOOK.md, release/README.md</read_first>
  <action>Create a bounded scenario runner for trigger/non-trigger, manual pause, stable checkpoint, Pre/PostCompact, restart resume, and duplicate event no-op. Bind evidence to packed release and receipt digests, and keep behavior certification below Agent Package/domain quality. Only after tests pass, update README and one concise release evidence record with commands, hashes, status, and remaining risk.</action>
  <acceptance_criteria>Fresh-process tests observe every scenario; a synthetic payload alone cannot yield verified behavior; maintenance docs reflect tested interfaces without raw logs or secrets.</acceptance_criteria>
  <done>Fresh-session behavior evidence covers all required scenarios with bounded certification.</done>
  <verify><automated>node --test test/codex-builder-behavior.test.js test/builder-lifecycle.test.js &amp;&amp; npm run check</automated></verify>
</task>

</tasks>

<verification>
- `node --test test/builder-lifecycle.test.js test/codex-builder-behavior.test.js`
- `npm run check`
- `git diff --check`
</verification>

<success_criteria>
- BLDR-06 and BLDR-07 are proven by hostile ownership and fresh-process behavior tests.
- README/release maintenance is limited to interfaces and verified evidence delivered by this phase.
</success_criteria>

## Artifacts this phase produces

- Ownership-safe lifecycle engine and bounded Codex Builder behavior evidence.
