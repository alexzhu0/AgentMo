---
phase: 02-codex-builder
plan: "16"
type: execute
wave: 16
depends_on: ["02-15"]
files_modified:
  - src/builder-codex-uat.js
  - src/builder-codex-uat-continuation.js
  - src/cli.js
  - src/builder-package.js
  - test/builder-codex-uat.test.js
  - test/builder-cli.test.js
  - test/builder-packed-install.test.js
  - test/artifact-surface-coverage.test.js
  - test/helpers/io-surface-inventory.js
autonomous: true
gap_closure: true
requirements: [BLDR-01, BLDR-06, BLDR-07]
must_haves:
  truths:
    - "An ephemeral synthetic packed fixture with deterministic package, version, release, tarball, continuation, manifest, and verifier identities proves that the continuation is admitted by `BUILDER_RELEASE_ASSET_INVENTORY` and the closed static import graph; the fixture is deleted after each test and is never UAT or upgrade evidence."
    - "The packed public `builder codex-uat continue` driver preloads only admitted code/identities and attempt handles before final uninstall, applies the exact approved uninstall, observes removal, appends uninstall-visibility, publishes the candidate leaf, then appends candidate-ready without recalling deleted assets or making a human decision."
    - "After all eleven scenarios, candidate bytes publish first as a content-addressed non-authoritative leaf containing the fixed successor/evidence bindings and no journal-head/predecessor-derived field or digest; only then may candidate-ready reference the candidate digest."
    - "A crash after candidate-leaf publication leaves an inert orphan and scenarios-complete journal that the exact packed continuation can resume; restart before uninstall-visibility, stale input, or a second continuation cannot fabricate observation or publish twice."
    - "Before uninstall or any journal/candidate mutation, the packed continuation checks `--expected-verifier-sha256` against the verifier identity bound by the successor UAT release manifest/inventory; a mismatch fails closed without executing a human verifier."
  artifacts:
    - path: src/builder-codex-uat.js
      provides: Candidate-leaf publisher, candidate-ready append, and exact basis validation
    - path: src/builder-codex-uat-continuation.js
      provides: Pre-uninstall loaded continuation that applies uninstall and completes scenario/candidate journal ordering from admitted in-memory state
    - path: src/cli.js
      provides: Exact packed public `builder codex-uat continue` command with closed lifecycle and bounded output
    - path: src/builder-package.js
      provides: Packed continuation release inventory and closed static import closure
    - path: test/helpers/io-surface-inventory.js
      provides: Exact incremental I/O allowlist for continuation, packaging, and CLI changes
    - path: test/builder-codex-uat.test.js
      provides: Candidate ordering, verifier identity, stale/replay, and interruption regressions
  key_links:
    - from: ephemeral synthetic packed fixture
      to: packed continuation child process
      via: Fixed test package/version inputs and independently derived release, tarball, continuation, manifest, and verifier identities are self-checked before hostile lifecycle cases run; cleanup removes every fixture byte afterward
    - from: packed public continuation
      to: uninstall-visibility and candidate-ready
      via: One process starts while installed assets exist, retains only admitted code/identity state plus open attempt handles, applies exact uninstall, observes absence, appends scenario 11, publishes the leaf, then appends its one-way reference
    - from: candidate leaf
      to: candidate-ready entry
      via: Candidate publishes durably first; candidate-ready references its exact digest in one direction
    - from: successor release manifest/inventory
      to: packed continuation preflight
      via: The supplied expected verifier digest must equal the bound identity before uninstall, journal mutation, candidate publication, or candidate-ready
---

<objective>
Ship and prove the pre-uninstall packed continuation through candidate-ready without creating actual UAT releases.

Purpose: Implement the packed lifecycle gate while leaving the release builder, actual UAT tarballs, and separately extracted verifier/admission boundary to Plan 02-17.
Output: Packed public continuation, candidate-leaf-first publisher, manifest-bound verifier preflight, and an ephemeral synthetic packed-fixture adversarial matrix.
</objective>

## Phase Goal

**As a** human operator crossing final uninstall, **I want to** reach candidate-ready through one exact prestarted packed continuation, **so that** the separate successor verifier can later inspect an acyclic immutable basis.

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-codex-builder/02-CONTEXT.md
@.planning/phases/02-codex-builder/02-VERIFICATION.md
@.planning/spikes/002-append-only-journal-order/README.md
@.planning/spikes/003-post-uninstall-verifier/README.md
@.planning/phases/02-codex-builder/02-15-SUMMARY.md
@src/builder-codex-uat.js
@src/builder-package.js
@test/artifact-surface-coverage.test.js
@test/helpers/io-surface-inventory.js

<interfaces>
- Packed-install tests use existing test helpers to create one ephemeral synthetic package in a temporary directory with fixed package/version/release metadata and exact derived tarball, continuation, manifest-bound verifier, and closure identities. Tests delete the fixture in cleanup, publish nothing, retain no tarball, and make no claim about a real attempt, baseline/successor upgrade, live execution, or UAT evidence.
- `publishCodexUatCandidate(options)` requires the unique scenarios-complete admission and exact successor/evidence basis. It writes absent content-addressed `agentmo.codex-uat.v1` bytes first and returns the candidate digest without mutating the journal.
- Candidate fields are exactly attempt id, package name, successor version, successor release digest, successor tarball digest, ordered evidence digest, scenario count `11`, `humanAdmissionRequired: true`, and false real-host/domain/package/production/wider-compatibility flags. No journal head/predecessor/decision/path field is legal.
- `appendCodexUatCandidateReady(options)` reloads the unique scenarios-complete head, exact-validates the already durable candidate leaf/basis, and appends one candidate-ready successor referencing candidate digest.
- The exact public continuation command is `node SUCCESSOR_EXTRACT/package/bin/agentmo.js builder codex-uat continue --attempt-dir ATTEMPT_DIR --expected-head-sha256 HEAD --approved-uninstall-plan-sha256 PLAN --successor-tarball SUCCESSOR_TGZ --expected-successor-version VERSION --expected-release-sha256 RELEASE --expected-tarball-sha256 TARBALL --expected-verifier-sha256 VERIFIER`. At a scenario-10 head it may start only while the exact successor launcher/current receipt still exist; it preloads and self-checks the packed continuation closure, opens admitted attempt/evidence handles, discards path-based authority, applies the exact approved uninstall, observes post-removal state through retained handles/ownership identities, appends uninstall-visibility, publishes the candidate, and appends candidate-ready. It never calls preview/decide.
- The same command has exactly one restart-safe recovery branch: when the journal already ends at scenarios-complete and the exact matching orphan candidate leaf exists, it may reuse that leaf and append candidate-ready without re-observing uninstall. If removal occurred but scenario 11 is absent, a restarted process cannot recover the lost observation and must append nothing; operator may separately record failure/interruption.
- The continuation compares `--expected-verifier-sha256` with the identity bound by the supplied release manifest/inventory before uninstall and before any journal, leaf, or candidate mutation. It does not load or execute the human verifier. Plan 02-17 implements the release builder and verifier, packages and tests both, and only then builds the actual UAT tarballs.
</interfaces>

<parallel_work_contract>
- A separately completed usecase-pool workstream may be merged before execution. Its reported surface includes `src/usecase-pool.js`, `agentmo usecase-pool import/select`, the `agentmo.usecase-pool.v1`/card/selection schemas, CLI/discovery/artifact registry/subjects wiring, `src/builder-package.js` release inventory, artifact/I-O rows, docs/examples, and the OpenClaw blood-pool release section.
- Re-read current bytes before modifying `src/cli.js`, `src/builder-package.js`, either inventory test, or another shared file. Apply incremental edits that preserve all usecase-pool routes, schemas, release assets, artifact owners, and exact I/O rows.
- `npm run check` must keep the reported 620-test baseline or a higher merged baseline; no usecase-pool test or row may be removed, skipped, or weakened.
</parallel_work_contract>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the acyclic candidate and pre-uninstall continuation contract</name>
  <files>src/builder-codex-uat.js, src/builder-codex-uat-continuation.js, src/cli.js, test/builder-codex-uat.test.js, test/builder-cli.test.js</files>
  <read_first>AGENTS.md, .planning/phases/02-codex-builder/02-CONTEXT.md, .planning/spikes/002-append-only-journal-order/README.md, .planning/spikes/003-post-uninstall-verifier/README.md, src/builder-codex-uat.js, src/builder-lifecycle.js, src/cli.js, src/usecase-pool.js if present from the parallel merge, test/builder-codex-uat.test.js, test/builder-cli.test.js</read_first>
  <behavior>
    - Candidate publication is allowed only from unique scenarios-complete state after uninstall-visibility; the content-addressed leaf is durable before candidate-ready and contains no journal-head/predecessor-derived field or digest.
    - The public command accepts only the exact argument set in `interfaces`; at scenario 10 it requires the exact successor launcher/current receipt to exist, self-checks loaded packed code/identities, and opens admitted attempt/evidence handles before applying uninstall.
    - One already-running process applies the exact approved uninstall, observes launcher/current receipt absence plus shared/foreign preservation without importing or re-reading deleted assets, writes checkpoint/observation leaves with no journal back-reference, appends scenario 11, publishes the candidate, then appends candidate-ready.
    - If candidate-ready append fails after leaf publication, the leaf is inert and the journal remains scenarios-complete; the same exact packed command may exact-reuse that orphan in its sole recovery branch.
    - A mismatched `--expected-verifier-sha256` rejects in child process preflight before uninstall, journal/checkpoint/observation mutation, candidate publication, or candidate-ready; the comparison is against the successor manifest/inventory binding and never executes the verifier.
    - The driver never performs verifier preview/decide or any human decision and emits only bounded status/head/candidate/package/version/release/tarball/continuation/verifier identities.
  </behavior>
  <action>Re-read current CLI bytes and preserve any merged `agentmo usecase-pool import/select` routing and schemas before editing. Write transition/order, module-boundary, and mismatched expected-verifier child-process tests first. Keep candidate publication and candidate-ready append as separate closed operations: candidate bytes bind only the fixed successor/evidence basis, remain non-authoritative until the later entry, and reject every journal head/predecessor field or derived digest per D-30. Implement `src/builder-codex-uat-continuation.js` as the statically imported orchestration module and expose only the exact `builder codex-uat continue` command/arguments from `src/cli.js`. The very first mutating-boundary preflight must compare `--expected-verifier-sha256` with the successor UAT release manifest/inventory binding and reject mismatch before uninstall, journal/checkpoint/observation mutation, candidate publication, or candidate-ready; this identity check does not run the human verifier. On scenario-10 startup, require the installed successor launcher/current receipt and exact journal head, verify the supplied tarball plus packed release/continuation/verifier identities, recompute the uninstall preview and match `--approved-uninstall-plan-sha256`, load all continuation code, and open the admitted attempt/evidence/owned-parent handles before mutation. Then discard caller path authority, apply that exact uninstall in-process, verify absence and shared/foreign preservation through retained handles and admitted ownership identities, publish head-independent checkpoint/observation leaves, append uninstall-visibility with the original head as CAS and leaf digests as one-way references, publish the candidate leaf, and append candidate-ready. Never dynamically import checkout/private files, spawn the deleted launcher, reload the deleted receipt, consult `.env`/historical trust markers, or call human preview/decide. Permit restart only from scenarios-complete plus an exact matching orphan leaf; this branch validates/reuses the leaf and appends candidate-ready without re-observing uninstall. All other post-removal restarts append nothing and leave failure/interruption to the separate terminal command.</action>
  <acceptance_criteria>The packed public contract has one exact command and state machine; verifier-identity mismatch mutates nothing and occurs before uninstall; uninstall-visibility can be recorded only by the prestarted admitted process; candidate leaf precedes candidate-ready and is one-way; orphan-leaf recovery is exact; no deleted/checkout/private code recall or human decision path exists; any merged usecase-pool CLI route/schema remains byte-semantically intact.</acceptance_criteria>
  <verify><automated>node --test test/builder-codex-uat.test.js test/builder-cli.test.js</automated></verify>
  <done>The production continuation contract closes the uninstall-to-candidate-ready gap without creating a second authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Package and prove the continuation with an ephemeral synthetic fixture</name>
  <files>src/builder-package.js, test/builder-packed-install.test.js, test/artifact-surface-coverage.test.js, test/helpers/io-surface-inventory.js</files>
  <read_first>AGENTS.md, .planning/spikes/001-codex-marketplace-ownership/README.md, .planning/spikes/002-append-only-journal-order/README.md, .planning/spikes/003-post-uninstall-verifier/README.md, src/builder-package.js, src/builder-codex-uat-continuation.js, src/cli.js, src/usecase-pool.js if present from the parallel merge, package.json, test/builder-packed-install.test.js, test/artifact-surface-coverage.test.js, test/helpers/io-surface-inventory.js</read_first>
  <behavior>
    - `BUILDER_RELEASE_ASSET_INVENTORY`, syntax coverage, and the static import closure include the public CLI and continuation module without dropping any pre-existing or merged usecase-pool asset/row.
    - Existing test helpers create an ephemeral synthetic packed fixture from fixed package/version/release metadata, bind exact derived tarball/continuation/manifest/verifier identities, and delete the temporary package, extraction, attempt, and tarball bytes in cleanup.
    - A child process launched through that synthetic packed fixture before uninstall alone can complete exact apply, post-removal observation, scenario 11, candidate publication, and candidate-ready with bounded output; this is mechanism coverage, not an actual attempt, baseline/successor release, upgrade, live run, or publishable evidence.
    - Driver-not-prestarted, wrong package/version/tarball/release, loaded-code digest mismatch, mismatched expected verifier digest, stale head, caller-supplied/stale candidate path, post-removal restart before scenario 11, and deleted-entrypoint recall all fail closed without candidate-ready.
    - Failure between candidate leaf and candidate-ready leaves one inert orphan and scenarios-complete journal; exact packed reinvocation reuses it once, while second continuation/double publication appends nothing.
    - The exact I/O allowlist includes every continuation/package/test change without deleting merged usecase-pool assets or existing artifact/I/O rows.
  </behavior>
  <action>Re-read current `src/builder-package.js`, artifact coverage, and exact I/O inventory bytes before editing. Preserve any merged `src/usecase-pool.js` release asset, usecase-pool exact rows, and every unrelated existing inventory row. Write packed child-process tests first using the existing test helpers: create a newly absent temporary root, construct one synthetic package with fixed test package name, semantic version, release metadata, and manifest-bound verifier identity, derive and assert the exact tarball/continuation/manifest/verifier SHA-256 values, exercise it, and remove the package, extraction, attempt, and tarball in `finally` cleanup. The fixture is never an actual UAT attempt, actual baseline or successor artifact, retained release, upgrade proof, live evidence, or publication input. Incrementally add the continuation module to `BUILDER_RELEASE_ASSET_INVENTORY`, its static import closure, syntax coverage, and exact I/O allowlist; update exact ownership assertions without replacing the allowlist or owner taxonomy. Exercise the exact command shown in `interfaces`, proving the already-running synthetic packed driver applies the approved uninstall itself, detects removal without recalling launcher/receipt, appends uninstall-visibility, and produces leaf-before-entry. Test driver-not-prestarted, wrong package/version/tarball/release, loaded continuation digest mismatch, an explicit mismatched `--expected-verifier-sha256` child process with zero uninstall/journal/candidate mutation, stale head/path, unrecoverable post-removal restart, exact orphan recovery, second continuation/double publication, and deleted/private import rejection. Do not implement or modify a real release builder, build or retain real release tarballs, create a real attempt, claim upgrade/live evidence, or implement/package/invoke/preview/decide with the standalone human verifier; Plan 02-17 owns those surfaces. Implement D-17 through D-19, D-24, D-26, D-28, and D-30.</action>
  <acceptance_criteria>The continuation is admitted by the release inventory/static closure and proven only through a deterministic ephemeral synthetic packed fixture; cleanup leaves no fixture package, extraction, attempt, or tarball; expected-verifier mismatch fails before every mutation; only a prestarted admitted child process crosses uninstall-to-scenario-11; all named mismatch/restart/recall/stale/double paths preserve journal authority; orphan recovery appends exactly one candidate-ready; all pre-existing and merged usecase-pool package/artifact/I-O rows remain; no actual release/attempt/upgrade/live evidence is created or claimed; `npm run check` retains the reported 620-test or higher baseline without removed/skipped usecase-pool coverage.</acceptance_criteria>
  <verify><automated>node --test test/builder-packed-install.test.js test/artifact-surface-coverage.test.js test/builder-codex-uat.test.js test/builder-cli.test.js &amp;&amp; npm run check &amp;&amp; git diff --check</automated></verify>
  <done>The packed continuation mechanism survives its synthetic fixture's uninstall boundary and deterministically reaches candidate-ready without producing actual UAT artifacts.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Threat Register

| Threat ID | STRIDE | Component | Severity | Disposition | Mitigation / limitation |
| --- | --- | --- | --- | --- | --- |
| T-02-16-01 | Tampering | successor package identity | critical | mitigate | Exact package/version/release/tarball/continuation plus manifest-bound expected-verifier identity reject no-op and swaps. |
| T-02-16-02 | Tampering | candidate publication | critical | mitigate | Content-addressed leaf publishes before candidate-ready and contains no reverse edge. |
| T-02-16-03 | Tampering | expected verifier preflight | critical | mitigate | Digest mismatch rejects before uninstall, journal/leaf mutation, or candidate publication; continuation never executes verifier code. |
| T-02-16-04 | Information disclosure | continuation/candidate output | high | mitigate | Bounded digest/enums only; no paths, payloads, transcripts, raw output, environment, or credentials. |
| T-02-16-05 | Denial of service | crash/stale/replay | high | mitigate | Orphan leaf reuse, full-chain reload, exact-head CAS, and single-publication rules fail closed. |
| T-02-16-06 | Spoofing | real Codex origin | critical | transfer | D-29 reserves actual observation for Plan 02-17; continuation creates no host-origin proof. |
</threat_model>

<finding_coverage>
| Finding / spike result | Task | Negative proof | Disposition |
| --- | --- | --- | --- |
| Reverse candidate cycle | Task 1 | Candidate back-reference fields rejected; leaf exists before candidate-ready | mitigated |
| Missing packed post-uninstall producer | Tasks 1/2 | The admitted public continuation is prestarted from an ephemeral synthetic packed fixture, performs uninstall, records scenario 11, publishes leaf, and appends candidate-ready without deleted/private imports | mitigated |
| Continuation restart/replay hazards | Task 2 | Not-prestarted, identity/code mismatch, stale path/head, post-removal restart, orphan recovery, and double publication regressions | mitigated |
| Expected verifier identity | Tasks 1/2 | Mismatched manifest-bound digest fails before uninstall or any journal/candidate mutation | mitigated |
| Weak release binding | Task 2 | Deterministic synthetic package/version/release/tarball/continuation and expected-verifier identities self-check; actual UAT release proof remains Plan 02-17 | mitigated |
| D-29/D-31 boundary | Tasks 1/2 | All broader flags false; actual observation and verifier admission remain Plan 02-17 human-owned | implemented |
</finding_coverage>

<source_coverage>
| Source | ID | Requirement / decision | Plan coverage | Status |
| --- | --- | --- | --- | --- |
| GOAL | — | Packed uninstall continuity mechanism and exact post-uninstall candidate handoff | Tasks 1-2 | COVERED |
| REQ | BLDR-01 | Continuation is present in the self-contained packed inventory/closure and executable from an ephemeral synthetic fixture | Task 2 | COVERED |
| REQ | BLDR-06 | Distinct approved lifecycle basis survives uninstall | Tasks 1-2 | COVERED |
| REQ | BLDR-07 | Candidate mechanism for observed session; verifier/admission follows in Plan 02-17 | Tasks 1-2 | COVERED |
| RESEARCH | — | Packed-only execution, exact identities, value-blind evidence | Tasks 1-2 | COVERED |
| CONTEXT | D-17/D-18/D-19/D-24/D-26/D-27/D-28/D-29/D-30/D-31 | Package, lifecycle, behavior, one-way evidence, human boundary | Tasks 1-2 | COVERED |
| SPIKE | 002/003 | Candidate leaf first, exact packed continuation, and verifier identity handoff to Plan 02-17 | Tasks 1-2 | COVERED |
</source_coverage>

<verification>
- Synthetic packed-fixture, public-continuation, candidate-order, expected-verifier mismatch, stale/replay, and artifact-surface suites pass.
- Namespace/package checks prove the continuation is loaded through `BUILDER_RELEASE_ASSET_INVENTORY` and the closed static import graph before uninstall and performs no standalone verifier/admission work.
- Fixture cleanup proves no package, extraction, attempt, or tarball persists; `npm run check` and `git diff --check` pass after Task 2.
</verification>

<success_criteria>
- A deterministic ephemeral synthetic fixture proves the continuation's package inventory, static closure, identity preflight, and child-process lifecycle, then is fully deleted.
- The admitted packed public continuation starts before uninstall and alone carries uninstall-visibility through leaf-first candidate-ready without recalling deleted assets.
- Candidate is published first, contains no journal head/back-reference, and is later referenced by candidate-ready.
- Manifest-bound expected-verifier mismatch fails before uninstall, journal mutation, candidate publication, or candidate-ready; the continuation never executes verifier code.
- Neither candidate nor decision certifies Codex origin, domain/package quality, production readiness, or wider compatibility.
- No actual UAT attempt, baseline/successor tarball, upgrade result, live evidence, or release publication is created or claimed in this plan.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

- Packed public pre-uninstall continuation with bounded state-derived orphan recovery and hostile lifecycle coverage.
- Content-addressed `agentmo.codex-uat.v1` candidate publisher plus candidate-ready append.
- Manifest-bound expected-verifier preflight with a child-process mismatch regression that proves zero mutation.
- Incremental continuation package inventory/static closure and deterministic ephemeral synthetic packed-fixture coverage with complete cleanup.
- Summary `.planning/phases/02-codex-builder/02-16-SUMMARY.md`.
</artifacts_produced>

<output>
Create `.planning/phases/02-codex-builder/02-16-SUMMARY.md` when done.
</output>
