---
phase: 02-codex-builder
plan: "26"
type: execute
wave: 25
depends_on: ["02-25"]
files_modified:
  - .omx/codex-uat/phase-02-final-retry/continuation.json
  - .planning/phases/02-codex-builder/02-26-UAT-RUN-EVIDENCE.json
autonomous: false
gap_closure: true
requirements: [BLDR-01, BLDR-04, BLDR-05, BLDR-06, BLDR-07]
must_haves:
  truths:
    - "Before any attempt effect, a fresh executor exact-admits the fixed preflight receipt, non-secret 02-25 approval, 02-24 aggregate/VALIDATION gates, and zero-Critical/no-blocking-high review."
    - "One fixed private authority root contains one absent mode-0700 actual-attempt subroot and a mode-0600 schema-validated `agentmo.codex-uat-continuation.v1` authority that survives process exit through CAS/immutable-journal exact state transitions."
    - "No active shell, PTY, or Codex process crosses the checkpoint: the operator runs one record-derived packed command locally, replies only `done`, and a fresh continuation exact-loads the record plus installed value-blind evidence before continuing."
    - "The fixed lifecycle ends at the first bounded failure terminal or candidate-ready; no branch retries, substitutes authority, or fabricates a candidate."
  artifacts:
    - path: .omx/codex-uat/phase-02-final-retry/continuation.json
      provides: Mode-0600 operational `agentmo.codex-uat-continuation.v1` record with fixed-root-relative paths/opaque handles and exact digests
    - path: .planning/phases/02-codex-builder/02-26-UAT-RUN-EVIDENCE.json
      provides: Public value-blind not-authorized, terminal, or candidate-ready status
  key_links:
    - from: fixed preflight receipt plus 02-25 approval
      to: actual-attempt creation
      via: `admit-prior-preflight-receipt` followed by production `init-continuation`; the latter creates the absent actual subroot and O_EXCL continuation only after authorization
    - from: src/builder-codex-uat-private-authority.js
      to: scripts/preflight-codex-uat-prior-attempt.js
      via: production `publishCodexUatContinuation`/`loadCodexUatContinuation`/`transitionCodexUatContinuation` exports consumed by fixed-target init/admit/transition subcommands
    - from: continuation.json packedCommand
      to: operator local terminal
      via: fixed packed invocation completing normal trust/auth and persisting hook/journal evidence without a retained process
    - from: continuation.json transition head plus installed evidence
      to: fresh Task 3 continuation
      via: production `admit-continuation` exact parent/uid/mode/no-symlink/identity/digest/predecessor/head/branch admission before every transition
---

<objective>
Run one authorized packed Codex UAT through a durable private continuation authority that fresh executors can exact-admit (D-17～D-31).

Purpose: Remove cross-checkpoint process assumptions while preserving one-attempt, fail-closed UAT semantics.
Output: One private continuation authority and one public value-blind bounded outcome; no human candidate decision.
</objective>

## Phase Goal

**As a** developer installing AgentMo Builder in Codex, **I want to** install and run the packaged Builder on a clean Codex host and recover its workflow across pause, compaction, restart, upgrade, and uninstall with provable asset ownership, **so that** I can use and audit AgentMo Builder safely without depending on the AgentMo source repository.

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-codex-builder/02-CONTEXT.md
@.planning/phases/02-codex-builder/02-24-SUMMARY.md
@.planning/phases/02-codex-builder/02-24-REVIEW.md
@.planning/phases/02-codex-builder/02-25-SUMMARY.md
@scripts/build-builder-uat-releases.js
@scripts/verify-codex-uat-candidate.js
@scripts/preflight-codex-uat-prior-attempt.js
@src/builder-codex-uat-private-authority.js
@src/builder-codex-uat-continuation.js
@docs/MVP_RUNBOOK.md

<interfaces>
- Fixed authority root: `.omx/codex-uat/phase-02-final-retry/`; fixed actual subroot: `actual-attempt/`; fixed record: `continuation.json`; immutable transition entries live beneath the same root.
- The root/subroot are gitignored/current-user-owned/non-symlink/mode `0700`; records/journal leaves are mode `0600`, absent-only/CAS/immutable-journal published, exact-admitted, and never overwritten on ambiguity.
- `agentmo.codex-uat-continuation.v1` may contain fixed-root-relative internal paths, opaque handles, packed command argv as non-secret strings, expected states, and exact package/release/tarball/manifest/verifier/journal/candidate/hook digests. It excludes credentials, machine-absolute paths, raw transcripts/payloads/output, environment values, and raw evidence bytes.
- No process handle is continuation authority. You will NOT be resumed after Task 2; Task 3 is a fresh executor that exact-loads durable authority and evidence.
- Every private mutation/load uses the 02-23 production entry points: `admit-prior-preflight-receipt`; `init-continuation`; `admit-continuation`; and `transition-continuation`. `init-continuation` creates the absent fixed `actual-attempt/` subroot and O_EXCL `continuation.json`; each later command calls `src/builder-codex-uat-private-authority.js` directly. Public `02-26-UAT-RUN-EVIDENCE.json` is a projection only and never establishes state/head/branch.
- `admit-continuation --compare-projection .planning/phases/02-codex-builder/02-26-UAT-RUN-EVIDENCE.json --require-head --allow-absent-not-authorized` must exact-admit the actual private chain first, prove its transition head and branch, then compare the bounded projection; `not-authorized` is legal only when 02-25 denied authority and the command proves both actual subroot and continuation absent.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exact-admit authority and persist pre-invocation continuation</name>
  <files>.omx/codex-uat/phase-02-final-retry/continuation.json, .planning/phases/02-codex-builder/02-26-UAT-RUN-EVIDENCE.json</files>
  <read_first>AGENTS.md, .planning/phases/02-codex-builder/02-24-REVIEW.md, .planning/phases/02-codex-builder/02-VALIDATION.md, .planning/phases/02-codex-builder/02-25-SUMMARY.md, src/builder-codex-uat-private-authority.js, scripts/preflight-codex-uat-prior-attempt.js, scripts/build-builder-uat-releases.js, src/builder-codex-uat.js, src/builder-install.js, src/builder-codex-host.js, src/builder-codex-uat-continuation.js</read_first>
  <action>In a fresh executor, first run production `admit-prior-preflight-receipt --require-pass`, then exact-admit the non-secret 02-25 decision, 02-24 aggregate/VALIDATION, and zero-Critical/no-blocking-high review before any effect. On `do-not-run`, run production `admit-continuation --expect-absent --allow-absent-not-authorized` to prove the fixed actual subroot/record are absent, and write only the public `not-authorized` projection. On approval, invoke `init-continuation` backed by `publishCodexUatContinuation` to anchored-absent-create only the fixed `actual-attempt/` subroot mode `0700` and O_EXCL/mode-`0600` `continuation.json`; do not create either through ad hoc filesystem calls. Build/admit distinct packed releases and complete setup/activation through closed operations, then invoke `transition-continuation` with the production-admitted predecessor/head to reach `awaiting-local-invocation`, or one legal terminal on the first failure. Include only fixed-root-relative operational paths/opaque handles, fixed packed command, and exact digests. Before process exit, run production `admit-continuation` to exact-reload no-follow root/record/transition journal, prove current head/branch, and compare the bounded public projection. Any stale/wrong head, replacement, partial/duplicate entry, unsafe parent, illegal transition, or ambiguity stops; never scan, guess, reuse, or create a second root.</action>
  <acceptance_criteria>`not-authorized` leaves attempt/record absent; otherwise one exact root reaches one terminal or `awaiting-local-invocation`, and process exit loses no required authority because all continuation state is exact-admitted and durable.</acceptance_criteria>
  <verify><automated>node --test test/builder-codex-uat-prior-attempt.test.js test/builder-codex-uat.test.js test/builder-packed-install.test.js test/builder-codex-host.test.js test/artifact-surface-coverage.test.js &amp;&amp; node scripts/preflight-codex-uat-prior-attempt.js admit-continuation --compare-projection .planning/phases/02-codex-builder/02-26-UAT-RUN-EVIDENCE.json --require-head --allow-absent-not-authorized --expect-one-of awaiting-local-invocation,terminal,not-authorized</automated></verify>
  <done>No active process is required after Task 1; the next action is fully derived from durable exact authority.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Run the fixed packed Codex command locally</name>
  <files>None</files>
  <read_first>.planning/phases/02-codex-builder/02-26-UAT-RUN-EVIDENCE.json, src/builder-codex-uat-private-authority.js, scripts/preflight-codex-uat-prior-attempt.js</read_first>
  <action>Enter only after the prior executor ran production `admit-continuation` and proved the actual private head is `awaiting-local-invocation`; retain no active shell, PTY, or Codex process before this checkpoint. Ask the operator to run in a local terminal the fixed packed command obtained from that admitted private record, complete normal Codex trust/auth and the fresh invocation, and allow the installed hook/journal path to persist only value-blind evidence beneath the fixed authority root. The operator replies only `done` after the command reaches its bounded terminal status. Pause immediately: this checkpoint executes no command, reads no credential/output, appends no entry, and writes no file. You will NOT be resumed. Skip this checkpoint for production-admitted `not-authorized` or `terminal`.</action>
  <continuation_consumer>Fresh Task 3 executor; it exact-loads `continuation.json`, immutable transitions, and installed evidence from the fixed root and trusts no shell/PTY/process state.</continuation_consumer>
  <acceptance_criteria>The response is exactly `done`; no secret/log/path/payload is pasted; no process continuity is assumed.</acceptance_criteria>
  <resume_signal>Reply `done` after the fixed packed command finishes; paste nothing else.</resume_signal>
  <done>Task 3 receives only a bounded signal and must independently admit durable evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Fresh-load durable evidence and continue to one bounded outcome</name>
  <files>.omx/codex-uat/phase-02-final-retry/continuation.json, .planning/phases/02-codex-builder/02-26-UAT-RUN-EVIDENCE.json</files>
  <read_first>.planning/phases/02-codex-builder/02-26-UAT-RUN-EVIDENCE.json, .planning/phases/02-codex-builder/02-25-SUMMARY.md, src/builder-codex-uat-private-authority.js, scripts/preflight-codex-uat-prior-attempt.js, scripts/verify-codex-uat-candidate.js, src/builder-codex-uat.js, src/builder-codex-uat-continuation.js, src/builder-hook-bridge.js</read_first>
  <action>Start as a fresh process with no prior shell/process authority. For `not-authorized`, run production `admit-continuation --expect-absent --allow-absent-not-authorized` and preserve absence; for every authorized branch, first run production `admit-continuation` so `loadCodexUatContinuation` exact-admits the fixed root, no-follow stable record handle, immutable transitions, predecessor/head, journal, fixed packed-command identity, and installed hook evidence before accepting `done`. Never derive the private branch/head from the public projection. On admissible mismatch invoke `transition-continuation` once with the exact admitted predecessor/head to append the legal interruption/failure terminal; stale/replaced/partial/duplicate/unsafe authority stops without mutation. Continue each of the fixed eleven scenarios and packed uninstall from durable exact inputs only, invoking production `admit-continuation` at every fresh continuation and `transition-continuation` after every bounded step. Stop at the first terminal, or publish leaf-before-ready and a read-only packed-verifier preview before the exact-head transition to `candidate-ready`; never append a human candidate decision. Exact-admit the final private head/branch again, then write public evidence only as a bounded status/digest/count/code projection with false/unproven certification fields.</action>
  <acceptance_criteria>A fresh executor independently reaches exactly `not-authorized`, one first terminal, or one exact candidate-ready preview; no retry, second root, stale process authority, or fabricated candidate exists.</acceptance_criteria>
  <verify><automated>node --test test/builder-codex-uat-prior-attempt.test.js test/builder-codex-uat.test.js test/codex-builder-behavior.test.js test/builder-packed-install.test.js test/builder-hook-bridge.test.js test/artifact-surface-coverage.test.js &amp;&amp; node scripts/preflight-codex-uat-prior-attempt.js admit-continuation --compare-projection .planning/phases/02-codex-builder/02-26-UAT-RUN-EVIDENCE.json --require-head --allow-absent-not-authorized --expect-one-of terminal,candidate-ready,not-authorized --require-no-human-decision &amp;&amp; git diff --check</automated></verify>
  <done>02-27 can exact-load one durable bounded branch from the same fixed root without any retained process.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 (block_on: high)

| Threat | STRIDE | Severity | Disposition | Mitigation / test |
| --- | --- | --- | --- | --- |
| Unauthorized host mutation | Elevation | critical | mitigate | Receipt, approval, aggregate/VALIDATION, and review exact-admitted before first effect. |
| Cross-checkpoint authority loss | Spoofing/Tampering | critical | mitigate | Fixed durable continuation schema plus CAS/immutable state; no process handle authority. |
| Root/record replacement | Tampering | critical | mitigate | Fixed root, 0700/0600, uid/no-symlink, absent-only publication, exact reload. |
| Credential/transcript disclosure | Information disclosure | critical | mitigate | Local trust/auth; record/evidence exclude raw/private material. |
| Retry or fabricated candidate | Repudiation/Elevation | high | mitigate | First terminal stops; leaf-before-ready and packed preview required. |
</threat_model>

<verification>Fresh executors exact-admit all durable transitions; no shell/process crosses the checkpoint; the lifecycle reaches one bounded terminal or candidate-ready preview.</verification>

<success_criteria>The authorized attempt is fully continuable from fixed private durable authority and produces one exact bounded outcome without a human-decision append.</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

- One fixed private actual-attempt subroot and `agentmo.codex-uat-continuation.v1` authority only when approved.
- `.planning/phases/02-codex-builder/02-26-UAT-RUN-EVIDENCE.json` with bounded public status.
- No canonical VERIFICATION/STATE/ROADMAP/REQUIREMENTS changes and no human candidate decision.
</artifacts_produced>

<output>Create `.planning/phases/02-codex-builder/02-26-SUMMARY.md` without private paths/raw material; preserve the exact fixed-root record digest and bounded branch for 02-27.</output>
