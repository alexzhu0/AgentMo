---
phase: 02-codex-builder
plan: "20"
type: execute
wave: 19
depends_on: ["02-18", "02-19"]
files_modified:
  - src/builder-install.js
  - src/cli.js
  - test/builder-install-security.test.js
  - test/builder-cli.test.js
  - test/builder-packed-install.test.js
  - test/builder-lifecycle.test.js
autonomous: true
gap_closure: true
requirements: [BLDR-01, BLDR-05, BLDR-06]
must_haves:
  truths:
    - "A failed setup restores the exact project pre-state or leaves one loadable exact recovery transaction; it never returns with canonical managed files that have neither receipt nor recovery authority."
    - "Staging publication remains bound to the open admitted inode through the final absent-only canonical publication."
    - "Receiptless recovery is read-only by default and mutates only after an exact preview digest plus explicit apply; legacy partial state without a recovery record remains read-only and untouched."
  artifacts:
    - path: src/builder-install.js
      provides: Project install transaction, retained publication authority, and receiptless recovery contract
    - path: src/cli.js
      provides: Explicit inspect/preview/apply recovery route
    - path: test/builder-install-security.test.js
      provides: Partial-projection, final-window, and recovery regressions
  key_links:
    - from: project managed-file publication
      to: receipt-last commit
      via: one retained transaction covering every created/replaced inode and prior
    - from: recovery preview
      to: recovery apply
      via: exact transaction/current-state digest and no implicit doctor repair
---

<objective>
关闭 CR-03 与 CR-04，并为未来 receiptless partial projection 提供显式、exact、可重启的 recovery；旧失败 attempt 仍永久只读（D-18、D-19、D-24～D-26、D-28、D-30）。

Purpose: 让 setup 失败不再把开发者困在无 receipt、无修复权威的 canonical partial projection。
Output: 项目安装事务、retained-handle publication、read-only diagnosis + approved recovery、packed regressions。
</objective>

## Phase Goal

**As a** developer installing AgentMo Builder in Codex, **I want to** install and run the packaged Builder on a clean Codex host and recover its workflow across pause, compaction, restart, upgrade, and uninstall with provable asset ownership, **so that** I can use and audit AgentMo Builder safely without depending on the AgentMo source repository.

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-codex-builder/02-CONTEXT.md
@.planning/phases/02-codex-builder/02-REVIEW.md
@.planning/phases/02-codex-builder/02-VERIFICATION.md
@.planning/phases/02-codex-builder/02-19-SUMMARY.md
@src/builder-install.js
@src/builder-codex-host.js
@src/builder-lifecycle.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce partial projection and staging replacement</name>
  <files>test/builder-install-security.test.js, test/builder-packed-install.test.js, test/builder-lifecycle.test.js</files>
  <read_first>AGENTS.md, .planning/phases/02-codex-builder/02-REVIEW.md, src/builder-install.js, src/builder-codex-host.js, src/builder-lifecycle.js, test/builder-install-security.test.js, test/builder-packed-install.test.js, test/builder-lifecycle.test.js</read_first>
  <behavior>
    - Every injected host or receipt failure checks every project managed path, retained prior, receipt, host state, and recovery transaction.
    - A stage replacement injected after the final check but before publication never appears at the canonical destination and is preserved.
    - Recovery refuses missing, corrupt, stale, symlinked, unsafe-ancestor, wrong-root, wrong-inode, or extra-field authority with zero writes.
  </behavior>
  <action>Add failing CR-03/CR-04 tests before implementation. Cover create and replace paths, failures before/after host mutation and receipt publication, process-restart recovery, and a legacy receiptless projection with no recovery record. The legacy case must diagnose only and prove all bytes/inodes unchanged.</action>
  <acceptance_criteria>The existing code fails at least the partial-prestate and final-window assertions; every test snapshots canonical, retained, host, receipt, and recovery surfaces.</acceptance_criteria>
  <verify><automated>node --test test/builder-install-security.test.js test/builder-packed-install.test.js test/builder-lifecycle.test.js</automated></verify>
  <done>CR-03/CR-04 and recovery authority gaps are captured before production changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement receipt-last project transaction and explicit recovery</name>
  <files>src/builder-install.js, src/cli.js, test/builder-install-security.test.js, test/builder-cli.test.js</files>
  <read_first>src/builder-install.js, src/cli.js, src/builder-codex-host.js, src/builder-lifecycle.js, test/builder-install-security.test.js, test/builder-cli.test.js</read_first>
  <action>Keep each stage handle open through publication and bind destination verification/rollback to that inode and a retained parent authority; never link a closed stage pathname. Treat all managed files, host state, and receipt-last publication as one transaction. Before the first project mutation, durably publish a value-blind recovery record identified by an exported schema-version constant; exact-bind project scope/root identity, approved pre-state, created/replaced inode identities, retained priors, host reservation, intended receipt digest, and transaction phase. On failure restore exact pre-state; if an unambiguous restore cannot complete, retain exact operation-owned evidence and leave the recovery record loadable. Add closed `inspectBuilderInstallRecovery`, `planBuilderInstallRecovery`, and `applyBuilderInstallRecovery` operations plus an explicit CLI inspect/preview/apply route; inspect is strictly read-only, and apply requires the exact plan digest. A legacy partial projection without this record has no authority and must remain unchanged. Preserve D-25 no implicit doctor repair and D-26 modified/unknown asset preservation.</action>
  <acceptance_criteria>Successful install still commits receipt last; every rejected install has exact pre-state or a loadable recovery record; recovery is restart-safe, preview-bound, and cannot mutate foreign/changed state; old evidence is never repaired in place.</acceptance_criteria>
  <verify><automated>node --test test/builder-install-security.test.js test/builder-cli.test.js</automated></verify>
  <done>CR-03/CR-04 are closed with an explicit receiptless recovery path rather than manual deletion.</done>
</task>

<task type="auto">
  <name>Task 3: Prove packed recovery closure</name>
  <files>test/builder-packed-install.test.js, test/builder-lifecycle.test.js</files>
  <read_first>src/builder-install.js, src/cli.js, src/builder-package.js, test/builder-packed-install.test.js, test/builder-lifecycle.test.js</read_first>
  <action>Exercise install failure, fresh-process recovery inspection, exact preview/apply, and lifecycle preservation through a packed fixture with no checkout import. Assert the existing packed closure remains complete-or-reject and record the recovery schema producer/loader/CLI consumer plus every I/O/import delta in the summary for 02-23. If a new production module or package asset is unavoidable, add it to this plan's `files_modified` before editing and update the package closure in the same task. Keep output value-blind and non-certifying.</action>
  <acceptance_criteria>Packed recovery works without repository paths; package closure is complete-or-reject; the summary provides a complete recovery I/O/artifact delta; mechanism tests make no domain, production, or wider compatibility claim.</acceptance_criteria>
  <verify><automated>node --test test/builder-install-security.test.js test/builder-cli.test.js test/builder-packed-install.test.js test/builder-lifecycle.test.js &amp;&amp; git diff --check</automated></verify>
  <done>The project transaction and recovery contract are packed and hostile-case tested, ready for the final inventory gate.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 (block_on: high)

| Threat | STRIDE | Severity | Disposition | Mitigation / test |
| --- | --- | --- | --- | --- |
| Pathname replacement | Tampering | critical | mitigate | Retain stage/parent handles through absent-only publication; seam tests cover the final link window. |
| Foreign inode deletion | Tampering | critical | mitigate | Only exact operation-owned inodes may retire; ambiguity is preserved in private retained evidence. |
| Receipt/recovery authority forgery | Spoofing/Elevation | critical | mitigate | Exact schema, project/root/inode/digest CAS, preview digest, and closed CLI apply. |
| False-negative durable commit | Repudiation/DoS | high | mitigate | Transaction phase and receipt-last commit distinguish restored, recovery-required, and committed outcomes. |
| Symlink/unsafe ancestor | Tampering | high | mitigate | No-follow retained root/parent checks before every publication and recovery effect. |
| Secret/transcript disclosure | Information disclosure | high | mitigate | Recovery artifacts are value-blind; no `.env`, raw output, payload, transcript, or private UAT path is persisted. |
</threat_model>

<verification>Focused install/CLI/packed/lifecycle gates and `git diff --check` pass; 02-23 owns the final exact inventory reconciliation. Only disposable temporary fixtures are mutated.</verification>

<success_criteria>CR-03/CR-04 regressions pass; failed setup is always restorable or exact-recoverable; staging replacement is never published; legacy failed evidence stays read-only.</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

- `agentmo.builder-install-recovery.v1` durable value-blind recovery record.
- Public `inspectBuilderInstallRecovery`, `planBuilderInstallRecovery`, and `applyBuilderInstallRecovery` operations plus the exact CLI route.
- Internal retained project transaction/publication helpers.
- No actual UAT artifact and no repair of the terminated attempt.
</artifacts_produced>

<output>Create `.planning/phases/02-codex-builder/02-20-SUMMARY.md` when done.</output>
