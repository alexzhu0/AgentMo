---
phase: 02-codex-builder
plan: "19"
type: execute
wave: 18
depends_on: ["02-17"]
files_modified:
  - src/builder-codex-host.js
  - test/builder-codex-host.test.js
  - test/builder-lifecycle.test.js
autonomous: true
gap_closure: true
requirements: [BLDR-06]
must_haves:
  truths:
    - "Every mutable shared host-state ancestor is current-user-owned, non-symlink, and not group/world writable before mutation."
    - "Owner/ledger publication, retirement, restore, retract, and cleanup remain bound to retained inode and directory authority through the final filesystem operation."
    - "A final-window replacement is preserved byte-for-byte and is never adopted as owner/ledger authority or deleted."
  artifacts:
    - path: src/builder-codex-host.js
      provides: Retained shared-host root and owner/ledger CAS authority
    - path: test/builder-codex-host.test.js
      provides: Unsafe-ancestor and link/unlink/restore final-window regressions
  key_links:
    - from: resolveStateRoot
      to: owner/ledger writers
      via: retained root/ancestor identities carried for the entire operation
    - from: reservation
      to: restore/retract cleanup
      via: exact inode plus operation-owned retained evidence, never pathname-only authority
---

<objective>
关闭 CR-05 与 CR-06，确保共享 Codex host root、selector owner 与 consumer ledger 在竞争和补偿下仍有可证明所有权（D-19、D-24～D-26、D-28）。

Purpose: 防止不安全 ancestor、pathname replacement 或 restore/retract race 伪造或删除共享 host authority。
Output: Retained-root authority、identity-bound state CAS/cleanup、host/lifecycle hostile tests。
</objective>

## Phase Goal

**As a** developer installing AgentMo Builder in Codex, **I want to** install and run the packaged Builder on a clean Codex host and recover its workflow across pause, compaction, restart, upgrade, and uninstall with provable asset ownership, **so that** I can use and audit AgentMo Builder safely without depending on the AgentMo source repository.

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-codex-builder/02-CONTEXT.md
@.planning/phases/02-codex-builder/02-REVIEW.md
@.planning/phases/02-codex-builder/02-VERIFICATION.md
@src/builder-codex-host.js
@src/builder-lifecycle.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce unsafe-root and final-window host races</name>
  <files>test/builder-codex-host.test.js, test/builder-lifecycle.test.js</files>
  <read_first>AGENTS.md, .planning/phases/02-codex-builder/02-REVIEW.md, src/builder-codex-host.js, src/builder-install.js, src/builder-lifecycle.js, test/builder-codex-host.test.js, test/builder-lifecycle.test.js</read_first>
  <behavior>
    - Foreign-owned, group-writable, world-writable, symlinked, or replaced state-root components fail before mutation.
    - Replacements injected after final validation but before link, unlink, restore, or cleanup remain present with exact bytes/inode.
    - Owner/ledger current state never adopts the injected inode, and compensation never deletes it.
  </behavior>
  <action>Add deterministic seams immediately after the last identity validation and before each filesystem effect named by CR-06. Write failing tests for root ownership/mode and for publication, retirement, restore, retract, and cleanup final windows. Snapshot owner, ledger, reservation, retained evidence, and unrelated files before/after.</action>
  <acceptance_criteria>The tests fail against the reviewed lstat/link/unlink implementation and prove both canonical authority and competitor preservation.</acceptance_criteria>
  <verify><automated>node --test test/builder-codex-host.test.js test/builder-lifecycle.test.js</automated></verify>
  <done>Every CR-05/CR-06 race has a deterministic failing reproduction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retain shared-root and inode authority through CAS finalization</name>
  <files>src/builder-codex-host.js, test/builder-codex-host.test.js, test/builder-lifecycle.test.js</files>
  <read_first>src/builder-codex-host.js, src/builder-lifecycle.js, test/builder-codex-host.test.js, test/builder-lifecycle.test.js</read_first>
  <action>Require current UID and `(mode &amp; 0o022) === 0` on every mutable state-root ancestor, open no-follow directory handles, and recheck retained/current identity across reservation, write, restore, retract, and cleanup. Keep source handles open through absent-only publication. Replace validate-then-unlink cleanup with rename-to-operation-owned private retained evidence; on any ambiguity preserve all entries and return a bounded conflict. Apply the same invariant to `writeStateArtifact`, `restoreExactRetained`, `retractStateArtifactIfExact`, and `restoreRetainedIfDestinationAbsent`. Preserve the fixed official Codex commands and D-26 sibling/modified asset rules.</action>
  <acceptance_criteria>Unsafe ancestors cause zero writes; every host-state effect is handle/root bound; exact state commits or a bounded conflict preserves all competitors; lifecycle tests retain sibling and user-owned evidence.</acceptance_criteria>
  <verify><automated>node --test test/builder-codex-host.test.js test/builder-lifecycle.test.js</automated></verify>
  <done>CR-05/CR-06 are closed at the shared-host trust boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Close shared-host caller and evidence parity</name>
  <files>test/builder-codex-host.test.js, test/builder-lifecycle.test.js</files>
  <read_first>src/builder-codex-host.js, src/builder-lifecycle.js, test/builder-codex-host.test.js, test/builder-lifecycle.test.js</read_first>
  <action>Exercise activation, project lifecycle, selector removal, compensation, and restart through the same retained-root authority. Assert diagnostics remain relative, bounded, value-blind, and non-certifying. Record every added or removed open, rename, link, unlink, sync, durable identity, and producer/consumer edge in the summary so 02-23 can reconcile the canonical inventories once after all repairs.</action>
  <acceptance_criteria>All production callers preserve the retained authority and competitor bytes; the summary contains a complete host I/O/artifact delta; focused tests and diff hygiene pass.</acceptance_criteria>
  <verify><automated>node --test test/builder-codex-host.test.js test/builder-lifecycle.test.js &amp;&amp; git diff --check</automated></verify>
  <done>Host authority callers are closed and their inventory delta is ready for 02-23.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 (block_on: high)

| Threat | STRIDE | Severity | Disposition | Mitigation / test |
| --- | --- | --- | --- | --- |
| Symlink/unsafe ancestor | Tampering/Elevation | critical | mitigate | UID/mode/no-follow retained ancestor checks before every mutation. |
| Pathname replacement | Tampering | critical | mitigate | Post-validation seams and retained handles/root identity cover every final window. |
| Foreign inode deletion | Tampering | critical | mitigate | Rename exact operation-owned entries to private retention; preserve ambiguity. |
| Authority forgery | Spoofing | high | mitigate | Owner/ledger writes require reservation plus exact digest and inode CAS. |
| False-negative durable commit | Repudiation/DoS | high | mitigate | Commit/compensation returns explicit bounded state; journal-specific contract is completed in 02-21 before UAT. |
| Information disclosure | Information disclosure | high | mitigate | State and diagnostics contain digests/relative identities only; no raw host output or credentials. |
</threat_model>

<verification>Focused host/lifecycle hostile tests pass, the exact inventory delta is handed to 02-23, and `git diff --check` passes. No host mutation outside temporary fixtures is allowed.</verification>

<success_criteria>CR-05/CR-06 regressions pass, unsafe roots fail before effects, and final-window competitors are preserved rather than adopted or deleted.</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

- Internal retained shared-root and inode-bound CAS helpers; no new public CLI or support claim.
- Optional internal recovery record only if required for exact restart recovery; if created, its schema and inventory rows are mandatory.
- No real host state, UAT artifact, or release evidence is produced.
</artifacts_produced>

<output>Create `.planning/phases/02-codex-builder/02-19-SUMMARY.md` when done.</output>
