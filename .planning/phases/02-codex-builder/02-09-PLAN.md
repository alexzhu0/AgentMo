---
phase: 02-codex-builder
plan: "09"
type: execute
wave: 9
depends_on: ["02-08"]
files_modified:
  - src/builder-codex-uat.js
  - src/builder-package.js
  - src/builder-behavior-eval.js
  - src/cli.js
  - package.json
  - test/builder-codex-uat.test.js
  - test/codex-builder-behavior.test.js
  - test/builder-packed-install.test.js
  - test/helpers/io-surface-inventory.js
autonomous: true
gap_closure: true
requirements: [BLDR-01, BLDR-07]
must_haves:
  truths:
    - Real-Codex evidence has exact identity agentmo.codex-uat.v1, canonical value-blind schema, producer, validator, CLI and digest admission.
    - The fixed scenario set binds official activation/trust/auth/isolation observations to installed bridge/checkpoint provenance, never standalone hooks or synthetic events.
    - Behavior evaluation keeps packed mechanism evidence separate from a complete exact-admitted real-session candidate and never upgrades domain/package/production certification.
    - The new UAT module is added to canonical runtime inventory, package syntax checks and clean packed import closure in the same plan.
    - This plan consumes the challenge/observation interface from 02-08 without modifying builder-hook-bridge.js.
  artifacts:
    - path: src/builder-codex-uat.js
      provides: Versioned UAT run/result schema, producer, validator and exact admission
    - path: src/builder-behavior-eval.js
      provides: Connected exact UAT admission separate from mechanism evidence
    - path: test/builder-codex-uat.test.js
      provides: Producer, schema, value-blind and provenance negative matrix
    - path: test/builder-packed-install.test.js
      provides: Clean packed UAT module/CLI import-closure proof
  key_links:
    - from: src/builder-codex-uat.js
      to: src/builder-hook-bridge.js
      via: Stable challenge/observation interface delivered by 02-08
    - from: src/builder-behavior-eval.js
      to: src/builder-codex-uat.js
      via: Exact candidate bytes/digest and full provenance validation
    - from: src/builder-codex-uat.js
      to: src/builder-package.js
      via: New module is part of the fixed packed runtime inventory
---

<objective>
Create the formal value-blind real-Codex UAT artifact route and exact behavior-evaluator admission gate.

Purpose: Make real host evidence durable and non-self-certifying without editing the installed bridge or mixing mechanism tests with support claims.
Output: UAT producer/schema/validator/CLI, exact evaluator admission, packed closure and negative provenance tests.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-codex-builder/02-CONTEXT.md
@.planning/phases/02-codex-builder/02-PATTERNS.md
@.planning/phases/02-codex-builder/02-VALIDATION.md
@.planning/phases/02-codex-builder/02-VERIFICATION.md
@.planning/phases/02-codex-builder/02-08-SUMMARY.md
@src/builder-hook-bridge.js
@src/builder-behavior-eval.js
@src/cli.js
@package.json

<interfaces>
- Result identity is exactly agentmo.codex-uat.v1.
- Fixed ordered scenarios are skill-discovery, session-start, user-prompt-non-trigger, manual-pause, pre-compact, post-compact, restart-resume, duplicate-replay, second-compaction, upgrade-visibility and uninstall-visibility.
- Each scenario contains only ID, pass/fail, before/after checkpoint digests, bounded observation digest and producer provenance. Raw prompt/payload/transcript/stdout/stderr/path/environment/credential fields are forbidden.
- Top-level bindings include release/install/owner/consumer/project digests, hostScope user, selector, Codex version, normalized host observation digest, normal trust/auth observation provenance, isolation flags, launcher provenance, scenario-set digest, ordered-results digest and false domain/package/production certification flags.
- Commands are builder codex-uat begin, record and finalize with absent outputs and one-to-one exact digest bindings, plus builder behavior --uat with builder-codex-uat digest admission.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the formal value-blind UAT producer, validator and CLI</name>
  <files>src/builder-codex-uat.js, src/cli.js, test/builder-codex-uat.test.js</files>
  <read_first>src/builder-hook-bridge.js, src/builder-codex-host.js, src/builder-checkpoint.js, src/cli.js, AGENTS.md</read_first>
  <behavior>
    - begin exact-admits release/install/owner/consumer/host observations, isolated HOME/CODEX_HOME and normal trust/auth provenance, then creates only an absent run file.
    - record requires exact prior run digest and fixed next scenario; unknown, duplicate, reordered or stale requests are zero-write.
    - Host-delivery scenarios are admitted only from 02-08 challenge-bound bridge observations; non-trigger uses unchanged checkpoint/bridge counters; lifecycle scenarios use official observations.
    - finalize requires every ordered scenario, canonicalizes exact result bytes to an absent path and returns their digest.
    - Validator rejects private/raw fields, source-checkout/global-launcher dependence, trust bypass, non-isolation, mismatched selector/scope/digests, caller booleans, incomplete scenarios and any non-false quality/production flag.
  </behavior>
  <action>Write exact schema, journal CAS, ordering, private-field and disconnected-provenance failures first. Implement canonical run/result builders and validators with absent-output publication and prior-digest CAS. Consume the challenge/observation API delivered by 02-08 without changing the bridge. Expose only fixed begin/record/finalize CLI routes with one-to-one digest parsing; derive observations from official host/checkpoint interfaces rather than user-entered success. Keep the artifact a candidate pending the 02-10 human gate. This implements D-17, D-27 and D-28.</action>
  <acceptance_criteria>Only a complete connected, isolated, normally trusted/authenticated and value-blind scenario run can become exact UAT candidate bytes.</acceptance_criteria>
  <verify>
    <automated>node --test test/builder-codex-uat.test.js</automated>
  </verify>
  <done>Real-host evidence has one versioned producer/schema/validator/CLI and exact digest route.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exact-admit connected UAT in behavior evaluation</name>
  <files>src/builder-behavior-eval.js, src/builder-codex-uat.js, test/codex-builder-behavior.test.js, test/builder-codex-uat.test.js, test/helpers/io-surface-inventory.js</files>
  <read_first>src/builder-behavior-eval.js, src/builder-codex-uat.js, test/codex-builder-behavior.test.js, test/helpers/io-surface-inventory.js</read_first>
  <behavior>
    - Packed connected tests remain fresh-process-mechanism with activation/host verification false.
    - Exact complete UAT admission reports a bounded real-codex-session-candidate and every bound digest; human admission remains required for requirement completion.
    - Standalone hook plus synthetic event, fake observer, stale receipt/release/owner/consumer, partial scenarios, trust bypass, non-isolated home or caller result claims cannot become a candidate.
    - Reports always keep agentPackageQualityCertified, domainQualityCertified and productionApproved false.
    - Exact production I/O inventory is updated only after source lines settle and covers the new UAT writer/reader surfaces.
  </behavior>
  <action>Replace disconnected aggregation as a host-evidence source with exact UAT admission while retaining the installed-chain mechanism lane. Bind all release, install, owner, consumer, project, observation, scenario/result and final-evidence digests. Reject injected observation/result/verification flags and preserve non-transitive certification. Update the exact I/O allowlist after code stabilizes and test its static closure. This implements D-17, D-27 and D-28.</action>
  <acceptance_criteria>Behavior evaluation distinguishes mechanism proof from one exact connected real-session candidate and cannot synthesize either host verification or broader certification.</acceptance_criteria>
  <verify>
    <automated>node --test test/codex-builder-behavior.test.js test/builder-codex-uat.test.js test/artifact-surface-coverage.test.js</automated>
  </verify>
  <done>Connected UAT provenance is exact-admitted and evidence levels remain separate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close UAT production code over package inventory and syntax checks</name>
  <files>src/builder-package.js, package.json, test/builder-packed-install.test.js</files>
  <read_first>src/builder-package.js, package.json, src/builder-codex-uat.js, test/builder-packed-install.test.js</read_first>
  <behavior>
    - builder-codex-uat.js is in the fixed release/runtime inventory and release digest.
    - package.json explicitly executes node --check for the new module.
    - Clean unpacked tarball imports the UAT module and exercises its CLI help/validation surface through the local launcher.
    - Omitting the module from inventory, syntax closure or packed import closure fails focused tests.
  </behavior>
  <action>Add the new production module to the canonical runtime allowlist and package.json check command, then extend packed import/CLI closure in the same task. Assert exact source/destination/digest membership from the tarball. Do not treat npm files inclusion as proof that the runtime can import the module. This preserves D-18 and D-28.</action>
  <acceptance_criteria>UAT production code cannot land without exact package inventory, syntax-check and clean packed import coverage.</acceptance_criteria>
  <verify>
    <automated>node --test test/builder-packed-install.test.js &amp;&amp; npm run check</automated>
  </verify>
  <done>The formal UAT route is fully present in the co-released local runtime.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Trust Boundaries

| Boundary | Description |
| --- | --- |
| real session -> UAT journal | Host observations may be forged, partial or private. |
| UAT artifact -> evaluator | Durable evidence may be stale, disconnected or self-certifying. |
| packed runtime -> UAT producer | Missing production imports could make the clean install unusable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-02-09-01 | Spoofing | producer provenance | high | mitigate | Challenge-bound bridge and official observation digests with fixed scenarios. |
| T-02-09-02 | Tampering | UAT journal/result | high | mitigate | Canonical schema, prior-digest CAS, absent output and exact admission. |
| T-02-09-03 | Repudiation | scenario completion | medium | mitigate | Fixed order and scenario/result digests bound to one run. |
| T-02-09-04 | Information disclosure | real session data | critical | mitigate | Value-blind closed fields and rejection of raw/private keys or values. |
| T-02-09-05 | Denial of service | incomplete UAT | low | accept | Candidate remains incomplete with zero support claim and bounded first failure. |
| T-02-09-06 | Elevation of privilege | self-certifying evidence | critical | mitigate | Candidate-only status, connected provenance and external human gate in 02-10. |
| T-02-09-SC | Tampering | dependency installation | low | accept | No dependency install; only exact repository pack is tested. |
</threat_model>

<source_coverage>
- GOAL real install/recovery/behavior evidence: Tasks 1-3.
- REQ BLDR-01/BLDR-07: formal candidate mechanism is covered; human support closure is 02-10.
- RESEARCH/PATTERNS trust/auth/isolation, connected provenance, exact digest and non-certification: Tasks 1-3.
- CONTEXT D-17/D-18/D-27/D-28: cited in actions; prior lifecycle/recovery decisions are consumed unchanged.
</source_coverage>

<verification>
- node --test test/builder-codex-uat.test.js test/codex-builder-behavior.test.js test/builder-packed-install.test.js test/artifact-surface-coverage.test.js
- npm pack --dry-run --json
- npm run check
- git diff --check
</verification>

<success_criteria>
- Exact agentmo.codex-uat.v1 candidate bytes come only from fixed connected value-blind scenarios.
- Evaluator exact-admits candidate provenance while keeping mechanism and broader certifications separate.
- UAT module is covered by package inventory, package.json syntax checks and clean packed imports.
- builder-hook-bridge.js is not modified by this plan.
</success_criteria>

<artifacts_produced>
- Packed src/builder-codex-uat.js producer/schema/validator/CLI.
- Exact UAT candidate admission in src/builder-behavior-eval.js.
- Value-blind, disconnected/synthetic and I/O inventory negative coverage.
- Updated package inventory, package.json syntax check and packed import closure.
</artifacts_produced>

<output>
Create .planning/phases/02-codex-builder/02-09-SUMMARY.md when done.
</output>
