---
phase: 02-codex-builder
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - plugin/.codex-plugin/plugin.json
  - plugin/skills/agentmo/SKILL.md
  - plugin/agents/agentmo.toml
  - plugin/hooks/hooks.json
  - plugin/hooks/agentmo-hook.js
  - src/builder-package.js
  - src/builder-install.js
  - src/builder-doctor.js
  - src/cli.js
  - package.json
  - test/builder-packed-install.test.js
  - test/builder-doctor.test.js
autonomous: true
requirements: [BLDR-01, BLDR-05]
must_haves:
  truths:
    - One packed release contains canonical core and Codex plugin assets and needs no source checkout.
    - Project-scoped setup is explicit, previews exact operations, and publishes a per-path receipt atomically.
    - Doctor is strictly read-only and separates declared, observed, and inconsistent state.
  artifacts: [plugin/.codex-plugin/plugin.json, src/builder-install.js, src/builder-doctor.js]
  key_links:
    - package.json packs the same plugin bytes whose digests appear in the setup receipt.
---

<objective>
Turn the working core into one self-contained Codex Builder release with project-first setup and read-only doctor, implementing D-18, D-19, and D-25.
</objective>

<threat_model>
- Path traversal/symlink escape: normalize every destination beneath the approved scope root and reject escapes before mutation.
- Partial install: stage all files, verify hashes, atomically publish receipt last, and leave no success marker on failure.
- Diagnostic mutation/leakage: doctor performs reads only and emits bounded value-blind observations.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build the canonical plugin projection and packed setup</name>
  <files>plugin/.codex-plugin/plugin.json, plugin/skills/agentmo/SKILL.md, plugin/agents/agentmo.toml, plugin/hooks/hooks.json, plugin/hooks/agentmo-hook.js, src/builder-package.js, src/builder-install.js, src/cli.js, package.json, test/builder-packed-install.test.js</files>
  <read_first>package.json, src/builder-probe.js, src/builders/codex.js, .planning/phases/02-codex-builder/02-RESEARCH.md</read_first>
  <action>Create one canonical plugin root. Generate an exact install plan for repo marketplace/plugin/agent/checkpoint state, defaulting to project scope; require explicit apply after probe; publish per-path source/destination digests, marker, version, release digest, and capability snapshot. Test with `npm pack` installed into an absent temporary Codex project.</action>
  <acceptance_criteria>Packed setup works without the source repository; repeat setup is deterministic; path conflicts or failed required probe leave no success receipt.</acceptance_criteria>
  <done>A packed release performs deterministic project-scoped setup and writes an exact receipt last.</done>
  <verify><automated>node --test test/builder-packed-install.test.js</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Implement a value-blind read-only doctor</name>
  <files>src/builder-doctor.js, src/cli.js, package.json, test/builder-doctor.test.js</files>
  <read_first>src/builder-install.js, src/builder-probe.js, src/secret-redaction.js, test/runtime-compatibility.test.js</read_first>
  <action>Report release/source identity, active/missing/degraded capability, skill/hook/agent visibility, receipt/marker/current digest state, and checkpoint consistency. Never repair, trust hooks, rewrite config, or mutate filesystem; expose separate explicit remediation guidance.</action>
  <acceptance_criteria>Filesystem snapshot before/after doctor is byte-identical; modified/missing/corrupt assets are reported without repair; output contains no secret/environment values.</acceptance_criteria>
  <done>Doctor reports lifecycle consistency without changing any observed byte.</done>
  <verify><automated>node --test test/builder-doctor.test.js test/builder-packed-install.test.js &amp;&amp; npm run check</automated></verify>
</task>

</tasks>

<verification>
- `node --test test/builder-packed-install.test.js test/builder-doctor.test.js`
- `npm pack --dry-run`
- `npm run check`
- `git diff --check`
</verification>

<success_criteria>
- BLDR-01 and BLDR-05 pass against a clean temporary project and packed artifact.
</success_criteria>

## Artifacts this phase produces

- Self-contained Codex plugin projection, setup receipt, and read-only doctor.
