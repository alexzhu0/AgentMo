---
phase: 03-build-contract
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - src/decision-ledger.js
  - src/design-plan.js
  - src/blueprint-draft.js
  - src/artifact-registry.js
  - src/artifact-subjects.js
  - src/cli.js
  - package.json
  - test/decision-ledger.test.js
  - test/design-plan.test.js
  - test/blueprint-draft.test.js
  - test/phase3-contracts.test.js
  - test/artifact-subjects.test.js
  - test/artifact-surface-coverage.test.js
  - test/command-docs.test.js
  - test/helpers/io-surface-inventory.js
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-03
must_haves:
  truths:
    - "A developer can resume planning from approved discovery artifacts and a durable predecessor-bound decision ledger rather than a raw transcript or session history."
    - "Every ledger entry is exactly one fact, inference, unknown, rejected option, or human decision with bounded source/decision references and append-only lineage."
    - "A developer can trace every requirement to supporting sources and decisions and traverse each source back to affected requirements, capabilities, and eval cases."
    - "Dangling, forked, stale-head, transcript-bearing, secret-bearing, or semantically overclaimed planning state fails closed."
  artifacts:
    - path: "src/decision-ledger.js"
      provides: "Typed predecessor-bound durable Plan state"
      exports: ["DECISION_LEDGER_SCHEMA_VERSION", "DECISION_ENTRY_KINDS", "appendDecisionEntry", "loadDecisionLedger", "validateDecisionLedger"]
    - path: "src/design-plan.js"
      provides: "Approved-source planning plus bidirectional trace graph"
    - path: "test/decision-ledger.test.js"
      provides: "Lineage, restart, ref-closure, secret, transcript, and concurrency tests"
  key_links:
    - from: "src/decision-ledger.js"
      to: "src/builder-immutable-journal.js"
      via: "generic immutable append primitive with typed, persistability-checked canonical payloads"
      pattern: "immutable|predecessorDigest|valueDigest"
    - from: "src/design-plan.js"
      to: "src/decision-ledger.js"
      via: "exact admitted ledger head is the only durable human-planning state input"
      pattern: "decision-ledger|headDigest"
    - from: "src/design-plan.js"
      to: "src/blueprint-draft.js"
      via: "closed forward/reverse source, requirement, capability, and eval edges carry into the draft without a reviewed claim"
      pattern: "trace|source.*requirement|capability|eval"
---

## Phase Goal

**As a** developer, **I want to** turn bounded Web, GitHub, paper, and local research into an approved, traceable blueprint and Package build contract, **so that** Produce receives exact-digest intent without relying on prompts, session ancestry, or unverified declarations.

<objective>
Make ongoing human planning portable, typed, append-only, and bidirectionally traceable.

Purpose: Satisfy PLAN-01 through PLAN-03 without turning a conversation transcript or generated plan into state authority.
Output: `agentmo.decision-ledger.v1`, public append/inspect behavior, exact ledger-head Plan admission, and closed source ↔ requirement/capability/eval trace edges.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03-build-contract/03-RESEARCH.md
@.planning/phases/03-build-contract/03-PATTERNS.md
@.planning/phases/03-build-contract/03-03-SUMMARY.md
@src/builder-immutable-journal.js
@src/design-plan.js
@src/blueprint-draft.js

<interfaces>
Ledger identity: `agentmo.decision-ledger.v1`.
Closed entry kinds: `fact`, `inference`, `unknown`, `rejected-option`, `human-decision`.
Every append binds the current authentic head digest, canonical sequence, predecessor digest, stable entry ID, bounded subject/reason, and canonical source/decision references.
The public `decision-ledger` command supports read-only inspect and explicit append from a typed entry JSON artifact; it accepts no free-form transcript/stdin body.
`design-plan` requires the exact current ledger head and emits normalized forward and reverse edge arrays with referential closure.
</interfaces>
</context>

## Artifacts this phase produces

This plan creates `src/decision-ledger.js` and `test/decision-ledger.test.js`; exports `DECISION_LEDGER_SCHEMA_VERSION`, `DECISION_ENTRY_KINDS`, `appendDecisionEntry`, `loadDecisionLedger`, and `validateDecisionLedger`; adds durable subject `decision-ledger`, public `decision-ledger inspect|append` behavior, and normalized `forwardTraceEdges`/`reverseTraceEdges` connecting source IDs, decision IDs, requirement IDs, capability IDs, and eval case IDs.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify typed append-only planning state and trace closure</name>
  <read_first>
    - test/builder-immutable-journal-v1.test.js
    - test/design-plan.test.js
    - test/blueprint-draft.test.js
    - src/builder-immutable-journal.js
    - .planning/phases/03-build-contract/03-PATTERNS.md
  </read_first>
  <files>test/decision-ledger.test.js, test/design-plan.test.js, test/blueprint-draft.test.js, test/phase3-contracts.test.js</files>
  <behavior>
    - Genesis and successors have contiguous sequence, exact predecessor digest, canonical value digest, stable entry IDs, and fresh-process head recovery.
    - Entry kinds are closed; each payload permits bounded typed fields and canonical refs but rejects raw transcript, messages, tool bodies, stdout/stderr, environment values, credentials, and unrestricted notes.
    - Wrong predecessor, fork, duplicate event conflict, tampered entry, stale head, interrupted publication, dangling source/decision ref, and secret-shaped payload fail closed.
    - Planning resumes from exact approval/DB/user-need/ledger artifacts; a fresh process reconstructs the same design plan without conversation history.
    - Trace edges are bidirectional and closed: every requirement has sources/decisions; every source records affected requirements/capabilities/evals; all edge endpoints exist.
    - Generated design/blueprint state remains draft/non-authoritative until the later exact plan approval.
  </behavior>
  <action>
Write the ledger durability/security suite and extend Plan/blueprint/phase-contract tests before implementation. Reuse the immutable-journal kill/restart and predecessor tests, but validate typed decoded payloads before append so generic canonical bytes cannot carry prohibited material. Use explicit edge equality and referential-closure assertions rather than snapshots. Assert that one-way support trace does not satisfy PLAN-03.
  </action>
  <acceptance_criteria>
    - Tests cover all five kinds, durable restart, stale/fork/tamper cases, secret/transcript rejection, and bidirectional edge closure.
    - Tests prove session/chat history is unnecessary and cannot be admitted as Plan authority.
    - Blueprint provenance no longer self-reports human review.
  </acceptance_criteria>
  <done>Ledger and trace tests fail on missing implementation while fully specifying durability, safety, and bidirectional closure.</done>
  <verify>
    <automated>node --test test/decision-ledger.test.js test/design-plan.test.js test/blueprint-draft.test.js test/phase3-contracts.test.js; test $? -ne 0</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the decision ledger and bidirectional Plan trace graph</name>
  <read_first>
    - src/builder-immutable-journal.js
    - src/persistability.js
    - src/artifact-admission.js
    - src/design-plan.js
    - src/blueprint-draft.js
  </read_first>
  <files>src/decision-ledger.js, src/design-plan.js, src/blueprint-draft.js, src/artifact-registry.js, src/artifact-subjects.js</files>
  <action>
Build the typed ledger on the existing generic immutable-journal primitive if its public contract fits; do not duplicate its crash-consistency mechanism. Validate the complete typed payload, refs, size, persistence safety, current authentic head, and predecessor before append. Load and validate the full bounded lineage in fresh processes. Extend design-plan construction to require the exact ledger head and to normalize source/decision/requirement/capability/eval nodes plus closed forward and reverse edges. Carry trace references into blueprint drafting while replacing any generated reviewed authority with draft/non-authoritative provenance. Preserve existing exact input admission and coverage semantics.
  </action>
  <acceptance_criteria>
    - The ledger is append-only, predecessor-bound, restart-safe, and rejects all prohibited raw-material fields before durable publication.
    - Design-plan construction fails on missing/dangling/one-way traces and is deterministic from the same exact artifacts.
    - Blueprint generation exposes trace and draft state without claiming human review.
  </acceptance_criteria>
  <done>Typed durable planning state and closed bidirectional trace graphs pass focused restart, security, and Plan tests.</done>
  <verify>
    <automated>node --test test/decision-ledger.test.js test/design-plan.test.js test/blueprint-draft.test.js test/persistability.test.js</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Expose bounded ledger inspect/append and close public inventories</name>
  <read_first>
    - src/cli.js
    - src/artifact-registry.js
    - src/artifact-subjects.js
    - test/artifact-subjects.test.js
    - test/artifact-surface-coverage.test.js
    - test/command-docs.test.js
    - test/helpers/io-surface-inventory.js
    - package.json
  </read_first>
  <files>src/cli.js, src/artifact-registry.js, src/artifact-subjects.js, package.json, test/artifact-subjects.test.js, test/artifact-surface-coverage.test.js, test/command-docs.test.js, test/helpers/io-surface-inventory.js, test/phase3-contracts.test.js</files>
  <action>
Add public `decision-ledger inspect` and `decision-ledger append` routes with exact current-head digest binding and a separately admitted typed entry file. Inspect is read-only; append writes one immutable successor and reports bounded identity/sequence/head/reference data only. Extend `design-plan` CLI subjects with the exact ledger head. Synchronize durable registry, package, syntax, help, output owner, command subject, and I/O inventories. Do not accept a raw prompt, stdin transcript, tool output, actor credential, or mutable ledger-head file as authority.
  </action>
  <acceptance_criteria>
    - A fresh CLI process can inspect and append exactly one typed successor, then build the same traceable Plan from its exact head.
    - Stale-head and duplicate/fork attempts fail without replacing prior durable state.
    - Closed public/artifact/I/O inventories contain no transcript or mutable-head bypass.
  </acceptance_criteria>
  <done>Fresh-process inspect/append/resume works with exact head authority and all public inventories are closed.</done>
  <verify>
    <automated>node --test test/decision-ledger.test.js test/design-plan.test.js test/blueprint-draft.test.js test/phase3-contracts.test.js test/artifact-subjects.test.js test/artifact-surface-coverage.test.js test/command-docs.test.js &amp;&amp; git diff --check</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| typed entry artifact → immutable ledger | Schema/ref/persistability validation must precede append publication. |
| prior head → successor | Exact authentic predecessor and contiguous sequence provide compare-and-set authority. |
| ledger/approved discovery → design plan | All artifacts are independently admitted; graph endpoints must be closed and bidirectional. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-16 | Tampering | predecessor/head lineage | high | mitigate | Exact authentic head, contiguous sequence, predecessor digest, canonical bytes, stale/fork/tamper/restart tests. |
| T-03-17 | Information Disclosure | ledger payload | high | mitigate | Closed typed schema and persistability gate reject transcripts, tool bodies, stdout/stderr, environment, credentials, and unbounded text before append. |
| T-03-18 | Repudiation | fact/inference/human-decision semantics | medium | mitigate | Closed entry kinds, explicit source and decision refs, stable entry IDs, and no authenticated-person overclaim. |
| T-03-19 | Spoofing / Tampering | trace graph | high | mitigate | Referential closure plus exact forward/reverse equality; dangling, one-way, duplicated, or conflicting ownership fails closed. |
</threat_model>

<verification>
Run ledger durability/security, design-plan, blueprint-draft, composed phase, persistability, artifact closure, and command-doc suites plus `git diff --check`.
</verification>

<success_criteria>
- Durable planning resumes without transcripts or session ancestry.
- Typed facts, inferences, unknowns, rejected options, and human decisions remain distinct.
- Every required source/decision/requirement/capability/eval relation is traversable both ways.
- Draft generation still cannot self-certify review or later runtime/package quality.
</success_criteria>

<output>
Create `.planning/phases/03-build-contract/03-04-SUMMARY.md` when done.
</output>
