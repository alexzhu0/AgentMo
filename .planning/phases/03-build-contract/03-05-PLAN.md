---
phase: 03-build-contract
plan: 05
type: execute
wave: 5
depends_on:
  - 03-04
files_modified:
  - src/build-contract.js
  - src/plan-approval.js
  - src/design-plan.js
  - src/blueprint-draft.js
  - src/artifact-registry.js
  - src/artifact-subjects.js
  - src/cli.js
  - package.json
  - test/build-contract.test.js
  - test/openclaw-build-contract.test.js
  - test/openclaw-resource-projection.test.js
  - test/phase3-contracts.test.js
  - test/artifact-subjects.test.js
  - test/artifact-surface-coverage.test.js
  - test/command-docs.test.js
  - test/helpers/io-surface-inventory.js
autonomous: true
requirements:
  - PLAN-03
  - PLAN-04
  - PLAN-05
must_haves:
  truths:
    - "Before approval, a developer can inspect a complete OpenClaw resource graph covering prompt/bootstrap context, skills, effective tools/policy, plugins, memory, RAG, storage, cron, harness, loop/runtime binding, permissions, installation, recovery, and evidence."
    - "Every resource has stable identity, source/decision trace, necessity, owner, lifecycle phase, trust/permission/failure semantics, projection disposition, and evidence obligation."
    - "The contract is grounded in the authorized local OpenClaw source revision 29d018f0/version 2026.6.11 and explicit Node predicate, while Phase 4 must fail closed on later unrecognized drift."
    - "A separate exact approval binds blueprint bytes and build-contract bytes; any requirement, capability, permission, acceptance criterion, blueprint, or contract change invalidates it."
    - "Phase 3 writes no OpenClaw state, package, config patch, plugin install, schedule registration, runtime DB, or live evidence and makes no transitive certification claim."
  artifacts:
    - path: "src/build-contract.js"
      provides: "Complete target-independent build contract with OpenClaw binding and exhaustive resource projection"
      exports: ["BUILD_CONTRACT_SCHEMA_VERSION", "OPENCLAW_RESOURCE_KINDS", "buildBuildContract", "validateBuildContract", "writeBuildContract"]
    - path: "src/plan-approval.js"
      provides: "Exact blueprint plus build-contract approval preview/apply"
      exports: ["PLAN_APPROVAL_SCHEMA_VERSION", "buildPlanApprovalPreview", "buildPlanApproval", "validatePlanApproval", "writePlanApproval"]
    - path: "test/openclaw-resource-projection.test.js"
      provides: "Exhaustive no-drop resource and lifecycle ownership matrix"
  key_links:
    - from: "src/build-contract.js"
      to: "src/design-plan.js"
      via: "exact approved discovery, ledger head, trace graph, requirements, capabilities, permissions, acceptance cases, and unknowns"
      pattern: "requirementsTrace|decisionLedger|acceptance|permissions"
    - from: "src/build-contract.js"
      to: ".planning/phases/03-build-contract/03-RESEARCH.md"
      via: "contract constants and tests encode the source-grounded OpenClaw 29d018f0 resource map"
      pattern: "29d018f0|2026\\.6\\.11|OPENCLAW_RESOURCE_KINDS"
    - from: "src/plan-approval.js"
      to: "src/build-contract.js"
      via: "final approval binds exact independently admitted blueprint and build-contract raw bytes"
      pattern: "plan-approval|build-contract|blueprint"
---

## Phase Goal

**As a** developer, **I want to** turn bounded Web, GitHub, paper, and local research into an approved, traceable blueprint and Package build contract, **so that** Produce receives exact-digest intent without relying on prompts, session ancestry, or unverified declarations.

<objective>
Specify, validate, and approve the complete Package build contract that Phase 4 can deterministically produce.

Purpose: Replace prompt-only scaffold intent with a source-grounded, exhaustive, traceable contract for a real OpenClaw Agent Package while preserving the Phase 3/4/5 ownership boundary.
Output: `agentmo.build-contract.v1`, exhaustive OpenClaw resource/projection tests, and `agentmo.plan-approval.v1` binding exact blueprint and build-contract bytes.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-build-contract/03-RESEARCH.md
@.planning/phases/03-build-contract/03-PATTERNS.md
@.planning/phases/03-build-contract/03-04-SUMMARY.md
@src/design-plan.js
@src/runtime-plan.js
@src/build-plan.js
@src/blueprint-draft.js

<interfaces>
Build contract identity: `agentmo.build-contract.v1`.
Plan approval identity: `agentmo.plan-approval.v1`.
Target binding constants: OpenClaw source revision `29d018f0`, observed version `2026.6.11`, Node range `>=22.19.0 <23 || >=23.11.0`.
Required resource kinds: prompt/bootstrap/workspace context, skills, tools/effective tool policy, plugins/install lanes, memory slot, RAG/embeddings/corpora, storage/database ownership, schedules/cron, harness, agent loop/runtime binding, permissions/trust boundaries/secrets refs, install/load/execute transitions, recovery, acceptance cases, and evidence obligations.
Projection disposition is exactly one of: generated file, config/install/schedule operation, explicit unsupported record, or runtime evidence obligation.
Approval preview/apply mirrors Plan 03-03: preview writes nothing; apply requires explicit approve plus exact preview digest.
</interfaces>
</context>

## Artifacts this phase produces

This plan creates `src/build-contract.js`, `src/plan-approval.js`, `test/build-contract.test.js`, `test/openclaw-build-contract.test.js`, and `test/openclaw-resource-projection.test.js`. It exports `BUILD_CONTRACT_SCHEMA_VERSION`, `OPENCLAW_RESOURCE_KINDS`, `buildBuildContract`, `validateBuildContract`, `writeBuildContract`, `PLAN_APPROVAL_SCHEMA_VERSION`, `buildPlanApprovalPreview`, `buildPlanApproval`, `validatePlanApproval`, and `writePlanApproval`; adds durable identities `agentmo.build-contract.v1` and `agentmo.plan-approval.v1`; and adds public `build-contract` and `plan-approve` commands.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify exhaustive OpenClaw build-contract and final-approval closure</name>
  <read_first>
    - test/design-plan.test.js
    - test/scaffold.test.js
    - test/runtime-compatibility.test.js
    - test/artifact-surface-coverage.test.js
    - src/design-plan.js
    - src/runtime-plan.js
    - src/build-plan.js
    - .planning/phases/03-build-contract/03-RESEARCH.md
    - .planning/phases/03-build-contract/03-PATTERNS.md
  </read_first>
  <files>test/build-contract.test.js, test/openclaw-build-contract.test.js, test/openclaw-resource-projection.test.js, test/phase3-contracts.test.js</files>
  <behavior>
    - Contract provenance binds exact blueprint, discovery approval, design plan, and decision-ledger head; forward/reverse trace endpoints close over requirements, capabilities, permissions, acceptance cases, and evals.
    - The OpenClaw binding requires revision 29d018f0, version 2026.6.11, exact Node predicate, and source-grounded symbols/mechanisms for every required resource family.
    - Prompt covers ordered static/dynamic/bootstrap files and budgets; skills cover roots/precedence/eligibility/snapshot/limits; tools cover effective policy pipeline/side effects/approval/failure; plugins cover necessity/install lane/scan/activation/rollback.
    - Memory selects exactly one slot owner and declares root/additional files, retention/flush/public artifacts; RAG declares corpus/chunk/index/provider/model/dimensions/SecretRef/search/fallback/citations/data boundary; storage declares owner/scope/schema/lifecycle/migration/backup/retention/sensitivity/growth/recovery without production absolute paths.
    - Schedules declare cadence/timezone/agent-session/input/delivery/timeout/retry/concurrency/idempotency/snapshot/retention/permissions; harness remains distinct from provider/model; loop/runtime binding declares auth profile ref, fallback, attempts, timeout, lane, compaction, tool-loop guard, hooks, stop reasons, channel/transport/session.
    - Permissions/trust boundaries, install/load/execute transitions, recovery, acceptance cases, unsupported alternatives, evidence obligations, and remaining risks are explicit; every resource has exactly one Phase 3/4/5 owner and exactly one projection disposition.
    - Omitted, duplicated, conflicting, untraced, secret-bearing, absolute-path, generic-memory, prompt-only-tool, or silent resource projections fail validation.
    - Plan approval preview/apply binds exact blueprint and contract; byte changes to requirements/capabilities/permissions/acceptance/blueprint/contract invalidate it.
    - No Phase 4/5 mutation or certification artifact is created.
  </behavior>
  <action>
Create all three contract suites and extend the composed phase test before implementation. Use exact arrays/sets and referential-closure assertions, not loose presence checks. Encode the complete resource map from 03-RESEARCH.md and reject any missing or multiply owned projection. Test deterministic bytes, canonical validation, forged clones, whole-candidate preflight, stale approval, memory-slot exclusivity, effective tool authority distinct from `TOOLS.md`, plugin install/load/execute separation, schedule generation distinct from application, and all non-certification flags.
  </action>
  <acceptance_criteria>
    - The tests enumerate every required OpenClaw resource and source-grounded lifecycle responsibility.
    - A prompt plus tool list, generic memory field, or scaffold file inventory alone cannot satisfy the contract.
    - Final approval staleness covers every PLAN-05 change class and proves zero Phase 4/5 outputs.
  </acceptance_criteria>
  <done>The contract tests exhaustively encode every required OpenClaw resource, projection, owner, boundary, and stale approval case.</done>
  <verify>
    <automated>node --test test/build-contract.test.js test/openclaw-build-contract.test.js test/openclaw-resource-projection.test.js test/phase3-contracts.test.js; test $? -ne 0</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the complete build contract and exact final approval artifacts</name>
  <read_first>
    - src/design-plan.js
    - src/runtime-plan.js
    - src/build-plan.js
    - src/blueprint-draft.js
    - src/artifact-admission.js
    - src/persistability.js
    - .planning/phases/03-build-contract/03-RESEARCH.md
  </read_first>
  <files>src/build-contract.js, src/plan-approval.js, src/design-plan.js, src/blueprint-draft.js, src/artifact-registry.js, src/artifact-subjects.js</files>
  <action>
Implement a target-independent build-contract envelope with the complete OpenClaw binding described in the tests and research. Construct only from authentic admitted artifacts and closed design/ledger trace state; validate stable IDs, required fields, one owner, one lifecycle assignment, one projection disposition, exact refs, and forward/reverse closure. Use structured unsupported records with reason, source refs, owner, and alternative. Preserve SecretRef/presence only and false certification boundaries. Mark generated blueprint provenance draft/non-authoritative; approval is external to both objects. Implement deterministic plan-approval preview and explicit apply from authentic blueprint/build-contract admissions, with complete preflight before absent-only write. Phase 3 must not inspect or mutate a live OpenClaw target, generate package files, patch config, install plugins, register schedules, create runtime databases, execute a harness, or publish runtime/eval evidence.
  </action>
  <acceptance_criteria>
    - The contract validates only when the entire research resource graph is present, trace-closed, singly owned, and singly projected.
    - Exact target source revision/version/runtime predicate and Phase 3/4/5 responsibility assignments are inspectable.
    - Final approval is separate, deterministic, exact-bound, stale-sensitive, and non-certifying beyond admission to Produce.
  </acceptance_criteria>
  <done>A complete trace-closed build contract and separate exact final approval pass all completeness, projection, and safety tests.</done>
  <verify>
    <automated>node --test test/build-contract.test.js test/openclaw-build-contract.test.js test/openclaw-resource-projection.test.js test/design-plan.test.js test/blueprint-draft.test.js test/persistability.test.js</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Expose build-contract and plan-approve through closed public surfaces</name>
  <read_first>
    - src/cli.js
    - src/artifact-registry.js
    - src/artifact-subjects.js
    - test/artifact-subjects.test.js
    - test/artifact-surface-coverage.test.js
    - test/command-docs.test.js
    - test/helpers/io-surface-inventory.js
    - package.json
  </read_first>
  <files>src/cli.js, src/artifact-registry.js, src/artifact-subjects.js, package.json, test/artifact-subjects.test.js, test/artifact-surface-coverage.test.js, test/command-docs.test.js, test/helpers/io-surface-inventory.js, test/phase3-contracts.test.js</files>
  <action>
Add `build-contract` with mandatory exact blueprint, design-plan, discovery-approval, and decision-ledger-head inputs, and add `plan-approve` preview/apply with mandatory exact blueprint/build-contract inputs. Synchronize help, package/syntax lists, durable descriptors/companions, command subjects, output owners, and I/O inventories. Public output must disclose feasibility, capabilities, permissions, trust surfaces, unsupported alternatives, projection/lifecycle owners, acceptance/evidence obligations, and remaining risks without leaking secrets or raw source/decision material. The composed CLI test must end at plan approval and assert absence of package, install, config mutation, schedule application, runtime, birth, and delivery outputs.
  </action>
  <acceptance_criteria>
    - A fresh process can create and inspect the exact complete contract, preview approval, explicitly approve it, and replay admission.
    - Any missing/unknown/optional bypass subject or stale input fails before output.
    - The public Phase 3 flow stops at approved build contract and exposes all non-certification boundaries.
  </acceptance_criteria>
  <done>Fresh-process build-contract and plan-approve commands are complete, closed, and stop before every Phase 4/5 side effect.</done>
  <verify>
    <automated>node --test test/build-contract.test.js test/openclaw-build-contract.test.js test/openclaw-resource-projection.test.js test/phase3-contracts.test.js test/artifact-subjects.test.js test/artifact-surface-coverage.test.js test/command-docs.test.js &amp;&amp; git diff --check</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved Plan artifacts → build contract | Exact authentic inputs and referential closure precede contract construction. |
| OpenClaw research map → portable target binding | Source revision and mechanisms are fixed research provenance, not current live-target authority. |
| blueprint/build-contract → final approval | Separate preview/apply binds exact raw bytes and explicit operator intent. |
| approved contract → Phase 4 | Contract authorizes only Produce admission; later runtime/mutation gates remain independent. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-20 | Tampering | incomplete/silent resource projection | high | mitigate | Exhaustive resource-kind equality, one owner/lifecycle/projection, trace closure, omission/duplication/conflict tests. |
| T-03-21 | Elevation of Privilege | tool/plugin/permission authority | high | mitigate | Effective policy and install/load/execute transitions are explicit; guidance is non-authoritative; least privilege and approval/failure semantics required. |
| T-03-22 | Tampering / DoS | competing memory/storage owners | high | mitigate | Exactly one memory slot owner, explicit corpus/index/storage ownership/lifecycle, conflict and secret/absolute-path rejection. |
| T-03-23 | Repudiation | OpenClaw/version drift | high | mitigate | Bind 29d018f0/2026.6.11/exact Node predicate as research provenance; require Phase 4 current-target revalidation and fail-closed drift handling. |
| T-03-24 | Elevation of Privilege | contract or blueprint self-approval | high | mitigate | Separate exact plan-approval artifact from authentic admissions; internal validity/review flags have no authority. |
| T-03-25 | Information Disclosure | portable contract | high | mitigate | SecretRef/presence only, no raw production paths/state/transcripts/provider bodies, complete persistability and public-output inventory gates. |
| T-03-26 | Spoofing | transitive certification | high | mitigate | Explicit false boundaries for install/load/runtime/domain/production/wider compatibility; composed test asserts no Phase 4/5 artifacts. |
</threat_model>

<verification>
Run build-contract, OpenClaw completeness/projection, design/blueprint, approval composition, persistability, artifact closure, command-doc, and `git diff --check` gates.
</verification>

<success_criteria>
- The approved build contract covers the entire locked OpenClaw resource graph, not a prompt-only scaffold.
- Every requirement/capability/permission/acceptance/eval/resource is source/decision-traceable and phase-owned.
- Exact plan approval invalidates on all promised change classes.
- Phase 3 performs specification and approval only; package generation/install/runtime proof remain in Phases 4/5.
</success_criteria>

<output>
Create `.planning/phases/03-build-contract/03-05-SUMMARY.md` when done.
</output>
