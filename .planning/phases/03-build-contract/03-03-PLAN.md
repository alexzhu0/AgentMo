---
phase: 03-build-contract
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/discovery-approval.js
  - src/design-plan.js
  - src/artifact-registry.js
  - src/artifact-subjects.js
  - src/cli.js
  - package.json
  - test/discovery-approval.test.js
  - test/design-plan.test.js
  - test/phase3-contracts.test.js
  - test/artifact-subjects.test.js
  - test/artifact-surface-coverage.test.js
  - test/command-docs.test.js
  - test/helpers/io-surface-inventory.js
autonomous: true
requirements:
  - DISC-06
  - PLAN-01
must_haves:
  truths:
    - "A developer can preview and explicitly approve exactly one source-manifest digest plus one derived discovery-database digest."
    - "Plan admission re-hashes and validates the manifest, database, and approval raw bytes independently; a changed source, summary, confidence, coverage, version, or manifest invalidates the old approval."
    - "A fresh process can resume Plan from portable exact artifacts without command ancestry, session state, sidecars, or a generated self-approval flag."
    - "Local approval proves explicit operator intent for the enter-Plan transition only; it is not authenticated organizational authority or source-quality certification."
  artifacts:
    - path: "src/discovery-approval.js"
      provides: "Exact two-digest approval preview, artifact, validation, admission, and writer"
      exports: ["DISCOVERY_APPROVAL_SCHEMA_VERSION", "buildDiscoveryApprovalPreview", "buildDiscoveryApproval", "validateDiscoveryApproval", "writeDiscoveryApproval"]
    - path: "src/design-plan.js"
      provides: "Three-artifact discovery approval admission before planning"
    - path: "test/discovery-approval.test.js"
      provides: "Exact binding, stale/tampered, and zero-output approval matrix"
  key_links:
    - from: "src/cli.js"
      to: "src/discovery-approval.js"
      via: "discovery-approve preview defaults to no write; explicit apply binds the exact preview digest and two admitted artifacts"
      pattern: "discovery-approve|buildDiscoveryApproval"
    - from: "src/design-plan.js"
      to: "src/discovery-approval.js"
      via: "design-plan independently re-admits manifest, discovery DB, and approval bytes before using the DB"
      pattern: "discovery-approval|validateDiscoveryApproval"
---

## Phase Goal

**As a** developer, **I want to** turn bounded Web, GitHub, paper, and local research into an approved, traceable blueprint and Package build contract, **so that** Produce receives exact-digest intent without relying on prompts, session ancestry, or unverified declarations.

<objective>
Create the source approval boundary and require it at Plan admission.

Purpose: Ensure no discovery database enters Plan merely because it is schema-valid or carries its own success fields; human-operated approval must bind the exact manifest and derived DB bytes.
Output: `agentmo.discovery-approval.v1`, a preview/apply CLI, and a fresh-process `design-plan` gate over three independently admitted artifacts.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03-build-contract/03-RESEARCH.md
@.planning/phases/03-build-contract/03-PATTERNS.md
@.planning/phases/03-build-contract/03-02-SUMMARY.md
@src/artifact-admission.js
@src/design-plan.js

<interfaces>
Approval identity: `agentmo.discovery-approval.v1`.
Exact bindings: only canonical provenance tuples for `discovery-manifest` and `discovery-db`, each containing `identity`, `subject`, and `digest`.
Public route:
- preview: `agentmo discovery-approve <manifest> --discovery-db <db> --digest discovery-manifest=... --digest discovery-db=... --json`
- apply: same exact inputs plus `--approve --preview-digest sha256:<64hex> --out <approval.json>`.
Updated `design-plan` requires `--manifest`, `--discovery-approval`, and exact digest bindings for `discovery-manifest`, `discovery-db`, `discovery-approval`, and `user-need`.
</interfaces>
</context>

## Artifacts this phase produces

This plan creates `src/discovery-approval.js` and `test/discovery-approval.test.js`; exports `DISCOVERY_APPROVAL_SCHEMA_VERSION`, `buildDiscoveryApprovalPreview`, `buildDiscoveryApproval`, `validateDiscoveryApproval`, and `writeDiscoveryApproval`; adds the durable identity `agentmo.discovery-approval.v1`, canonical subject `discovery-approval`, public command `discovery-approve`, and mandatory approved-discovery inputs on `design-plan`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify exact discovery approval and stale-input rejection</name>
  <read_first>
    - test/artifact-admission.test.js
    - test/design-plan.test.js
    - test/stage-contracts.test.js
    - src/artifact-admission.js
    - .planning/phases/03-build-contract/03-PATTERNS.md
  </read_first>
  <files>test/discovery-approval.test.js, test/design-plan.test.js, test/phase3-contracts.test.js</files>
  <behavior>
    - Preview exact-admits the manifest and DB once, returns their canonical provenance plus a deterministic preview digest, and writes nothing.
    - Apply requires an explicit approve action and the exact preview digest; it writes one absent approval artifact whose bindings contain only canonical identity/subject/digest tuples.
    - Any byte change to source inventory, retrieved record, summary, confidence, coverage, version, or manifest makes the prior approval unusable.
    - Missing, duplicate, unknown, malformed, self-referential, or parsed-object digest bindings fail with bounded codes and zero output.
    - `design-plan` in a fresh subprocess independently admits manifest, DB, approval, and user need; no command-history, sidecar, or in-memory object authority is accepted.
  </behavior>
  <action>
Create the approval and fresh-process tests before production code. Follow the exact-binding matrix in `test/artifact-admission.test.js`, including duplicate/unknown/malformed cases and raw-byte mutations that preserve parsed meaning. Assert preview no-write, absent-only approval publication, bounded output, and a strict certification boundary. Update Plan tests so the old unapproved DB route is expected to fail rather than silently remain compatible.
  </action>
  <acceptance_criteria>
    - Tests cover approval creation, exact replay, every listed stale source/derived field, malformed bindings, and fresh-process Plan resumption.
    - Tests never use a generated `ok`, `reviewed`, or approval boolean inside the DB as authority.
    - Approval language is scoped to explicit local operator intent and the enter-Plan transition.
  </acceptance_criteria>
  <done>Approval and fresh-process Plan tests cover exact success, every stale class, malformed authority, and no-write failure.</done>
  <verify>
    <automated>node --test test/discovery-approval.test.js test/design-plan.test.js test/phase3-contracts.test.js; test $? -ne 0</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the discovery approval artifact and Plan admission gate</name>
  <read_first>
    - src/artifact-admission.js
    - src/design-plan.js
    - src/persistability.js
    - src/artifact-registry.js
    - src/artifact-subjects.js
  </read_first>
  <files>src/discovery-approval.js, src/design-plan.js, src/artifact-registry.js, src/artifact-subjects.js</files>
  <action>
Implement deterministic preview and explicit approval construction from authentic admissions only. Validate exact binding shape, canonical identities/subjects, exact SHA-256 syntax, enter-Plan-only scope, and false certification flags. Preflight canonical approval bytes before absent-only publication. Register the identity and its required companions. Change the design-plan builder/loader boundary so it receives authentic manifest, DB, approval, and user-need admissions, rechecks approval bindings against recomputed raw-byte digests, and only then uses DB facts. Do not require Stage 1 command ancestry or sidecars, and do not treat approval as source quality, runtime, package, domain, production, or organizational identity proof.
  </action>
  <acceptance_criteria>
    - An authentic matching approval admits the exact DB to Plan in a fresh process.
    - Every stale/tampered/mismatched approval case fails before design-plan construction or output creation.
    - Artifact self-certification and parsed/reserialized digest authority remain impossible.
  </acceptance_criteria>
  <done>Only an authentic exact manifest/DB approval can admit discovery evidence into Plan.</done>
  <verify>
    <automated>node --test test/discovery-approval.test.js test/design-plan.test.js test/artifact-admission.test.js test/persistability.test.js</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Publish the approval preview/apply and approved design-plan CLI contracts</name>
  <read_first>
    - src/cli.js
    - src/artifact-subjects.js
    - src/artifact-registry.js
    - test/artifact-subjects.test.js
    - test/artifact-surface-coverage.test.js
    - test/command-docs.test.js
    - test/helpers/io-surface-inventory.js
    - package.json
  </read_first>
  <files>src/cli.js, src/artifact-subjects.js, src/artifact-registry.js, package.json, test/artifact-subjects.test.js, test/artifact-surface-coverage.test.js, test/command-docs.test.js, test/helpers/io-surface-inventory.js, test/phase3-contracts.test.js</files>
  <action>
Add `discovery-approve` as a thin preview-by-default route and an explicit `--approve --preview-digest --out` apply route. Extend `design-plan` arguments and mandatory command subjects with the exact manifest/approval inputs. Synchronize help, package files, syntax checks, output ownership, durable descriptors, command subjects, and I/O inventory. JSON output must contain bounded identities/digests/decision scope only; no actor credential, transcript, provider payload, source body, or host path. Remove any public unapproved `design-plan` bypass.
  </action>
  <acceptance_criteria>
    - Public preview writes nothing; public apply requires explicit operator action and exact preview digest.
    - Every `design-plan` invocation requires and re-admits all four canonical input subjects.
    - All command/artifact/I/O inventories are exhaustive and tests prove no optional bypass.
  </acceptance_criteria>
  <done>The preview/apply approval route and approved design-plan route pass all command, subject, artifact, and I/O closure tests.</done>
  <verify>
    <automated>node --test test/discovery-approval.test.js test/design-plan.test.js test/phase3-contracts.test.js test/artifact-subjects.test.js test/artifact-surface-coverage.test.js test/command-docs.test.js &amp;&amp; git diff --check</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest/DB bytes → approval preview | Both inputs are independently exact-admitted before their digests enter the preview. |
| preview → explicit approval apply | Apply requires the exact preview digest and explicit operator action. |
| manifest/DB/approval bytes → Plan | Plan independently re-admits all artifacts and recomputes every binding. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-12 | Elevation of Privilege | DB self-approval | high | mitigate | Separate approval identity built only from authentic admissions; internal DB status fields confer no authority. |
| T-03-13 | Tampering | stale manifest/DB replay | high | mitigate | Approval binds exact raw-byte digests; design-plan independently re-hashes all artifacts and rejects any mismatch. |
| T-03-14 | Repudiation | approval meaning | medium | mitigate | Scope evidence as explicit local operator intent only; preserve false authenticated-organization/source-quality/runtime/domain flags. |
| T-03-15 | Information Disclosure | approval/CLI output | high | mitigate | Persist only canonical tuples, bounded decision/scope fields, and false certification boundary; use persistability and output inventory gates. |
</threat_model>

<verification>
Run the approval, design-plan, phase composition, artifact admission, persistability, subject, surface, and command-doc suites plus `git diff --check`.
</verification>

<success_criteria>
- Plan cannot consume an unapproved or stale discovery database.
- Exact approval is portable across fresh processes and independent of command ancestry.
- Preview/apply semantics expose the exact decision without allowing self-certification.
- Approval evidence remains explicitly non-authenticated and non-certifying beyond its transition scope.
</success_criteria>

<output>
Create `.planning/phases/03-build-contract/03-03-SUMMARY.md` when done.
</output>
