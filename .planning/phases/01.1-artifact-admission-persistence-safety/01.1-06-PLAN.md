---
phase: 01.1-artifact-admission-persistence-safety
plan: "06"
type: execute
wave: 6
depends_on: ["01.1-05"]
files_modified:
  - src/artifact-registry.js
  - src/artifact-subjects.js
  - src/blueprint.js
  - src/handoff.js
  - src/cli.js
  - test/blueprint.test.js
  - test/handoff.test.js
  - test/stage-contracts.test.js
  - test/cli.test.js
  - test/artifact-surface-coverage.test.js
autonomous: true
requirements: [CORE-04, EVID-03]
must_haves:
  truths:
    - "Blueprint and handoff artifacts are exact family-bound fresh-process contracts; `plan` continues to consume blueprint, never discovery inputs."
    - "Handoff package and manifest bytes are persistable before output creation and carry admitted identity/subject/digest provenance."
  artifacts:
    - { path: src/handoff.js, provides: "admitted handoff loader and gated writer", exports: [loadHandoffPackage] }
    - { path: src/artifact-registry.js, provides: "blueprint and handoff descriptors" }
  key_links:
    - { from: src/cli.js, to: src/artifact-subjects.js, via: "validate/report/plan/handoff subject declarations", pattern: "subjectsForCommand" }
    - { from: src/handoff.js, to: src/persistability.js, via: "complete package before filesystem writes", pattern: "serializePersistableJson" }
---

<objective>
Complete the blueprint-to-handoff Produce entry slice with the correct `plan <blueprint>` command, closed family admission, and safe handoff persistence.
</objective>
## Phase Goal
**As a** developer, **I want to** hand off admitted artifacts safely, **so that** fresh processes can trust them without retaining secrets.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Close blueprint and handoff admission plus public subjects</name>
  <read_first><file>src/blueprint.js</file><file>src/handoff.js</file><file>src/artifact-registry.js</file><file>src/cli.js</file><file>test/stage-contracts.test.js</file></read_first>
  <files>src/artifact-registry.js, src/artifact-subjects.js, src/blueprint.js, src/handoff.js, src/cli.js, test/blueprint.test.js, test/handoff.test.js, test/stage-contracts.test.js, test/cli.test.js</files>
  <action>Add blueprint/design-contract and handoff descriptors with their real validators and allowed subjects. Register exact inputs for validate, report, plan, handoff, and any handoff consumer; route each public path through exact admission. Add `loadHandoffPackage` rather than generic JSON loading. Prove every blueprint/handoff cross-family pair fails, legacy blueprint produces migration-required, unknown identity fails unsupported, and each happy path starts in a fresh subprocess. Explicitly assert `plan` requires only `blueprint`, while `design-plan` remains the two-input Stage 2 command per D-13.</action>
  <verify><automated>node --test test/blueprint.test.js test/handoff.test.js test/stage-contracts.test.js test/cli.test.js test/artifact-admission.test.js</automated></verify>
  <acceptance_criteria>
    - `plan <blueprint>` and `handoff <blueprint>` accept only the blueprint subject/digest and no ancestry.
    - Handoff loader accepts only its own identity/subject/digest.
    - Human/JSON legacy/unknown/family mismatch codes are value-blind.</acceptance_criteria>
  <done>Produce entry cannot confuse Stage 2 inputs, blueprint, or handoff families.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate handoff persistence and bind exact provenance</name>
  <read_first><file>src/persistability.js</file><file>src/handoff.js</file><file>test/handoff.test.js</file><file>test/helpers/io-surface-inventory.js</file></read_first>
  <files>src/handoff.js, test/handoff.test.js, test/artifact-surface-coverage.test.js</files>
  <action>Build handoff provenance from the admitted blueprint result `{identity,subject,digest}` and remove host-path authority from durable fields. Validate the complete handoff object, manifest, and generated operation content before output directory/temp/write/rename. Reject secret/path/raw runtime material with zero output root; keep valid bytes deterministic and evidence claims non-transitive. Mark handoff sinks gated and its artifact stdout pending Plan 11.</action>
  <verify><automated>node --test test/handoff.test.js test/persistability.test.js test/artifact-surface-coverage.test.js</automated></verify>
  <acceptance_criteria>
    - Unsafe handoff material creates no directory/file/temp and exposes no canary.
    - Stored provenance is identity+subject+digest, not a host absolute path.
    - Inventory has no pending handoff physical sink.</acceptance_criteria>
  <done>Blueprint handoff is independently admissible and safe to persist.</done>
</task>
</tasks>

<threat_model>ASVS L1; block high/critical. High subject/family spoofing is mitigated by exact matrices. High provenance/path disclosure and partial handoff writes are mitigated by admitted provenance plus pre-side-effect policy.</threat_model>
<prohibitions>- Do not read `.env`, swap `plan`/`design-plan` semantics, trust host paths/upstream flags, or turn handoff success into runtime/domain approval. Do not stage/commit.</prohibitions>

## Artifacts this phase produces
- Blueprint and handoff descriptor/command-subject entries.
- `loadHandoffPackage` and persistable handoff writer.
- Correct fresh-process `plan <blueprint>` and handoff contract tests.

## Multi-Source Coverage Audit
- CORE-04/D-13: blueprint/handoff family covered.
- EVID-03: handoff physical writer covered; stdout is Plan 11.
- Scaffold/build/runtime/evidence remain Plans 07–09.

<verification>Focused tests, `npm run check`, `git diff --check`.</verification>
<success_criteria>Blueprint and handoff artifacts cross fresh processes with exact bytes and no secret/path authority.</success_criteria>
<output>Create `01.1-06-SUMMARY.md`; do not commit.</output>
