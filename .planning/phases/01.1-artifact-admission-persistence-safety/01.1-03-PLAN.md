---
phase: 01.1-artifact-admission-persistence-safety
plan: "03"
type: execute
wave: 3
depends_on: ["01.1-02"]
files_modified:
  - src/artifact-registry.js
  - src/artifact-subjects.js
  - src/discovery.js
  - src/discovery-db.js
  - src/discovery-source-workspace.js
  - src/cli.js
  - test/discovery.test.js
  - test/discovery-db.test.js
  - test/discovery-source-workspace.test.js
  - test/artifact-surface-coverage.test.js
autonomous: true
requirements: [CORE-04, EVID-03]
must_haves:
  truths:
    - "Discover manifest/database consumers work in fresh processes from exact subject-bound bytes and closed validators."
    - "Discovery pack/workspace candidates pass the shared gate before directories, temp files, JSON, or JSONL bytes are written."
    - "Local source-document reads are explicitly non-artifact bounded intake and cannot masquerade as admitted AgentMo artifacts."
  artifacts:
    - { path: src/artifact-registry.js, provides: "discovery-manifest and discovery-db descriptors" }
    - { path: test/artifact-surface-coverage.test.js, provides: "gated Discover reads/writes and classified source intake" }
  key_links:
    - { from: src/cli.js, to: src/artifact-subjects.js, via: "discover commands resolve discovery-manifest/discovery-db subjects", pattern: "subjectsForCommand" }
    - { from: src/discovery-db.js, to: src/persistability.js, via: "complete pack candidates before write", pattern: "serializePersistableJson|writePersistable" }
---

<objective>
Add the complete Discover family as one end-to-end admitted and persistable slice without treating bounded local-source intake as a durable-artifact bypass.
</objective>
## Phase Goal
**As a** developer, **I want to** hand off admitted artifacts safely, **so that** fresh processes can trust them without retaining secrets.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Close Discover identities, subjects, loaders, and intake classification</name>
  <read_first><file>src/artifact-admission.js</file><file>src/artifact-registry.js</file><file>src/discovery.js</file><file>src/discovery-db.js</file><file>src/discovery-source-workspace.js</file></read_first>
  <files>src/artifact-registry.js, src/artifact-subjects.js, src/discovery.js, src/discovery-db.js, src/discovery-source-workspace.js, test/discovery.test.js, test/discovery-db.test.js, test/discovery-source-workspace.test.js</files>
  <action>Add frozen descriptors and real validators for canonical discovery-manifest and discovery-db identities. Make applicable ordinary loaders require `{expectedDigest,subject}` and delegate the same Buffer to admission; preserve precise migration/unsupported results. Register exact subject sets for discover-report, discover-pack, and discover-workspace. Classify bounded source-root document reads, source stat calls, and chunk reads as `non-artifact-intake` with size/type/path/secret screening; prove they cannot return an AgentMo durable identity. Add family×loader mismatch, read-count, whitespace/key-order, legacy, and unknown-identity tests per D-13.</action>
  <verify><automated>node --test test/discovery.test.js test/discovery-db.test.js test/discovery-source-workspace.test.js test/artifact-admission.test.js test/artifact-subjects.test.js</automated></verify>
  <acceptance_criteria>
    - All Discover commands consume exact registered subjects once and work in isolated subprocesses.
    - A discovery-db cannot enter a manifest loader or vice versa; unknown/legacy remain distinct.
    - Every non-artifact read has a bounded reason and fails if an AgentMo identity is returned.</acceptance_criteria>
  <done>Discover has no path-only durable loader or ambiguous intake read.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate discovery writers before their first side effect</name>
  <read_first><file>src/persistability.js</file><file>src/discovery-db.js</file><file>src/discovery-source-workspace.js</file><file>test/helpers/io-surface-inventory.js</file></read_first>
  <files>src/discovery-db.js, src/discovery-source-workspace.js, src/cli.js, test/discovery-db.test.js, test/discovery-source-workspace.test.js, test/artifact-surface-coverage.test.js</files>
  <action>Validate the complete discovery DB, facts JSONL collection, coverage, source-card, and source-chunk candidates before mkdir/temp/write/rename. Serialize JSONL only from individually persistable records and audit the joined exact text. Route the three Discover CLI inputs through admitted loaders; leave their artifact stdout owner as explicit `pending:01.1-11`. Update the architectural allowlist so every Discover physical sink is `gated` and no new sink is unclassified.</action>
  <verify><automated>node --test test/discovery-db.test.js test/discovery-source-workspace.test.js test/artifact-surface-coverage.test.js test/cli.test.js</automated></verify>
  <acceptance_criteria>
    - Synthetic hostile key/value/path/raw material produces a stable code and an absent/unchanged output root.
    - Happy-path discovery bytes remain deterministic.
    - I/O inventory shows no pending Discover filesystem writer.</acceptance_criteria>
  <done>Discover inputs and outputs share the final admission/persistability boundaries.</done>
</task>
</tasks>

<threat_model>ASVS L1; block high/critical. High cross-family spoofing is mitigated by exact descriptors/subjects. High disclosure in imported sources is mitigated by existing bounded intake plus the shared candidate/text gate. High partial-write risk is mitigated by pre-mkdir tests.</threat_model>
<prohibitions>- Do not read `.env`, treat arbitrary source files as durable artifacts, persist hostile source material, or emit stronger evidence claims. Do not stage/commit.</prohibitions>

## Artifacts this phase produces
- Discovery manifest/database descriptor and command-subject entries.
- Admitted Discover loaders and persistable pack/workspace writers.
- Discover family mismatch/read-count/zero-write tests and inventory rows.

## Multi-Source Coverage Audit
- CORE-04/D-13: Discover family covered.
- EVID-03: Discover physical writers covered; stdout remains assigned to Plan 11.
- Other families remain explicitly assigned to Plans 04–08.

<verification>Focused tests, `npm run check`, `git diff --check`.</verification>
<success_criteria>Discover can be resumed fresh from exact artifacts and cannot write unsafe candidates.</success_criteria>
<output>Create `01.1-03-SUMMARY.md`; do not commit.</output>
