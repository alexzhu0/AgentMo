---
phase: 01.1-artifact-admission-persistence-safety
plan: "05"
type: execute
wave: 5
depends_on: ["01.1-04"]
files_modified:
  - src/artifact-migration.js
  - src/migration-filesystem.js
  - src/artifact-subjects.js
  - src/cli.js
  - test/artifact-migration.test.js
  - test/migration-filesystem.test.js
  - test/cli.test.js
  - test/artifact-surface-coverage.test.js
autonomous: true
requirements: [CORE-04, EVID-03]
must_haves:
  truths:
    - "Each migration input digest is verified inside the retained-handle bounded read, and that same Buffer flows directly to duplicate scan, legacy inspection, validation, and transformation planning."
    - "Preview/apply source revalidation and ownership-safe publication remain intact; no digest pre-read/re-read or pathname swap window is introduced."
    - "Migration output, receipt, and staging/committed markers pass persistability before staging begins and rejected batches write nothing."
  artifacts:
    - { path: src/migration-filesystem.js, provides: "retained-handle digest-bound migration read and existing safe publisher" }
    - { path: src/artifact-subjects.js, provides: "frozen ordinal migration subject contract" }
  key_links:
    - { from: src/cli.js, to: src/artifact-subjects.js, via: "migration-input-N exact binding set", pattern: "subjectsForCommand" }
    - { from: src/migration-filesystem.js, to: src/artifact-migration.js, via: "same verified Buffer, never a pathname pre-read", pattern: "planArtifactMigrationBytes" }
---

<objective>
Make legacy migration a complete digest-bound fresh-process journey while preserving every Phase 01 source-immutability, ownership, and TOCTOU guarantee.
</objective>
## Phase Goal
**As a** developer, **I want to** hand off admitted artifacts safely, **so that** fresh processes can trust them without retaining secrets.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Bind ordinal digests to the exact retained migration Buffers</name>
  <read_first><file>src/artifact-migration.js</file><file>src/migration-filesystem.js</file><file>src/artifact-subjects.js</file><file>test/migration-filesystem.test.js</file></read_first>
  <files>src/artifact-migration.js, src/migration-filesystem.js, src/artifact-subjects.js, src/cli.js, test/artifact-migration.test.js, test/migration-filesystem.test.js, test/cli.test.js</files>
  <action>Define the frozen `migrate` subject entry as ordinal `migration-input-0..N-1` derived only from input count, with no gaps/extras/duplicates. Refactor inspection to `planArtifactMigrationBytes` so the existing no-follow retained-handle reader reads once under byte/stat budgets, hashes that Buffer against its ordinal external digest, and passes it directly to duplicate-member scan, legacy inspection, per-family input validator, and transform plan. Preview uses that path; apply performs its existing independent retained-handle source revalidation and hashes the revalidated Buffer before transformation. Never pre-read a pathname to satisfy D-13. Add instrumented read-count, symlink/parent swap, byte replacement between preview/apply, ordinal swap, and all-or-nothing batch tests.</action>
  <verify><automated>node --test test/artifact-migration.test.js test/migration-filesystem.test.js test/cli.test.js test/artifact-subjects.test.js</automated></verify>
  <acceptance_criteria>
    - Each inspection/revalidation pass hashes the same Buffer it inspects; no call reads solely for digest then reopens for parse.
    - Source replacement/swap fails value-blindly and original inputs remain unchanged.
    - Legacy invalid shape is rejected or transformed by its closed per-family mapping; identity-only relabeling cannot pass.</acceptance_criteria>
  <done>Migration digest authority shares the retained-handle trust boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate migration publication candidates without weakening ownership</name>
  <read_first><file>src/persistability.js</file><file>src/migration-filesystem.js</file><file>test/persistability.test.js</file><file>test/helpers/io-surface-inventory.js</file></read_first>
  <files>src/artifact-migration.js, src/migration-filesystem.js, test/artifact-migration.test.js, test/migration-filesystem.test.js, test/artifact-surface-coverage.test.js</files>
  <action>Validate transformed artifacts, value-blind receipt, and both marker states as complete logical candidates and exact bytes before the existing staging directory/retained handles are created. Keep the secure Phase 01 publisher, verifier, orphan ownership, and cleanup algorithms; do not replace them with generic atomic rename helpers. Route migration preview/apply stdout to explicit `pending:01.1-11` owners. Mark all migration filesystem sinks `gated` only after hostile nested/raw/secret/path cases prove zero staging/output/orphan bytes.</action>
  <verify><automated>node --test test/artifact-migration.test.js test/migration-filesystem.test.js test/persistability.test.js test/artifact-surface-coverage.test.js</automated></verify>
  <acceptance_criteria>
    - Unsafe input or derived output fails before staging and yields only code/category/guidance.
    - Valid preview/apply remains deterministic, idempotent, source-preserving, and verifier-backed.
    - Architectural inventory has no pending migration physical sink.</acceptance_criteria>
  <done>Migration is both exact-byte admitted and safely persisted with its stronger publisher intact.</done>
</task>
</tasks>

<threat_model>ASVS L1; block high/critical. High TOCTOU/tampering is mitigated by digest-inside-retained-handle reads and preview/apply revalidation. High disclosure/partial publication is mitigated by pre-staging candidate gates. High source destruction risk is mitigated by unchanged Phase 01 ownership verifier.</threat_model>
<prohibitions>- Do not read `.env`, pre-read/re-read for digest, weaken no-follow/ownership checks, transform by identity rename only, or replace secure publication. Do not stage/commit.</prohibitions>

## Artifacts this phase produces
- `planArtifactMigrationBytes` and digest-bound retained-handle inspection.
- Frozen ordinal migration subject entry.
- Persistability-gated transformed bytes/receipt/markers with Phase 01 publication preserved.
- Read-count/swap/zero-staging tests.

## Multi-Source Coverage Audit
- CORE-04/D-13: legacy migration special path covered without canonical-loader contradiction.
- EVID-03: migration output/receipt/markers covered.
- Canonical families continue in Plans 06–08.

<verification>Focused tests, `npm run check`, `git diff --check`.</verification>
<success_criteria>Migration uses the exact retained bytes, writes only safe derived artifacts, and preserves all Phase 01 safety evidence.</success_criteria>
<output>Create `01.1-05-SUMMARY.md`; do not commit.</output>
