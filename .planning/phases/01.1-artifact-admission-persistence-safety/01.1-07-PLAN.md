---
phase: 01.1-artifact-admission-persistence-safety
plan: "07"
type: execute
wave: 7
depends_on: ["01.1-06"]
files_modified:
  - src/artifact-registry.js
  - src/artifact-subjects.js
  - src/build-state.js
  - src/control-snapshot.js
  - src/scaffold.js
  - src/scaffold-files.js
  - src/cli.js
  - test/build-state.test.js
  - test/control-snapshot.test.js
  - test/scaffold.test.js
  - test/artifact-surface-coverage.test.js
autonomous: true
requirements: [CORE-04, EVID-03]
must_haves:
  truths:
    - "Build-state and scaffold/control inputs are exact subject-bound artifacts with family mismatch rejection."
    - "Every scaffold operation content and final build-state candidate is persistable before the scaffold root or any target file is created."
    - "Durable build provenance uses admitted digest/relative managed references, never absolute host paths or credential-bearing runtime state."
  artifacts:
    - { path: src/build-state.js, provides: "build-state descriptor-compatible serialization" }
    - { path: src/scaffold.js, provides: "preflighted all-operation persistability before writes" }
  key_links:
    - { from: src/scaffold.js, to: src/persistability.js, via: "all planned contents plus build state checked before mkdir", pattern: "assertPersistable|writePersistable" }
    - { from: src/control-snapshot.js, to: src/artifact-admission.js, via: "exact build-state subject/digest", pattern: "loadAdmittedArtifact" }
---

<objective>
Deliver the scaffold/build-state slice: a blueprint can produce a deterministic scaffold only after all files and build evidence are safe, and later control reads can trust the exact build-state bytes.
</objective>
## Phase Goal
**As a** developer, **I want to** hand off admitted artifacts safely, **so that** fresh processes can trust them without retaining secrets.

## Writer Owner Contract
| Owner | Complete candidate | First side effect | Unsafe-field replacement | Focused proof |
|---|---|---|---|---|
| `scaffoldAgent` | full managed operation list and every content string | scaffold-root mkdir | reject secret/path/raw material; keep intended package prompts only when schema-owned and audited | root absent on any bad operation |
| build-state writer | final `agentmo.build-state.v1` object | state temp/write | admitted blueprint digest + managed relative refs; no source host path | no state/temp on bad provenance |
| control/scaffold stdout | report/result object | stdout write | owned by Plan 11 emitter | inventory remains `pending:01.1-11` |

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Close build-state admission and scaffold/control subjects</name>
  <read_first><file>src/build-state.js</file><file>src/control-snapshot.js</file><file>src/scaffold.js</file><file>src/artifact-registry.js</file><file>src/artifact-subjects.js</file></read_first>
  <files>src/artifact-registry.js, src/artifact-subjects.js, src/build-state.js, src/control-snapshot.js, src/scaffold.js, src/cli.js, test/build-state.test.js, test/control-snapshot.test.js, test/scaffold.test.js</files>
  <action>Add the build-state descriptor/validator and exact subjects for scaffold plus status/control build-state options. Ensure loaders require external digest and family; keep optionality explicit (no file means no subject, a supplied file requires its subject). Carry the admitted blueprint identity/subject/digest into build state and use managed relative paths, not host path authority. Add fresh-process, family-cross-product, optional binding, byte mutation, and unknown/legacy tests per D-13.</action>
  <verify><automated>node --test test/build-state.test.js test/control-snapshot.test.js test/scaffold.test.js test/cli.test.js test/artifact-admission.test.js</automated></verify>
  <acceptance_criteria>
    - Build-state cannot enter blueprint/run-state loaders and vice versa.
    - Supplying a build-state path without its exact subject binding fails before snapshot/scaffold logic.
    - Valid status/scaffold journeys require no earlier command in the process.</acceptance_criteria>
  <done>Build/control state is recoverable from exact files rather than session memory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preflight the full scaffold and build-state write set</name>
  <read_first><file>src/persistability.js</file><file>src/scaffold-files.js</file><file>src/scaffold.js</file><file>src/build-state.js</file><file>test/helpers/io-surface-inventory.js</file></read_first>
  <files>src/build-state.js, src/scaffold-files.js, src/scaffold.js, test/build-state.test.js, test/scaffold.test.js, test/artifact-surface-coverage.test.js</files>
  <action>Before creating the output root, validate the full operation list, each relative path, every text/JSON content candidate, and final build state. Intended package prompt/skill content is ordinary schema-owned product data, but embedded secret values, host paths, raw runtime transcripts/tool bodies/stdout/stderr are rejected; secret requirements use exact SecretRef. Use prevalidated bytes with existing deterministic operation ordering. Add one hostile case per candidate row in the Writer Owner Contract and mark only scaffold/build physical sinks gated; stdout stays pending Plan 11.</action>
  <verify><automated>node --test test/scaffold.test.js test/build-state.test.js test/persistability.test.js test/artifact-surface-coverage.test.js</automated></verify>
  <acceptance_criteria>
    - One unsafe planned file prevents all scaffold directories/files/state from appearing.
    - Valid scaffold files and digests remain deterministic.
    - I/O inventory has no pending scaffold/build physical writer.</acceptance_criteria>
  <done>Scaffold is all-set preflighted and build state is safe durable evidence.</done>
</task>
</tasks>

<threat_model>ASVS L1; block high/critical. High partial scaffold disclosure is mitigated by all-operation preflight. High family spoofing is mitigated by build-state descriptor/subjects. High path/credential persistence is mitigated by relative managed refs and exact SecretRef.</threat_model>
<prohibitions>- Do not read `.env`, confuse intended package prompts with raw runtime transcripts, create a partial scaffold, persist host paths, or implement OpenClaw version gates. Do not stage/commit.</prohibitions>

## Artifacts this phase produces
- Build-state descriptor/subjects and admitted control loader.
- All-operation scaffold persistability preflight.
- Managed relative/digest build provenance and zero-root tests.

## Multi-Source Coverage Audit
- CORE-04/D-13: build-state/control family covered.
- EVID-03: scaffold/build physical writers covered; stdout remains Plan 11.
- Runtime/evidence/generator/doc surfaces remain Plans 08–11.

<verification>Focused tests, `npm run check`, `git diff --check`.</verification>
<success_criteria>Scaffold/build state can be independently trusted and unsafe content cannot create partial output.</success_criteria>
<output>Create `01.1-07-SUMMARY.md`; do not commit.</output>
