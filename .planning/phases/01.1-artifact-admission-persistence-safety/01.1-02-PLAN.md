---
phase: 01.1-artifact-admission-persistence-safety
plan: "02"
type: execute
wave: 2
depends_on: ["01.1-01"]
files_modified:
  - src/persistability.js
  - src/evidence-audit.js
  - src/secret-redaction.js
  - test/persistability.test.js
  - test/helpers/io-surface-inventory.js
  - test/artifact-surface-coverage.test.js
  - test/secret-redaction.test.js
autonomous: true
requirements: [EVID-03]
must_haves:
  truths:
    - "SecretRef, SecretPresence, and RedactedSummary each have one exact closed schema; lookalikes and self-asserted safe objects fail."
    - "A bounded descriptor-only traversal rejects hostile nested keys/values/subjects/pointers/paths/raw material without invoking getters or revealing locations."
    - "An architectural I/O allowlist discovers every filesystem/output sink spelling and assigns it one phase-plan owner; unclassified sinks fail tests."
  artifacts:
    - { path: src/persistability.js, provides: "closed carriers and value-blind pre-side-effect gate", exports: [PersistabilityError, isSecretRef, isSecretPresence, isRedactedSummary, assertPersistable, serializePersistableJson, writePersistableJsonAtomic, writePersistableTextAtomic, emitPersistableOutput] }
    - { path: test/helpers/io-surface-inventory.js, provides: "node:fs/output sink discovery and exact ownership allowlist" }
  key_links:
    - { from: src/persistability.js, to: src/evidence-audit.js, via: "normalized bounded raw-material audit", pattern: "auditEvidence" }
    - { from: test/artifact-surface-coverage.test.js, to: test/helpers/io-surface-inventory.js, via: "zero unclassified I/O surfaces", pattern: "scanIoSurfaces" }
---

<objective>
Create the reusable EVID-03 kernel and a mechanical inventory that prevents later writer plans from self-certifying against hand-maintained regex rows.
</objective>

## Phase Goal
**As a** developer, **I want to** hand off admitted artifacts safely, **so that** fresh processes can trust them without retaining secrets.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify closed carriers, hostile traversal, and exhaustive I/O discovery</name>
  <read_first><file>src/evidence-audit.js</file><file>src/secret-redaction.js</file><file>test/secret-redaction.test.js</file><file>src/run-state.js</file><file>src/runtime-env.js</file></read_first>
  <files>test/persistability.test.js, test/helpers/io-surface-inventory.js, test/artifact-surface-coverage.test.js, test/secret-redaction.test.js</files>
  <action>Write RED positive/negative controls for three exact no-extra-key carriers: `SecretRef={kind:'SecretRef',source:'runtime-env',name:<UPPER_SNAKE max128>}`; `SecretPresence={kind:'SecretPresence',source:'runtime-env',allowedNames,presentNames,missingNames,valuesPersisted:false}` with sorted unique uppercase arrays of at most 128 names; and `RedactedSummary={kind:'RedactedSummary',summaryKind,sha256,length,redactedLength,text,redacted:true}` with safe summaryKind enum, exact digest, nonnegative bounded lengths, text at most 512 chars, and recursive secret/path/raw audit. Add hostile getter/symbol/cycle/prototype/nonfinite/depth/node/byte cases and nested key/value/subject/pointer/path/prompt/transcript/tool/stdout/stderr variants. Build a source scanner that resolves named/namespace aliases from `node:fs`, `node:fs/promises`, stream/file-handle write calls, managed operation writers, `process.stdout/stderr.write`, console output, and shell redirections. Compare discovered `{file,symbol,sink}` records to an exact owner/status allowlist; every sink must be `gated`, `diagnostic`, `non-artifact`, or `pending:<plan>` and unknown spellings fail.</action>
  <verify><automated>node --test test/persistability.test.js test/artifact-surface-coverage.test.js test/secret-redaction.test.js</automated></verify>
  <acceptance_criteria>
    - Every current sink is discovered independent of local alias spelling and has exactly one owner/status.
    - No carrier accepts extra fields, a value field, unsafe text, invalid digest, unsorted/duplicate/oversized names, or self-declared safety.
    - Getter canaries are not invoked and error objects contain code/category/guidance only.
  </acceptance_criteria>
  <done>Safe carriers and the complete current I/O work queue are executable contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement value-blind preparation and zero-side-effect primitives</name>
  <read_first><file>test/persistability.test.js</file><file>src/evidence-audit.js</file><file>src/secret-redaction.js</file><file>src/discovery-db.js</file></read_first>
  <files>src/persistability.js, src/evidence-audit.js, src/secret-redaction.js, test/persistability.test.js</files>
  <action>Implement the exact carrier validators and a descriptor-only own-property traversal under fixed limits; normalize raw field spellings by case-folding and removing separators. Reject secret/path/raw material anywhere, including object keys, subject/pointer fields, without returning the rejected key/value/path. Validate the entire logical candidate, serialize exactly once, then audit the exact output text. Atomic write helpers must complete all validation before mkdir/temp/open/write/rename; the output emitter must validate candidate and formatted text before invoking its sink. Expose no bypass, caller predicate, mutable registry, or redact-and-continue mode.</action>
  <verify><automated>node --test test/persistability.test.js test/secret-redaction.test.js test/evidence-boundaries.test.js</automated></verify>
  <acceptance_criteria>
    - Promised exports exist; only exact carriers pass their dedicated validators.
    - Every hostile direct case produces `AGENTMO_PERSISTABILITY_*`, no canary, and zero sink/filesystem calls.
    - Ordinary non-sensitive AgentMo schema data still passes.
  </acceptance_criteria>
  <done>Later family slices can gate complete candidates without inventing policy.</done>
</task>
</tasks>

<threat_model>ASVS L1; block high/critical. High disclosure from carrier spoofing and nested raw material is mitigated by exact schemas plus recursive audit. High tampering from validate-after-write is mitigated by injected-sink and empty-directory assertions. Medium DoS from getters/cycles/budgets is mitigated by descriptor-only bounded traversal.</threat_model>

<prohibitions>
- Do not read `.env`, trust self-asserted carrier kinds, expose a bypass, or create any output before validation completes.
- Do not use a known-call regex list as the only closure proof; new aliases/imports/sinks must fail discovery.
- Do not stage or commit.
</prohibitions>

## Artifacts this phase produces
- `src/persistability.js` with the nine named policy/write/output exports.
- Exact `SecretRef`, `SecretPresence`, and `RedactedSummary` schemas.
- `test/helpers/io-surface-inventory.js` architectural scanner and owner allowlist.
- `test/persistability.test.js` hostile/positive/zero-side-effect matrix.

## Multi-Source Coverage Audit
- EVID-03 and hostile zero-write context: covered by closed carriers and primitives.
- CORE-04/D-13: preserved; no read path changes in this plan.
- Writer integration: explicitly owned by Plans 03–10 in the allowlist.

<verification>Focused tests, then `npm run check` and `git diff --check`.</verification>
<success_criteria>Policy is exact and value-blind; every present/future sink becomes mechanically visible and owned.</success_criteria>
<output>Create `01.1-02-SUMMARY.md`; do not commit.</output>
