---
phase: 01.1-artifact-admission-persistence-safety
plan: "10"
type: execute
wave: 10
depends_on: ["01.1-09"]
files_modified:
  - src/artifact-registry.js
  - src/artifact-subjects.js
  - src/report.js
  - src/birth-report.js
  - src/domain-eval.js
  - src/delivery-report.js
  - src/cli.js
  - test/report.test.js
  - test/birth-report.test.js
  - test/domain-eval.test.js
  - test/delivery-report.test.js
  - test/evidence-boundaries.test.js
  - test/artifact-surface-coverage.test.js
autonomous: true
requirements: [CORE-04, EVID-03]
must_haves:
  truths:
    - "Every report/eval input is independently admitted by exact identity, subject, and raw-byte digest; no source success flag certifies the aggregate."
    - "Report builders revalidate scope/freshness/identity and produce persistable safe summaries with no raw private runtime material or host paths."
    - "Declared-ready, live-success, bounded domain eval, delivery aggregation, and production approval remain explicitly non-transitive."
  artifacts:
    - { path: src/artifact-registry.js, provides: "report/eval/domain-case descriptors" }
    - { path: src/artifact-subjects.js, provides: "multi-input birth/domain/delivery subject contracts" }
  key_links:
    - { from: src/cli.js, to: src/artifact-subjects.js, via: "exact multi-input report bindings", pattern: "subjectsForCommand" }
    - { from: "report builders", to: src/persistability.js, via: "safe complete candidate assertion before output ownership", pattern: "assertPersistable" }
---

<objective>
Complete the report/evidence family so every aggregate is rebuilt from independently admitted evidence and remains safe, bounded, and non-self-certifying.
</objective>
## Phase Goal
**As a** developer, **I want to** hand off admitted artifacts safely, **so that** fresh processes can trust them without retaining secrets.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Close evidence identities and exact multi-input subject contracts</name>
  <read_first><file>src/birth-report.js</file><file>src/domain-eval.js</file><file>src/delivery-report.js</file><file>src/artifact-registry.js</file><file>src/artifact-subjects.js</file></read_first>
  <files>src/artifact-registry.js, src/artifact-subjects.js, src/report.js, src/birth-report.js, src/domain-eval.js, src/delivery-report.js, src/cli.js, test/report.test.js, test/birth-report.test.js, test/domain-eval.test.js, test/delivery-report.test.js</files>
  <action>Add closed descriptors/real validators for current report/eval identities, including a versioned domain-cases identity if those files are durable inputs; otherwise classify the bounded fixture reader non-artifact and do not generic-load it. Define exact subject sets for report, birth-report, domain-eval, and delivery-report, including optional domain-eval presence semantics. Route each source through same-Buffer admission and remove generic caller-selected JSON validators. Add all family×slot swaps, missing/extra bindings, byte mutation, stale/scope mismatch, legacy/unknown, and fresh-process tests per D-13.</action>
  <verify><automated>node --test test/report.test.js test/birth-report.test.js test/domain-eval.test.js test/delivery-report.test.js test/cli.test.js test/artifact-admission.test.js</automated></verify>
  <acceptance_criteria>
    - A valid artifact in the wrong report slot fails before aggregation.
    - Multi-input commands consume every exact subject once, including optional-presence rules.
    - Fresh-process reports require no producer command and never trust an upstream `ok` alone.</acceptance_criteria>
  <done>Report/eval inputs are closed contracts, not generic JSON or self-certification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Produce persistable, non-transitive evidence candidates</name>
  <read_first><file>src/persistability.js</file><file>src/report.js</file><file>src/birth-report.js</file><file>src/domain-eval.js</file><file>src/delivery-report.js</file></read_first>
  <files>src/report.js, src/birth-report.js, src/domain-eval.js, src/delivery-report.js, test/report.test.js, test/birth-report.test.js, test/domain-eval.test.js, test/delivery-report.test.js, test/evidence-boundaries.test.js, test/artifact-surface-coverage.test.js</files>
  <action>Revalidate admitted source identity/digest/scope/freshness in each builder, derive only bounded RedactedSummary/presence/ref evidence, and call `assertPersistable` on the complete report before returning it to the Plan 11 output owner. Remove host path/raw transcript/tool/output/prompt carry-through. Keep birth-report fail-closed and non-self-certifying; make declared/live/domain/delivery/production fields independent and add cross-level negative assertions. Classify all report builder returns as `safe-candidate` and their CLI stdout as `pending:01.1-11`.</action>
  <verify><automated>node --test test/report.test.js test/birth-report.test.js test/domain-eval.test.js test/delivery-report.test.js test/evidence-boundaries.test.js test/persistability.test.js test/artifact-surface-coverage.test.js</automated></verify>
  <acceptance_criteria>
    - Hostile evidence fails with no canary/path/key/pointer in the error and no report candidate returned.
    - No evidence level automatically sets another level or production approval true.
    - All current report/eval candidates pass the exact persistability policy before output.</acceptance_criteria>
  <done>Evidence aggregation is safe, independently revalidated, and semantically non-transitive.</done>
</task>
</tasks>

<threat_model>ASVS L1; block high/critical. High slot spoofing/self-certification is mitigated by exact multi-subject admission and revalidation. High private-evidence disclosure is mitigated by safe carriers and whole-report policy. High claim elevation is mitigated by independent fields and cross-level negatives.</threat_model>
<prohibitions>- Do not read `.env`, generic-load report JSON, trust upstream success, persist source paths/raw material, or promote evidence levels. Do not stage/commit.</prohibitions>

## Artifacts this phase produces
- Report/eval/domain-case descriptor and subject entries.
- Exact birth/domain/delivery multi-input admission.
- Persistable non-transitive report candidates and boundary tests.

## Multi-Source Coverage Audit
- CORE-04/D-13: report/evidence family covered.
- EVID-03: report candidates safe; artifact stdout is Plan 11.
- Generator/script/docs final closure remains Plans 11–12.

<verification>Focused tests, `npm run check`, `git diff --check`.</verification>
<success_criteria>Reports are rebuilt from exact admitted sources and cannot leak data or invent stronger certification.</success_criteria>
<output>Create `01.1-10-SUMMARY.md`; do not commit.</output>
