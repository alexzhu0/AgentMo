---
phase: 01.1-artifact-admission-persistence-safety
plan: "09"
type: execute
wave: 9
depends_on: ["01.1-08"]
files_modified:
  - src/artifact-registry.js
  - src/artifact-subjects.js
  - src/run-observation.js
  - src/observation.js
  - src/cli.js
  - test/run-observation.test.js
  - test/observation.test.js
  - test/artifact-surface-coverage.test.js
autonomous: true
requirements: [CORE-04, EVID-03]
must_haves:
  truths:
    - "Observation records load only from exact admitted observation bytes and derive only from exact admitted run state."
    - "Observation persistence contains admitted subject/digest refs and safe summaries, never run-state host paths or raw runtime material."
    - "Observation remains proposal-only and cannot mutate blueprint, package, runtime, or eval artifacts."
  artifacts:
    - { path: src/run-observation.js, provides: "persistable proposal writer" }
    - { path: src/observation.js, provides: "admitted observation loader" }
  key_links:
    - { from: src/run-observation.js, to: src/persistability.js, via: "complete proposal before output write", pattern: "writePersistableJsonAtomic" }
    - { from: src/observation.js, to: src/artifact-admission.js, via: "observation subject/digest", pattern: "loadAdmittedArtifact" }
---

<objective>
Complete the proposal-only observation slice with exact run-state/observation admission and safe durable summaries.
</objective>
## Phase Goal
**As a** developer, **I want to** hand off admitted artifacts safely, **so that** fresh processes can trust them without retaining secrets.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Close observation identities, subjects, and fresh-process derivation</name>
  <read_first><file>src/run-observation.js</file><file>src/observation.js</file><file>src/run-state.js</file><file>src/artifact-registry.js</file><file>src/artifact-subjects.js</file></read_first>
  <files>src/artifact-registry.js, src/artifact-subjects.js, src/run-observation.js, src/observation.js, src/cli.js, test/run-observation.test.js, test/observation.test.js</files>
  <action>Add the observation descriptor/validator and exact subjects for observe-run (`run-state`) and observe (`observation`). Route loaders through exact admission; build observation source evidence from the admitted run-state identity/subject/digest, not its host path. Add fresh-process, family mismatch, byte mutation, legacy/unknown, and non-mutation tests per D-13.</action>
  <verify><automated>node --test test/run-observation.test.js test/observation.test.js test/cli.test.js test/artifact-admission.test.js</automated></verify>
  <acceptance_criteria>
    - Observe commands require exactly their registered subject and work without producer ancestry.
    - Run-state/observation cross-family inputs fail before report/proposal construction.
    - Source evidence contains no host path or raw runtime material.</acceptance_criteria>
  <done>Observation provenance is exact and independent of the producing session.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate proposal persistence and preserve non-mutation</name>
  <read_first><file>src/persistability.js</file><file>src/run-observation.js</file><file>test/helpers/io-surface-inventory.js</file></read_first>
  <files>src/run-observation.js, test/run-observation.test.js, test/observation.test.js, test/artifact-surface-coverage.test.js</files>
  <action>Validate the complete observation proposal and exact serialized text before mkdir/temp/write/rename. Permit only RedactedSummary/ordinary proposal metadata and admitted refs; reject raw prompt/transcript/tool/stdout/stderr, secret, and path material. Add hostile matrix cases asserting absent observation/temp/root and unchanged source run state/blueprint/scaffold/evals. Mark the observation physical sink gated and its report stdout pending Plan 11.</action>
  <verify><automated>node --test test/run-observation.test.js test/observation.test.js test/persistability.test.js test/artifact-surface-coverage.test.js</automated></verify>
  <acceptance_criteria>
    - Unsafe proposal input writes no bytes and exposes no canary/location.
    - Valid observation bytes remain deterministic and proposal-only.
    - I/O inventory has no pending observation physical writer.</acceptance_criteria>
  <done>Observation is a safe, admitted proposal—not an automatic evolution channel.</done>
</task>
</tasks>

<threat_model>ASVS L1; block high/critical. High runtime-data disclosure is mitigated by admitted refs/safe summaries. High unauthorized mutation is mitigated by proposal-only assertions over all source artifacts. High partial write is covered by pre-side-effect tests.</threat_model>
<prohibitions>- Do not read `.env`, persist run-state paths/raw material, or mutate blueprint/package/runtime/evals. Do not stage/commit.</prohibitions>

## Artifacts this phase produces
- Observation descriptor/subjects and admitted loaders.
- Persistable observation writer with admitted source refs.
- Proposal-only zero-write/non-mutation tests.

## Multi-Source Coverage Audit
- CORE-04/D-13: observation family complete.
- EVID-03: observation physical writer complete; stdout is Plan 11.
- Evidence reports remain Plan 10; output/generator/docs remain Plans 11–12.

<verification>Focused tests, `npm run check`, `git diff --check`.</verification>
<success_criteria>Observation artifacts are exact, private-material-free, and incapable of mutating governed artifacts.</success_criteria>
<output>Create `01.1-09-SUMMARY.md`; do not commit.</output>
