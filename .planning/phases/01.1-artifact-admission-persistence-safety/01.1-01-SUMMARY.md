---
phase: 01.1-artifact-admission-persistence-safety
plan: "01"
subsystem: artifact-admission
tags: [agentmo, sha256, raw-bytes, fresh-process, cli, node-test]

requires:
  - phase: 01-canonical-artifact-kernel
    provides: "Canonical AgentMo identities, closed legacy migration behavior, and value-blind structured artifact errors"
provides:
  - "Single-capture exact-Buffer SHA-256 admission before decode, parse, audit, identity lookup, or validation"
  - "Frozen design-plan command authority for discovery-db and user-need subjects"
  - "Fresh-process design-plan CLI path with mandatory one-to-one digest bindings"
  - "Closed canonical durable descriptors for the first two Phase 01.1 artifact families"
affects: [artifact-admission, durable-loaders, command-generation, persistence-safety]

tech-stack:
  added: []
  patterns:
    - "Externally supplied exact-byte digest verified on one bounded Buffer before content-derived work"
    - "Deep-frozen command-to-subject authority shared by CLI and later command emitters"
    - "Value-blind fixed-code errors with no path, raw subject input, key, pointer, or content echo"

key-files:
  created:
    - src/artifact-admission.js
    - src/artifact-subjects.js
    - test/artifact-admission.test.js
    - test/artifact-subjects.test.js
  modified:
    - src/artifact-registry.js
    - src/cli.js
    - docs/STAGE_CONTRACTS.md
    - test/stage-contracts.test.js
    - test/design-plan.test.js
    - test/cli-mvp.test.js

key-decisions:
  - "Only design-plan receives the Phase 01.1 digest contract in this slice; plan remains the existing blueprint scaffold-planning command."
  - "The durable descriptor registry is frozen and initially contains only agentmo.discovery-db.v1 and agentmo.user-need.v1."
  - "Admission authority is the externally supplied sha256 digest of the captured raw Buffer, never canonical or parsed JSON."
  - "Binding, digest, read, decode, parse, unsafe-content, and identity failures expose bounded codes without raw material."

patterns-established:
  - "D-13 order: bounded read -> raw digest proof -> UTF-8 decode -> duplicate identity scan -> JSON parse -> content audit -> closed identity/validator resolution."
  - "Fresh-process recovery: files plus named exact digests are sufficient; producer-command ancestry is irrelevant."

requirements-completed: [CORE-04]

coverage:
  - id: D1
    description: "A fresh process builds a normal design-plan only from exact discovery-db and user-need byte bindings."
    requirement: CORE-04
    verification:
      - kind: e2e
        ref: "test/artifact-admission.test.js#runs design-plan in a fresh process from two exact bindings and writes nothing on mismatch"
        status: pass
      - kind: integration
        ref: "test/stage-contracts.test.js#Stage 2 plans from a prebuilt DB plus need before drafting a valid blueprint"
        status: pass
    human_judgment: false
  - id: D2
    description: "Raw-byte changes and every binding-map ambiguity fail before artifact output with value-blind diagnostics."
    requirement: CORE-04
    verification:
      - kind: unit
        ref: "test/artifact-admission.test.js#hash-before-parse and binding failure matrix"
        status: pass
    human_judgment: false
  - id: D3
    description: "The command-subject and initial durable descriptor registries are closed and frozen while Phase 01 migration behavior remains intact."
    requirement: CORE-04
    verification:
      - kind: unit
        ref: "test/artifact-subjects.test.js; test/artifact-admission.test.js#keeps this slice closed to the two canonical input descriptors"
        status: pass
      - kind: integration
        ref: "node --test test/artifact-migration.test.js"
        status: pass
    human_judgment: false

duration: 10min
completed: 2026-07-12
status: complete
---

# Phase 01.1 Plan 01: Exact-Byte Design-Plan Admission Summary

**AgentMo now admits the design-plan input pair from one bounded raw Buffer per file, with mandatory named SHA-256 bindings and no dependency on producer-command ancestry.**

## Performance

- **Duration:** 10 min
- **Started:** 2026-07-12T05:57:26Z
- **Completed:** 2026-07-12T06:07:38Z
- **Tasks:** 2/2
- **Files modified:** 11 including this summary

## Accomplishments

- Added `src/artifact-admission.js` with bounded single-capture reads, strict lowercase `sha256:` parsing, constant-time equal-length comparison, and D-13 hash-before-content ordering.
- Added `src/artifact-subjects.js` as a deep-frozen command authority: `design-plan` requires exactly `discovery-db` and `user-need`.
- Extended the closed artifact registry with only the two canonical descriptors needed by this slice while preserving all three Phase 01 migration families and their migration-required behavior.
- Wired `agentmo design-plan` to repeatable subject-bound `--digest` flags and left `agentmo plan <blueprint>` unchanged.
- Added fresh-process, byte mutation, binding ambiguity, hostile content, duplicate identity, unknown/swapped identity, legacy, size ceiling, no-write, and value-blind human/JSON coverage.
- Updated the Stage 2 contract and both affected Stage 2/composed vertical tests to calculate exact file-byte digests externally.

## Verification

- RED: `node --test test/artifact-admission.test.js test/artifact-subjects.test.js test/stage-contracts.test.js` failed on the two absent modules and existing CLI rejection of `--digest`; unrelated Stage 1/3 cases remained green.
- Focused Plan 01 + Phase 01 protection: **117/117 passed** across admission, subjects, CLI, stage contracts, design-plan, composed MVP, and migration suites.
- Stage 2 contract gate: `node --test test/design-plan.test.js test/stage-contracts.test.js` — **13/13 passed**.
- Composed support-triage gate: `node --test test/cli-mvp.test.js test/design-plan.test.js test/stage-contracts.test.js` — **14/14 passed**.
- Repository gate: `npm run check` — **302/302 passed across 35 suites**.
- New-module syntax: `node --check src/artifact-admission.js` and `node --check src/artifact-subjects.js` — passed.
- Whitespace gate: `git diff --check` — passed.

## Task Commits

Commits: none (not authorized). No files were staged and no branch or HEAD operation was performed. TDD RED/GREEN evidence is recorded through command results instead of commits because the project contract overrides the executor's default commit protocol.

## Files Created/Modified

- `src/artifact-admission.js` — bounded raw read, digest parser/comparison, value-blind errors, ordered admission, and closed descriptor resolution.
- `src/artifact-subjects.js` — frozen `DURABLE_COMMAND_SUBJECTS` and fail-closed `subjectsForCommand`.
- `src/artifact-registry.js` — frozen discovery-db/user-need descriptors without widening legacy compatibility.
- `src/cli.js` — mandatory double binding on `design-plan`, structured admission errors, and corrected help; `plan` remains blueprint-based.
- `test/artifact-admission.test.js`, `test/artifact-subjects.test.js` — exact-byte, registry, binding, fresh-process, value-blind, and zero-write proofs.
- `test/stage-contracts.test.js` — Stage 2 independent journey now supplies exact file digests.
- `test/design-plan.test.js`, `test/cli-mvp.test.js` — direct and composed CLI journeys retain their intended success/unsafe coverage under the mandatory contract.
- `docs/STAGE_CONTRACTS.md` — D-13 ordering, exact command syntax, external digest calculation, and non-certification boundary.

## Decisions Made

- Used one immutable command-subject source rather than duplicating subject lists between the parser, tests, and later command emitters.
- Kept the initial durable registry intentionally closed to two exact schema identities; unknown identities fail instead of selecting a similar validator.
- Reused Phase 01 duplicate-member and legacy-inspection semantics after digest proof, preserving explicit migration rather than adding implicit normalization.
- Treated digest admission as byte/schema mechanism evidence only; it does not imply source approval, runtime success, domain quality, or production approval.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Test regression] Kept Stage 2 and the composed birth loop on their real success paths**

- **Found during:** Required Stage 2 and repository gates.
- **Issue:** `test/design-plan.test.js` and `test/cli-mvp.test.js` still invoked `design-plan` without the now-mandatory bindings, so one success test failed and one unsafe-input test could have failed too early.
- **Fix:** Both tests now hash the exact files they pass and bind the canonical subjects before exercising the original behavior.
- **Files modified:** `test/design-plan.test.js`, `test/cli-mvp.test.js`.
- **Verification:** Stage 2 13/13, composed focused 14/14, repository 302/302.
- **Commit:** none (not authorized).

**2. [Rule 2 - Missing critical] Closed string-valued raw-content admission**

- **Found during:** Hostile-content GREEN test.
- **Issue:** The shared evidence audit did not flag a string-valued raw transcript field at this new trust boundary.
- **Fix:** Added a bounded, value-blind raw-field check inside `src/artifact-admission.js` after parse and before identity lookup/validation.
- **Files modified:** `src/artifact-admission.js`, `test/artifact-admission.test.js`.
- **Verification:** Hostile raw field is rejected with `AGENTMO_ARTIFACT_UNSAFE_CONTENT`; focused and repository gates pass.
- **Commit:** none (not authorized).

**Total deviations:** 2 auto-fixed (1 Rule 1, 1 Rule 2).
**Impact on plan:** Both changes preserve the mandatory digest contract and its security boundary; neither expands to remaining Phase 01.1 loader/writer families or Phase 1.2 runtime work.

## Issues Encountered

- One new assertion initially treated the existing build-plan target descriptor as a string; it was corrected to assert the established `target.id` field.
- The working tree already contained Phase 01 and user changes. They were preserved, including the unrelated `.env.example` deletion and `.gitignore` modification.

## TDD Gate Compliance

- RED and GREEN were both observed for the two planned tasks.
- TDD mode was disabled in executor configuration, and commits were additionally prohibited by the project/user contract.

## Known Stubs

None. The scoped stub scan found no TODO, FIXME, placeholder, coming-soon, or unavailable implementation marker in the new admission/subject modules and tests.

## User Setup Required

None. No external service or credential is required; tests use only local sanitized fixtures and synthetic canaries.

## Next Phase Readiness

- Later Phase 01.1 plans can add command families to the frozen subject authority and durable descriptors without changing the exact-byte admission order.
- Persistability/writer closure and the remaining loader inventory are not claimed by this summary.
- Node/OpenClaw runtime version gates remain exclusively Phase 1.2 work.

## Self-Check: PASSED

- All four new source/test artifacts and this summary exist.
- Summary frontmatter validates against the GSD summary schema.
- Focused 117/117 and repository-wide 302/302 gates are recorded from successful runs.
- `git diff --check` passes and the staged index is empty.
- Commits: none (not authorized).

---
*Phase: 01.1-artifact-admission-persistence-safety*
*Completed: 2026-07-12*
