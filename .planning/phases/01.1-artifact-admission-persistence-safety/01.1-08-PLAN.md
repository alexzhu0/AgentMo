---
phase: 01.1-artifact-admission-persistence-safety
plan: "08"
type: execute
wave: 8
depends_on: ["01.1-07"]
files_modified:
  - src/artifact-registry.js
  - src/artifact-subjects.js
  - src/runtime-plan.js
  - src/runtime-env.js
  - src/run-state.js
  - src/cli.js
  - test/runtime-plan.test.js
  - test/runtime-env.test.js
  - test/run-state.test.js
  - test/runtime-replay-eval.test.js
  - test/artifact-surface-coverage.test.js
autonomous: true
requirements: [CORE-04, EVID-03]
must_haves:
  truths:
    - "Runtime plan/state/index inputs are exact admitted artifacts, and all runtime commands can start fresh from named files/digests."
    - "Durable runtime artifacts contain SecretPresence and RedactedSummary carriers, never env values, raw messages/prompts, copied message files, raw transcript/tool bodies, or unclean stdout/stderr."
    - "Exact replay requires the operator to re-supply transient message material whose raw-byte digest matches; absence is honestly unavailable/reconstructed, never silently exact."
  artifacts:
    - { path: src/run-state.js, provides: "admitted and persistable run-state/index with honest replay" }
    - { path: src/runtime-env.js, provides: "SecretPresence descriptor without values" }
  key_links:
    - { from: src/run-state.js, to: src/persistability.js, via: "run state/index/output summaries before writes", pattern: "SecretPresence|RedactedSummary" }
    - { from: src/cli.js, to: src/artifact-subjects.js, via: "runtime command subject contracts", pattern: "subjectsForCommand" }
---

<objective>
Complete the runtime state/replay slice so fresh processes trust exact runtime artifacts while raw prompts, credentials, and process output remain transient.
</objective>
## Phase Goal
**As a** developer, **I want to** hand off admitted artifacts safely, **so that** fresh processes can trust them without retaining secrets.

## Writer Owner Contract
| Owner | Complete candidate | First side effect | Unsafe-field replacement | Focused proof |
|---|---|---|---|---|
| runtime plan/result | durable command descriptor + message evidence | artifact stdout (Plan 11) | message `RedactedSummary`; transient args kept outside durable object | JSON contains digest/length, not canary |
| run writer | run-state and run-index objects | run directory/temp write | env `SecretPresence`; stdout/stderr `RedactedSummary`; no raw message file | output root unchanged on hostile summary |
| replay | re-supplied transient message | process spawn | digest match required; no stored raw fallback | no input => not exact/no spawn when exact required |

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Close runtime identities, subjects, and transient-message replay</name>
  <read_first><file>src/runtime-plan.js</file><file>src/runtime-env.js</file><file>src/run-state.js</file><file>src/artifact-registry.js</file><file>src/artifact-subjects.js</file></read_first>
  <files>src/artifact-registry.js, src/artifact-subjects.js, src/runtime-plan.js, src/runtime-env.js, src/run-state.js, src/cli.js, test/runtime-plan.test.js, test/runtime-env.test.js, test/run-state.test.js, test/runtime-replay-eval.test.js</files>
  <action>Add runtime-plan, run-state, and run-index descriptors/validators and exact subjects for run-plan, run, run-report, replay-run, run-eval, and status runtime options. Durable command descriptors contain a RedactedSummary message record (digest/length/safe summary, no raw message or host message-file path); actual execution receives the message only through a transient nonserialized argument. Remove managed raw-message artifact copying. Replay can be exact only when newly supplied message bytes match the stored digest; otherwise label reconstructed/unavailable and fail when exact is required. Add fresh-process/family-cross-product/read-count/digest mutation and no-raw-canary tests per D-13.</action>
  <verify><automated>node --test test/runtime-plan.test.js test/runtime-env.test.js test/run-state.test.js test/runtime-replay-eval.test.js test/cli.test.js</automated></verify>
  <acceptance_criteria>
    - Runtime artifacts/CLI JSON contain no inline message, copied prompt bytes, host message path, or env value.
    - Re-supplied exact bytes enable exact replay; missing/mismatched bytes cannot.
    - Each runtime loader rejects every nonmatching family/subject before builder/spawn.</acceptance_criteria>
  <done>Runtime recovery depends on exact safe artifacts plus explicitly transient message material.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate run state/index and safe process summaries before writes</name>
  <read_first><file>src/persistability.js</file><file>src/run-state.js</file><file>src/runtime-env.js</file><file>test/helpers/io-surface-inventory.js</file></read_first>
  <files>src/runtime-env.js, src/run-state.js, test/runtime-env.test.js, test/run-state.test.js, test/runtime-replay-eval.test.js, test/artifact-surface-coverage.test.js</files>
  <action>Refactor environment evidence to the exact SecretPresence carrier and stdout/stderr/message evidence to exact RedactedSummary carriers. Validate complete run-state and index candidates before run-directory/temp/write/rename; ensure process execution never persists raw runner output even on failure/timeout. Remove the raw message-file writer sink. Add hostile env/output/fallback/selector nested cases proving zero state/index/message bytes and value-blind errors; mark runtime physical sinks gated and artifact stdout pending Plan 11.</action>
  <verify><automated>node --test test/runtime-env.test.js test/run-state.test.js test/runtime-replay-eval.test.js test/persistability.test.js test/artifact-surface-coverage.test.js</automated></verify>
  <acceptance_criteria>
    - No raw-message writer remains in the I/O scanner.
    - Invalid runtime evidence creates no run directory/state/index/temp and contains no canary.
    - Valid declared/live distinctions and replay labels remain non-transitive and honest.</acceptance_criteria>
  <done>Runtime state is safely persistent; credentials, prompts, and raw output are not.</done>
</task>
</tasks>

<threat_model>ASVS L1; block high/critical. High credential/prompt/output disclosure is mitigated by exact carriers and removal of raw message copying. High replay spoofing is mitigated by newly supplied raw-byte digest checks. High partial state writes are mitigated by complete-candidate preflight.</threat_model>
<prohibitions>- Do not read `.env`, persist raw messages/output/env values, retain host paths, claim exact replay without re-supplied matching bytes, or add Phase 1.2 runtime-version gates. Do not stage/commit.</prohibitions>

## Artifacts this phase produces
- Runtime-plan/run-state/run-index descriptors and subject entries.
- SecretPresence environment evidence and RedactedSummary message/stdout/stderr evidence.
- Digest-bound explicit replay contract with no stored raw prompt fallback.
- Gated run-state/index writers and zero-output tests.

## Multi-Source Coverage Audit
- CORE-04/D-13: runtime family complete.
- EVID-03: runtime env/message/output/state physical writers complete.
- Observation/evidence/stdout/generator/docs remain Plans 09–12.

<verification>Focused tests, `npm run check`, `git diff --check`.</verification>
<success_criteria>Runtime artifacts are resumable and exact without durably retaining private runtime material.</success_criteria>
<output>Create `01.1-08-SUMMARY.md`; do not commit.</output>
