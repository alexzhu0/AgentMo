---
phase: 01.1-artifact-admission-persistence-safety
plan: "04"
type: execute
wave: 4
depends_on: ["01.1-03"]
files_modified:
  - src/artifact-registry.js
  - src/artifact-subjects.js
  - src/user-need.js
  - src/design-plan.js
  - src/blueprint-draft.js
  - src/cli.js
  - test/user-need.test.js
  - test/design-plan.test.js
  - test/blueprint-draft.test.js
  - test/artifact-surface-coverage.test.js
autonomous: true
requirements: [CORE-04, EVID-03]
must_haves:
  truths:
    - "User-need, design-plan, and blueprint-draft flows consume exact admitted artifacts in a fresh process and cannot cross family/subject boundaries."
    - "Design-plan ledger and blueprint-draft bytes are approved before any output side effect and contain subject+digest provenance rather than host paths or raw conversation material."
  artifacts:
    - { path: src/artifact-registry.js, provides: "Plan-family descriptors and validators" }
    - { path: src/design-plan.js, provides: "persistable Stage 2 ledger writer" }
  key_links:
    - { from: src/blueprint-draft.js, to: src/artifact-admission.js, via: "admitted design-plan provenance", pattern: "subject|digest" }
    - { from: src/design-plan.js, to: src/persistability.js, via: "pre-side-effect candidate serialization", pattern: "serializePersistableJson" }
---

<objective>
Complete the Plan-stage family: user needs, decision ledgers, and blueprint drafts can be independently resumed and safely persisted without raw transcript or host-path authority.
</objective>
## Phase Goal
**As a** developer, **I want to** hand off admitted artifacts safely, **so that** fresh processes can trust them without retaining secrets.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Admit every Plan-stage input from exact family-bound bytes</name>
  <read_first><file>src/user-need.js</file><file>src/design-plan.js</file><file>src/blueprint-draft.js</file><file>src/artifact-registry.js</file><file>src/artifact-subjects.js</file></read_first>
  <files>src/artifact-registry.js, src/artifact-subjects.js, src/user-need.js, src/design-plan.js, src/blueprint-draft.js, src/cli.js, test/user-need.test.js, test/design-plan.test.js, test/blueprint-draft.test.js</files>
  <action>Add closed descriptors/validators and exact command subject sets for need-report, design-plan, and blueprint-draft. All loaders accept only expected digest+subject or a non-forgeable admission result derived from the same Buffer; eliminate generic subject-qualified JSON loaders from public paths. Blueprint drafting receives provenance `{subject,digest,identity}` from admission rather than a source path or upstream success flag. Add every family×loader negative pair, fresh-process happy paths, single-read sentinels, and migration/unknown distinction tests per D-13.</action>
  <verify><automated>node --test test/user-need.test.js test/design-plan.test.js test/blueprint-draft.test.js test/artifact-admission.test.js test/cli.test.js</automated></verify>
  <acceptance_criteria>
    - Each Plan command declares and consumes its exact frozen subjects with no ignored binding.
    - Cross-family, missing digest, changed bytes, legacy, and unknown inputs fail before builders run.
    - Durable provenance contains identity/subject/digest only, not absolute paths or command ancestry.
  </acceptance_criteria>
  <done>Every Plan-stage input is independently trustworthy from admitted bytes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist Plan ledgers and blueprint drafts through the shared policy</name>
  <read_first><file>src/persistability.js</file><file>src/design-plan.js</file><file>src/blueprint-draft.js</file><file>test/helpers/io-surface-inventory.js</file></read_first>
  <files>src/design-plan.js, src/blueprint-draft.js, test/design-plan.test.js, test/blueprint-draft.test.js, test/artifact-surface-coverage.test.js</files>
  <action>Prepare the complete design-plan and blueprint candidates with `serializePersistableJson` before mkdir/temp/write/rename, then preserve deterministic formatting and atomic rename. Convert any sensitive requirement reference to exact SecretRef/SecretPresence/RedactedSummary carriers; reject raw conversation/tool/output fields and host paths rather than copying or redacting in place. Mark only these exact writer symbols `gated` in the architectural allowlist and add zero-directory/temp/output tests.</action>
  <verify><automated>node --test test/design-plan.test.js test/blueprint-draft.test.js test/persistability.test.js test/artifact-surface-coverage.test.js</automated></verify>
  <acceptance_criteria>
    - Hostile nested material yields no design-plan/blueprint/temp/directory bytes and no value-bearing error.
    - Valid output stays deterministic and validates with its existing schema.
    - Inventory has no pending Plan-stage physical writer.</acceptance_criteria>
  <done>Plan artifacts are safe durable contracts rather than transcript-derived state.</done>
</task>
</tasks>

<threat_model>ASVS L1; block high/critical. High family spoofing is closed by descriptor/subject matrices. High disclosure from raw planning conversation/path provenance is closed by explicit admitted provenance and pre-write policy. High partial-write risk is covered by zero-root tests.</threat_model>
<prohibitions>- Do not read `.env`, persist raw conversation/tool output, keep a generic validator argument, or treat upstream success/path as authority. Do not stage/commit.</prohibitions>

## Artifacts this phase produces
- User-need/design-plan/blueprint-draft descriptors and subject entries.
- Admitted Plan loaders and persistable deterministic writers.
- Subject+digest provenance contract and family/zero-write tests.

## Multi-Source Coverage Audit
- CORE-04/D-13: Plan family complete.
- EVID-03: Plan ledger/draft physical writers complete; their CLI stdout is Plan 11.
- Produce/runtime/evidence families stay assigned to Plans 06–08.

<verification>Focused tests, `npm run check`, `git diff --check`.</verification>
<success_criteria>Plan artifacts cross fresh processes by exact bytes and persist without secrets, raw conversation, or path authority.</success_criteria>
<output>Create `01.1-04-SUMMARY.md`; do not commit.</output>
