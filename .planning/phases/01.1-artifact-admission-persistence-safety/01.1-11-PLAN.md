---
phase: 01.1-artifact-admission-persistence-safety
plan: "11"
type: execute
wave: 11
depends_on: ["01.1-10"]
files_modified:
  - src/cli.js
  - src/persistability.js
  - src/report.js
  - test/cli.test.js
  - test/persistability.test.js
  - test/stage-contracts.test.js
  - test/artifact-surface-coverage.test.js
autonomous: true
requirements: [CORE-04, EVID-03]
must_haves:
  truths:
    - "Every artifact/report human or JSON stdout branch passes its complete candidate and exact formatted text through `emitPersistableOutput` before any byte is emitted."
    - "All structured errors have fixed value-blind JSON/human schemas; migration-required and other admission errors never fall back to raw human stderr in JSON mode."
    - "Help, progress, and diagnostics are explicitly non-artifact/diagnostic owners and cannot print secrets, hostile keys/pointers, raw runtime material, or host absolute paths."
  artifacts:
    - { path: src/cli.js, provides: "single artifact emitter and centralized value-blind error boundary" }
    - { path: test/artifact-surface-coverage.test.js, provides: "zero pending stdout/stderr/console owner rows" }
  key_links:
    - { from: src/cli.js, to: src/persistability.js, via: "candidate + formatted text preflight before stdout", pattern: "emitPersistableOutput" }
    - { from: src/cli.js, to: src/artifact-admission.js, via: "one fixed structured error envelope", pattern: "artifactErrorEnvelope" }
---

<objective>
Close the shared CLI output boundary so any command can be redirected as durable evidence without leaking unsafe material or changing error semantics between human and JSON modes.
</objective>
## Phase Goal
**As a** developer, **I want to** hand off admitted artifacts safely, **so that** fresh processes can trust them without retaining secrets.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify every CLI sink owner and zero-byte failure behavior</name>
  <read_first><file>src/cli.js</file><file>src/persistability.js</file><file>test/cli.test.js</file><file>test/helpers/io-surface-inventory.js</file></read_first>
  <files>test/cli.test.js, test/persistability.test.js, test/stage-contracts.test.js, test/artifact-surface-coverage.test.js</files>
  <action>Turn the architectural sink inventory into an exact branch table for all current commands: each stdout call is `artifact` or bounded `non-artifact`; each stderr call is `diagnostic`; console output is forbidden in production. For every artifact branch inject hostile nested value/key/subject/pointer/path/raw prompt/transcript/tool/stdout/stderr and assert exit nonzero plus empty stdout. For every diagnostic path assert fixed code/category/guidance only in both human and `--json`, including migration-required. For progress/help assert no synthetic secret/raw/path canary and no absolute host path.</action>
  <verify><automated>node --test test/cli.test.js test/persistability.test.js test/stage-contracts.test.js test/artifact-surface-coverage.test.js</automated></verify>
  <acceptance_criteria>
    - Every discovered stdout/stderr/console sink has one exact branch owner.
    - JSON-mode failures always emit the fixed JSON envelope to stdout and no raw stderr; human mode emits bounded stderr and no artifact stdout.
    - No hostile case emits even a partial prefix before failure.</acceptance_criteria>
  <done>The complete CLI output corpus is an executable zero-byte-on-rejection contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Centralize artifact emission, progress redaction, and errors</name>
  <read_first><file>src/cli.js</file><file>src/persistability.js</file><file>src/report.js</file><file>test/cli.test.js</file></read_first>
  <files>src/cli.js, src/persistability.js, src/report.js, test/cli.test.js, test/artifact-surface-coverage.test.js</files>
  <action>Replace per-branch `JSON.stringify`/formatter writes with one `emitPersistableOutput({candidate,format,json,sink})` call that validates candidate and exact text before invoking stdout. Centralize all structured admission/migration/persistability errors under one fixed value-blind human/JSON envelope. Route help/progress through bounded text helpers; replace absolute path display with managed relative refs or `[REDACTED_PATH]`, never raw host paths. Retain exit codes and non-transitive evidence fields. Mark all output sinks gated/diagnostic/non-artifact and require zero `pending:` rows for CLI output.</action>
  <verify><automated>node --test test/cli.test.js test/stage-contracts.test.js test/persistability.test.js test/artifact-surface-coverage.test.js</automated></verify>
  <acceptance_criteria>
    - No artifact branch directly stringifies/writes stdout outside the shared emitter.
    - Migration/admission/persistability failures have equivalent bounded meaning in human/JSON modes.
    - I/O inventory reports no pending CLI stdout/stderr/console sink.</acceptance_criteria>
  <done>CLI output is a single safe persistence boundary with no partial or mode-specific leak.</done>
</task>
</tasks>

<threat_model>ASVS L1; block high/critical. High partial-output disclosure is mitigated by candidate/text preflight before sink invocation. High diagnostic leakage is mitigated by fixed envelopes. High path disclosure is mitigated by managed refs/redaction in non-artifact text.</threat_model>
<prohibitions>- Do not read `.env`, write any artifact prefix before validation, keep branch-local JSON/string writes, print absolute paths, or promote evidence claims. Do not stage/commit.</prohibitions>

## Artifacts this phase produces
- Central CLI `emitPersistableOutput` usage for all artifact/report branches.
- One value-blind human/JSON error envelope.
- Explicit help/progress/diagnostic classifications and zero pending output sinks.

## Multi-Source Coverage Audit
- CORE-04: all admitted command outputs preserve exact subject semantics.
- EVID-03: CLI artifact/diagnostic/progress outputs fully covered.
- Generated/scripted/docs command parity remains Plan 12.

<verification>Focused tests, `npm run check`, `git diff --check`.</verification>
<success_criteria>Any CLI result can be safely redirected; rejected material emits no artifact bytes and no diagnostic value.</success_criteria>
<output>Create `01.1-11-SUMMARY.md`; do not commit.</output>
