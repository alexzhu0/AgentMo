---
phase: 01.1-artifact-admission-persistence-safety
plan: "12"
type: execute
wave: 12
depends_on: ["01.1-11"]
files_modified:
  - src/artifact-subjects.js
  - src/scaffold-files.js
  - src/handoff.js
  - scripts/openclaw-live-smoke.sh
  - test/artifact-subjects.test.js
  - test/artifact-surface-coverage.test.js
  - test/canonical-identity.test.js
  - test/handoff.test.js
  - test/scaffold.test.js
  - test/live-smoke-script.test.js
autonomous: true
requirements: [CORE-04, EVID-03]
must_haves:
  truths:
    - "Every generated or scripted durable-input command derives its exact subject set from, or is mechanically checked against, `DURABLE_COMMAND_SUBJECTS`."
    - "Generated examples and live-smoke commands calculate each digest from the exact file bytes immediately before invocation and bind every subject once."
    - "The live-smoke helper stays value-blind and isolated and does not implement or bypass the Phase 1.2 OpenClaw runtime-version gate."
  artifacts:
    - { path: src/artifact-subjects.js, provides: "canonical command rendering/inspection helpers" }
    - { path: test/artifact-subjects.test.js, provides: "generated/script command corpus checked against production registry" }
  key_links:
    - { from: src/scaffold-files.js, to: src/artifact-subjects.js, via: "canonical subject rendering", pattern: "renderDigestBindings" }
    - { from: scripts/openclaw-live-smoke.sh, to: test/artifact-subjects.test.js, via: "script commands parsed against registry", pattern: "--digest" }
---

<objective>
Synchronize generated scaffold/handoff commands and the maintained live-smoke script with the production command-subject authority.
</objective>
## Phase Goal
**As a** developer, **I want to** hand off admitted artifacts safely, **so that** fresh processes can trust them without retaining secrets.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build one registry-backed generated/script command verifier</name>
  <read_first><file>src/artifact-subjects.js</file><file>src/scaffold-files.js</file><file>src/handoff.js</file><file>scripts/openclaw-live-smoke.sh</file><file>test/artifact-subjects.test.js</file></read_first>
  <files>src/artifact-subjects.js, test/artifact-subjects.test.js, test/artifact-surface-coverage.test.js, test/canonical-identity.test.js</files>
  <action>Add `renderDigestBindings(command, subjectToFileExpression)` and a test-only command-corpus parser that loads the production frozen registry, extracts each generated/script invocation, identifies its command and durable file operands, and asserts exact required subjects with no duplicate/extra/unknown binding. Dynamic optional/ordinal entries use the same production resolver. Make tests fail for a new generated/script command not present in the registry or for hard-coded duplicated expectations. Extend the architectural allowlist to classify shell digest reads as bounded non-artifact exact-byte operations and every shell redirection/output sink.</action>
  <verify><automated>node --test test/artifact-subjects.test.js test/artifact-surface-coverage.test.js test/canonical-identity.test.js</automated></verify>
  <acceptance_criteria>
    - Tests derive expected subjects only by importing production registry symbols.
    - Any removed/renamed/extra subject in generated or shell text fails.
    - New shell file reads/redirections cannot escape I/O ownership.</acceptance_criteria>
  <done>Generated/script command checks cannot self-certify against a copied subject table.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit exact digest bindings in scaffold, handoff, and live-smoke commands</name>
  <read_first><file>src/artifact-subjects.js</file><file>src/scaffold-files.js</file><file>src/handoff.js</file><file>scripts/openclaw-live-smoke.sh</file><file>test/live-smoke-script.test.js</file></read_first>
  <files>src/scaffold-files.js, src/handoff.js, scripts/openclaw-live-smoke.sh, test/handoff.test.js, test/scaffold.test.js, test/live-smoke-script.test.js, test/artifact-subjects.test.js</files>
  <action>Render one digest binding per production-registry subject in all generated runbook/handoff commands. Use a value-blind Node/crypto exact-file-byte helper immediately before invocation; do not hash parsed JSON or embed stale constants. Update the live-smoke script to calculate/bind every artifact it consumes, quote all values, and preserve isolated state/cleanup. The helper may use transient provider credentials for execution but must never echo/persist them or read `.env` in tests. Do not add the Phase 1.2 target-version preflight.</action>
  <verify><automated>node --test test/artifact-subjects.test.js test/handoff.test.js test/scaffold.test.js test/live-smoke-script.test.js test/artifact-surface-coverage.test.js && bash -n scripts/openclaw-live-smoke.sh</automated></verify>
  <acceptance_criteria>
    - Every generated/script durable-input invocation matches production subjects exactly.
    - Digest calculations read the same exact file passed to the command immediately before use.
    - Script syntax, value blindness, isolated state, and non-certification wording remain green.</acceptance_criteria>
  <done>All machine-generated and scripted recovery paths use the final exact-byte contract.</done>
</task>
</tasks>

<threat_model>ASVS L1; block high/critical. High command drift/bypass is mitigated by registry-derived rendering and parsing. High stale-digest tampering is mitigated by immediate exact-file hashing. High credential disclosure is mitigated by value-blind script tests and no persistent credential output.</threat_model>
<prohibitions>- Do not read `.env`, copy the subject table into tests, hash parsed JSON, embed digest constants, leak script credentials, or implement Phase 1.2 runtime gates. Do not stage/commit.</prohibitions>

## Artifacts this phase produces
- `renderDigestBindings` production helper.
- Registry-derived generated/script command corpus verifier.
- Digest-bound scaffold/handoff/live-smoke commands.

## Multi-Source Coverage Audit
- CORE-04/D-13: generated and scripted command paths covered by the same registry.
- EVID-03: live-smoke remains value-blind; all script I/O classified.
- Maintained prose command corpus/release closure remains Plan 13.

<verification>Focused tests, shell syntax, `npm run check`, `git diff --check`.</verification>
<success_criteria>Copied generated/script commands cannot omit or invent a subject and never use stale/canonicalized digests.</success_criteria>
<output>Create `01.1-12-SUMMARY.md`; do not commit.</output>
