---
phase: 01.1-artifact-admission-persistence-safety
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/artifact-admission.js
  - src/artifact-subjects.js
  - src/artifact-registry.js
  - src/cli.js
  - test/artifact-admission.test.js
  - test/artifact-subjects.test.js
  - test/stage-contracts.test.js
  - docs/STAGE_CONTRACTS.md
autonomous: true
requirements: [CORE-04]
must_haves:
  truths:
    - "A fresh process can run `agentmo design-plan` from exact discovery-db and user-need files only when both externally supplied raw-byte digests match."
    - "Digest verification of the single Buffer occurs before decode, duplicate-member scan, parse, audit, identity lookup, or schema validation."
    - "One frozen command-to-subject registry is the authority for CLI binding parsing and all later generated/scripted/documented commands."
  artifacts:
    - { path: src/artifact-admission.js, provides: "single-read hash-before-parse admission", exports: [ArtifactAdmissionError, digestRawBytes, parseDigestBindings, loadAdmittedArtifact] }
    - { path: src/artifact-subjects.js, provides: "closed command subject contracts", exports: [DURABLE_COMMAND_SUBJECTS, subjectsForCommand] }
  key_links:
    - { from: src/cli.js, to: src/artifact-subjects.js, via: "design-plan resolves exact required subjects", pattern: "subjectsForCommand" }
    - { from: src/artifact-admission.js, to: src/artifact-registry.js, via: "identity and validator resolution after digest proof", pattern: "resolveDurableArtifactDescriptor" }
---

<objective>
Deliver the first correct vertical slice for D-13: `agentmo design-plan <discovery-db> --need <user-need>` works in a fresh process from two subject-bound exact-byte digests, while the existing `agentmo plan <blueprint>` command remains unchanged until its own family slice.
</objective>

## Phase Goal
**As a** developer, **I want to** hand off admitted artifacts safely, **so that** fresh processes can trust them without retaining secrets.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify exact-byte admission and the frozen command-subject authority</name>
  <read_first>
    <file>src/cli.js</file><file>src/artifact-registry.js</file><file>src/design-plan.js</file><file>test/stage-contracts.test.js</file>
  </read_first>
  <files>test/artifact-admission.test.js, test/artifact-subjects.test.js, test/stage-contracts.test.js</files>
  <action>Write RED tests that calculate `sha256:` digests from the exact fixture Buffers, spawn a new process, and call the existing `design-plan` command with exactly `discovery-db` and `user-need` bindings. Add byte-order sentinels proving whitespace/key-order/one-byte changes fail before parser or validator sentinels. Define test expectations for a deep-frozen `DURABLE_COMMAND_SUBJECTS` entry whose `design-plan` value is exactly `['discovery-db','user-need']`; require missing, duplicate, extra, malformed, unknown, and swapped bindings to return bounded codes and zero artifact stdout. Assert the existing `plan` command is still blueprint-based.</action>
  <verify><automated>node --test test/artifact-admission.test.js test/artifact-subjects.test.js test/stage-contracts.test.js</automated></verify>
  <acceptance_criteria>
    - RED fails only because the new admission/subject symbols and digest flags are absent.
    - Tests prove a fresh subprocess has no producer-command ancestry.
    - No diagnostic contains fixture path, subject input, key, pointer, or synthetic canary.
  </acceptance_criteria>
  <done>The first journey and its single subject authority are locked before production changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement single-read admission and wire the real design-plan command</name>
  <read_first>
    <file>test/artifact-admission.test.js</file><file>src/artifact-migration.js</file><file>src/artifact-registry.js</file><file>src/cli.js</file>
  </read_first>
  <files>src/artifact-admission.js, src/artifact-subjects.js, src/artifact-registry.js, src/cli.js, docs/STAGE_CONTRACTS.md</files>
  <action>Create strict lowercase `sha256:<64hex>` parsing, constant-time equality for equal-length digests, one Buffer read, fixed resource ceilings, value-blind error categories, and exact one-use subject consumption. Seed the closed descriptor registry only with discovery-db and user-need identities/validators needed by this slice; unknown identities fail and supported legacy identities preserve migration-required semantics. In `src/cli.js`, add repeatable `--digest subject=sha256:...` only to `design-plan`, route both files through `loadAdmittedArtifact`, and leave `plan` consuming a blueprint. Per D-13, no decode or content-derived operation may precede digest success. Document the corrected command and evidence boundary.</action>
  <verify><automated>node --test test/artifact-admission.test.js test/artifact-subjects.test.js test/stage-contracts.test.js test/cli.test.js test/artifact-migration.test.js</automated></verify>
  <acceptance_criteria>
    - The exact support-triage pair succeeds in a fresh process and writes the normal design-plan artifact.
    - All binding/digest mutations fail before Plan output with value-blind human/JSON meaning.
    - `agentmo plan <blueprint>` behavior and Phase 01 migration tests remain green.
  </acceptance_criteria>
  <done>The first real cross-stage handoff uses exact raw bytes and the correct public command.</done>
</task>
</tasks>

<threat_model>
ASVS L1; block high/critical. T-01 tampering at bytes-to-parser is mitigated by one-read hash-before-parse tests. T-02 subject spoofing is mitigated by a frozen exact command subject set and one-use binding map. T-03 disclosure is mitigated by stable codes and canary-negative human/JSON tests.
</threat_model>

<prohibitions>
- Do not read `.env`, hash parsed JSON, add optional digest fallback, expose mutable registration, or change `plan` into the Stage 2 command.
- Do not implement Phase 1.2 runtime gates or claim digest admission proves quality/approval.
- Do not stage or commit.
</prohibitions>

## Artifacts this phase produces
- `src/artifact-admission.js`: `ArtifactAdmissionError`, `digestRawBytes`, `parseDigestBindings`, `loadAdmittedArtifact`.
- `src/artifact-subjects.js`: `DURABLE_COMMAND_SUBJECTS`, `subjectsForCommand`.
- CLI: repeatable `--digest <subject>=sha256:<64hex>` for `design-plan`.
- `test/artifact-admission.test.js` and `test/artifact-subjects.test.js`.

## Multi-Source Coverage Audit
- GOAL/CORE-04/D-13: covered by the fresh-process design-plan slice.
- EVID-03: deferred to Plans 02–10, not dropped.
- Node/OpenClaw runtime gate: excluded to Phase 1.2.

<verification>Run focused tests above, then `npm run check` and `git diff --check`.</verification>
<success_criteria>The correct design-plan journey succeeds only from exact named raw bytes; command subjects have one frozen authority; no ancestry or content leak is required.</success_criteria>
<output>Create `01.1-01-SUMMARY.md`; do not commit.</output>
