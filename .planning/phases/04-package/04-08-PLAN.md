---
phase: 04-package
plan: "08"
type: execute
wave: 8
depends_on:
  - 04-07
files_modified:
  - src/artifact-contract.js
  - src/artifact-registry.js
  - src/artifact-subjects.js
  - src/cli.js
  - test/artifact-admission.test.js
  - test/openclaw-install-plan.test.js
  - test/openclaw-install-approval.test.js
  - test/phase4-contracts.test.js
autonomous: true
requirements:
  - OCLW-02
  - OCLW-04
  - EVID-05
must_haves:
  truths:
    - "Fresh processes create and exact re-admit absent-genesis, install-plan, ordinary approval, each sensitive-action decision and the exact conflict-set approval."
    - "Upgrade, uninstall and rollback preview exact-admit the current receipt through the Plan 04-07 receipt descriptor using subject, file and caller-supplied external SHA-256."
    - "Install uses the mutually exclusive verified absent-genesis alternative and never requires or accepts a fabricated prior receipt."
    - "Preview accepts only a deterministic archive path plus external SHA-256 and revalidates the complete D-42 closure before target observation or durable output."
    - "Receipt descriptor registration is preserved before any fresh-process prior-receipt admission test or CLI consumer executes."
  artifacts:
    - path: "src/artifact-registry.js"
      provides: "Closed descriptors for the five lifecycle authorities while preserving the receipt descriptor"
    - path: "src/artifact-subjects.js"
      provides: "Exact command input/output subjects for genesis capture, preview and approval"
    - path: "src/cli.js"
      provides: "Fresh-process archive-only lifecycle authority producers"
    - path: "test/openclaw-install-plan.test.js"
      provides: "Fresh-process predecessor receipt and absent-genesis preview matrix"
    - path: "test/artifact-admission.test.js"
      provides: "Subject/file/external-digest exact admission matrix"
  key_links:
    - from: "src/cli.js"
      to: "src/artifact-registry.js"
      via: "preview exact-admits prior receipt before lifecycle model construction"
      pattern: "openclaw-install-receipt"
    - from: "src/cli.js"
      to: "src/package-archive.js"
      via: "retained archive read plus external digest and complete member closure"
      pattern: "archive(-sha256|Binding|Inventory)"
    - from: "src/cli.js"
      to: "src/openclaw-install-approval.js"
      via: "one review produces independent ordinary, sensitive and conflict files"
      pattern: "writeOpenClawInstallReviewDecisions"
---

## Phase Goal

**As a** developer, **I want to** deterministically generate an offline-inspectable, least-trust Agent Package from the approved build contract, **so that** I can separately authorize a safe OpenClaw installation.

<objective>
Expose the already modeled and registered lifecycle authorities through fresh-process archive-only production and exact admission.

Purpose: Complete preview/approval only after prior-receipt identity and admission are available, preserving D-35 through D-42.
Output: Five authority descriptors, command subjects, CLI routes and fresh-process genesis/predecessor tests.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-package/04-07-SUMMARY.md
@src/openclaw-install-plan.js
@src/openclaw-install-approval.js
@src/openclaw-install-receipt.js
@src/package-archive.js
@src/artifact-admission.js
@src/artifact-contract.js
@src/artifact-registry.js
@src/artifact-subjects.js
@src/cli.js

<interfaces>
Plan 04-07 already registers `openclaw-install-receipt`; this plan must preserve that descriptor while adding the five lifecycle authority identities.
`openclaw-install-preview` accepts archive path/external digest, probe file/external digest, lifecycle action, target facts, output path, and exactly one lifecycle basis: absent genesis for install, current receipt for upgrade/uninstall, current plus selected predecessor receipt/archive for rollback.
Every receipt input is an explicit subject/file/external-digest tuple exact-admitted before model construction; parsed objects, embedded digests and same-process caches are invalid.
`openclaw-install-approve` exact-admits the install plan and publishes ordinary, per-sensitive-action and whole-conflict authorities to independent absent files.
</interfaces>
</context>

## Artifacts this phase produces

| Kind | Name | Contract |
|---|---|---|
| Artifact contracts | `src/artifact-contract.js` | Public schemas/templates for absent genesis and four decision/plan subjects |
| Artifact registry | `src/artifact-registry.js` | Closed validators for five lifecycle authorities, retaining receipt registration |
| Command subjects | `src/artifact-subjects.js` | Exact input/output sets for genesis, preview and approval |
| CLI | `src/cli.js` | Archive-only fresh-process lifecycle producers |
| Tests | `test/artifact-admission.test.js`, `test/openclaw-install-plan.test.js`, `test/openclaw-install-approval.test.js` | RED-first transport, receipt/genesis and exact admission chain |
| Incremental test | `test/phase4-contracts.test.js` | Wave 8 durable-authority and non-certification extension |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing fresh-process receipt/genesis preview and approval journeys</name>
  <read_first>
    - src/openclaw-install-receipt.js
    - src/artifact-admission.js
    - src/package-archive.js
    - src/openclaw-install-plan.js
    - src/openclaw-install-approval.js
  </read_first>
  <files>test/artifact-admission.test.js, test/openclaw-install-plan.test.js, test/openclaw-install-approval.test.js, test/phase4-contracts.test.js</files>
  <behavior>
    - A spawned install preview exact-admits verified absent genesis, rejects any supplied prior receipt, and publishes one install plan to an absent file.
    - Spawned upgrade and uninstall previews exact-admit one current receipt by receipt subject, file and external SHA-256.
    - Spawned rollback preview exact-admits current and selected predecessor receipts plus the predecessor archive closure.
    - Wrong receipt subject, wrong path, missing digest mate, stale external digest, post-digest byte mutation, duplicate receipt and same-process object fail before archive/target observation or output.
    - A second process exact-admits every produced plan/approval file and its caller-supplied external digest under the correct subject.
  </behavior>
  <action>Create RED fresh-process tests only after the Plan 04-07 receipt descriptor exists. For receipt-consuming lifecycle previews, explicitly cover subject/file/external-digest admission, wrong subject, wrong file, stale digest, byte drift, missing mate, duplicate input and action-basis substitution. Add the verified absent-genesis install alternative and prove it is mutually exclusive with prior receipt input. Exercise D-42 transport and internal closure drift independently: stale outer digest, member byte, extra/missing member, type/mode, symlink or retained identity swap, manifest mismatch and inventory mismatch; recompute the outer digest for internal attacks. Then spawn approval and a separate admission process for ordinary, every sensitive-action and exact conflict artifacts. Every negative asserts no target write, OpenClaw process, credential route or durable successor.</action>
  <acceptance_criteria>
    - Receipt registration from Plan 04-07 is a prerequisite, not recreated inside the test process.
    - Fresh-process tests cover subject, file and external-digest binding for prior receipt.
    - Install has an explicit absent-genesis success path and rejects predecessor substitution.
    - RED failures identify missing five-authority descriptors or CLI routes.
  </acceptance_criteria>
  <verify>
    <automated>! node --test test/artifact-admission.test.js test/openclaw-install-plan.test.js test/openclaw-install-approval.test.js test/phase4-contracts.test.js</automated>
  </verify>
  <done>The RED chain proves lifecycle production cannot precede exact receipt/genesis and archive admission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register and wire archive-only fresh-process lifecycle producers</name>
  <read_first>
    - test/artifact-admission.test.js
    - test/openclaw-install-plan.test.js
    - test/openclaw-install-approval.test.js
    - src/artifact-contract.js
    - src/artifact-registry.js
    - src/artifact-subjects.js
    - src/cli.js
  </read_first>
  <files>src/artifact-contract.js, src/artifact-registry.js, src/artifact-subjects.js, src/cli.js</files>
  <behavior>
    - Registry first contains receipt plus all five lifecycle authority descriptors before CLI consumers run.
    - Preview exact-admits archive, probe and action-specific genesis/receipt authorities before target observation.
    - Approval exact-admits install plan and emits independent externally digest-bound files.
  </behavior>
  <action>First register the absent-genesis, install-plan, ordinary-approval, sensitive-action and conflict-approval contracts/subjects/validators while preserving the receipt descriptor added in Plan 04-07. Then wire genesis capture, archive-only preview and one-screen approval. Preview must exact-admit archive path/external SHA-256, full D-42 closure, probe and action-specific lifecycle basis before target observation: absent genesis for install; current receipt for upgrade/uninstall; current and selected predecessor receipt/archive for rollback. Every receipt uses the registered receipt subject plus caller-supplied file and external digest. Re-probe per D-41, enforce project/user scope per D-35, independent sensitive decisions per D-36 and exact conflict-set approval per D-37. Approval publishes independent absent files through Plan 04-07 writers. Reject package-root, manifest-only, force, purge, blanket overwrite, MCP and missing digest options.</action>
  <acceptance_criteria>
    - Descriptor registration occurs before CLI parsing can dispatch a receipt-consuming preview.
    - Fresh-process producer → separate exact admission succeeds for all five authority subjects.
    - Every receipt/genesis/archive drift case fails before target effects or durable output.
    - Human and JSON review share one frozen model without merging authority families.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/artifact-admission.test.js test/openclaw-install-plan.test.js test/openclaw-install-approval.test.js test/phase4-contracts.test.js</automated>
  </verify>
  <done>Lifecycle preview and approval are public, archive-only, receipt-aware and independently exact-admissible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Prior receipt/genesis bytes → lifecycle preview | Exact predecessor state becomes proposed mutation authority |
| Archive/probe/target facts → install plan | Untrusted transport and observed state become durable review facts |
| One-screen review → multiple files | One interaction produces non-interchangeable authority artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-28 | Spoofing | prior receipt/genesis authority | high | mitigate | Closed subject plus explicit file/external digest and mutually exclusive action basis before model construction. |
| T-04-29 | Tampering | archive/member/retained identity | high | mitigate | External SHA-256 and complete retained D-42 closure validation precede target observation. |
| T-04-30 | Elevation of Privilege | ordinary/sensitive/conflict substitution | high | mitigate | Independent descriptors, output files and exact action/conflict cardinality preserve D-36/D-37. |
| T-04-31 | Tampering | stale target/conflict state | high | mitigate | Immediate re-probe and exact target/scope/conflict equality invalidate stale preview and approvals. |
</threat_model>

<verification>
Wave 8 gate: `node --test test/artifact-admission.test.js test/openclaw-install-plan.test.js test/openclaw-install-approval.test.js test/phase4-contracts.test.js`.
</verification>

<success_criteria>
- Receipt descriptor registration precedes every prior-receipt consumer and test.
- Fresh-process receipt admission binds exact subject, file and external digest.
- Install has a verified absent-genesis path; upgrade/uninstall/rollback require exact receipt predecessors.
- Preview and approval accept deterministic archive transport only and bind the full D-42 closure.
- Ordinary, sensitive and exact-conflict authority families remain independent.
</success_criteria>

<output>
Create `.planning/phases/04-package/04-08-SUMMARY.md` when done.
</output>
