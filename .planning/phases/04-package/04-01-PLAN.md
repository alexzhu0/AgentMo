---
phase: 04-package
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/package-contract.js
  - src/package-carriers.js
  - test/package-contract.test.js
  - test/package-carriers.test.js
  - test/phase4-contracts.test.js
autonomous: true
requirements:
  - PACK-01
  - PACK-02
  - PACK-03
  - PACK-04
  - EVID-05
must_haves:
  truths:
    - "Produce rejects a build contract whose declared hook names lack an exact approved OpenClaw event mapping, owner, canonical byte recipe/content digest, version range, permission, timeout, or failure semantics."
    - "The currently approved Phase 3 contract fails closed because it proves no bundled owner for the four typed hooks; Phase 4 cannot repair that authority."
    - "Phase 4 v1 introduces no MCP carrier because the approved resource graph contains no external executable tool."
    - "Every declared capability is assigned the lowest-trust carrier allowed by D-33, and a declaration without executable bytes cannot satisfy D-32."
    - "The canonical manifest model remains target-neutral per D-34 and exposes complete ownership and non-certification fields."
    - "The package contract models D-42's complete canonical member inventory with portable relative path, regular-file type, fixed mode, byte length and per-member SHA-256; installation authorities can bind this closure without treating the directory as transport."
  artifacts:
    - path: "src/package-contract.js"
      provides: "Closed canonical package and capability-ledger validators"
      exports: ["AGENT_PACKAGE_SCHEMA_VERSION", "validateAgentPackageManifest", "validatePackageCapabilityLedger"]
    - path: "src/package-carriers.js"
      provides: "Lowest-trust carrier selection and hook contract-consistency gate"
      exports: ["PackageCarrierError", "selectPackageCarriers", "assertApprovedHookCarrierConsistency"]
    - path: "test/package-contract.test.js"
      provides: "RED-first package contract and executable inventory contract"
    - path: "test/package-carriers.test.js"
      provides: "RED-first carrier and hook owner blocker matrix"
    - path: "test/phase4-contracts.test.js"
      provides: "Incremental cross-wave gate seeded in Wave 1 and extended by every later behavior wave"
  key_links:
    - from: "src/package-carriers.js"
      to: "src/build-contract.js"
      via: "OPENCLAW_RESOURCE_KINDS and admitted resource graph"
      pattern: "assertApprovedHookCarrierConsistency"
    - from: "src/package-contract.js"
      to: "src/persistability.js"
      via: "closed, value-blind canonical manifest validation"
      pattern: "validatePackageCapabilityLedger"
---

## Phase Goal

**As a** developer, **I want to** deterministically generate an offline-inspectable, least-trust Agent Package from the approved build contract, **so that** I can separately authorize a safe OpenClaw installation.

<objective>
Establish the earliest fail-closed Produce gate and the canonical package contract before any package bytes are emitted.

Purpose: Resolve the phase's hardest constraint first: the approved build contract currently names typed hooks without a proven carrier owner.
Output: Closed package/capability validators and a carrier consistency result that either proves an approved owner or blocks Phase 4 without changing Phase 3 intent.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-package/04-CONTEXT.md
@.planning/phases/04-package/04-RESEARCH.md
@.planning/phases/04-package/04-PATTERNS.md
@.planning/phases/04-package/04-VALIDATION.md
@.planning/phases/03-build-contract/03-05-SUMMARY.md
@src/build-contract.js
@src/plan-approval.js
@src/persistability.js
@test/openclaw-resource-projection.test.js

<interfaces>
Use `BUILD_CONTRACT_SCHEMA_VERSION`, `OPENCLAW_RESOURCE_KINDS`, and `validateBuildContract` from `src/build-contract.js`.
Use `serializePersistableJson` and `assertPersistable` from `src/persistability.js`.
The approved contract pins OpenClaw 2026.6.11 / revision 29d018f0af5e92ff1c131f08dd9308e6c9e38e59 and may not be silently substituted.
</interfaces>
</context>

## Artifacts this phase produces

| Kind | Name | Contract |
|---|---|---|
| Module | `src/package-contract.js` | Exports `AGENT_PACKAGE_SCHEMA_VERSION`, `validateAgentPackageManifest`, `validatePackageCapabilityLedger` |
| Module | `src/package-carriers.js` | Exports `PackageCarrierError`, `selectPackageCarriers`, `assertApprovedHookCarrierConsistency` |
| Artifact schema | `agentmo.package-manifest.v1` | Target-neutral manifest identity; manifest does not self-index its own digest |
| Test | `test/package-contract.test.js` | Closed schema and real executable member requirements |
| Test | `test/package-carriers.test.js` | Lowest-trust and typed-hook owner/carrier contract |
| Test | `test/phase4-contracts.test.js` | Wave 1 bootstrap for the incremental Phase 4 requirement, authority and evidence-boundary gate |
| CLI | None in this plan | Public generation is added only after this gate is executable |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing package and carrier contracts</name>
  <read_first>
    - src/build-contract.js
    - src/plan-approval.js
    - test/openclaw-resource-projection.test.js
    - .planning/phases/04-package/04-VALIDATION.md
  </read_first>
  <files>test/package-contract.test.js, test/package-carriers.test.js, test/phase4-contracts.test.js</files>
  <behavior>
    - A complete target-neutral manifest, capability ledger and canonical member inventory validate deterministically.
    - A capability declaration without a deterministic canonical byte recipe/content specification is rejected per D-32; the recipe is authority for later materialization, not a claim that implementation files already exist.
    - Workspace/content/skill wins unless an isolated executable requires MCP or a proven in-process/typed-hook need requires native plugin per D-33.
    - Each abstract hook requires exact event mapping, approved owner, canonical recipe path/content/mode/digest closure, version range, permission, timeout, failure semantics, and unsupported behavior.
    - The current Phase 3 contract always fails with a stable carrier-consistency code because its approved bytes identify no bundled hook owner.
    - A passing successor requires Phase 3 reapproval of a package-local native plugin with a complete deterministic canonical byte recipe/content specification and hook mappings.
    - MCP is rejected for the current v1 resource graph rather than emitted as an unused or speculative carrier.
    - Per D-42, inventory members require portable relative path, regular-file type, fixed mode, byte length and SHA-256; extra/missing/duplicate/type/mode/path/digest drift is rejected.
  </behavior>
  <action>Create the two focused Node `node:test` suites and the incremental `test/phase4-contracts.test.js` gate first. Derive fixtures from the actual Phase 3 contract/resource graph, include missing/duplicate resource and carrier escalation negatives, and assert that the current approved contract is rejected because no bundled owner is approved. Encode the only admissible successor as a Phase 3-reapproved package-local native plugin whose authority is a sorted canonical recipe of exact relative paths, regular-file modes, UTF-8 contents and recomputed per-file/recipe digests plus event mappings; do not require a pre-existing implementation file. Assert that the current graph admits no MCP carrier. Add the D-42 canonical inventory contract and independently mutate member relative path, type, mode, byte length and digest plus the complete member set. This enforces D-32, D-33, D-34, D-42 and EVID-05 before implementation.</action>
  <acceptance_criteria>
    - The tests name the approved revision and all four abstract hooks.
    - At least one assertion proves a hook-name declaration without the exact canonical byte recipe/content closure is insufficient; materialized plugin files remain Plan 04-03 output.
    - The only initial failure is missing Phase 4 production exports or the intended consistency behavior.
  </acceptance_criteria>
  <verify>
    <automated>! node --test test/package-contract.test.js test/package-carriers.test.js test/phase4-contracts.test.js</automated>
  </verify>
  <done>The RED suites encode the exact package, least-trust carrier, and hook-owner contracts without reducing or substituting approved intent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the canonical validators and blocker</name>
  <read_first>
    - test/package-contract.test.js
    - test/package-carriers.test.js
    - src/persistability.js
    - src/artifact-registry.js
  </read_first>
  <files>src/package-contract.js, src/package-carriers.js</files>
  <behavior>
    - Closed validators return bounded errors for unknown fields, missing members, invalid carrier ownership, and lifecycle promotion.
    - Carrier selection is deterministic and records necessity/trust rationale for MCP/native plugin.
    - The current inconsistent hook contract stops with a stable code and no filesystem/process side effect.
    - No bundled-owner inference and no MCP surface can be synthesized from unapproved facts.
  </behavior>
  <action>Implement kebab-case ESM modules with named exports, exact-key validators, sorted unique resource/capability inventories, a closed D-42 member inventory, and immutable results. Use the admitted build contract as the only source of required capabilities. Preserve D-34 target-neutral canonical semantics while allowing an OpenClaw mapping field. Treat the current approved bundled lane as unresolved because its bytes do not identify an owner. A package-local native plugin is admissible only when a new Phase 3 approval binds its complete deterministic canonical recipe/content closure, mappings, permissions, timeout and failure semantics. Reject caller-supplied or pre-existing plugin implementation paths at this gate, reject MCP for the current resource graph, and leave recipe-to-byte materialization exclusively to Plan 04-03. Any unresolved carrier or incomplete member closure stops Produce and directs the operator to Phase 3 reapproval without writing package state.</action>
  <acceptance_criteria>
    - Validators are deterministic and value-blind.
    - No caller-supplied success flag can bypass hook ownership checks.
    - Native plugin selection requires a newly approved Phase 3 contract binding; no dependency is installed.
    - The Wave 1 focused command creates and runs `test/phase4-contracts.test.js` before any later plan relies on it.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/package-contract.test.js test/package-carriers.test.js test/phase4-contracts.test.js</automated>
  </verify>
  <done>The package model exists and the carrier gate honestly proves an approved owner or fails closed before generation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Phase 3 contract → Produce compiler | Exact approved intent crosses into executable materialization |
| Capability declaration → carrier bytes | Metadata must not impersonate executable ownership |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01 | Tampering | package member paths and inventory | high | mitigate | Closed D-42 member entries and exact set equality reject traversal, links/devices, mode/type drift, digest drift, and case/Unicode collisions in `package-contract.js`. |
| T-04-02 | Elevation of Privilege | plugin/MCP carrier choice | high | mitigate | `package-carriers.js` rejects the unowned bundled lane, requires a Phase 3-approved canonical native-plugin recipe/content closure, rejects pre-existing implementation inputs, and rejects MCP for the current resource graph. |
| T-04-03 | Information Disclosure | manifest/capability ledger | high | mitigate | Shared persistability audit rejects secret/raw-state fields before any later writer can consume the candidate. |
| T-04-04 | Spoofing | package certification fields | medium | mitigate | Closed schema fixes runtime/domain/production certification to false and leaves Phase 5 evidence external. |
</threat_model>

<verification>
Wave 1 gate: `node --test test/package-contract.test.js test/package-carriers.test.js test/phase4-contracts.test.js`.
</verification>

<success_criteria>
- The current approved contract receives a reproducible carrier-consistency result.
- No package-generation path can treat declaration-only hooks/tools as executable resources.
- All high-severity threats in this plan have implemented blocking mitigations.
</success_criteria>

<output>
Create `.planning/phases/04-package/04-01-SUMMARY.md` when done.
</output>
