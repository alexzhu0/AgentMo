---
phase: 04-package
plan: 17
type: execute
wave: 14
depends_on:
  - 04-13
files_modified:
  - src/package-produce.js
  - src/openclaw-target-admission.js
  - src/openclaw-target-descriptor.js
  - src/targets/openclaw-package.js
  - test/package-produce.test.js
  - test/openclaw-target-admission.test.js
  - test/openclaw-target-descriptor.test.js
  - test/openclaw-package.test.js
  - .planning/ROADMAP.md
autonomous: true
gap_closure: true
requirements:
  - PACK-03
  - PACK-04
  - OCLW-04
  - EVID-05
must_haves:
  truths:
    - "package directory、archive、target descriptor 与 admission 的 post-publication failure 永不按当前 pathname 清理；替换对象保持逐字节不变，已发布 owned artifact 被 itemize 为 preserved orphan/incomplete。"
    - "OpenClaw recipe resources 保留 canonical nested relative paths；不同目录同 basename 不碰撞、不扁平化，inspect/inventory/archive closure 仍确定。"
    - "ROADMAP Phase 4 Goal 使用 canonical `As a ... I want to ... so that ...` 元数据，语义与现有 Phase 4 scope 完全等价。"
  artifacts:
    - path: src/package-produce.js
      provides: "identity-safe create-only publication failure semantics"
    - path: src/openclaw-target-admission.js
      provides: "descriptor/admission partial-publication preservation"
    - path: src/targets/openclaw-package.js
      provides: "nested recipe relative-path preservation"
    - path: .planning/ROADMAP.md
      provides: "canonical Phase 4 MVP user-story goal metadata"
  key_links:
    - from: src/package-produce.js
      to: src/openclaw-safe-fs.js
      via: "publish-no-replace and exact identity observation; no cleanup by reopened pathname"
    - from: src/targets/openclaw-package.js
      to: package manifest/inventory
      via: "full normalized recipe relativePath remains the member path and collision key"
    - from: .planning/ROADMAP.md
      to: .planning/phases/04-package/04-12-PLAN.md
      via: "same canonical user story is used by every gap plan"
---

## Phase Goal

**As a** developer, **I want to** deterministically generate an offline-inspectable least-trust Agent Package from an approved build contract and safely install it into OpenClaw after probe, preview, and exact-plan approval, **so that** I can authorize the exact package, target, and effects before any mutation.

<objective>
关闭 root gap 6，并修复 CR-10、WR-03，同时完成 MVP mode 要求的 Phase 4 Goal 元数据归一化：移除 publisher 的 pathname cleanup，保留 nested recipe paths。

Purpose: 防止 post-publication catch 删除替换后的第三方对象，恢复 PACK-03/04 的结构语义，并让 Phase 4 计划元数据可被 GSD MVP gate 机器识别。
Output: publisher race tests、preserved-orphan failure semantics、nested recipe mapping、ROADMAP 目标 scoped edit。
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-package/04-CONTEXT.md
@.planning/phases/04-package/04-REVIEW.md
@.planning/phases/04-package/04-VERIFICATION.md
@.planning/phases/04-package/04-13-SUMMARY.md
@src/package-produce.js
@src/openclaw-target-admission.js
@src/openclaw-target-descriptor.js
@src/targets/openclaw-package.js
</context>

## Artifacts this phase produces

- package/descriptor/admission publisher 的 preserved-orphan 失败语义。
- replacement-after-publication 与 partial-publication race regressions。
- nested recipe path-preserving projection 与 collision tests。
- canonical Phase 4 MVP user-story Goal line。

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — 固化 post-publication replacement 与 nested-path collision 攻击</name>
  <files>test/package-produce.test.js, test/openclaw-target-admission.test.js, test/openclaw-target-descriptor.test.js, test/openclaw-package.test.js</files>
  <read_first>src/package-produce.js, src/openclaw-target-admission.js, src/openclaw-target-descriptor.js, src/targets/openclaw-package.js, test/package-produce.test.js, test/openclaw-target-admission.test.js, test/openclaw-target-descriptor.test.js, test/openclaw-package.test.js</read_first>
  <behavior>
    - "在 package directory/archive 已发布后、后续步骤失败前用 unknown sentinel 替换当前 pathname，catch 不删除或修改 sentinel。"
    - "target descriptor/admission 在 create/verify/failure 窗口被替换时，替换对象保持不变，错误 itemize expected/observed identity。"
    - "`recipes/a/setup.md` 与 `recipes/b/setup.md` 同时进入 package，保留各自完整 relative path、bytes、mode、digest；不因 basename 碰撞。"
    - "nested traversal、absolute path、normalized collision、case-policy collision 仍 fail closed。"
  </behavior>
  <action>先为 `produceAgentPackage` 的 output directory 与 archive 分别加入 replacement-after-publication hook；为 target descriptor/admission writer 加同类 hook。断言 current replacement identity/bytes 不变，并断言错误中记录已发布 owned artifact 或 unknown replacement 的 preserve disposition。把现有 basename-positive fixture 改为 path-preservation fixture，并加入同 basename 不同目录与 normalization collision。按 D-32/D-34 保留真实 resource closure；按 D-39/D-42 任何无法绑定 exact published identity 的 cleanup 都不得执行。运行 RED，确认现有 `rm/unlink` 和 basename projection 触发失败。</action>
  <acceptance_criteria>
    - "directory、archive、descriptor、admission 四个 publisher 各有 post-publication replacement negative。"
    - "每个 negative 都断言替换对象 identity/bytes 保持，不能只断言 promise rejected。"
    - "nested recipe positive 同时证明 manifest/inventory/member closure 与 deterministic order。"
  </acceptance_criteria>
  <verify>
    <automated>node -e "const {spawnSync}=require('node:child_process');const r=spawnSync(process.execPath,['--test','--test-name-pattern','post-publication replacement|preserved orphan|nested recipe|basename collision','test/package-produce.test.js','test/openclaw-target-admission.test.js','test/openclaw-target-descriptor.test.js','test/openclaw-package.test.js'],{stdio:'ignore'});if(r.status===0)process.exit(1)"</automated>
  </verify>
  <done>CR-10/WR-03 攻击测试 RED，且所有 sentinel fixture 位于隔离临时目录。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — publisher preserve-orphan 与 recipe relative-path 修复</name>
  <files>src/package-produce.js, src/openclaw-target-admission.js, src/openclaw-target-descriptor.js, src/targets/openclaw-package.js, test/package-produce.test.js, test/openclaw-target-admission.test.js, test/openclaw-target-descriptor.test.js, test/openclaw-package.test.js</files>
  <read_first>src/openclaw-safe-fs.js, src/package-produce.js, src/openclaw-target-admission.js, src/openclaw-target-descriptor.js, src/targets/openclaw-package.js, test/package-produce.test.js, test/openclaw-target-admission.test.js, test/openclaw-target-descriptor.test.js, test/openclaw-package.test.js</read_first>
  <behavior>
    - "publisher 在 private temp 完整构建/验证/fsync 后以 safe-fs publish-no-replace 发布；发布后的失败保留 artifact 并返回 structured incomplete/orphan evidence，绝不 reopen current pathname 后 rm/unlink。"
    - "descriptor/admission 同样 temp-complete then publish-no-replace；partial private temp 仅在同一 retained session exact-owned 时可清理，否则 preserve。"
    - "recipe output 使用完整 normalized relativePath；collision key 与 manifest member path 相同。"
  </behavior>
  <action>按 D-32、D-34、D-39、D-40、D-42 改造四个 publisher。package directory/archive 先在 private owned temp 完成并验证，再通过 04-13 safe-fs 原子 no-replace 发布；一旦发布，catch 只能 reobserve 并返回 `preservedPublications`/`recoveryRequired` 的 bounded identity/digest，不能 `rm` 当前 output/archive path。target descriptor/admission 使用相同 temp/publish contract，删除 blind unlink。对 private temp 的 cleanup 只允许同一 session exact-owned identity。`openclaw-package` 从 `basename` 改为完整 normalized relativePath，并以该路径计算 collision、inventory、manifest/archive member；拒绝 traversal/absolute/normalization/case-policy collision。不得改变 PACK-01..05 canonical hashing 规则之外的字节语义。</action>
  <acceptance_criteria>
    - "production publisher catch/finally 中不存在对已发布 current pathname 的 blind `rm`/`unlink`。"
    - "replacement attacks 保持 unknown sentinel，已发布 owned artifact 被保留并 itemize，不伪称 rolled back。"
    - "同 basename nested recipes 均可 inspect，bytes/mode/digest/relative path 与 source manifest 一致。"
  </acceptance_criteria>
  <verify>
    <automated>node --test test/package-produce.test.js test/openclaw-target-admission.test.js test/openclaw-target-descriptor.test.js test/openclaw-package.test.js</automated>
  </verify>
  <done>CR-10、WR-03 转绿；publisher 不再按 reopened pathname cleanup，recipe 不再扁平化。</done>
</task>

<task type="auto">
  <name>Task 3: 归一化 Phase 4 MVP Goal 与未完成状态 metadata</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>.planning/ROADMAP.md, .planning/phases/04-package/04-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <action>ROADMAP 只允许两处 scoped metadata normalization。第一处：`## Overview` 的 Phase 4 条目必须保持未勾选、无 completed date，并明确 Needs Review/gaps_found 直到 canonical post gate pass。第二处：`### Phase 4` 下 `**Goal:**` 必须是 canonical 单行 user story：`As a developer, I want to deterministically generate an offline-inspectable least-trust Agent Package from an approved build contract and safely install it into OpenClaw after probe, preview, and exact-plan approval, so that I can authorize the exact package, target, and effects before any mutation.` 不改 requirements、success criteria、plan inventory、其他 phase 或 D-32..D-42 scope；不把 Phase 5 run/schedule/restart/eval/Birth/Delivery 引入目标。只有 execute-phase 在 04-19 SUMMARY 后运行的 canonical review/verifier post gate 通过，后续 completion transition 才可勾选 Overview 或写 completed date；本任务不得完成 Phase 4。</action>
  <acceptance_criteria>
    - "Phase 4 Goal 精确包含 `As a`、`I want to`、`so that` 三段。"
    - "Overview Phase 4 保持 `[ ]`、无 completed date，并包含 Needs Review/gaps_found。"
    - "Goal 仍覆盖 approved build contract、deterministic/offline-inspectable least-trust package、probe/preview/exact approval、safe OpenClaw mutation。"
    - "除 Overview Phase 4 status 与 Phase 4 Goal 两处 scoped metadata 外，不修改 ROADMAP 其他语义。"
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('node:fs');const s=fs.readFileSync('.planning/ROADMAP.md','utf8');const overview=s.slice(s.indexOf('## Phases'),s.indexOf('## Phase Details'));const line=overview.match(/^- \\[[ x]\\] \\*\\*Phase 4:[^\\n]+$/m)?.[0]||'';const p=s.slice(s.indexOf('### Phase 4:'),s.indexOf('### Phase 5:'));const g=p.match(/^\\*\\*Goal:\\*\\* (.+)$/m)?.[1]||'';if(!line.startsWith('- [ ]')||/completed\\s+\\d/i.test(line)||!/Needs Review \\/ gaps_found/.test(line)||!/^As a .+, I want to .+, so that .+\\.$/.test(g))process.exit(1)"</automated>
  </verify>
  <done>Phase 4 Goal canonical，Overview 与 detail status 一致保持未完成；canonical post gate 前无 completion claim。</done>
</task>

</tasks>

## Gap / Review Coverage

| Root gap | Findings | Attack path | Target invariant | Tasks |
|---|---|---|---|---|
| 6 publisher cleanup | CR-10 | 发布后替换 current pathname，catch 按 path 删除 | 发布后只 reobserve/preserve/itemize，不 cleanup reopened name | 1, 2 |
| 6 recipe projection | WR-03 | basename flattening 覆盖同名 nested recipes | full normalized relativePath 是唯一 member/collision key | 1, 2 |
| MVP metadata | ROADMAP Goal/status inconsistent | GSD MVP gate 无法解析 user story或 Overview 提前完成 | canonical Goal + Needs Review/gaps_found；post gate 前不勾选 | 3 |

## Multi-Source Coverage Audit

| Source | Items covered | Disposition |
|---|---|---|
| GOAL | deterministic offline package and safe mutation | COVERED; metadata canonicalized without scope change |
| REQ | PACK-03, PACK-04, OCLW-04, EVID-05 | COVERED |
| RESEARCH | create-only publication、identity-bound cleanup、preserve nested resources | COVERED |
| CONTEXT | D-32, D-34, D-39, D-40, D-42 | COVERED |
| DEFERRED | Phase 5 runtime/Birth/Delivery work | EXCLUDED |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| private temp → published package/authority path | create-only artifact becomes externally visible |
| failure handler → current namespace | an attacker may replace a published pathname before cleanup |
| recipe source path → package member path | nested untrusted relative paths enter deterministic archive |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-G17-01 | Tampering | post-publication cleanup | critical | mitigate | Task 2 removes reopened-path cleanup and preserves/itemizes published artifacts |
| T-04-G17-02 | Tampering | descriptor/admission partial cleanup | high | mitigate | Task 2 temp-complete/publish-no-replace; cleanup only exact private session object |
| T-04-G17-03 | Tampering | nested recipe collision/traversal | high | mitigate | Tasks 1-2 normalize full relative paths and reject traversal/collision |
| T-04-G17-04 | Repudiation | orphaned publication | medium | mitigate | Task 2 returns stable identity/digest/disposition and never claims rollback |
| T-04-G17-05 | Tampering | ROADMAP scope/status expansion | medium | mitigate | Task 3 limits edits to Overview Phase 4 status and Phase 4 Goal; completion remains post-gate-only |
| T-04-G17-SC | Tampering | external package install | low | accept | 本计划不引入 package-manager install 或第三方依赖 |
</threat_model>

<verification>
- `node --test test/package-produce.test.js test/openclaw-target-admission.test.js test/openclaw-target-descriptor.test.js test/openclaw-package.test.js`
- `npm run check`
- `git diff --check`
</verification>

<success_criteria>
- CR-10、WR-03 全部关闭。
- publisher replacement attacks 不再删除或修改 unknown replacements。
- nested recipe closure 与 PACK-03/04 deterministic inspect 保持。
- Phase 4 ROADMAP Goal 符合 MVP user-story template，Overview 保持未完成且 completion 仅由 canonical post gate 转换。
</success_criteria>

<output>
Create `.planning/phases/04-package/04-17-SUMMARY.md` when done.
</output>
