---
phase: 04-package
plan: 16
type: execute
wave: 16
depends_on:
  - 04-15
files_modified:
  - src/openclaw-install-receipt.js
  - src/openclaw-install-transaction.js
  - src/artifact-contract.js
  - src/artifact-registry.js
  - src/cli.js
  - test/openclaw-install-transaction.test.js
  - test/artifact-admission.test.js
  - test/artifact-contract.test.js
  - test/phase4-contracts.test.js
autonomous: true
gap_closure: true
requirements:
  - OCLW-03
  - OCLW-04
  - EVID-05
must_haves:
  truths:
    - "`complete` 只在每个 planned managed operation 与 sensitive/external action 都有唯一成功 result、所有 approvals/nonces 已 exact consumed、post-state 匹配且 preserved/recovery 集合为空时成立。"
    - "receipt itemize exact plan/archive/target/probe/journal、三类 approval/nonce consumption、每个 before/after effect、external dependency/action result、rollback/preservation disposition；删除/重复/替换任一项都 admission fail closed。"
    - "任一 failed/unsupported/preserved/ambiguous/recovery-required 或证据缺口只能产生 `incomplete`，并给出 bounded、可机器判断且不含秘密的 reason。"
  artifacts:
    - path: src/openclaw-install-receipt.js
      provides: "closed exact-authority/effect-ledger receipt schema and strict completion theorem"
    - path: src/openclaw-install-transaction.js
      provides: "receipt-last aggregation of all managed/external/authority results"
    - path: test/artifact-admission.test.js
      provides: "false-complete, missing/duplicate/substitution authority/result attacks"
    - path: test/openclaw-install-transaction.test.js
      provides: "all lifecycle receipt completeness and incomplete recovery evidence"
  key_links:
    - from: src/openclaw-install-transaction.js
      to: src/openclaw-install-receipt.js
      via: "transaction passes plan, approvals, durable ledger, managed/external results and recovery observations to one strict builder"
    - from: src/openclaw-install-receipt.js
      to: src/artifact-registry.js
      via: "admission recomputes one-to-one closure against exact plan/decision/ledger companions"
    - from: src/cli.js
      to: src/openclaw-install-receipt.js
      via: "stable human summary and JSON expose the same closed receipt data"
---

## Phase Goal

**As a** developer, **I want to** deterministically generate an offline-inspectable least-trust Agent Package from an approved build contract and safely install it into OpenClaw after probe, preview, and exact-plan approval, **so that** I can authorize the exact package, target, and effects before any mutation.

<objective>
关闭 root gap 4，并修复 CR-07、CR-08：把 receipt 从宽松 outcome 摘要提升为 exact authority/result ledger，建立可验证的 `complete` 充分必要条件。

Purpose: 防止 preserved、missing external result、approval/nonce substitution 或少项结果被包装成成功，并让 OCLW-03/EVID-05 的每个 effect 可审计。
Output: closed receipt schema、strict admission、transaction receipt-last wiring、稳定 human/JSON 输出与 false-complete adversarial tests。
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-package/04-CONTEXT.md
@.planning/phases/04-package/04-REVIEW.md
@.planning/phases/04-package/04-VERIFICATION.md
@.planning/phases/04-package/04-15-SUMMARY.md
@src/openclaw-install-receipt.js
@src/openclaw-install-transaction.js
@src/artifact-contract.js
@src/artifact-registry.js
</context>

## Artifacts this phase produces

- exact authority/result receipt ledger 与严格 completion theorem。
- receipt admission 的 companion-backed one-to-one closure。
- false-complete、少项/重复/替换/reorder、external result 丢失攻击测试。
- 人类摘要与 `--json` 一致的稳定 evidence surface。

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — 构造 false-complete 与 authority/result substitution 矩阵</name>
  <files>test/openclaw-install-transaction.test.js, test/artifact-admission.test.js, test/artifact-contract.test.js, test/phase4-contracts.test.js</files>
  <read_first>src/openclaw-install-receipt.js, src/openclaw-install-transaction.js, src/artifact-contract.js, src/artifact-registry.js, test/openclaw-install-transaction.test.js, test/artifact-admission.test.js, test/artifact-contract.test.js, test/phase4-contracts.test.js</read_first>
  <behavior>
    - "complete receipt 含 preserved asset、unsupported/failed operation、recovery item、未执行 sensitive action、missing external result、未 consumed nonce 时均被 producer/admission 拒绝。"
    - "删除、重复、替换、重排 operation/action/approval/nonce/result，或把另一个 plan/target/probe/journal 的条目移入，均被 one-to-one closure 拒绝。"
    - "incomplete receipt 必须逐项记录未完成原因、已执行结果与 durable nonce reservation，且不能用缺失数组掩盖失败。"
    - "receipt JSON/human surface 不包含 credentials、raw stdout/stderr/provider payload。"
  </behavior>
  <action>先以 04-15 四 lifecycle fixture 为 source-of-truth，生成合法 companions 后逐一深拷贝攻击 receipt：preserved+complete、drop externalResults、drop ordinary/conflict/sensitive approval、swap nonce、duplicate result、wrong action digest、wrong plan/target/probe/journal、complete recovery-required。每个 mutation 都重算顶层 digest，证明 validator 不能只依赖 self hash。按 D-36/D-37 覆盖三类 approval 与 whole conflict set；按 D-38 检查 secret/raw-output forbidden keys；按 D-40 同时检查 human/JSON 的稳定 itemization。运行 RED 并确认当前宽松 receipt schema/builder 接受至少这些攻击。</action>
  <acceptance_criteria>
    - "CR-07 至少覆盖 preserved、failed、unsupported、recovery、missing external result 五类 false complete。"
    - "CR-08 覆盖 plan、三类 approval、nonce ledger、managed result、external result 五种 authority substitution。"
    - "每个攻击在重新计算 receipt digest 后仍被预期拒绝。"
  </acceptance_criteria>
  <verify>
    <automated>node -e "const {spawnSync}=require('node:child_process');const r=spawnSync(process.execPath,['--test','--test-name-pattern','false complete|receipt substitution|missing external|nonce ledger|receipt secret','test/openclaw-install-transaction.test.js','test/artifact-admission.test.js','test/artifact-contract.test.js','test/phase4-contracts.test.js'],{stdio:'ignore'});if(r.status===0)process.exit(1)"</automated>
  </verify>
  <done>CR-07/08 攻击矩阵 RED，且失败不是因为未重算 identity/digest。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — 定义 closed receipt ledger 与 strict completion theorem</name>
  <files>src/openclaw-install-receipt.js, src/artifact-contract.js, src/artifact-registry.js, test/artifact-admission.test.js, test/artifact-contract.test.js</files>
  <read_first>src/openclaw-install-receipt.js, src/artifact-contract.js, src/artifact-registry.js, src/openclaw-install-plan.js, src/openclaw-install-approval.js, src/openclaw-authority-consumption.js, test/artifact-admission.test.js, test/artifact-contract.test.js</read_first>
  <behavior>
    - "receipt ledger 绑定 exact external plan file/digest、archive/target/probe/journal/attempt，以及 ordinary/conflict/sensitive decision identities 与 durable consumption entry/head digests。"
    - "managedResults 与 plan operations、externalResults 与 sensitiveActions 按稳定 id/digest 一对一；每项含 before/after identity/digest、owner/marker、disposition、rollback/preserve result。"
    - "`complete` 的 closed validator 同时验证 cardinality、唯一性、全部 applied/succeeded、nonce consumed、post-state exact、preserved/recovery empty。"
  </behavior>
  <action>按 D-36、D-37、D-38、D-39、D-40、D-41、D-42 重写 receipt contract：加入 `authorityLedger`（plan/archive/target/probe/journal/attempt）、`approvals` 三族、`nonceConsumption`、`managedResults`、`externalResults`、`preservedAssets`、`recovery` 的 closed keys。每个 external dependency/action 项都满足 EVID-05 的 owner/version/digest/permission/approval/failure/unsupported 字段。validator 通过真实 plan/decisions/ledger companions 重算 set/cardinality，不接受 receipt 自己提供 expected list。`status=complete` 必须满足所有 result success、无 preserve/recovery、所有 nonce consumed；其他任何组合只允许 `incomplete` 并要求 bounded reasons。禁止 credential value、raw child output 与自认证 companion。</action>
  <acceptance_criteria>
    - "complete validator 是 exact plan/authority/result closure 的充分必要检查，不依赖 outcome 字符串。"
    - "EVID-05 每个 tool/hook/plugin/effect/external dependency 都有 owner/version/digest/permission/approval/failure/unsupported 字段或显式 `not_applicable` reason。"
    - "三类 approvals/nonces 与 operation/action results 均 stable-sort、unique、one-to-one。"
  </acceptance_criteria>
  <verify>
    <automated>node --test test/artifact-admission.test.js test/artifact-contract.test.js</automated>
  </verify>
  <done>closed receipt schema/admission 完成，所有 self-hashed false-complete/substitution attacks 被拒绝。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — transaction receipt-last 聚合与 stable human/JSON evidence</name>
  <files>src/openclaw-install-transaction.js, src/openclaw-install-receipt.js, src/cli.js, test/openclaw-install-transaction.test.js, test/phase4-contracts.test.js</files>
  <read_first>src/openclaw-install-transaction.js, src/openclaw-install-receipt.js, src/openclaw-authority-consumption.js, src/openclaw-official-action-runner.js, src/cli.js, test/openclaw-install-transaction.test.js, test/phase4-contracts.test.js</read_first>
  <behavior>
    - "transaction 保留所有 managed/external results，并在 postobservations、durable nonce finalization 与 recovery disposition 完成后最后写 receipt。"
    - "成功与失败路径都 itemize 已尝试/未尝试 actions、decision/nonce、before/after、reason；失败不能 drop externalResults。"
    - "human summary 与 JSON 来源相同，稳定顺序显示 target/path/before/after/authority/action/preserve/recovery，不展示秘密。"
  </behavior>
  <action>修复 `buildReceipt` 调用遗漏 `externalResults` 的数据流，并让 transaction 从 durable reservation 到每个 effect/result/postobserve/nonce final state 累积一个 closed execution ledger。只有 ledger 经 strict builder 验证为 complete 后才能原子发布 complete receipt；中途错误也必须用已知 ledger 生成 admitted incomplete receipt，若 receipt 自身无法安全发布则返回 recovery-required 且绝不声称成功。CLI `openclaw-install-apply` 的 human 与 `--json` 输出都读取同一 receipt，按 D-40 固定排序与字段；按 D-38 redaction 所有 child/provider/credential raw data。</action>
  <acceptance_criteria>
    - "每个 04-15 lifecycle 的 successful receipt 都与 exact plan operations/sensitiveActions/approvals/nonces 一对一。"
    - "每个 injected failure 的 incomplete receipt 保留此前 external/managed results、reservation 与 recovery state。"
    - "human/JSON 对相同 receipt 的 digest、status、result counts、preserve/recovery counts 一致。"
  </acceptance_criteria>
  <verify>
    <automated>node --test test/openclaw-install-transaction.test.js test/phase4-contracts.test.js test/artifact-admission.test.js test/artifact-contract.test.js</automated>
  </verify>
  <done>CR-07/08 全部转绿；receipt 对每个 authority/effect/result 完整 itemize，false complete 不可生产或 admission。</done>
</task>

</tasks>

## Gap / Review Coverage

| Root gap | Findings | Attack path | Target invariant | Tasks |
|---|---|---|---|---|
| 4 strict complete | CR-07 | preserved/unsupported/missing external result 仍标 complete | complete iff exact results 全成功、nonce consumed、无 preserve/recovery | 1, 2, 3 |
| 4 authority/result ledger | CR-08 | receipt 丢 plan/approvals/nonces/externalResults 或可替换 | companion-backed one-to-one ledger，任何少项/重复/替换拒绝 | 1, 2, 3 |

## Multi-Source Coverage Audit

| Source | Items covered | Disposition |
|---|---|---|
| GOAL | exact package/target/effects 可在 mutation 后核验 | COVERED by receipt ledger |
| REQ | OCLW-03, OCLW-04, EVID-05 | COVERED |
| RESEARCH | receipt-last、itemized before/after、preserve/recovery、external deps | COVERED |
| CONTEXT | D-36, D-37, D-38, D-39, D-40, D-41, D-42 | COVERED |
| DEFERRED | runtime/domain/Birth/Delivery certification | EXCLUDED; receipt explicitly disclaims |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| execution ledger → receipt producer | mutable runtime results become durable evidence |
| receipt + companions → artifact admission | potentially self-consistent JSON claims completion |
| receipt → human/JSON output | evidence crosses operator and automation surfaces |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-G16-01 | Spoofing | receipt complete status | critical | mitigate | Task 2 derives completion from exact companion/result theorem |
| T-04-G16-02 | Tampering | authority/result substitution | high | mitigate | Tasks 1-2 one-to-one set/cardinality/digest validation against external companions |
| T-04-G16-03 | Repudiation | dropped external/failed result | high | mitigate | Task 3 receipt-last closed execution ledger retains every attempted/unattempted action |
| T-04-G16-04 | Information Disclosure | credential/raw process data | high | mitigate | Tasks 2-3 closed value-blind fields and redacted bounded reasons |
| T-04-G16-05 | Tampering | receipt publication failure | medium | mitigate | Task 3 cannot claim complete until exact receipt publication/admission succeeds |
| T-04-G16-SC | Tampering | external package install | low | accept | 本计划不新增 package-manager install 或第三方依赖 |
</threat_model>

<verification>
- `node --test test/openclaw-install-transaction.test.js test/artifact-admission.test.js test/artifact-contract.test.js test/phase4-contracts.test.js`
- `npm run check`
- `git diff --check`
</verification>

<success_criteria>
- CR-07、CR-08 的全部攻击矩阵通过。
- complete/incomplete 状态不能由 caller/outcome 字符串自报。
- OCLW-03 与 EVID-05 的 itemized authority/effect/result closure 可从外部 companions 重算。
</success_criteria>

<output>
Create `.planning/phases/04-package/04-16-SUMMARY.md` when done.
</output>
