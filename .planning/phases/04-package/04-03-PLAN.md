---
phase: 04-package
plan: "03"
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/package-produce.js
  - src/package-archive.js
  - src/targets/openclaw-package.js
  - src/artifact-subjects.js
  - src/cli.js
  - test/package-produce.test.js
  - test/package-determinism.test.js
  - test/openclaw-package.test.js
  - test/phase4-contracts.test.js
autonomous: true
requirements:
  - PACK-01
  - PACK-02
  - PACK-03
  - PACK-04
must_haves:
  truths:
    - "A fresh process can re-admit the exact Phase 3 inputs and materialize a complete executable package rather than declarations."
    - "Produce also re-admits the exact target/carrier admission artifact selected in Plan 04-02; missing or stale admission bytes block generation."
    - "Two builds under different roots, clocks and locale settings have identical logical members, per-file digests, manifest bytes and archive bytes."
    - "The canonical package is target-neutral while its OpenClaw projection is complete and directly installable."
    - "The package contains package-local native hook plugin bytes generated solely from the Phase 3-approved canonical recipe/content specification, with every generated digest recomputed and matched, and no MCP server/config/activation surface."
    - "Per D-42, the canonical directory remains deterministic build authority while the emitted deterministic archive is the only preview/approval/apply transport and is externally SHA-256 bound to its internal manifest digest and complete member inventory."
  artifacts:
    - path: "src/package-produce.js"
      provides: "Exact approved inputs to canonical package directory"
      exports: ["PackageProduceError", "produceAgentPackage"]
    - path: "src/package-archive.js"
      provides: "Deterministic transport archive encoder, retained reader and complete archive/manifest/inventory closure"
      exports: ["PackageArchiveError", "buildPackageArchive", "readPackageArchiveInventory"]
    - path: "src/targets/openclaw-package.js"
      provides: "Canonical package to OpenClaw-native projection"
      exports: ["buildOpenClawPackageProjection"]
  key_links:
    - from: "src/package-produce.js"
      to: "src/package-carriers.js"
      via: "exact target/carrier admission revalidation before staging any output"
      pattern: "assertApprovedHookCarrierConsistency"
    - from: "src/package-produce.js"
      to: "src/targets/openclaw-package.js"
      via: "target-neutral resource graph projection"
      pattern: "buildOpenClawPackageProjection"
    - from: "src/cli.js"
      to: "src/package-produce.js"
      via: "`package-produce` public command with exact digest bindings"
      pattern: "package-produce"
    - from: "src/package-archive.js"
      to: "src/package-contract.js"
      via: "D-42 archive SHA-256, internal manifest digest, canonical inventory digest and full member closure"
      pattern: "readPackageArchiveInventory"
---

## Phase Goal

**As a** developer, **I want to** deterministically generate an offline-inspectable, least-trust Agent Package from the approved build contract, **so that** I can separately authorize a safe OpenClaw installation.

<objective>
Deliver the first real user slice: exact approved inputs become a complete, deterministic package directory and transport archive.

Purpose: Close the declaration-only POC failure while preserving target-neutral canonical authority.
Output: Package compiler, OpenClaw native projection, deterministic archive and `package-produce` CLI.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-package/04-02-SUMMARY.md
@.planning/phases/03-build-contract/03-05-SUMMARY.md
@src/package-contract.js
@src/package-carriers.js
@src/artifact-admission.js
@src/persistability.js
@src/targets/operations.js
@src/cli.js

<interfaces>
`loadAdmittedArtifact` hashes the retained raw Buffer before decode and returns authentic branded admission.
`fileMapToWriteOperations(files, context, source)` provides deterministic target operation metadata but its legacy `scaffoldOnly` semantics must not certify Phase 4 materialization.
`produceAgentPackage(options)` must require exact blueprint, design-plan, discovery-approval, decision-ledger, recipe-bearing build-contract, plan-approval, and `openclaw-target-carrier-admission` inputs plus an absent output root and absent deterministic archive path. It accepts no native-plugin source path: it renders exact files only from the admitted recipe and recomputes every recipe/member digest before staging.
The archive is emitted from the committed canonical directory, externally SHA-256 reported, and internally binds the manifest digest plus every member `{relativePath,type,mode,byteLength,sha256}` and canonical inventory digest. The directory is build authority; later preview/approval/apply accept only this archive transport per D-42.
</interfaces>
</context>

## Artifacts this phase produces

| Kind | Name | Contract |
|---|---|---|
| Module | `src/package-produce.js` | Exports `PackageProduceError`, `produceAgentPackage`; re-admits subject `openclaw-target-carrier-admission` |
| Module | `src/package-archive.js` | Exports `PackageArchiveError`, `buildPackageArchive`, `readPackageArchiveInventory`; validates archive external digest plus manifest/inventory/member closure |
| Adapter | `src/targets/openclaw-package.js` | Exports `buildOpenClawPackageProjection` |
| Command subjects | `src/artifact-subjects.js` | Extends `DURABLE_COMMAND_SUBJECTS["package-produce"]` with every exact Phase 3 subject and `openclaw-target-carrier-admission` |
| CLI | `src/cli.js` | Adds `package-produce` with exact `--digest` bindings, `--target-carrier-admission`, `--out <absent-dir>`, required `--archive <absent-file>`, `--json`; reports archive SHA-256, manifest digest and inventory digest |
| Test | `test/package-produce.test.js` | Complete executable member, exact admission and zero-write failure journey |
| Test | `test/package-determinism.test.js` | Dual-root/clock/locale/archive equality |
| Test | `test/openclaw-package.test.js` | Complete native projection, recipe-derived plugin bytes and no-MCP boundary |
| Incremental test | `test/phase4-contracts.test.js` | Wave 3 package-generation and Phase 4/5 boundary extension |
| Generated artifact | `agentmo.package.json` plus package tree | Identity `agentmo.package-manifest.v1`; real `resources/**` and `projections/openclaw/**` members |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the failing end-to-end package build</name>
  <read_first>
    - src/package-contract.js
    - src/package-carriers.js
    - src/build-contract.js
    - test/openclaw-resource-projection.test.js
  </read_first>
  <files>test/package-produce.test.js, test/package-determinism.test.js, test/openclaw-package.test.js, test/phase4-contracts.test.js</files>
  <behavior>
    - Exact approved support-triage inputs materialize all D-32 resources and a complete OpenClaw workspace/skill/config/runtime projection.
    - Any input-byte, approval, carrier, path, duplicate, extra-member or persistability mutation leaves the absent output root absent.
    - Missing/stale/forged target-carrier admission leaves both package and archive outputs absent.
    - Any canonical recipe path/order/mode/content/member-digest/recipe-digest drift or any attempt to supply plugin source bytes leaves both outputs absent.
    - Canonical paths reject absolute segments, traversal, backslash, NUL, symlink/hardlink/device, and case/Unicode-normalization collisions.
    - Repeated builds and archives are byte-identical and contain no wall clock, host, temp-root, random, uid/gid or locale dependency.
    - Archive inspection binds the external SHA-256 to the internal manifest digest, canonical inventory digest and full member closure; archive-byte, member, set, type, mode and manifest mismatch fail closed.
  </behavior>
  <action>Write the three focused RED suites and extend the incremental Phase 4 gate around the public service contract and a fresh CLI subprocess. Assert real prompt, skill, tool binding, native hook, memory policy, eval and OpenClaw workspace bytes per D-32; for every native-plugin member, compute expected bytes solely from the exact recipe content admitted in Plan 04-02 and compare byte-for-byte plus mode and SHA-256. Reject any plugin-source/path option and mutate recipe path/order/mode/content/member digest/recipe digest independently before output. Re-admit the exact Plan 04-02 artifact and prove its absence or digest drift blocks before output. Add D-42 golden vectors for external archive SHA-256, internal manifest digest, canonical inventory digest and every member path/type/mode/length/digest. Mutate archive bytes, one member, the member set, type, mode and manifest binding independently. Assert there is no MCP server, config, registration or activation member. Use only sanitized fixtures and exact Phase 3 admission helpers.</action>
  <acceptance_criteria>
    - The happy path is a single exact input-to-package user journey.
    - The archive is rebuilt from the canonical directory and compared byte-for-byte, including the complete closure returned by `readPackageArchiveInventory`.
    - Failure cases assert zero committed package/archive output.
  </acceptance_criteria>
  <verify>
    <automated>! node --test test/package-produce.test.js test/package-determinism.test.js test/openclaw-package.test.js test/phase4-contracts.test.js</automated>
  </verify>
  <done>The RED tests describe a complete executable and deterministic package, not a horizontal file-format exercise.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic Produce and the public command</name>
  <read_first>
    - test/package-produce.test.js
    - test/package-determinism.test.js
    - test/openclaw-package.test.js
    - src/builder-package.js
    - src/persistability.js
    - src/cli.js
  </read_first>
  <files>src/package-produce.js, src/package-archive.js, src/targets/openclaw-package.js, src/artifact-subjects.js, src/cli.js</files>
  <behavior>
    - Produce performs all exact admissions and full-candidate validation before creating the output root.
    - Staging uses a private absent directory, no-follow member reads, fixed path/mode/order/metadata, then one directory-level commit.
    - OpenClaw output includes native workspace documents, workspace skills, tool/policy bindings, package-local native hook files generated solely from the approved canonical recipe, memory policy/files, evals, permission/evidence files, schedule proposals and credential proposal.
  </behavior>
  <action>Implement the compiler and deterministic transport using Node built-ins only. Re-admit the recipe-bearing build contract, its Phase 3 plan approval and the Plan 04-02 target/carrier admission; recompute the canonical recipe digest and require equality across both authorities. Materialize each native-plugin file directly from the recipe's exact normalized UTF-8 content and fixed mode, recompute its SHA-256, and accept no source path, caller bytes, template substitution, wall clock, environment or filesystem implementation input. Index every member except the manifest itself with relative path, regular-file type, byte length, fixed mode and SHA-256; bind the manifest digest outside the manifest to avoid a cycle. Encode stable member order and metadata, then produce the required archive from the canonical committed directory and report its exact external SHA-256, manifest digest and canonical inventory digest. `readPackageArchiveInventory` must no-follow retain the archive identity and verify the complete D-42 closure. Add the `package-produce` command and its exact subject mapping, including the exact target/carrier admission artifact path and digest. Keep schedule files proposal-only and credential files value-blind. Reject absent/stale admission, the old bundled-owner contract, the mismatched installed target, any unsafe/incomplete archive member and every MCP member before committed output.</action>
  <acceptance_criteria>
    - `package-produce` works from a fresh process using only exact named artifacts.
    - Canonical directory authority and archive reconstruction produce identical complete inventories, while the archive is explicitly marked as the sole later install transport per D-42.
    - No live OpenClaw process, plugin code, MCP server, schedule, credential route, model, agent task or Phase 5 evidence runs.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/package-produce.test.js test/package-determinism.test.js test/openclaw-package.test.js test/phase4-contracts.test.js</automated>
  </verify>
  <done>A developer can generate a complete deterministic package and archive from the exact approved contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Exact artifacts → compiler | Untrusted files become executable package bytes |
| Canonical package → OpenClaw projection | Target-specific paths/carriers are derived without becoming canonical authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-08 | Tampering | package paths/archive | high | mitigate | Normalize portable paths, reject link/device/case-Unicode collisions, fix metadata, and bind external archive SHA-256 to the manifest/inventory/member closure per D-42. |
| T-04-09 | Elevation of Privilege | plugin/MCP resources | high | mitigate | Materialize plugin bytes only from the exact approved canonical recipe after digest recomputation; accept no local source path and perform no auto-install, runtime load, force, link, process or network action. |
| T-04-10 | Information Disclosure | package members | high | mitigate | Re-audit full candidate and serialized bytes through persistability before output creation. |
| T-04-11 | Repudiation | build result | medium | mitigate | Manifest binds exact source digests and per-member digests while all runtime/domain/production flags remain false. |
</threat_model>

<verification>
Wave 3 gate: `node --test test/package-contract.test.js test/package-carriers.test.js test/openclaw-build-contract.test.js test/openclaw-target-admission.test.js test/package-produce.test.js test/package-determinism.test.js test/openclaw-package.test.js test/phase4-contracts.test.js`.
</verification>

<success_criteria>
- The canonical directory contains real executable resources for every declared capability.
- Two isolated builds and archives are byte-identical and yield the same archive/manifest/inventory/member closure.
- OpenClaw projection is complete but no OpenClaw runtime behavior is claimed.
</success_criteria>

<output>
Create `.planning/phases/04-package/04-03-SUMMARY.md` when done.
</output>
