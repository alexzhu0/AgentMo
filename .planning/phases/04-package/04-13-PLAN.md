---
phase: 04-package
plan: 13
type: execute
wave: 13
depends_on:
  - 04-12
files_modified:
  - native/openclaw-fs-kernel.c
  - src/openclaw-safe-fs.js
  - src/openclaw-install-transaction.js
  - src/cli.js
  - package.json
  - src/builder-package.js
  - test/openclaw-safe-fs.test.js
  - test/openclaw-install-transaction.test.js
  - test/cli.test.js
autonomous: true
gap_closure: true
requirements:
  - OCLW-02
  - OCLW-04
  - OCLW-05
  - EVID-05
must_haves:
  truths:
    - "所有 AgentMo 自管目标观察、create-only publication、journal 与私有 authority-state I/O 都相对 retained directory descriptors 完成，绝不回落到已验证后的 pathname I/O。"
    - "ancestor/final symlink、directory/file swap、恢复期替换与不安全权限均在写入前拒绝；不确定对象只 preserve，不按 cached boolean/pathname 删除。"
    - "Linux publication 使用 `renameat2(..., RENAME_NOREPLACE)`，Darwin 使用 `renameatx_np(..., RENAME_EXCL)`；等价安全原语仅在同一 retained-dirfd、create-exclusive、不可覆盖语义可被测试证明时准入。"
    - "原生 helper 由仓库源码、固定 `/usr/bin/cc`、closed environment 与固定 argv 构建，并产生 durable closed build receipt；public apply 必须显式接收 helper path、receipt path 与 receipt 外部 digest 后重新准入。"
    - "helper 不允许 PATH lookup、apply-time autobuild、预编译 blob、下载、package lifecycle hook 或 unsupported-platform JS mutation fallback。"
  artifacts:
    - path: native/openclaw-fs-kernel.c
      provides: "openat/fstatat/renameat/unlinkat based retained-dirfd kernel"
    - path: src/openclaw-safe-fs.js
      provides: "durable build-receipt schema/admission、closed bounded protocol 与 JS fail-closed facade"
    - path: test/openclaw-safe-fs.test.js
      provides: "ancestor/final swap、ownership、crash/recovery、platform/build supply-chain attacks"
  key_links:
    - from: src/openclaw-install-transaction.js
      to: src/openclaw-safe-fs.js
      via: "transaction obtains one retained root session and routes observation/journal/create-only effects through it"
    - from: src/openclaw-safe-fs.js
      to: native/openclaw-fs-kernel.c
      via: "explicit helper/receipt/external-digest admission and length-bounded JSON-lines protocol over shell:false private copy"
    - from: src/builder-package.js
      to: native/openclaw-fs-kernel.c
      via: "packed inventory includes auditable source/facade, never a host-built binary"
    - from: src/cli.js
      to: src/openclaw-safe-fs.js
      via: "`openclaw-install-apply` passes explicit helper path, receipt path and externally supplied receipt digest; no lookup or build occurs"
---

## Phase Goal

**As a** developer, **I want to** deterministically generate an offline-inspectable least-trust Agent Package from an approved build contract and safely install it into OpenClaw after probe, preview, and exact-plan approval, **so that** I can authorize the exact package, target, and effects before any mutation.

<objective>
关闭 root gap 3 的文件系统基础，并修复 CR-03、CR-04：引入仓库自有、可审计、跨 darwin/linux fail-closed 的 native retained-dirfd kernel，替换 lexical path/cached recovery predicates。

Purpose: Node 的路径 API不能把 ancestor identity 与最终 open/rename/unlink 绑定为同一内核对象；本计划把该保证缩到一个小型 POSIX helper，并让任何无法证明的 cleanup 变成 preserved/incomplete。
Output: C source、构建/打包 contract、JS facade、transaction I/O wiring 与 adversarial race tests。
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-package/04-CONTEXT.md
@.planning/phases/04-package/04-RESEARCH.md
@.planning/phases/04-package/04-REVIEW.md
@.planning/phases/04-package/04-VERIFICATION.md
@.planning/phases/04-package/04-12-SUMMARY.md
@src/openclaw-install-transaction.js
@src/builder-package.js
@package.json
</context>

## Artifacts this phase produces

- `native/openclaw-fs-kernel.c`：只承担 retained-dirfd、no-follow、identity/digest 与 create-only/owned-state 原语。
- `src/openclaw-safe-fs.js`：durable closed build receipt、显式 admission、bounded IPC、无 fallback 的唯一 facade。
- `openclaw-fs-kernel-build --binary-out <absent-private-path> --receipt-out <absent-private-path>`：唯一显式 build surface。
- `openclaw-install-apply --fs-helper <path> --fs-helper-receipt <path> --fs-helper-receipt-digest <sha256>`：唯一 public apply helper admission surface。
- 打包源码 closure 与 extracted-tarball build smoke。
- filesystem swap、partial write、recovery 与 unsupported platform 的 fail-first 证据。

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — 固化 ancestor/final swap、recovery 与 helper supply-chain 攻击</name>
  <files>test/openclaw-safe-fs.test.js, test/openclaw-install-transaction.test.js, test/cli.test.js</files>
  <read_first>src/openclaw-install-transaction.js, src/builder-package.js, package.json, test/openclaw-install-transaction.test.js, test/builder-packed-install.test.js</read_first>
  <behavior>
    - "ancestor 在 admission 后换成 symlink、final file 在 lstat/read/write/recovery 窗口替换时，外部 sentinel 不变，unknown object 不被 unlink/overwrite。"
    - "partial write/crash 只留下 itemized owned orphan/recovery record；新进程不能凭 cached booleans 删除当前 pathname。"
    - "compiler path/env/argv/source/output/receipt 任一漂移、unsupported OS/arch、helper protocol 超长/多余 keys/异常退出均 fail closed。"
    - "Linux no-replace publication 不能退化为 plain `renameat`；Darwin no-replace publication 不能退化为 plain `renameat`；destination 已存在时原对象逐字节保持。"
    - "public API/CLI 缺少 helper path、receipt path 或 external receipt digest，或 receipt/source/compiler/argv/binary/file identity 复验不一致时，在 journal/effect 前拒绝。"
    - "packed tarball 含 C source 与 JS facade，不含预编译 helper；构建过程无网络、无 package lifecycle hook。"
  </behavior>
  <action>先增加 `test/openclaw-safe-fs.test.js`，使用 fixture hook 在每个内核原语前交换 ancestor/final entry，并记录 sentinel digest、inode 与 mode；分别在 Linux fixture 断言 `renameat2(RENAME_NOREPLACE)`，在 Darwin fixture 断言 `renameatx_np(RENAME_EXCL)`，destination 已存在时不得覆盖。增加 focused build/admit/apply 攻击矩阵：缺 `--receipt-out`、receipt partial/unknown key、external digest 错误、source bytes/identity 漂移、继承环境注入、`/usr/bin/cc` fingerprint/argv 漂移、binary digest/identity 替换、PATH-only helper、apply-time 缺 binary。`test/cli.test.js` 与 transaction public API 均断言缺少显式 helper/receipt/external-digest tuple 时 journal/effects 不存在。攻击必须覆盖 CR-03 的 final symlink write 和 CR-04 的 recovery pathname unlink。按 D-38 禁止把环境值与 raw helper output 写入断言快照。运行 RED 命令，确认当前纯 JS lexical/cached 实现不能满足这些用例。extracted-tarball build/admit/apply regression 由 04-18 独占。</action>
  <acceptance_criteria>
    - "RED 至少覆盖 ancestor symlink、final symlink、same-path replacement、recovery replacement、unsupported platform、compiler/source drift 六类。"
    - "每个攻击用例都断言外部 sentinel bytes/identity 未改变，并断言没有误删替换对象。"
    - "focused tests 明确禁止 PATH lookup、apply-time autobuild、预编译 helper 与 package lifecycle compilation；packed regression 由 04-18 验证。"
    - "build receipt 与 public apply tuple 的 negative tests 同时覆盖 direct API 与 CLI；任一失败发生在 journal/effect 前。"
  </acceptance_criteria>
  <verify>
    <automated>node -e "const {spawnSync}=require('node:child_process');const r=spawnSync(process.execPath,['--test','--test-name-pattern','safe fs|ancestor swap|final swap|recovery replacement|rename no replace|build receipt|fs helper admission','test/openclaw-safe-fs.test.js','test/openclaw-install-transaction.test.js','test/cli.test.js'],{stdio:'ignore'});if(r.status===0)process.exit(1)"</automated>
  </verify>
  <done>对抗用例因缺少 retained-dirfd kernel/构建 contract 而 RED，且没有依赖真实用户 OpenClaw。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — 实现可审计 retained-dirfd kernel 与固定构建 contract</name>
  <files>native/openclaw-fs-kernel.c, src/openclaw-safe-fs.js, src/cli.js, package.json, src/builder-package.js, test/openclaw-safe-fs.test.js, test/cli.test.js</files>
  <read_first>AGENTS.md, package.json, src/builder-package.js, src/builder-posix-effect.js, src/openclaw-install-transaction.js, test/builder-packed-install.test.js, test/openclaw-safe-fs.test.js</read_first>
  <behavior>
    - "helper 在 darwin/linux 使用 `openat`/`fstatat(AT_SYMLINK_NOFOLLOW)`/`unlinkat` 从 retained root fd 逐段打开；publication 在 Linux 明确使用 `renameat2(RENAME_NOREPLACE)`、Darwin 明确使用 `renameatx_np(RENAME_EXCL)`，拒绝 destination replacement、symlink、`..`、absolute path、cross-device、非当前 uid 与 group/world-writable ancestor。"
    - "helper 只提供 observe、create-private、publish-no-replace、fsync、identity-bound quarantine/preserve 与 retained-session closure；任何无法条件绑定 exact object 的物理删除都返回 preserve，不执行 pathname unlink。"
    - "build 只接受仓库内 source、固定 `/usr/bin/cc`、fixed argv、shell:false 与 closed env；显式写入 absent binary path 和 absent receipt path，receipt durable fsync 后才成功。"
    - "closed receipt 绑定 schema/kind/platform/arch、source path/digest/file identity、`/usr/bin/cc` path/toolchain fingerprint/file identity、完整 argv、closed env digest、binary path/digest/file identity 与 receipt identity；未知/缺失 key 拒绝。"
    - "public apply 显式接收 helper path、receipt path、external receipt digest，重新验证 receipt/source/closed env/toolchain/argv/binary digest/file identity 后从 retained handle 复制到私有执行目录；不 lookup、不 build。"
  </behavior>
  <action>按 D-35、D-39、D-41、D-42 实现小型 C helper：以长度上限 JSON-lines 接收 closed request，以 retained root fd 解析 repo-relative components，返回 dev/ino/mode/uid/size/digest 与具体 disposition；Linux `publish-no-replace` 只调用 `renameat2(..., RENAME_NOREPLACE)`，Darwin 只调用 `renameatx_np(..., RENAME_EXCL)`。若采用等价原语，必须同时满足 retained source/destination dirfd、destination-exists 原子拒绝、无覆盖、跨进程单赢家，并由同一 focused 测试矩阵证明；plain `renameat` 和 check-then-rename 不合格。不要加入通用 shell、任意 syscall、任意 absolute path 或 raw output。对 destructive cleanup 采用 fail-closed 策略：只有 helper 在同一 retained session 内创建且仍由 exact identity 占有的私有 temp 才可清理；已发布或重新打开的名字无法在最终 syscall 处绑定时必须保留并报告 orphan，而不是按 pathname 删除。`src/openclaw-safe-fs.js` 定义 closed `agentmo.openclaw-fs-build-receipt.v1`，build env 只含 `PATH=/usr/bin:/bin`、`LC_ALL=C`、`LANG=C`、private `HOME` 与 `TMPDIR`，编译器固定 `/usr/bin/cc`，argv 固定且 shell:false；toolchain fingerprint 绑定 compiler no-follow file identity/digest 与 bounded `--version` digest。CLI build surface 必须是 `openclaw-fs-kernel-build --binary-out <absent-private-path> --receipt-out <absent-private-path>`：binary 与 receipt 分别 create-exclusive、fsync，receipt 父目录 fsync 后才返回 external receipt digest。public `applyOpenClawInstallTransaction`/CLI 必须接收 `helperPath`/`receiptPath`/`receiptDigest`（CLI flags `--fs-helper`、`--fs-helper-receipt`、`--fs-helper-receipt-digest`），重读仓库 source、closed env descriptor、固定 compiler/toolchain fingerprint、argv、binary digest 与 no-follow file identity，复验 external receipt digest，并从 retained binary handle 复制到 0700 私有 execution dir 后执行。任何 tuple 缺失/漂移均在 journal 前失败；禁止 PATH lookup、apply-time autobuild 与 JS mutation fallback。把 C source/facade 的 inventory metadata 加入 package/builder declarations；packed build/admit/apply journey与最终 packed regression完全由 04-18 修改和拥有。</action>
  <acceptance_criteria>
    - "native source 无 `system`/`popen`/shell，协议拒绝未知 operation/keys 与超限 payload。"
    - "JS facade 没有纯 JS mutation fallback；helper 或 receipt 不可信时返回 unsupported/refused。"
    - "package tarball 含 source/facade，不含本机 binary；`agentmo.openclaw-fs-build-receipt.v1` 的 source/compiler/env/argv/binary/identity closure 可由 external digest 重算。"
    - "public API/CLI 不接受省略 helper/receipt/digest 的 apply，也不从 PATH 查找或在 apply 内构建 helper。"
    - "所有 swap/symlink/supply-chain RED 用例转绿。"
  </acceptance_criteria>
  <verify>
    <automated>node --test test/openclaw-safe-fs.test.js test/cli.test.js</automated>
  </verify>
  <done>retained-dirfd kernel 在隔离 darwin/linux fixture 上通过，unsupported/build drift 无 mutation，package closure 可离线审计。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — transaction 全面改用 safe-fs session 与重算 recovery predicates</name>
  <files>src/openclaw-install-transaction.js, src/openclaw-safe-fs.js, src/cli.js, test/openclaw-install-transaction.test.js, test/openclaw-safe-fs.test.js</files>
  <read_first>src/openclaw-install-transaction.js, src/openclaw-safe-fs.js, src/cli.js, test/openclaw-install-transaction.test.js, test/openclaw-safe-fs.test.js</read_first>
  <behavior>
    - "transaction 的 observe、journal、private temp、create-only target write 和 recovery 都通过一个 admitted retained-root session。"
    - "recovery asset 每次执行前重算 identity/digest/owner/marker/ancestor closure；不再信任 WeakSet/cached authentic flag。"
    - "不匹配、ambiguous、unknown、modified、final-window 无法证明的对象一律 preserved 并导致 incomplete，不删除或覆盖。"
  </behavior>
  <action>按 D-39、D-41、D-42 替换 `resolveInsideRoot` + `lstat/readFile/writeFile/unlink` mutation seam：transaction 在 fresh reprobe 通过后，使用 public apply 显式传入并已重验的 helper/receipt/external-digest tuple 创建 safe-fs session，把 exact target root identity、allowed relative paths、operation preconditions 交给 helper；此路径不得从 PATH 查找 helper、隐式执行 build 命令或选择 JS mutation fallback。删除 `AUTHENTIC_RECOVERY_ASSETS` 等 cached truth；恢复时从 durable journal 读取期望值，再由 helper 实时复验。当前计划只迁移 observation、journal 与 absent create primitive；patch/remove 的完整四 lifecycle 在 04-15 完成。任一 preserve/recovery item 必须带 bounded reason 与 observed/expected identity，不能标记 complete。</action>
  <acceptance_criteria>
    - "`src/openclaw-install-transaction.js` 的 target/journal/recovery path 不直接调用 pathname `writeFile` 或 `unlink`。"
    - "跨进程恢复重新打开并验证 retained root，不依赖内存 WeakSet 或旧 booleans。"
    - "direct API 与 CLI apply 均只使用显式 admitted helper tuple；tuple 漂移时无 journal、target mutation 或 fallback。"
    - "所有 swap 测试都证明 unknown replacement 未删除、sentinel 未修改且 receipt outcome 不是 complete。"
  </acceptance_criteria>
  <verify>
    <automated>node --test test/openclaw-safe-fs.test.js test/openclaw-install-transaction.test.js</automated>
  </verify>
  <done>CR-03/04 的 RED 用例转绿；transaction 的 AgentMo-owned I/O 全部经 safe-fs session，无法证明的 cleanup 保留且显式 incomplete。</done>
</task>

</tasks>

## Gap / Review Coverage

| Root gap | Findings | Attack path | Target invariant | Tasks |
|---|---|---|---|---|
| 3 safe mutation/recovery | CR-03 | ancestor/final symlink 或 swap 让 lexical path 逃逸 | retained dirfd 逐段 no-follow；未知对象零 overwrite/delete | 1, 2, 3 |
| 3 safe mutation/recovery | CR-04 | 首次恢复缓存 authenticity，随后 pathname 被替换 | 每次恢复在最终原语前重算 identity/digest/owner/marker | 1, 3 |
| native helper supply chain | EVID-05 | 环境替换 compiler/source/binary/receipt 或 apply 通过 PATH/autobuild 注入 | repo source + `/usr/bin/cc` toolchain fingerprint + closed env/argv + durable external-digest receipt；无 lookup/下载/预编译/fallback | 1, 2, 3 |

## Multi-Source Coverage Audit

| Source | Items covered | Disposition |
|---|---|---|
| GOAL | preview/exact approval 后安全 mutation | COVERED by retained-dirfd mutation boundary |
| REQ | OCLW-02, OCLW-04, OCLW-05, EVID-05 | COVERED |
| RESEARCH | no-follow handles、final-window revalidation、private owner、fail closed | COVERED with native helper because Node lacks dirfd-relative primitives |
| CONTEXT | D-35, D-39, D-41, D-42; D-38 value-blind output | COVERED |
| DEFERRED | live agent invocation、schedule、restart、domain certification | EXCLUDED |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| JS transaction → native helper | closed filesystem intents cross into OS syscalls |
| package source → local compiler → helper binary | repository source and trusted compiler create an executable |
| retained target root → relative effects | untrusted same-host namespace may change between observations |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-G13-01 | Tampering | ancestor/final path resolution | critical | mitigate | Tasks 2-3 use retained dirfds, no-follow component walk and exact identity/digest dispositions |
| T-04-G13-02 | Elevation of Privilege | symlink/cross-device/unsafe-owner root | high | mitigate | Task 2 rejects unsafe mode/uid, symlink, absolute/traversal and cross-device closure |
| T-04-G13-03 | Tampering | recovery pathname replacement | critical | mitigate | Task 3 recomputes every predicate and preserves unprovable objects |
| T-04-G13-04 | Tampering | compiler/helper/receipt substitution | high | mitigate | Task 2 fixes `/usr/bin/cc`, closed env/argv, hashes source/toolchain/binary/file identities and re-admits external-digest receipt at apply |
| T-04-G13-05 | Denial of Service | malformed/oversized helper protocol | medium | mitigate | Task 2 enforces request/output/time/operation bounds and terminates fail closed |
| T-04-G13-SC | Tampering | package install hooks/download | high | mitigate | Task 1/2 forbid prebuilt binary, network fetch and preinstall/install/postinstall; no new package dependency |
</threat_model>

<verification>
- `node --test test/openclaw-safe-fs.test.js test/openclaw-install-transaction.test.js test/cli.test.js`
- `npm pack --dry-run`
- `npm run check`
- `git diff --check`
</verification>

<success_criteria>
- CR-03、CR-04 攻击矩阵全部通过，且不存在 lexical-path/cached-auth cleanup seam。
- helper 的 source/build/package/cross-platform/fail-closed/supply-chain 证据完整。
- 任何无法在 syscall 边界证明 exact-owned 的对象都 preserved/incomplete，不被静默清理。
</success_criteria>

<output>
Create `.planning/phases/04-package/04-13-SUMMARY.md` when done.
</output>
