---
phase: 04-package
plan: 15
type: execute
wave: 15
depends_on:
  - 04-14
files_modified:
  - src/openclaw-official-action-runner.js
  - src/openclaw-install-plan.js
  - src/openclaw-install-transaction.js
  - src/openclaw-credential-handoff.js
  - src/cli.js
  - test/openclaw-install-plan.test.js
  - test/openclaw-install-transaction.test.js
  - test/openclaw-credential-handoff.test.js
  - test/phase4-contracts.test.js
autonomous: true
gap_closure: true
requirements:
  - OCLW-02
  - OCLW-03
  - OCLW-04
  - OCLW-05
  - EVID-05
must_haves:
  truths:
    - "install、upgrade、rollback、uninstall 四条 lifecycle 都在隔离 fixture 上产生真实、可观察的 OpenClaw workspace/config 状态转换，而不是仅通过 plan grammar。"
    - "absent genesis 由 safe-fs 对 plan 派生路径的现场观察生成；caller 不能声明 `verifiedAbsent`、checkedPaths 或 absenceDigest。"
    - "workspace 使用 create-only immutable generations；config 只走 OpenClaw 官方 `config patch` 的 exact dry-run/actual pair，credential 只走 04-14 official route；未知字段与未知/修改资产始终保留。"
  artifacts:
    - path: src/openclaw-official-action-runner.js
      provides: "exact config dry-run/actual and credential action dispatcher"
    - path: src/openclaw-install-plan.js
      provides: "filesystem-observed genesis and four-lifecycle exact effect model"
    - path: src/openclaw-install-transaction.js
      provides: "real write/patch/remove lifecycle execution and before/after observations"
    - path: test/openclaw-install-transaction.test.js
      provides: "install→upgrade→rollback→uninstall vertical journey and preservation attacks"
  key_links:
    - from: src/openclaw-install-plan.js
      to: src/openclaw-safe-fs.js
      via: "genesis derives checked paths and observes them through the retained target session"
    - from: src/openclaw-install-transaction.js
      to: src/openclaw-official-action-runner.js
      via: "config/credential actions dispatch only after exact decisions and durable nonce reservation"
    - from: src/openclaw-official-action-runner.js
      to: src/openclaw-probe.js
      via: "dry-run and actual execute the same verified private OpenClaw binary/cwd/env binding"
---

## Phase Goal

**As a** developer, **I want to** deterministically generate an offline-inspectable least-trust Agent Package from an approved build contract and safely install it into OpenClaw after probe, preview, and exact-plan approval, **so that** I can authorize the exact package, target, and effects before any mutation.

<objective>
关闭 root gap 5，并修复 WR-01、WR-02：把 absent-only write seam 扩展为可执行的 install/upgrade/rollback/uninstall 垂直旅程，接通 official config/credential dispatcher，并由现场文件系统观察生产 genesis authority。

Purpose: 证明 Phase 4 是四生命周期、exact-effect、fail-closed 的真实机制，而不是只生成计划与批准 JSON。
Output: official action runner、observed genesis、write/patch/remove execution、CLI wiring、四 lifecycle 与 preservation adversarial tests。
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-package/04-CONTEXT.md
@.planning/phases/04-package/04-RESEARCH.md
@.planning/phases/04-package/04-REVIEW.md
@.planning/phases/04-package/04-VERIFICATION.md
@.planning/phases/04-package/04-14-SUMMARY.md
@src/openclaw-install-plan.js
@src/openclaw-install-transaction.js
@src/openclaw-credential-handoff.js
@src/cli.js
</context>

## Artifacts this phase produces

- OpenClaw official config/credential exact action dispatcher。
- 从现场 no-follow observation 产生的 absent genesis authority。
- install→upgrade→rollback→uninstall 的真实隔离 target journey。
- unknown/modified/ambiguous preservation 与 sensitive-action fail-closed 证据。

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — 固化四 lifecycle、现场 genesis 与 preservation 旅程</name>
  <files>test/openclaw-install-plan.test.js, test/openclaw-install-transaction.test.js, test/openclaw-credential-handoff.test.js, test/phase4-contracts.test.js</files>
  <read_first>src/openclaw-install-plan.js, src/openclaw-install-transaction.js, src/openclaw-credential-handoff.js, src/cli.js, test/openclaw-install-plan.test.js, test/openclaw-install-transaction.test.js, test/openclaw-credential-handoff.test.js, test/phase4-contracts.test.js</read_first>
  <behavior>
    - "单一隔离 fixture 依次 install、upgrade、rollback、uninstall；每一步都检查实际 workspace generation、OpenClaw config 与 inactive generations，而非只检查 operation 数组。"
    - "caller 伪造 verifiedAbsent/checkedPaths/absenceDigest 或在 genesis observation 窗口创建 path 时，preview 在批准前失败。"
    - "upgrade/rollback/uninstall 遇到未知字段、unknown asset、modified owned generation、ambiguous marker、config base drift 时，不覆盖/删除，结果 preserved/incomplete。"
    - "official config dry-run 与 actual 的 argv/input digest 不同、credential runner 缺失、sensitive action 未逐项批准/未 reserve 时，零 external effect。"
  </behavior>
  <action>先扩展 tests 为一个真实垂直旅程，使用 fake OpenClaw executable 实现官方 `config patch --file ... --dry-run` 与实际执行的封闭 fixture 协议，并记录调用 argv/input digest 与 target config before/after。分别加入 forged genesis、path appears、unknown field、modified generation、config drift、missing runner、dry-run/actual substitution 攻击。按 D-35 默认使用隔离 project workspace，user/shared 变体必须有单独 plan/approval；按 D-39 旧 generation 作为 inactive preserved evidence，不用 pathname cleanup。运行 RED 并确认当前仅 write/unsupported patch-remove 与 caller genesis 无法通过。</action>
  <acceptance_criteria>
    - "四 lifecycle 每一步都有文件系统与 config 的具体 before/after 断言。"
    - "至少一个 upgrade 和一个 uninstall 攻击证明 unknown/modified 内容逐字节保留。"
    - "伪造 genesis 即使 digest 自洽也不能进入 preview。"
  </acceptance_criteria>
  <verify>
    <automated>node -e "const {spawnSync}=require('node:child_process');const r=spawnSync(process.execPath,['--test','--test-name-pattern','lifecycle journey|observed genesis|preserves unknown|official config|sensitive runner','test/openclaw-install-plan.test.js','test/openclaw-install-transaction.test.js','test/openclaw-credential-handoff.test.js','test/phase4-contracts.test.js'],{stdio:'ignore'});if(r.status===0)process.exit(1)"</automated>
  </verify>
  <done>WR-01/02 的真实旅程与攻击用例 RED，且 fake OpenClaw 只模拟正式 route，不执行 agent/plugin/MCP/schedule/restart。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — 由现场观察生产 genesis 与四 lifecycle exact plan</name>
  <files>src/openclaw-install-plan.js, src/cli.js, test/openclaw-install-plan.test.js, test/phase4-contracts.test.js</files>
  <read_first>src/openclaw-install-plan.js, src/openclaw-safe-fs.js, src/openclaw-probe.js, src/cli.js, test/openclaw-install-plan.test.js, test/phase4-contracts.test.js</read_first>
  <behavior>
    - "genesis builder 只接收 exact archive/target/probe 与 retained session，从 plan operations 派生 checked paths 并现场观察 absent。"
    - "四 lifecycle plan 都绑定 full archive、target、probe fingerprint、whole conflict set、workspace generation、config patch、rollback/preservation recipe 与 external dependencies。"
    - "user/shared scope 与每个 sensitive action 必须有独立 exact decisions；ordinary approval 不能授权它们。"
  </behavior>
  <action>按 D-34、D-35、D-36、D-37、D-39、D-41、D-42 重构 `buildOpenClawAbsentGenesisAuthority`：删除 caller 提供 `checkedPaths`、`verifiedAbsent`、`absenceDigest` 的 API，改由 exact plan/archive/target 计算路径并通过 04-13 retained session 逐项 no-follow observe；任一路径存在、ambiguous 或 observation 漂移即不生产 authority。为 install/upgrade/rollback/uninstall 生成真实 operations：workspace payload 写入新 content-addressed generation；upgrade/rollback 用 exact minimal config patch 切换到批准 generation；uninstall 用 exact patch 删除 AgentMo-owned config entry，但保留 inactive generation。每个 patch 绑定 base config digest 与 preserve-unknown rule；user/shared 和 sensitive actions 按 D-35/D-36 分离。CLI genesis request 只保留可验证输入路径，不接受 absence claims。</action>
  <acceptance_criteria>
    - "public genesis API/CLI 不再接受 caller absence truth；checked paths 可从 exact plan 重算。"
    - "四 lifecycle 计划包含可执行 effect、pre/postcondition、rollback/preserve recipe 与 exact approvals，且 whole conflict set 不可部分批准。"
    - "uninstall 不直接删除 workspace generation；只移除 exact AgentMo-owned config reference，保留未知/修改/ambiguous assets。"
  </acceptance_criteria>
  <verify>
    <automated>node --test test/openclaw-install-plan.test.js test/phase4-contracts.test.js</automated>
  </verify>
  <done>WR-02 转绿；preview/genesis 基于现场观察，四 lifecycle plan 的效果与批准边界完整可重算。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — 执行 workspace generations、official config patch 与 credential actions</name>
  <files>src/openclaw-official-action-runner.js, src/openclaw-install-transaction.js, src/openclaw-credential-handoff.js, src/cli.js, test/openclaw-install-transaction.test.js, test/openclaw-credential-handoff.test.js</files>
  <read_first>src/openclaw-install-transaction.js, src/openclaw-install-plan.js, src/openclaw-safe-fs.js, src/openclaw-probe.js, src/openclaw-credential-handoff.js, src/cli.js, test/openclaw-install-transaction.test.js, test/openclaw-credential-handoff.test.js</read_first>
  <behavior>
    - "write operation 通过 safe-fs publish-no-replace 创建 immutable generation；patch/remove operation 通过官方 `config patch --file` exact dry-run/actual pair 实现最小字段变更。"
    - "actual 前重新 fresh-probe、revalidate base config digest 与 decisions/nonces；actual 后重新观察 exact target，差异进入 incomplete/recovery，不伪造 success。"
    - "credential actions 通过 04-14 closed runner；任一 external action failed/unsupported 时停止后续 effects并产生可恢复 incomplete evidence。"
  </behavior>
  <action>新增 `openclaw-official-action-runner.js` 作为唯一 external dispatcher。config route 只允许 `config patch --file <private-admitted-patch> --dry-run` 与同一 file/digest 的 actual pair；patch 是 config-shaped closed object，递归 merge、`null` 删除 exact owned path，不允许 whole-file replacement。执行 04-12 verified private executable、private cwd/env、shell:false，dry-run 结果只保留 bounded digest/status。transaction 按 plan 顺序：durable reserve → write immutable generation → fresh revalidate → official config/credential action → safe-fs postobserve；每个 result 绑定 operation/action/decision/nonce digest。rollback 切回 exact pristine predecessor generation；uninstall 只移除 exact managed config reference。按 D-39 遇到 unknown/modified/ambiguous 时停止并 preserve，绝不直接 unlink generations；按 D-38 不保存 credential/raw child output。</action>
  <acceptance_criteria>
    - "install/upgrade/rollback/uninstall 垂直 fixture 的真实 target/config 状态均符合 exact plan。"
    - "patch/remove 不再返回 `unsupported`，也不通过 Node pathname write/unlink 实现。"
    - "official dry-run/actual 的 executable、cwd/env、argv、input digest、target、decision 与 nonce 完全相同；漂移在 actual 前拒绝。"
    - "未知/修改/ambiguous assets 逐字节保留，且 transaction 不标记 complete。"
  </acceptance_criteria>
  <verify>
    <automated>node --test test/openclaw-install-transaction.test.js test/openclaw-credential-handoff.test.js test/openclaw-install-plan.test.js test/phase4-contracts.test.js</automated>
  </verify>
  <done>WR-01 转绿：四 lifecycle 与 official config/credential seams 均真实执行，D-39 preservation 与 exact authority invariants 保持。</done>
</task>

</tasks>

## Gap / Review Coverage

| Root gap | Findings | Attack path | Target invariant | Tasks |
|---|---|---|---|---|
| 5 lifecycle seam | WR-01 | patch/remove unsupported，CLI 不传 sensitive runner | 四 lifecycle 在隔离 target 上有真实 before/after effects | 1, 2, 3 |
| 5 genesis provenance | WR-02 | caller 自报 checkedPaths/absenceDigest/verifiedAbsent | genesis 只由 plan-derived paths 的 retained observation 生成 | 1, 2 |
| 5 preservation | OCLW-04 | upgrade/uninstall 覆盖未知字段或删除修改资产 | immutable generation + exact minimal config patch + preserve/incomplete | 1, 3 |

## Multi-Source Coverage Audit

| Source | Items covered | Disposition |
|---|---|---|
| GOAL | approved contract → package → exact-plan-safe OpenClaw install | COVERED by end-to-end lifecycle execution |
| REQ | OCLW-02, OCLW-03, OCLW-04, OCLW-05, EVID-05 | COVERED |
| RESEARCH | official config/credential route、project isolation、four lifecycle、preserve unknown | COVERED |
| CONTEXT | D-34, D-35, D-36, D-37, D-38, D-39, D-41, D-42 | COVERED |
| DEFERRED | agent invocation、MCP/plugin activation、schedule/restart、runtime/domain certification | EXCLUDED |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| live target → genesis authority | filesystem absence becomes first-install authority |
| exact plan → workspace/config effects | approved operations mutate isolated OpenClaw state |
| AgentMo → official OpenClaw CLI | config/credential intent crosses external executable boundary |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-G15-01 | Spoofing | absent genesis | high | mitigate | Task 2 derives paths and observes through retained session; caller absence claims removed |
| T-04-G15-02 | Tampering | config patch/base drift | critical | mitigate | Task 3 exact dry-run/actual pair, base digest revalidation and postobservation |
| T-04-G15-03 | Tampering | upgrade/rollback/uninstall assets | critical | mitigate | Task 3 immutable generations and preserve unknown/modified/ambiguous objects |
| T-04-G15-04 | Elevation of Privilege | official action dispatcher | high | mitigate | Task 3 closed route grammar, verified executable, private cwd/env, exact decisions/nonces |
| T-04-G15-05 | Information Disclosure | credential/config child output | high | mitigate | Task 3 stores bounded status/digests only; no raw output or secret values |
| T-04-G15-SC | Tampering | plugin/MCP/package installation | high | mitigate | No such route exists; adversarial tests prove official dispatcher rejects them |
</threat_model>

<verification>
- `node --test test/openclaw-install-plan.test.js test/openclaw-install-transaction.test.js test/openclaw-credential-handoff.test.js test/phase4-contracts.test.js`
- `npm run check`
- `git diff --check`
</verification>

<success_criteria>
- WR-01、WR-02 全部关闭。
- 四 lifecycle 在隔离 fixture 中产生真实、可复验、可 itemize 的 effects。
- unknown/modified/ambiguous preservation、official route、separate authority 与 no-secret evidence 均保持。
</success_criteria>

<output>
Create `.planning/phases/04-package/04-15-SUMMARY.md` when done.
</output>
