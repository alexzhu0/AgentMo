---
phase: 04-package
plan: 14
type: execute
wave: 14
depends_on:
  - 04-13
files_modified:
  - src/openclaw-authority-consumption.js
  - src/openclaw-install-approval.js
  - src/openclaw-credential-handoff.js
  - src/openclaw-install-transaction.js
  - src/cli.js
  - test/openclaw-install-approval.test.js
  - test/openclaw-install-transaction.test.js
  - test/phase4-contracts.test.js
autonomous: true
gap_closure: true
requirements:
  - OCLW-02
  - OCLW-05
  - EVID-05
must_haves:
  truths:
    - "ordinary、conflict 与每个 sensitive decision 的 useNonce 都在任何 effect 前，以 per-authority/per-nonce、dirfd-relative、`O_CREAT|O_EXCL|O_NOFOLLOW` marker 原子绑定 exact attempt/plan/target/lifecycle/action。"
    - "每个完整 marker 与其父目录均在 effect 前 fsync；同进程、fresh process、双 writer 与 crash/resume 都不能重用已 reserved nonce，且只有 fully durable exact-attempt marker set 可以恢复。"
    - "crash-before-fsync、partial/stale/unknown marker 永不自动回收或授权 effect；它们保持 fail-closed recovery-required，不能依赖未交付的全局锁、mutable head、predecessor chain 或 CAS。"
    - "credential handoff 只接受正式 OpenClaw credential routes 的封闭 argv grammar，且对完整 decision/plan/target/action/expiry/nonce/probe executable binding 做统一验证。"
  artifacts:
    - path: src/openclaw-authority-consumption.js
      provides: "per-authority/per-nonce durable reservation markers and exact-attempt recovery admission"
    - path: src/openclaw-install-approval.js
      provides: "all-three-family exact decision validation"
    - path: src/openclaw-credential-handoff.js
      provides: "official credential route closed grammar and exact authority validation"
    - path: test/openclaw-install-transaction.test.js
      provides: "fresh-process/concurrent/crash replay and forged credential attacks"
  key_links:
    - from: src/openclaw-install-transaction.js
      to: src/openclaw-authority-consumption.js
      via: "reserve exact authority set before journal/effects; finalize after receipt authority is durable"
    - from: src/openclaw-authority-consumption.js
      to: src/openclaw-safe-fs.js
      via: "each marker is created relative to retained authority-root dirfd with O_CREAT|O_EXCL|O_NOFOLLOW, then marker and parent directory are fsynced"
    - from: src/openclaw-credential-handoff.js
      to: src/openclaw-install-approval.js
      via: "credential runner calls the same canonical sensitive-decision validator as transaction"
---

## Phase Goal

**As a** developer, **I want to** deterministically generate an offline-inspectable least-trust Agent Package from an approved build contract and safely install it into OpenClaw after probe, preview, and exact-plan approval, **so that** I can authorize the exact package, target, and effects before any mutation.

<objective>
关闭 root gap 2，并修复 CR-05、CR-06：把内存 `usedNonces: []` 换成 durable atomic one-use authority ledger，同时将 credential runner 从任意 OpenClaw argv 黑名单改为正式 route 的完整 exact decision allowlist。

Purpose: 让批准成为不可重放的一次性能力，并阻止合法 sensitive envelope 携带未经批准的 plugin/MCP/config/agent/schedule 命令。
Output: durable authority-consumption module、三类 decision 统一验证、official credential grammar、CLI state-root wiring 与 adversarial replay/argv tests。
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-package/04-CONTEXT.md
@.planning/phases/04-package/04-REVIEW.md
@.planning/phases/04-package/04-VERIFICATION.md
@.planning/phases/04-package/04-13-SUMMARY.md
@src/openclaw-install-approval.js
@src/openclaw-credential-handoff.js
@src/openclaw-install-transaction.js
@src/cli.js
</context>

## Artifacts this phase produces

- 三类 authority 的 per-authority/per-nonce durable create-exclusive reservation markers。
- exact attempt resume/recovery contract 与并发赢家语义。
- `config patch`、`secrets apply` 与 `models auth login` 的封闭正式 route grammar；credential action 仅允许后两类。
- fresh-process replay、并发 race、crash window 与 argv confusion 攻击证据。

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — 固化跨进程 nonce replay 与 credential argv confusion</name>
  <files>test/openclaw-install-approval.test.js, test/openclaw-install-transaction.test.js, test/phase4-contracts.test.js</files>
  <read_first>src/openclaw-install-approval.js, src/openclaw-credential-handoff.js, src/openclaw-install-transaction.js, src/cli.js, test/openclaw-install-approval.test.js, test/openclaw-install-transaction.test.js, test/phase4-contracts.test.js</read_first>
  <behavior>
    - "同一个 ordinary/conflict/sensitive nonce 在同进程、fresh process 与两个并发 apply 中最多一个 attempt 通过 marker create-exclusive 获得 reservation。"
    - "crash-before-marker-fsync、crash-after-marker-fsync-before-effect、zero-byte/truncated/unknown-key/stale marker 均不释放 nonce；只有完整 durable marker set 且 exact attempt/plan/target/lifecycle/authority set 相等时可以继续。"
    - "合法 envelope 内的 `plugins install`、MCP、agent invoke、schedule、restart、arbitrary config、额外 flags、参数重排/重复均被拒绝。"
    - "credential runner 拒绝截断 decision、错 plan/target/action digest、expired/not-yet-valid、错 nonce 与错 verified executable。"
  </behavior>
  <action>先写 child-process 与并发 fixture：两个独立 Node writer 对同一 authority-family/nonce marker 同时执行 dirfd-relative create，断言只有一个 `O_CREAT|O_EXCL|O_NOFOLLOW` 成功；不能用进程内 mutex、全局 lock file、mutable ledger head 或 CAS 协调。分别在 marker create 后但内容/fsync 前、marker fsync 后但 parent fsync/effect 前注入 crash，再由新进程尝试新-attempt replay 与 exact-attempt resume。手工放置 zero-byte、truncated、unknown-key、错误 mode/uid、symlink、stale attempt 与完整但 digest 错误 marker；断言全部 fail closed 且不被自动 unlink/reclaim。三类 decision 分别测试，不能只覆盖 ordinary approval。credential attacks 使用 value-blind provider/id/path，覆盖 `plugins install` 与其他非 credential surfaces；明确断言 runner 未调用、target/journal 未变。按 D-36、D-37、D-38、D-41 将 exact action、whole conflict set、时间与 probe executable 都放入预期不变量。运行 RED 并确认当前内存数组/黑名单实现失败。</action>
  <acceptance_criteria>
    - "三类 authority 各有 fresh-process replay negative；至少一个并发测试证明单赢家。"
    - "双 writer 测试不依赖全局锁/CAS，且 marker create syscall 返回值直接证明单赢家。"
    - "crash-before/after-fsync 测试区分新 attempt replay、exact-attempt resume 与 partial-marker recovery-required。"
    - "stale/partial/symlink/unsafe-owner marker 均保留且拒绝授权，不自动回收。"
    - "credential attack matrix 覆盖 plugin/MCP/config/agent/schedule/restart 及 argv 重排/重复。"
  </acceptance_criteria>
  <verify>
    <automated>node -e "const {spawnSync}=require('node:child_process');const r=spawnSync(process.execPath,['--test','--test-name-pattern','nonce replay|concurrent reservation|crash recovery|credential argv|forged sensitive','test/openclaw-install-approval.test.js','test/openclaw-install-transaction.test.js','test/phase4-contracts.test.js'],{stdio:'ignore'});if(r.status===0)process.exit(1)"</automated>
  </verify>
  <done>CR-05/06 的新增攻击矩阵 RED，失败原因是缺少 durable ledger/closed grammar，而不是测试进程协调问题。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — 实现 per-authority/per-nonce durable one-use markers</name>
  <files>src/openclaw-authority-consumption.js, src/openclaw-install-approval.js, src/openclaw-install-transaction.js, src/cli.js, test/openclaw-install-approval.test.js, test/openclaw-install-transaction.test.js</files>
  <read_first>src/openclaw-safe-fs.js, src/openclaw-install-approval.js, src/openclaw-install-transaction.js, src/cli.js, test/openclaw-install-approval.test.js, test/openclaw-install-transaction.test.js</read_first>
  <behavior>
    - "private authority-state root 下按 authority family 与 nonce digest 派生固定目录/marker name；每个 marker 仅以 retained dirfd + `O_CREAT|O_EXCL|O_NOFOLLOW` 创建。"
    - "marker closed record 包含 attemptId、plan/target/archive/lifecycle/probe fingerprint、decision/action/conflict digests、nonce 与 time window；写全后 fsync marker，再 fsync parent directory，之后才允许 effects。"
    - "排序 authority set 逐项 reserve；中途冲突/崩溃时已创建 marker 永久保留为 used，绝不 rollback/reclaim。fully durable exact set 才能 resume；partial/stale/unknown set 只能 recovery-required。"
  </behavior>
  <action>新建 `openclaw-authority-consumption.js`，按 D-36、D-37、D-41、D-42 设计 closed marker store，不引入全局锁、mutable ledger head、predecessor chain 或 CAS。用 04-13 safe-fs primitive 创建/复验 private owner state root；按 `ordinary|conflict|sensitive/<sha256(nonce)>` 派生 bounded relative marker path，并以 retained directory fd 调用 `openat` 的 `O_WRONLY|O_CREAT|O_EXCL|O_NOFOLLOW` 模式 0600。marker record 绑定 authority family、nonce digest、exact `attemptId`、plan/target/archive/lifecycle/probe/action/conflict/time-window digests；write-all 后 `fsync(markerFd)`，close 后 `fsync(parentDirFd)`，两步完成前 transaction 不得创建 journal 或 effect。对排序 authority set 逐项 reserve；若后项冲突或 crash，前项 marker 不删除、不复用。exact resume 必须逐项 no-follow reopen 并验证 closed record、mode/uid/file identity/digest 与 durable journal；只有所有 markers 完整 durable 且 exact match 才继续。zero-byte/truncated/stale/unsafe/symlink/unknown marker 返回 recovery-required 并 preserve，绝不自动修复或 reclaim。`validateOpenClawInstallDecision` 不再接收 caller `usedNonces` 数组，而接收 admitted marker-set context。CLI 要求显式 authority-state path 或从 AgentMo 自建 0700 state root 推导，不能放进 OpenClaw workspace/config/credential/session 目录。所有错误仅返回 bounded digests/status。</action>
  <acceptance_criteria>
    - "不存在 `usedNonces: []` 或 caller-supplied used-nonce truth。"
    - "不存在 authority-wide/global lock、mutable head、predecessor chain 或 compare-and-swap requirement。"
    - "每个 marker fsync 与 parent-dir fsync 都发生在 journal/effect/credential runner 之前。"
    - "并发、fresh process、crash-before/after-fsync、stale/partial marker 测试在三类 authority 上通过；任一 marker digest/identity/owner 漂移 fail closed。"
  </acceptance_criteria>
  <verify>
    <automated>node --test test/openclaw-install-approval.test.js test/openclaw-install-transaction.test.js</automated>
  </verify>
  <done>per-authority/per-nonce durable one-use markers 完成，双 writer 单赢家；只有 fully durable exact attempt 可受控恢复，partial/stale marker 永不再次授权。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — 封闭 official credential routes 并复用完整 decision validator</name>
  <files>src/openclaw-credential-handoff.js, src/openclaw-install-approval.js, src/openclaw-install-transaction.js, src/cli.js, test/openclaw-install-transaction.test.js, test/phase4-contracts.test.js</files>
  <read_first>src/openclaw-credential-handoff.js, src/openclaw-install-approval.js, src/openclaw-install-transaction.js, src/openclaw-probe.js, src/cli.js, test/openclaw-install-transaction.test.js, test/phase4-contracts.test.js</read_first>
  <behavior>
    - "credential action 仅允许 exact `secrets apply --from <admitted-plan> [--dry-run]` 或交互式 `models auth login --provider <safe-id>` grammar；其他子命令/flags/order 均拒绝。"
    - "`config patch --file <admitted-patch> --dry-run`/实际执行属于 config action，不可伪装成 credential action；plugin/MCP/run/schedule/restart 永无 grammar。"
    - "runner 在执行前调用 canonical sensitive decision validator，验证 plan/target/archive/lifecycle/action digest/argv/probe executable/expiry/per-nonce marker set 全集。"
  </behavior>
  <action>按 D-33、D-36、D-38、D-41 把 `validateCredentialProposal` 从 executable-name + blacklist 改成 discriminated closed routes。`secrets apply` 只接受 admitted ref-only plan path，缺省禁止 `--allow-exec`；若计划显式包含 exec SecretRef，则必须是单独 sensitive action/decision 且 route grammar exact。`models auth login` 只接受安全 provider id 与批准中逐项列出的 optional method/profile flags，不接受 `--set-default`/`--force`，除非这些字段作为独立 exact sensitive action 明示。runner 复用 `validateOpenClawInstallDecision` 与 admitted fully durable marker-set context，不再做截断 equality。只执行 04-12 verified private OpenClaw copy、shell:false、private cwd/env；evidence 记录 exit/status/action/decision/runner digest，不记录凭据或 raw output。</action>
  <acceptance_criteria>
    - "任意 plugin/MCP/config/agent/schedule/restart argv 即使 envelope digest 自洽也在 spawn 前拒绝。"
    - "credential route 的每个 argv token 都由 closed schema 推导并与 action digest/decision/nonce reservation 匹配。"
    - "credential result 是 value-blind；没有 token/stdout/stderr/raw provider payload 字段。"
  </acceptance_criteria>
  <verify>
    <automated>node --test test/openclaw-install-transaction.test.js test/phase4-contracts.test.js</automated>
  </verify>
  <done>CR-05/06 全部转绿；三类 nonce durable one-use，credential runner 不能承载非正式或未逐项批准的命令。</done>
</task>

</tasks>

## Gap / Review Coverage

| Root gap | Findings | Attack path | Target invariant | Tasks |
|---|---|---|---|---|
| 2 durable nonce | CR-05 | 每次 apply 传空数组，fresh process/双 writer 重放 approval | 三类 nonce 各自由 create-exclusive marker 在 effect 前 durable reserve；无全局锁/CAS，任何新 attempt 永不复用 | 1, 2 |
| 2 credential exact route | CR-06 | 黑名单外任意 `openclaw` argv；截断 decision equality | closed official grammar + full canonical decision/ledger validation | 1, 3 |

## Multi-Source Coverage Audit

| Source | Items covered | Disposition |
|---|---|---|
| GOAL | exact-plan approval before effects | COVERED by durable reservation and closed routes |
| REQ | OCLW-02, OCLW-05, EVID-05 | COVERED |
| RESEARCH | one-use approval、per-authority marker、separate credential authority、official route | COVERED without unshipped global lock/CAS |
| CONTEXT | D-33, D-36, D-37, D-38, D-41, D-42 | COVERED |
| DEFERRED | actual agent invocation/MCP/plugin/schedule/restart | EXCLUDED and explicitly rejected |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| signed/approved JSON → durable authority ledger | untrusted authority claims become one-use capabilities |
| concurrent processes → authority state root | contenders race on independent per-family/per-nonce create-exclusive markers |
| sensitive decision → OpenClaw child process | approved credential action becomes an external effect |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-G14-01 | Replay/Spoofing | authority nonce | critical | mitigate | Task 2 per-family/per-nonce O_EXCL reservation across all three decision families |
| T-04-G14-02 | Tampering | concurrent/partial/stale marker | high | mitigate | Task 2 no-follow retained-dirfd create, marker + parent fsync, exact resume and preserve-on-unknown |
| T-04-G14-03 | Elevation of Privilege | credential argv | critical | mitigate | Task 3 closed official grammar and spawn only verified private executable |
| T-04-G14-04 | Tampering | truncated/mismatched decision | high | mitigate | Task 3 canonical full decision/plan/target/action/time/nonce validation |
| T-04-G14-05 | Information Disclosure | credential output/evidence | high | mitigate | Task 3 stores only value-blind status/digests; raw output and credentials forbidden |
| T-04-G14-SC | Tampering | external package install | low | accept | 本计划不引入 package-manager install 或第三方代码 |
</threat_model>

<verification>
- `node --test test/openclaw-install-approval.test.js test/openclaw-install-transaction.test.js test/phase4-contracts.test.js`
- `npm run check`
- `git diff --check`
</verification>

<success_criteria>
- CR-05、CR-06 攻击矩阵全部通过。
- ordinary/conflict/sensitive 三类 authority 的 nonce 均由独立 marker durable、atomic、one-use；双 writer 单赢家，partial/stale marker fail closed，fully durable exact attempt 可恢复但不可重放。
- credential runner 只能执行逐 token 批准的正式凭据入口，且 evidence value-blind。
</success_criteria>

<output>
Create `.planning/phases/04-package/04-14-SUMMARY.md` when done.
</output>
