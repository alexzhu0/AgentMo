---
phase: 04-package
plan: 12
type: execute
wave: 12
depends_on:
  - 04-11
files_modified:
  - src/openclaw-probe.js
  - src/openclaw-probe-contract.js
  - src/openclaw-install-transaction.js
  - src/cli.js
  - src/artifact-registry.js
  - test/openclaw-probe.test.js
  - test/openclaw-install-transaction.test.js
  - test/artifact-admission.test.js
  - test/phase4-contracts.test.js
autonomous: true
gap_closure: true
requirements:
  - OCLW-01
  - OCLW-02
  - EVID-05
must_haves:
  truths:
    - "每次 public apply 都在零目标写入之前执行一次不可省略的新鲜生产 reprobe，并把完整新 fingerprint 与批准基线逐字段比较。"
    - "probe、target descriptor、admission carrier 和 canonical authority 只能由各自真实 companion closure 证明，手工拼出的自洽 JSON 不能自证。"
    - "OpenClaw 只从已验证字节复制出的私有可执行副本、在私有空 cwd 与合成环境中执行；源路径、cwd 或 target 在窗口内漂移均 fail closed。"
  artifacts:
    - path: src/openclaw-probe.js
      provides: "companion-backed probe producer、私有 executable copy 与 fresh observation"
    - path: src/openclaw-probe-contract.js
      provides: "closed nested probe contract、compatible status 与 provenance bindings"
    - path: src/openclaw-install-transaction.js
      provides: "non-optional production reprobe gate before journal/effects"
    - path: test/openclaw-probe.test.js
      provides: "self-auth、cwd、executable/target swap adversarial regression"
  key_links:
    - from: src/cli.js
      to: src/openclaw-install-transaction.js
      via: "CLI supplies exact package/target companion authorities to the mandatory reprobe path"
    - from: src/openclaw-install-transaction.js
      to: src/openclaw-probe.js
      via: "apply invokes fresh reprobe unconditionally and rejects any normalized fingerprint drift"
    - from: src/openclaw-probe.js
      to: src/artifact-registry.js
      via: "probe and carrier admission use registered validators plus actual companion closure"
---

## Phase Goal

**As a** developer, **I want to** deterministically generate an offline-inspectable least-trust Agent Package from an approved build contract and safely install it into OpenClaw after probe, preview, and exact-plan approval, **so that** I can authorize the exact package, target, and effects before any mutation.

<objective>
关闭 root gap 1，并修复 CR-01、CR-02、CR-09：把 apply 的 reprobe 从可选 adapter/旧结果回退改成不可省略的生产安全边界，消除 probe/carrier 自认证和 pathname-to-exec/cwd 竞态。

Purpose: 恢复 OCLW-01、OCLW-02 与 EVID-05 的真实前置证明，同时保持 PACK-01..05 的离线确定性与最小信任面。
Output: fail-first 攻击测试、closed probe/provenance contract、私有 verified executable copy、CLI 到 transaction 的 mandatory fresh reprobe wiring。
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-package/04-CONTEXT.md
@.planning/phases/04-package/04-RESEARCH.md
@.planning/phases/04-package/04-REVIEW.md
@.planning/phases/04-package/04-VERIFICATION.md
@.planning/phases/04-package/04-09-SUMMARY.md
@src/openclaw-probe.js
@src/openclaw-probe-contract.js
@src/openclaw-install-transaction.js
@src/artifact-registry.js
@src/cli.js
</context>

## Artifacts this phase produces

- 新鲜且 companion-backed 的 `openclaw.probe` 生产证据。
- 从验证过的文件句柄复制并锁定的 OpenClaw 私有可执行副本与私有 cwd。
- public apply 零写入前不可绕过的 reprobe/fingerprint gate。
- 覆盖 hand-built probe、carrier 伪造、cwd 污染、executable/target swap 的 adversarial tests。

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — 固化 self-auth 与 reprobe/exec-window 攻击</name>
  <files>test/openclaw-probe.test.js, test/openclaw-install-transaction.test.js, test/artifact-admission.test.js, test/phase4-contracts.test.js</files>
  <read_first>src/openclaw-probe.js, src/openclaw-probe-contract.js, src/openclaw-install-transaction.js, src/artifact-registry.js, src/cli.js, test/openclaw-probe.test.js, test/openclaw-install-transaction.test.js, test/artifact-admission.test.js, test/phase4-contracts.test.js</read_first>
  <behavior>
    - "手工构造但内部 digest 自洽的 probe、identity-only carrier、缺 companion 的 target authority 均被拒绝。"
    - "public API 与 CLI apply 在未提供生产 reprobe 输入、reprobe 不兼容、target/archive/conflict/probe 任一字段漂移时，在 journal 和 target 写入前失败。"
    - "在验证后替换 OpenClaw pathname、在三次 observation 间替换源文件、或在调用 cwd 放置恶意模块/配置时，不执行替换内容且不读取调用 cwd。"
    - "probe 顶层与 nested keys、required/status/compatibility 关系、source/provenance bindings 均 closed-world。"
  </behavior>
  <action>先写命名为 `rejects ...` 的对抗测试，并使用受控 fixture hook 精确触发 read→copy、copy→exec、probe→apply 窗口；断言进程 marker、journal、target 均不存在。测试必须分别证明旧的 adapter 缺省回退、自认证 carrier、关闭句柄后 pathname 执行和继承 cwd 会失败，不能只断言一般 schema error。D-38 要求所有 fixture 只使用 value-blind 假凭据标记，禁止把 stdout/stderr 原文写入证据。运行 RED 命令并确认这些新增用例因缺少安全行为而失败。</action>
  <acceptance_criteria>
    - "至少一个用例经 public `applyOpenClawInstallTransaction` 证明缺少 fresh reprobe 会在任何目标写入前失败。"
    - "至少两个 swap-window 用例分别覆盖 executable 与 target authority；cwd sentinel 用例证明调用目录不进入解析或执行。"
    - "hand-built probe/carrier 在 digest 自洽时仍因缺少真实 companion provenance 被拒绝。"
  </acceptance_criteria>
  <verify>
    <automated>node -e "const {spawnSync}=require('node:child_process');const r=spawnSync(process.execPath,['--test','--test-name-pattern','fresh reprobe|self-auth|private cwd|executable swap|target swap','test/openclaw-probe.test.js','test/openclaw-install-transaction.test.js','test/artifact-admission.test.js','test/phase4-contracts.test.js'],{stdio:'ignore'});if(r.status===0)process.exit(1)"</automated>
  </verify>
  <done>新增攻击测试以预期安全不变量失败，且失败原因对应 CR-01/02/09，而不是 fixture 语法错误。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — 建立 companion-backed probe 与 verified executable copy</name>
  <files>src/openclaw-probe.js, src/openclaw-probe-contract.js, src/artifact-registry.js, test/openclaw-probe.test.js, test/artifact-admission.test.js</files>
  <read_first>src/openclaw-probe.js, src/openclaw-probe-contract.js, src/artifact-registry.js, src/artifact-admission.js, src/openclaw-target-admission.js, src/openclaw-target-descriptor.js, test/openclaw-probe.test.js, test/artifact-admission.test.js</read_first>
  <behavior>
    - "probe producer re-admits package manifest/inventory/archive authority、target descriptor/admission 及其真实 companions，并把 source identity/digest closure 写入 closed contract。"
    - "只有 `status=compatible` 且 required observations 全部满足的 probe 可以进入 install basis。"
    - "可执行文件从保留的 no-follow handle 复制到 mode 0700 的私有空目录，复制前后 identity/digest 相同；三次 observation 都运行该副本并使用该私有目录作为 cwd。"
  </behavior>
  <action>按 D-32、D-33、D-34、D-38、D-41 实现：`probeOpenClawTarget` 必须重新读取并验证 canonical package、archive、target descriptor、admission carrier 以及 validator 所需 companions，不能从 carrier 自己复制 expected 字段制造 context。把 `openclaw-probe-contract` 改为 closed nested contract，纳入 producer/sourceBindings、兼容状态与完整 observation closure，并在 registry validator 中要求真实 companion context。读取 OpenClaw executable 时保留 handle，复制已验证字节到 `mkdtemp` 生成的 0700 私有目录、fsync、重新 digest/fstat；随后只以 `shell:false`、固定 argv、合成 HOME/STATE/PATH、私有 cwd 执行副本。每次 child 返回后重新验证源 handle/target members；任何差异返回 bounded incompatible result，不保留原始 stdout/stderr。</action>
  <acceptance_criteria>
    - "所有 probe public entrypoint 都需要真实 companion closure；不存在 identity-only 或 caller-asserted validation 分支。"
    - "执行路径不再使用已关闭 handle 对应的原 pathname，也不继承 `process.cwd()`。"
    - "probe artifact 能稳定重算 fingerprint，并把 incompatible 与 required-missing 显式绑定。"
  </acceptance_criteria>
  <verify>
    <automated>node --test test/openclaw-probe.test.js test/artifact-admission.test.js</automated>
  </verify>
  <done>companion/provenance、private executable copy、private cwd 和 closed status contract 的 GREEN 用例全部通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — 将 fresh reprobe 变成 public apply 的硬门</name>
  <files>src/openclaw-install-transaction.js, src/cli.js, test/openclaw-install-transaction.test.js, test/phase4-contracts.test.js</files>
  <read_first>src/openclaw-install-transaction.js, src/openclaw-probe.js, src/openclaw-probe-contract.js, src/cli.js, test/openclaw-install-transaction.test.js, test/phase4-contracts.test.js</read_first>
  <behavior>
    - "transaction 删除返回旧 `basis.probe` 的 fallback；fresh reprobe 缺失或失败即拒绝。"
    - "CLI 从用户给定的 exact authority paths 加载完整 companion set，并显式传给 production reprobe，而不是注入可替换 adapter。"
    - "fresh probe 的 package/archive/target/conflict/provenance/full fingerprint 与批准 basis 精确相等后才允许创建 journal。"
  </behavior>
  <action>按 D-35、D-36、D-37、D-41、D-42 收紧 `applyOpenClawInstallTransaction`：移除 `adapters` public key 和所有复用旧 probe 的分支，直接调用生产 `probeOpenClawTarget`；比较完整 normalized probe、archive binding、target id/config/workspace/capabilities、conflict set 与 source bindings。CLI apply 必须解析并加载 preview/approval 已绑定的 exact authority files 与 companions，不能接受 caller-supplied probe result。把 drift check 放在 authority/nonce 消费和 journal 之前；在本计划中 nonce 尚未持久化的现状不得被描述为完成，04-14 将关闭该独立 root gap。</action>
  <acceptance_criteria>
    - "`rg -n 'adapters|return basis\\.probe' src/openclaw-install-transaction.js` 不命中 public apply 的 reprobe 实现。"
    - "CLI 与直接 API 在任何 reprobe 输入缺失或漂移时均无 journal、无 target mutation、无 credential runner 调用。"
    - "PACK-01..05 现有 inspect/produce 测试不回退。"
  </acceptance_criteria>
  <verify>
    <automated>node --test test/openclaw-install-transaction.test.js test/phase4-contracts.test.js test/openclaw-probe.test.js</automated>
  </verify>
  <done>CR-01/02/09 的 RED 用例转绿，public apply 没有可选 reprobe 或 caller-probe 绕过路径。</done>
</task>

</tasks>

## Gap / Review Coverage

| Root gap | Findings | Attack path | Target invariant | Tasks |
|---|---|---|---|---|
| 1 mandatory trustworthy reprobe | CR-01 | API/CLI 缺省 adapter 后复用批准时 probe | 每次 apply 必有新生产 probe 且零写入前全指纹相等 | 1, 3 |
| 1 non-self-auth provenance | CR-02 | 手工 JSON 同时伪造 subject 与 expected context | authority 只能由外部 companion closure 证明 | 1, 2 |
| 1 exec/cwd TOCTOU | CR-09 | 关闭 handle 后替换 pathname；从调用 cwd 注入 | 只执行 verified private copy，cwd/env 私有且漂移 fail closed | 1, 2 |

## Multi-Source Coverage Audit

| Source | Items covered | Disposition |
|---|---|---|
| GOAL | probe、preview、exact approval 后才允许安全安装 | COVERED by mandatory pre-write gate |
| REQ | OCLW-01, OCLW-02, EVID-05 | COVERED |
| RESEARCH | no-follow reobservation、fresh fingerprint、bounded shell-free probe | COVERED |
| CONTEXT | D-32, D-33, D-34, D-35, D-36, D-37, D-38, D-41, D-42 | COVERED; D-39/D-40 由后续 closure plans 实现 |
| DEFERRED | Phase 5 run/schedule/restart/eval/Birth/Delivery | EXCLUDED |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| package/target files → AgentMo probe | 不可信路径、JSON、symlink 与 companion provenance 进入验证器 |
| verified bytes → child process | OpenClaw executable、argv、cwd、env 与 bounded output 跨进程边界 |
| fresh probe → install transaction | 动态 target state 决定是否允许进入 mutation authority |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-G12-01 | Spoofing | probe/carrier provenance | high | mitigate | Task 2 以真实 companion closure 和 registered validator 替代 caller-asserted context |
| T-04-G12-02 | Tampering | executable/target pathname window | high | mitigate | Task 2 从 retained handle 复制、复验 identity/digest，并仅执行私有副本 |
| T-04-G12-03 | Elevation of Privilege | inherited cwd/env | high | mitigate | Task 2 使用 0700 私有 cwd、合成 allowlisted env、fixed argv 与 shell:false |
| T-04-G12-04 | Tampering | apply reprobe bypass | high | mitigate | Task 3 删除 adapter/fallback public seam，并在 journal 前比较全 fingerprint |
| T-04-G12-05 | Information Disclosure | observation output | medium | mitigate | 仅保留 status/digest/bounded reason，拒绝 raw stdout/stderr 与 credential material |
| T-04-G12-SC | Tampering | external package install | low | accept | 本计划不新增 npm/pip/cargo 包，不下载或执行第三方安装脚本 |
</threat_model>

<verification>
- `node --test test/openclaw-probe.test.js test/openclaw-install-transaction.test.js test/artifact-admission.test.js test/phase4-contracts.test.js`
- `npm run check`
- `git diff --check`
</verification>

<success_criteria>
- CR-01、CR-02、CR-09 的所有攻击测试通过。
- direct API 与 CLI 均无法跳过 fresh production reprobe。
- PACK-01..05 的已有测试保持通过；本计划不宣称关闭 nonce、lifecycle、receipt 或 cleanup 的独立 gaps。
</success_criteria>

<output>
Create `.planning/phases/04-package/04-12-SUMMARY.md` when done.
</output>
