---
phase: 04-package
plan: 18
type: execute
wave: 17
depends_on:
  - 04-16
  - 04-17
files_modified:
  - package.json
  - src/builder-package.js
  - test/builder-packed-install.test.js
  - test/artifact-surface-coverage.test.js
  - test/phase4-contracts.test.js
autonomous: true
gap_closure: true
requirements:
  - PACK-01
  - PACK-02
  - PACK-03
  - PACK-04
  - PACK-05
  - OCLW-01
  - OCLW-02
  - OCLW-03
  - OCLW-04
  - OCLW-05
  - EVID-05
must_haves:
  truths:
    - "从 `npm pack` 解出的 tarball 在隔离临时目录可显式构建并准入 safe-fs helper，随后完成 produce→inspect→fresh probe→preview→exact approvals→install→upgrade→rollback→uninstall→strict receipts 的完整 fixture 旅程。"
    - "packed inventory 包含全部 Phase 4 runtime/source/validator modules 与 native source，不含私有 authority、host-built binary、secret、raw transcript、网络下载或 package lifecycle hook。"
    - "packed journey 重跑六个 root gaps、CR-01..10、WR-01..03 的关键负向路径，并保持 PACK-01..05 regression coverage。"
  artifacts:
    - path: package.json
      provides: "exact published source/runtime inventory and check entrypoints"
    - path: src/builder-package.js
      provides: "Builder packed runtime/source closure"
    - path: test/builder-packed-install.test.js
      provides: "extracted-tarball full Phase 4 fixture journey"
    - path: test/artifact-surface-coverage.test.js
      provides: "exact public/runtime/native-source surface coverage"
  key_links:
    - from: package.json
      to: test/builder-packed-install.test.js
      via: "exact published files list is unpacked and exercised outside the repository"
    - from: src/builder-package.js
      to: test/artifact-surface-coverage.test.js
      via: "runtime inventory and source coverage include every 04-12..17 module and native source"
---

## Phase Goal

**As a** developer, **I want to** deterministically generate an offline-inspectable least-trust Agent Package from an approved build contract and safely install it into OpenClaw after probe, preview, and exact-plan approval, **so that** I can authorize the exact package, target, and effects before any mutation.

<objective>
完成 Phase 4 gap closure 的 packed/runtime closure：锁定发布 inventory，并在 extracted tarball 上重跑完整安全 lifecycle 与 adversarial regressions。

Purpose: 防止 source checkout 通过但发布包缺少 native source、runtime validator 或依赖仓库私有路径。
Output: exact packed inventory、surface coverage 与 extracted-tarball full lifecycle smoke。
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-package/04-CONTEXT.md
@.planning/phases/04-package/04-REVIEW.md
@.planning/phases/04-package/04-VERIFICATION.md
@.planning/phases/04-package/04-12-SUMMARY.md
@.planning/phases/04-package/04-13-SUMMARY.md
@.planning/phases/04-package/04-14-SUMMARY.md
@.planning/phases/04-package/04-15-SUMMARY.md
@.planning/phases/04-package/04-16-SUMMARY.md
@.planning/phases/04-package/04-17-SUMMARY.md
@package.json
@src/builder-package.js
@test/builder-packed-install.test.js
</context>

## Artifacts this phase produces

- `package.json.files` 与 `src/builder-package.js` 的 exact Phase 4 runtime/native-source closure。
- extracted-tarball full lifecycle fixture。
- packed negative regressions for six root gaps, CR-01..10, WR-01..03 and PACK-01..05。

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED→GREEN — 锁定 published inventory 与 public surface closure</name>
  <files>package.json, src/builder-package.js, test/artifact-surface-coverage.test.js, test/phase4-contracts.test.js</files>
  <read_first>package.json, src/builder-package.js, test/artifact-surface-coverage.test.js, test/phase4-contracts.test.js, .planning/phases/04-package/04-12-SUMMARY.md, .planning/phases/04-package/04-13-SUMMARY.md, .planning/phases/04-package/04-14-SUMMARY.md, .planning/phases/04-package/04-15-SUMMARY.md, .planning/phases/04-package/04-16-SUMMARY.md, .planning/phases/04-package/04-17-SUMMARY.md</read_first>
  <behavior>
    - "缺少任一 04-12..17 runtime、validator、native source 或 public check entry 时 inventory test 失败。"
    - "published inventory 包含 `native/openclaw-fs-kernel.c` 与 JS facade，但不包含 host-built helper binary、authority state 或 planning-private evidence。"
    - "package 不引入 preinstall/install/postinstall、network fetch、PATH-only helper 或 checkout-relative runtime import。"
  </behavior>
  <action>先扩展 `test/artifact-surface-coverage.test.js` 与 `test/phase4-contracts.test.js`，枚举 04-12..17 新增的 native source、safe-fs facade、authority marker store、official action runner、receipt/admission/runtime closure，运行 focused RED 并确认缺项会失败。随后按 D-32、D-33、D-34、D-38、D-42 scoped update `package.json.files`、`npm run check` entry 与 `src/builder-package.js` inventory；只发布 source/runtime/tests 所需内容，不发布 helper binary、build receipt、nonce markers、真实 OpenClaw state、`.env`、raw logs 或 private phase reports。04-13 owns native build/admit/apply semantics；本任务只 owns final packed inventory/regression closure。</action>
  <acceptance_criteria>
    - "inventory negative 先 RED 后 GREEN，且每个 security-critical module/native source 有 exact assertion。"
    - "tar listing 无 executable helper、authority state、secret-shaped file、install hook 或 checkout-private path。"
    - "PACK-01..05 surface assertions保持通过。"
  </acceptance_criteria>
  <verify>
    <automated>node --test test/artifact-surface-coverage.test.js test/phase4-contracts.test.js</automated>
  </verify>
  <done>published inventory 与 Builder runtime closure exact、可审计且不携带 host/private authority。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — 在 extracted tarball 重跑完整安全旅程</name>
  <files>test/builder-packed-install.test.js, test/phase4-contracts.test.js</files>
  <read_first>test/builder-packed-install.test.js, test/phase4-contracts.test.js, package.json, src/builder-package.js, .planning/phases/04-package/04-12-SUMMARY.md, .planning/phases/04-package/04-13-SUMMARY.md, .planning/phases/04-package/04-14-SUMMARY.md, .planning/phases/04-package/04-15-SUMMARY.md, .planning/phases/04-package/04-16-SUMMARY.md, .planning/phases/04-package/04-17-SUMMARY.md</read_first>
  <behavior>
    - "从 tarball 解出到临时目录，以 explicit `--binary-out`/`--receipt-out` 构建 helper，再以 helper/receipt/external-digest tuple 执行 packed apply。"
    - "packed CLI 完成 produce、inspect、fresh probe、preview、三类 exact approvals、四 lifecycle 与 strict receipts。"
    - "fresh-process nonce replay、double-writer marker、target/helper swap、false-complete、publisher replacement 与 nested recipe attacks 均 fail closed。"
    - "fixture 不读取 checkout-private import、真实 `.env`、真实 OpenClaw/user state 或网络。"
  </behavior>
  <action>本任务独占 `test/builder-packed-install.test.js` 的 packed source/build journey：扩展 extracted-tarball fixture，在隔离 target/authority/build roots 使用 fake OpenClaw，显式运行 04-13 `--binary-out`/`--receipt-out` build command，re-admit helper/receipt/external-digest tuple，再通过 public apply 复验 source、closed env、`/usr/bin/cc` toolchain fingerprint、argv、binary digest 与 file identity。依次执行 package produce/inspect、fresh probe、preview、ordinary/conflict/sensitive approval、install/upgrade/rollback/uninstall 与 receipt re-admission。加入 root gaps 1..6、CR-01..10、WR-01..03 的代表性 packed negatives，特别保留 helper receipt drift、PATH lookup/autobuild/fallback rejection、per-nonce double writer/crash marker、unknown replacement preservation、false complete 与 nested recipe closure；不得导入 source checkout、访问网络或真实 secret/OpenClaw state。</action>
  <acceptance_criteria>
    - "extracted tarball full journey 成功，PACK-01..05/OCLW-01..05/EVID-05 fixture assertions 全覆盖。"
    - "六个 root gaps、CR-01..10、WR-01..03 均至少有 packed positive/negative mapping。"
    - "fixture 证明 helper 只能显式 build/admit，不经 PATH lookup、autobuild 或 JS mutation fallback。"
  </acceptance_criteria>
  <verify>
    <automated>node --test test/builder-packed-install.test.js test/phase4-contracts.test.js</automated>
  </verify>
  <done>发布 tarball 自包含并通过完整 Phase 4 fixture 旅程与 packed adversarial regressions。</done>
</task>

</tasks>

## Global Gap / Review Coverage

| Root gap | Findings | Primary closure | Packed proof |
|---|---|---|---|
| 1 mandatory trustworthy reprobe | CR-01, CR-02, CR-09 | 04-12 | fresh probe/self-auth/swap/cwd |
| 2 durable nonce + credential route | CR-05, CR-06 | 04-14 | double-writer/crash/partial-marker replay + argv confusion |
| 3 safe mutation/recovery | CR-03, CR-04 | 04-13 | native retained-dirfd/no-replace/build-admission attacks |
| 4 strict receipt semantics | CR-07, CR-08 | 04-16 | false-complete and authority/result substitution |
| 5 four lifecycle/genesis seam | WR-01, WR-02 | 04-15 | actual install→upgrade→rollback→uninstall |
| 6 publisher cleanup/path projection | CR-10, WR-03 | 04-17 | replacement preservation + nested recipes |

## Multi-Source Coverage Audit

| Source | Complete item set | Coverage |
|---|---|---|
| GOAL | approved contract → deterministic offline package → probe/preview/exact approval → safe mutation | COVERED by packed journey |
| REQ | PACK-01..05, OCLW-01..05, EVID-05 | COVERED |
| RESEARCH | companion admission, fresh reprobe, safe filesystem, durable nonce, official routes, lifecycle, receipts, packed closure | COVERED |
| CONTEXT | D-32..D-42 | COVERED by primary plans and packed regression |
| REVIEW | CR-01..10, WR-01..03 | COVERED |
| VERIFICATION | root gaps 1..6 | COVERED |
| DEFERRED | Phase 5 live run/schedule/restart/eval/Birth/Delivery | EXCLUDED |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source tree → npm tarball → extracted runtime | publication may omit or substitute security-critical modules |
| extracted runtime → isolated fixture | packed code may attempt checkout/private/network dependencies |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-G18-01 | Tampering | npm packed inventory | high | mitigate | Task 1 exact inventory and public-surface assertions |
| T-04-G18-02 | Tampering | native source/build distribution | high | mitigate | Task 2 packs source/facade only and explicitly builds/admit receipt |
| T-04-G18-03 | Spoofing | checkout-private dependency | high | mitigate | Task 2 executes extracted tree in isolated roots with no checkout import |
| T-04-G18-04 | Information Disclosure | tarball/fixture evidence | high | mitigate | Tasks 1-2 reject secrets, authority state, raw logs and real user state |
| T-04-G18-SC | Tampering | dependency/install lifecycle | high | mitigate | No new dependency; tests reject install hooks/network/prebuilt helper |
</threat_model>

<verification>
- `node --test test/builder-packed-install.test.js test/artifact-surface-coverage.test.js test/phase4-contracts.test.js`
- `npm pack --dry-run`
- `git diff --check`
</verification>

<success_criteria>
- Packed inventory/runtime closure is exact and source-only for the native helper.
- Extracted tarball proves all six root gaps, CR-01..10, WR-01..03 and PACK-01..05 regression mappings.
- This plan produces no docs, release, review, verifier or phase-completion artifact; those remain in 04-19/post gate.
</success_criteria>

<output>
Create `.planning/phases/04-package/04-18-SUMMARY.md` when done.
</output>
