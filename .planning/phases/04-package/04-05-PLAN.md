---
phase: 04-package
plan: "05"
type: execute
wave: 5
depends_on:
  - 04-04
files_modified:
  - src/openclaw-probe.js
  - src/artifact-contract.js
  - src/artifact-registry.js
  - src/artifact-subjects.js
  - src/cli.js
  - test/openclaw-probe.test.js
  - test/phase4-contracts.test.js
autonomous: true
requirements:
  - OCLW-01
  - OCLW-05
must_haves:
  truths:
    - "A developer can create a durable normalized OpenClaw capability fingerprint without mutating the selected target or the operator's HOME/state."
    - "The fingerprint covers exact source/executable identity, runtime/version, CLI/JSON contract, workspace/skill/plugin/MCP surfaces, sandbox/tool policy, permission route, target roots/config and conflict state."
    - "Any drift from the approved exact target or any probe surface changes the fingerprint and invalidates compatibility."
    - "The probe re-admits the exact Plan 04-02 target/carrier authority; the installed 2026.7.1-2 target cannot pass under the old approval."
    - "The durable fingerprint also binds the D-42 archive SHA-256, internal manifest digest, canonical inventory digest and complete member closure; no package root or standalone manifest is accepted."
  artifacts:
    - path: "src/openclaw-probe.js"
      provides: "Direct-FS and synthetic-HOME capability observation"
      exports: ["OPENCLAW_PROBE_SCHEMA_VERSION", "OpenClawProbeError", "probeOpenClawTarget", "validateOpenClawProbe"]
    - path: "test/openclaw-probe.test.js"
      provides: "No-live-write, drift and shell/env isolation matrix"
  key_links:
    - from: "src/openclaw-probe.js"
      to: "src/runtime-compatibility.js"
      via: "current-process target runtime barrier before child process"
      pattern: "assertCurrentOpenClawTargetRuntime"
    - from: "src/cli.js"
      to: "src/openclaw-probe.js"
      via: "`openclaw-probe` durable command"
      pattern: "openclaw-probe"
---

## Phase Goal

**As a** developer, **I want to** deterministically generate an offline-inspectable, least-trust Agent Package from the approved build contract, **so that** I can separately authorize a safe OpenClaw installation.

<objective>
Deliver a read-only, drift-sensitive compatibility fingerprint before install planning.

Purpose: Replace version/command-existence guesses with the exact D-41 target capability basis.
Output: Durable probe artifact and `openclaw-probe` CLI that isolates every OpenClaw process in a disposable synthetic HOME.
</objective>

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-package/04-04-SUMMARY.md
@.planning/phases/04-package/04-02-SUMMARY.md
@src/runtime-compatibility.js
@src/builder-package.js
@src/artifact-admission.js
@src/cli.js
@docs/OPENCLAW_RUNTIME_NOTES.md

<interfaces>
Use `assertCurrentOpenClawTargetRuntime()` with no caller inputs before process/file effects.
Use fixed executable and argv arrays with `shell:false`; construct an explicit non-secret environment rather than inheriting the process environment.
The probe artifact identity is `agentmo.openclaw-probe.v1`; its digest binds normalized facts, never shadow paths or raw output.
</interfaces>
</context>

## Artifacts this phase produces

| Kind | Name | Contract |
|---|---|---|
| Module | `src/openclaw-probe.js` | Exports `OPENCLAW_PROBE_SCHEMA_VERSION`, `OpenClawProbeError`, `probeOpenClawTarget`, `validateOpenClawProbe` |
| Artifact contract | `src/artifact-contract.js` | Exposes schema/template for subject `openclaw-probe`, identity `agentmo.openclaw-probe.v1` |
| Artifact registry | `src/artifact-registry.js` | Registers `openclaw-probe` with `validateOpenClawProbe` |
| Command subjects | `src/artifact-subjects.js` | Extends `DURABLE_COMMAND_SUBJECTS["openclaw-probe"]` with deterministic archive and target-carrier-admission bindings |
| CLI | `src/cli.js` | Adds `openclaw-probe --archive <file> --archive-sha256 <exact> --target-carrier-admission <file> --target-root <path> --out <absent-file> --json` |
| Test | `test/openclaw-probe.test.js` | Direct-FS/synthetic-HOME isolation, exact version and drift invalidation |
| Incremental test | `test/phase4-contracts.test.js` | Wave 5 probe/admission and non-runtime boundary extension |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing read-only probe contract</name>
  <read_first>
    - src/runtime-compatibility.js
    - src/builder-package.js
    - test/runtime-compatibility.test.js
    - test/runtime-compatibility-seams.test.js
    - .planning/phases/04-package/04-RESEARCH.md
  </read_first>
  <files>test/openclaw-probe.test.js, test/phase4-contracts.test.js</files>
  <behavior>
    - Real HOME/state/config/workspace sentinels are byte-identical before and after every probe.
    - Only a newly created synthetic HOME/state/config/workspace may change and it is discarded afterward.
    - Same version with changed JSON fields, flags, policy, permission route, paths or conflict bytes yields a different fingerprint.
    - 2026.7.1-2 is incompatible with the old exact 2026.6.11 / 29d018f0 contract; exact reapproved targets use their own binding.
    - Missing/stale target-carrier admission or drift between its approved recipe digest and the recipe-derived native-plugin archive members fails before any child process.
    - Malformed/oversized/secret-shaped/raw-path child output fails closed without persisting raw stdout/stderr.
    - Archive digest, manifest digest, inventory digest, member set/path/type/mode/digest or retained identity drift fails before target observation or child process.
  </behavior>
  <action>Build the RED integration/security suite with fake OpenClaw executables and target trees, then extend the incremental Phase 4 gate. Model direct retained reads separately from CLI shadow observation. Require the Plan 04-03 deterministic archive path plus external SHA-256 and cover D-42 archive/manifest/inventory/member/identity drift before any target observation. Cover startup writes, source-revision mismatch, target-carrier admission drift, path/symlink/case-Unicode conflicts, shell metacharacters and inherited environment canaries. Enforce D-35 and D-41: default scope is isolated project and the fingerprint binds the entire target surface.</action>
  <acceptance_criteria>
    - Tests prove process execution cannot reach the operator HOME.
    - Tests compare normalized fingerprints, not raw command output.
    - No test reads `.env` or real credential/session content.
  </acceptance_criteria>
  <verify>
    <automated>! node --test test/openclaw-probe.test.js test/phase4-contracts.test.js</automated>
  </verify>
  <done>The RED suite specifies a complete read-only capability fingerprint and exact drift behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the isolated probe and public durable route</name>
  <read_first>
    - test/openclaw-probe.test.js
    - src/artifact-registry.js
    - src/cli.js
    - src/runtime-compatibility.js
  </read_first>
  <files>src/openclaw-probe.js, src/artifact-contract.js, src/artifact-registry.js, src/artifact-subjects.js, src/cli.js</files>
  <behavior>
    - Archive and target bytes use bounded retained no-follow observation and normalized relative identities.
    - CLI observations run under a disposable HOME with explicit state/config/workspace and bounded output.
    - Durable artifact is create-only, exact-admissible, stable and value-blind.
  </behavior>
  <action>Implement `probeOpenClawTarget` as the only production probe service. Re-admit the exact Plan 04-02 artifact and the deterministic archive path/external digest before observation; validate D-42's internal manifest digest, canonical inventory digest and complete member closure and record that binding in the probe. Capture target paths/config/conflicts directly, then run only manifest/eligibility/config-dry-run observations inside a synthetic HOME. Never runtime-load plugin code, connect MCP, invoke credentials, schedules, models or an agent. The current v1 has no MCP package or activation route. Register the probe schema and command subject, add human/JSON formatting, and fail closed on exact target mismatch per D-41.</action>
  <acceptance_criteria>
    - `openclaw-probe` writes one absent-path `agentmo.openclaw-probe.v1` artifact after full validation.
    - Explicit environment allowlist contains no credential values and `shell:false` is invariant.
    - The result separates compatible/incompatible/unsupported facts without support certification.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/openclaw-probe.test.js test/phase4-contracts.test.js</automated>
  </verify>
  <done>A developer can obtain an exact, read-only OpenClaw fingerprint that every later preview and approval can bind.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Target filesystem → fingerprint | Untrusted paths/config/conflicts become install authority inputs |
| AgentMo → OpenClaw child process | Executable, argv, env and output cross a process boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-16 | Tampering | archive/target paths/conflicts | high | mitigate | D-42 archive closure plus retained no-follow target reads, collision checks and exact conflict digests. |
| T-04-17 | Elevation of Privilege | child process | high | mitigate | Fixed executable/argv, `shell:false`, explicit env allowlist, timeout and bounded redacted output. |
| T-04-18 | Information Disclosure | HOME/auth/session state | high | mitigate | Disposable synthetic HOME/state/config/workspace; real-home sentinels and no credential/profile reads. |
| T-04-19 | Elevation of Privilege | plugin/MCP probe | high | mitigate | Manifest-only/disabled observation; no plugin import or MCP connection/start. |
</threat_model>

<verification>
Wave 5 gate: `node --test test/package-inspect.test.js test/openclaw-probe.test.js test/phase4-contracts.test.js`.
</verification>

<success_criteria>
- Fingerprint changes for every relevant target/CLI/policy/path/conflict drift.
- Real operator state is unchanged.
- Exact approved target mismatch blocks planning and apply.
</success_criteria>

<output>
Create `.planning/phases/04-package/04-05-SUMMARY.md` when done.
</output>
