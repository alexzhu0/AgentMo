---
phase: 01.2-openclaw-runtime-release-evidence
plan: "02"
subsystem: runtime-compatibility-seams
tags: [node, openclaw, runtime-gate, zero-effects, tdd]

requires:
  - phase: 01.2-openclaw-runtime-release-evidence
    plan: "01"
    provides: immutable runtime ranges and the zero-argument current-process OpenClaw assertion
provides:
  - closed inventory over three JavaScript OpenClaw mutation journeys and the final production spawn barrier
  - zero-effect incompatible-runtime contracts for scaffold, live run, live replay, and final child spawn
  - lowest-seam production gates that preserve AgentMo-target and non-live core behavior
affects: [01.2-03-runtime-caller-parity, 01.2-04-release-evidence]

tech-stack:
  added: []
  patterns:
    - target-specific orchestration gate before mutation preflight
    - unconditional final no-argument assertion immediately before the sole production JavaScript spawn
    - isolated current-process version simulation as contract evidence only

key-files:
  created:
    - test/runtime-compatibility-seams.test.js
  modified:
    - src/scaffold.js
    - src/run-state.js
    - src/runtime-execution.js
    - test/helpers/io-surface-inventory.js

key-decisions:
  - "Only OpenClaw scaffold and live run/replay are gated; AgentMo scaffold and dry/non-live core work remain available on Node >=20."
  - "runRuntimeCommand repeats the canonical zero-argument assertion immediately before the sole production spawn even after an orchestration-level check."
  - "Production APIs gain no runtime version, provider, override, or bypass authorization input."

patterns-established:
  - "Early plus final enforcement: orchestration rejects before runtime-env/index/file effects, and the child adapter rechecks at the spawn seam."
  - "Closed anti-bypass inventory: exactly three JavaScript mutation journeys plus one final spawn barrier are statically and dynamically verified."

requirements-completed: [COMP-01]

coverage:
  - id: D1
    description: "Incompatible OpenClaw scaffold, live run, live replay, and final spawn attempts leave requested roots, runner recorders, and marker files empty."
    requirement: COMP-01
    verification:
      - kind: integration
        ref: "test/runtime-compatibility-seams.test.js#zero-effect incompatible-runtime matrix"
        status: pass
      - kind: other
        ref: "node --test test/runtime-compatibility-seams.test.js"
        status: pass
    human_judgment: false
  - id: D2
    description: "OpenClaw JavaScript mutation seams call only the canonical current-process assertion before effects while core-only and non-live work remain callable."
    requirement: COMP-01
    verification:
      - kind: integration
        ref: "node --test test/runtime-compatibility-seams.test.js test/scaffold.test.js test/run-state.test.js test/runtime-replay-eval.test.js test/runtime-execution.test.js"
        status: pass
      - kind: other
        ref: "npm run check"
        status: pass
    human_judgment: false

duration: 7m 49s
completed: 2026-07-13
status: complete
---

# Phase 01.2 Plan 02: OpenClaw JavaScript Runtime Seam Summary

**OpenClaw scaffold、live run、live replay 与最终 child spawn 现在都在首个可观察副作用前执行同一零参数 current-process runtime assertion，同时保留 Node 20 core-only 路径。**

## Performance

- **Duration:** 7m 49s
- **Started:** 2026-07-13T04:29:43Z
- **Completed:** 2026-07-13T04:37:32Z
- **Tasks:** 2/2
- **Files modified:** 5

## Accomplishments

- 建立闭合 JavaScript inventory：三条 OpenClaw mutation journey 加唯一生产 spawn barrier，并以静态顺序和动态零副作用证据共同约束。
- OpenClaw scaffold 在 build-state/materialization/publication 前拒绝；live run/replay 在 runtime-env 与 run-index/output preflight 前拒绝。
- `runRuntimeCommand` 在唯一生产 `spawn` 前再次读取当前进程 runtime；生产调用者传入 version/provider/bypass-shaped options 也不能改变授权结果。
- AgentMo-target scaffold、dry plan/run-plan、non-live run/replay、report/eval/observation surface 不受 OpenClaw target floor 约束。

## Task Commits

| Task | Outcome | Commit |
| --- | --- | --- |
| Task 1 — Lock the closed JavaScript mutation inventory and empty-effect failures | RED contract recorded, then passed after production wiring | Not created — repository no-commit contract |
| Task 2 — Gate scaffold, live run, live replay, and final spawn at their lowest seams | Four lowest-seam checks wired and regressions passed | Not created — repository no-commit contract |

## TDD Gate Compliance

- **RED:** `node --test test/runtime-compatibility-seams.test.js` exited 1 with 2 passing and 5 failing tests. The five failures were missing expected rejection/canonical call failures for scaffold, live run, live replay, and final spawn; fixture admission and core-floor tests passed.
- **GREEN:** the same command passed 7/7 after only the planned production gates were added.
- **Regression:** the five-suite focused command passed 61/61.
- Commits were intentionally not created; RED/GREEN command evidence replaces commit hashes under the repository no-commit contract.

## Files Created/Modified

- `test/runtime-compatibility-seams.test.js` — closed seam inventory, anti-bypass checks, contract-only incompatible simulation, empty-effect matrix, and core-floor preservation.
- `src/scaffold.js` — gates only the selected OpenClaw target after target resolution and before build-state/materialization/publication.
- `src/run-state.js` — gates live run and live replay before runtime-env and run-index/output preflight.
- `src/runtime-execution.js` — repeats the zero-argument assertion immediately before the sole production JavaScript spawn.
- `test/helpers/io-surface-inventory.js` — rebases exact line identities moved by the new imports/assertions without changing owner/status classification.

## Decisions Made

- 使用既有 `assertCurrentOpenClawTargetRuntime()` 作为唯一 mutation authority；没有新增通用 semver、provider 或 options-based authorization 层。
- 上层 gate 保护 runtime-env/index/file preflight，最终 spawn gate 独立防止 alternate orchestration 绕过。
- Node 20 current-process simulation 只记为合约证据；没有把它写成真实 Node 20 execution evidence。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Rebased exact I/O surface line identities**

- **Found during:** Task 2 repository verification.
- **Issue:** 新增 import 与 assertion 移动了 `src/scaffold.js` 和 `src/run-state.js` 中既有 I/O surface 的精确行号，导致闭合 inventory 快照按设计失败。
- **Fix:** 只更新对应 exact line identities；没有增加、删除或重新分类任何 I/O surface，也没有放宽 scanner。
- **Files modified:** `test/helpers/io-surface-inventory.js`.
- **Verification:** `node --test test/artifact-surface-coverage.test.js` passed 10/10; `npm run check` passed 418/418.
- **Commit:** Not created — repository no-commit contract.

**Total deviations:** 1 auto-fixed (1 blocking correctness fix).
**Impact on plan:** 无架构或生产范围扩张；该修复保持 Phase 1.1 的 closed I/O inventory。

## Threat Model Verification

| Threat | Disposition | Evidence |
| --- | --- | --- |
| T-04 alternate mutation path | Mitigated | 闭合 inventory 固定 scaffold/live run/live replay/final spawn 四个 JavaScript seam，且唯一生产 spawn inventory 为 1。 |
| T-05 rejected filesystem flow | Mitigated | 不兼容时 scaffold/output roots、runner effect lists 与 child marker 均保持空/不存在。 |
| T-06 final child spawn | Mitigated | `assertCurrentOpenClawTargetRuntime();` 与 `spawn(...)` 相邻，且 marker child 未创建。 |
| T-07 core-only commands | Accepted as scoped | AgentMo scaffold、dry plan/run-plan 和 non-live run/replay/eval 在模拟 Node 20 下仍可调用。 |

## Threat Surface Scan

- 未引入新 network endpoint、auth path、schema、dependency 或 production filesystem operation。
- 生产变更只在既有 trust boundary 前增加拒绝；新增临时文件操作仅存在于隔离测试 fixture。
- 没有发现计划 threat model 之外的新 security-relevant surface，因此无 `Threat Flags`。

## Known Stubs

None — modified production and test files contain no TODO/FIXME/placeholder stub.

## Issues Encountered

- 唯一问题是 exact I/O line-address snapshot 的预期漂移；按 Rule 3 最小重基线后，focused inventory 与完整仓库门槛均通过。

## Verification

- `node --test test/runtime-compatibility-seams.test.js` — RED: exit 1, 2 pass / 5 fail; GREEN: PASS 7/7.
- `node --test test/runtime-compatibility-seams.test.js test/scaffold.test.js test/run-state.test.js test/runtime-replay-eval.test.js test/runtime-execution.test.js` — PASS 61/61.
- `node --test test/artifact-surface-coverage.test.js` — initial exact-line snapshot failure; after bounded rebase PASS 10/10.
- `npm run check` — PASS 418/418 across 44 suites.
- `git diff --check` — PASS.
- Static anti-bypass scan over the three production files — PASS, zero version/provider/bypass authorization identifiers.
- Stub scan over all five changed implementation/test files — PASS, zero matches.

## User Setup Required

None — no dependency, secret, provider, environment value, or external service configuration was added.

## Next Phase Readiness

- Plan 01.2-03 can gate direct shell mutations and align generated runbook, target hints, and maintained docs with this same runtime contract.
- Real Node 20 execution remains intentionally untested here and must be recorded only by Plan 01.2-04's actual lane.
- No live OpenClaw/provider command ran, and no runtime smoke, domain-quality certification, or production approval is claimed.

## Self-Check: PASSED

- All five implementation/test files and this SUMMARY exist on disk.
- Coverage metadata parses with 2/2 deliverables automatically backed by passing verification and zero schema errors.
- RED/GREEN, five-suite focused regression, closed I/O inventory, repository-wide, anti-bypass, stub, and whitespace gates passed as recorded above.
- Commit verification is not applicable: commits were intentionally not created under the repository no-commit contract.
- `.planning/STATE.md`, `.planning/ROADMAP.md`, and `.planning/REQUIREMENTS.md` were not modified by this executor.

---
*Phase: 01.2-openclaw-runtime-release-evidence*
*Completed: 2026-07-13*
