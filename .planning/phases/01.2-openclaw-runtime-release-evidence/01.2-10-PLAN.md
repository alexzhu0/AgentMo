---
phase: 01.2-openclaw-runtime-release-evidence
plan: "10"
type: execute
wave: 1
depends_on: ["01.2-07"]
files_modified:
  - src/scaffold-files.js
  - src/targets/openclaw.js
  - test/command-docs.test.js
  - test/helpers/openclaw-shell-contract.js
  - README.md
  - docs/MVP_RUNBOOK.md
  - docs/OMX_SESSION_MIGRATION.md
  - docs/OPENCLAW_RUNTIME_NOTES.md
  - docs/RUNTIME_EXECUTION.md
autonomous: true
gap_closure: true
requirements: [COMP-01]
must_haves:
  truths:
    - "Every generated OpenClaw mutation/effect is control-dependent on successful canonical runtime preflight, not merely textually later."
    - "The bounded caller checker preserves shell control-edge semantics and rejects newline, semicolon, pipe, OR-list, and unknown-wrapper bypasses."
    - "Only an explicit success edge such as && or an equivalently proven success branch can admit a mutation."
  artifacts:
    - { path: src/scaffold-files.js, provides: "fail-closed generated OpenClaw command blocks" }
    - { path: src/targets/openclaw.js, provides: "success-dominated target verification hints" }
    - { path: test/helpers/openclaw-shell-contract.js, provides: "bounded control-edge-aware shell contract parser" }
    - { path: test/command-docs.test.js, provides: "negative bypass matrix across generated and maintained command surfaces" }
  key_links:
    - { from: src/scaffold-files.js, to: test/command-docs.test.js, via: "generated command extraction and dominance assertion", pattern: "runtime-check.*&&" }
    - { from: test/helpers/openclaw-shell-contract.js, to: test/command-docs.test.js, via: "separator-preserving segment graph", pattern: "assertPreflightDominatesMutation" }
---

<objective>
Close the shell control-edge gap so OpenClaw effects occur only after a successful canonical preflight.

Purpose: Text order is not execution order; generated guidance and its checker must agree on fail-closed shell semantics.
Output: Success-chained guidance plus a bounded, separator-aware contract test harness.
</objective>

## Phase Goal

开发者可以在真实 Node.js 20 lane 运行 AgentMo core，并确保每一条 OpenClaw target mutation 路径在任何进程或文件副作用前经过不可由生产调用者绕过的 target runtime gate。

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-VERIFICATION.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-07-SUMMARY.md
@src/scaffold-files.js
@src/targets/openclaw.js
@test/command-docs.test.js

<interfaces>
This is a bounded contract parser for AgentMo-owned generated guidance, not a general shell parser. It must preserve the recognized separators `&&`, `||`, `;`, newline, and `|`; reject unsupported ambiguity; and prove a success edge from canonical `agentmo runtime-check --target openclaw` to each effect.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make bypass control edges fail before changing generated guidance</name>
  <read_first><file>test/command-docs.test.js</file><file>src/scaffold-files.js</file><file>src/targets/openclaw.js</file><file>.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-VERIFICATION.md</file></read_first>
  <files>test/helpers/openclaw-shell-contract.js, test/command-docs.test.js</files>
  <behavior>
    - `runtime-check || openclaw ...`, `runtime-check ; openclaw ...`, a bare newline, and `runtime-check | openclaw ...` are rejected.
    - Mutation before preflight, mutation after an unknown wrapper, or a later branch not dominated by success is rejected.
    - `runtime-check && openclaw ...` is accepted; any supported multiline form must prove equivalent fail-fast/success-branch semantics explicitly.
    - A previous preflight cannot permanently bless later disconnected mutations.
  </behavior>
  <action>Extract or replace the current splitter with a tiny dependency-free tokenizer/segment graph that retains recognized control operators. Add table-driven RED canaries for every bypass from the verifier, plus disconnected and unknown-token cases. Fail closed on quoting/control syntax outside the bounded grammar rather than guessing. Use the helper against actual generated runbooks and verification hints; do not invoke a shell or execute OpenClaw.</action>
  <verify><automated>node --test --test-name-pattern="control edge|dominates|bypass|generated OpenClaw" test/command-docs.test.js</automated></verify>
  <acceptance_criteria>
    - Every unsafe separator fixture fails for a distinct fail-closed reason.
    - A prior preflight followed by an unrelated safe command does not admit a later mutation.
    - At least one exact canonical `&&` fixture passes.</acceptance_criteria>
  <done>The checker proves success dominance rather than textual precedence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Put all generated OpenClaw effects behind the proven success edge</name>
  <read_first><file>src/scaffold-files.js</file><file>src/targets/openclaw.js</file><file>test/helpers/openclaw-shell-contract.js</file><file>test/command-docs.test.js</file><file>README.md</file><file>docs/MVP_RUNBOOK.md</file><file>docs/OMX_SESSION_MIGRATION.md</file><file>docs/OPENCLAW_RUNTIME_NOTES.md</file><file>docs/RUNTIME_EXECUTION.md</file></read_first>
  <files>src/scaffold-files.js, src/targets/openclaw.js, test/command-docs.test.js, README.md, docs/MVP_RUNBOOK.md, docs/OMX_SESSION_MIGRATION.md, docs/OPENCLAW_RUNTIME_NOTES.md, docs/RUNTIME_EXECUTION.md</files>
  <behavior>
    - Optional live blocks, `agents add`, binding examples, setup effects, and target verification hints chain canonical preflight to every effect with `&&` or another parser-proven success branch.
    - No `mkdir`, redirection, temp/state creation, OpenClaw command, or provider invocation is reachable on preflight failure.
    - Generated commands remain portable and repository/workspace-relative where required.
    - Maintained command blocks discovered by the same closed corpus obey the same success-edge contract; the checker is not weakened or skipped until Plan 12.
  </behavior>
  <action>Rewrite each affected generated and maintained block so the canonical preflight success edge dominates all subsequent effects. Prefer short explicit `&&` chains over implicit shell state. Apply the same contract to target verification hints. Extend extraction assertions so each concrete mutation/effect is checked, not just the first command in a block. Preserve value-blind examples and existing OpenClaw adapter boundaries. This Rule 2 scope closure is required because `test/command-docs.test.js` intentionally enforces generated and maintained surfaces together; do not relax or defer its maintained-corpus assertions.</action>
  <verify><automated>node --test test/command-docs.test.js test/scaffold.test.js test/targets.test.js</automated></verify>
  <acceptance_criteria>
    - All generated guidance passes the separator-aware dominance checker.
    - Mutation/effect inventory is explicit and has no unexamined command block.
    - Existing scaffold and target adapter contracts pass without OpenClaw execution.</acceptance_criteria>
  <done>Generated guidance is fail-closed in actual shell control flow and the contract suite rejects all known bypass shapes.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Trust Boundaries

| Boundary | Description |
| --- | --- |
| generated text -> operator shell | Documentation becomes executable guidance and must encode success dependence explicitly. |
| bounded parser -> admission assertion | Unsupported or lossy syntax must fail closed rather than be interpreted optimistically. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-10-01 | Elevation | shell OR/list separators | high | mitigate | Preserve control edges and require a canonical preflight success edge for each effect. |
| T-10-02 | Tampering | generated guidance | high | mitigate | Test extracted production strings against an explicit mutation inventory. |
| T-10-03 | Repudiation | caller closure evidence | medium | mitigate | Table-driven unsafe/pass fixtures demonstrate exact accepted grammar. |
</threat_model>

<prohibitions>
- Do not implement or claim a general shell parser, execute OpenClaw/provider commands, or accept lossy unknown syntax.
- Do not read `.env`, touch sibling repositories, add dependencies, or update release status in this plan.
- Do not stage or commit.
</prohibitions>

## Artifacts this phase produces

- A bounded control-edge-aware test helper and explicit bypass matrix.
- Generated scaffold/adapter guidance whose effects are success-dominated by canonical runtime preflight.

## Multi-Source Coverage Audit

- VERIFICATION gap 2: fully owned by this plan.
- COMP-01: proves generated caller closure only; it does not certify arbitrary third-party shell.
- D-14: success is derived from an executed preflight edge, not a caller-supplied marker.

<verification>Run both Task commands. Any accepted newline, `;`, `||`, pipe, disconnected mutation, or generated effect without a success edge leaves this plan incomplete.</verification>
<success_criteria>AgentMo-owned OpenClaw command guidance cannot continue into mutation/effect work when canonical runtime preflight fails.</success_criteria>
<output>Create `01.2-10-SUMMARY.md`; do not commit.</output>
