---
phase: 01.2-openclaw-runtime-release-evidence
plan: "06"
type: execute
wave: 1
depends_on: ["01.2-02"]
files_modified:
  - src/cli.js
  - test/cli.test.js
  - test/fixtures/incompatible-node-runtime.js
autonomous: true
gap_closure: true
requirements: [COMP-01]
must_haves:
  truths:
    - "OpenClaw scaffold and live run/replay reject an incompatible current process before blueprint, runtime-plan, run-state, message, env, or output-root intake."
    - "Mutating CLI parsing has a syntax-only pass; the zero-argument current-process assertion dominates every materializer and artifact loader while lower seam assertions remain intact."
    - "Missing and unreadable input canaries return the stable runtime rejection and leave all requested roots absent, while non-live/core-only commands preserve existing behavior."
  artifacts:
    - { path: src/cli.js, provides: "syntax-only mutating parsers and pre-intake OpenClaw assertion ordering" }
    - { path: test/cli.test.js, provides: "subprocess ordering canaries for scaffold, live run, and live replay" }
    - { path: test/fixtures/incompatible-node-runtime.js, provides: "isolated test-only current-process simulation, never execution evidence" }
  key_links:
    - { from: src/cli.js, to: src/runtime-compatibility.js, via: "assert immediately after syntax parse and before materialization/load", pattern: "assertCurrentOpenClawTargetRuntime" }
    - { from: test/cli.test.js, to: bin/agentmo.js, via: "preloaded incompatible-process subprocess with missing/unreadable canaries", pattern: "AGENTMO_OPENCLAW_RUNTIME_UNSUPPORTED" }
---

<objective>
Close the second verification gap by moving OpenClaw runtime rejection to the production CLI's zero-input boundary.

Purpose: Incompatible processes must not read artifacts, messages, or environment material before the canonical gate.
Output: Syntax-only parsing, pre-intake dispatch barriers, and fail-closed CLI ordering tests.
</objective>

## Phase Goal

开发者可以在真实 Node.js 20 lane 运行 AgentMo core，并确保每一条 OpenClaw target mutation 路径在任何进程或文件副作用前经过不可由生产调用者绕过的 target runtime gate。

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-CONTEXT.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-VERIFICATION.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-REVIEW.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-02-SUMMARY.md

<interfaces>
`assertCurrentOpenClawTargetRuntime()` is the zero-argument production authority. Current `parseRunArgs` and `parseReplayRunArgs` materialize message/env bytes; `parseScaffoldArgs` is already syntax-only. Lower barriers in `scaffoldAgent`, `executeRuntimeRun`, `replayRunState`, and `runRuntimeCommand` must remain defense in depth.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove runtime rejection dominates every CLI input canary</name>
  <read_first><file>src/cli.js</file><file>src/runtime-compatibility.js</file><file>test/cli.test.js</file><file>test/runtime-compatibility-seams.test.js</file></read_first>
  <files>test/cli.test.js, test/fixtures/incompatible-node-runtime.js</files>
  <behavior>
    - In an isolated test-only process reporting `20.19.0`, OpenClaw scaffold with a missing/unreadable blueprint returns `AGENTMO_OPENCLAW_RUNTIME_UNSUPPORTED` before artifact admission.
    - Live run and live replay with missing runtime artifacts plus unreadable message/env canaries return the same runtime error before request/admission errors.
    - Requested scaffold/run/replay roots and marker paths remain absent; output is value-blind and contains none of the canary paths or contents.
    - Non-live run/replay, `run-plan`, reports/evals/observation, and AgentMo-target scaffold retain their prior request/admission semantics.
  </behavior>
  <action>Add RED subprocess tests using a dedicated preload fixture that changes `process.versions.node` only inside the child test process and is explicitly labeled contract-only evidence. Cover missing file, directory-as-unreadable-file, message-file, env-file, and absent-output-root cases for all three mutating CLI journeys. Add source-order assertions that syntax parsers contain no `readFile`/materialization and that dispatch calls the zero-argument assertion before materializers/loaders. Do not add any production version override or dependency-injection bypass.</action>
  <verify><automated>node --test test/cli.test.js</automated></verify>
  <acceptance_criteria>
    - RED demonstrates the current `AGENTMO_CLI_REQUEST_REJECTED` ordering defect for scaffold, live run, and live replay.
    - Every canary is value-blind and every requested output root remains absent.
    - The fixture is not referenced by production code or release execution evidence.</acceptance_criteria>
  <done>The exact pre-intake failure contract is fixed before the CLI refactor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Split syntax parsing from materialization and assert before intake</name>
  <read_first><file>test/cli.test.js</file><file>src/cli.js</file><file>src/runtime-compatibility.js</file><file>src/run-state.js</file><file>src/scaffold.js</file></read_first>
  <files>src/cli.js, test/cli.test.js</files>
  <behavior>
    - Scaffold parses flags, then gates only target `openclaw`, then loads the blueprint.
    - Run/replay parse flags without reading or materializing; when `--live` is present they gate immediately, then materialize message/env/path options and load the runtime-plan/run-state.
    - Lower orchestration and final-spawn assertions remain present and production APIs still accept no runtime authorization input.
  </behavior>
  <action>Refactor per D-14 into unambiguous syntax-only parse helpers and separate post-gate materializers. Place `assertCurrentOpenClawTargetRuntime()` directly in each dispatch path after syntactic validity is known but before `loadAdmittedBlueprint`, `loadAdmittedArtifact`, `loadRunState`, `materializeTransientMessageOptions`, `materializeTransientEnvOptions`, Buffer creation, or path/output preflight. Keep malformed syntax errors ahead of the runtime gate, because no file/value intake occurs there; preserve all non-live/core paths and every lower barrier. Do not weaken digest validation, persistability, error redaction, birth evidence, or proposal-only observation.</action>
  <verify><automated>node --check src/cli.js &amp;&amp; node --test test/cli.test.js test/runtime-compatibility.test.js</automated></verify>
  <acceptance_criteria>
    - All incompatible-runtime canaries now return the stable runtime code before input-specific errors.
    - Existing successful CLI tests and non-live semantics remain green.
    - Production code has no runtime value/provider/options/bypass input.</acceptance_criteria>
  <done>The production CLI fails closed at zero input and retains lower defense-in-depth barriers.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Trust Boundaries

| Boundary | Description |
| --- | --- |
| CLI tokens -> artifact/message/env readers | Syntactically valid untrusted locations and values must not be consumed before runtime authorization. |
| CLI dispatch -> lower mutation seams | The outer barrier must complement, not replace, direct-import and final-spawn barriers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-06-01 | Elevation / Tampering | CLI mutation dispatch | high | mitigate | Syntax-only pass followed by zero-argument assertion before every loader/materializer. |
| T-06-02 | Information disclosure | message/env/artifact intake | high | mitigate | Missing/unreadable canaries prove no input-specific error or content crosses the boundary first. |
| T-06-03 | Repudiation | alternate CLI path | high | mitigate | Closed scaffold/live-run/live-replay source-order and subprocess matrix. |
| T-06-04 | Denial of service | non-mutating core commands | low | accept | Gate remains scoped to OpenClaw mutation; regression tests document the tolerable core-path behavior. |
</threat_model>

<prohibitions>
- Do not read `.env`, expose a production version provider, move validation after file reads, or remove lower seam assertions.
- Do not gate non-live/core/report/eval/observation paths or change artifact schemas/evidence promotion.
- Do not run live OpenClaw/provider commands, stage, or commit.
</prohibitions>

## Artifacts this phase produces

- New test-only `test/fixtures/incompatible-node-runtime.js` preload fixture.
- Modified/added internal CLI identifiers for syntax-only run/replay parsing and post-gate transient materialization; no net-new standalone production artifact.
- New CLI ordering canaries for missing/unreadable blueprint, runtime-plan, run-state, message, env, and absent output roots.

## Multi-Source Coverage Audit

- GOAL: closes pre-file/process-side-effect authorization at the production CLI boundary.
- REQ COMP-01: covers scaffold/live run/replay zero-input target enforcement.
- RESEARCH: preserves the lowest-seam barriers while adding the missing outer pre-intake defense.
- CONTEXT D-14: current-process evidence remains non-injectable and precedes every artifact/message/env intake.

<verification>Run focused CLI/runtime contracts. Shared exact I/O inventory rebasing, `npm run check`, and `git diff --check` occur once in dependent Plan 08 after parallel edits settle.</verification>
<success_criteria>All three mutating CLI journeys reject an incompatible process before any artifact/message/env read or output-root effect, without narrowing AgentMo core behavior.</success_criteria>
<output>Create `01.2-06-SUMMARY.md`; do not commit.</output>
