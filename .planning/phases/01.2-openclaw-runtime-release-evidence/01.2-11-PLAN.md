---
phase: 01.2-openclaw-runtime-release-evidence
plan: "11"
type: execute
wave: 1
depends_on: ["01.2-05"]
files_modified:
  - scripts/node20-core-lane.sh
  - scripts/node20-core-receipt.js
  - scripts/node20-distribution-trust.json
  - test/node20-core-runner.test.js
  - test/node20-core-lane.test.js
  - test/runtime-compatibility-seams.test.js
  - test/runtime-evidence-consumers.test.js
autonomous: true
gap_closure: true
requirements: [COMP-01]
must_haves:
  truths:
    - "A repo-owned immutable trust anchor, selected internally by exact version and architecture, binds the approved Node distribution independently of caller-supplied files."
    - "A caller-created archive and matching caller-created checksum manifest cannot produce or preserve a Node 20 receipt."
    - "Public AGENTMO_NODE20_* environment markers cannot bypass receipt/matrix/release consumer validation."
    - "Runtime identity, command-set digest, archive/provenance digests, and exact batch counts remain bound before atomic receipt publication."
  artifacts:
    - { path: scripts/node20-distribution-trust.json, provides: "repo-owned approved Node 20 distribution anchor" }
    - { path: scripts/node20-core-receipt.js, provides: "anchor-bound producer and receipt contract" }
    - { path: test/node20-core-runner.test.js, provides: "self-signed distribution rejection canary" }
    - { path: test/runtime-evidence-consumers.test.js, provides: "post-publication non-bypassable receipt consumer checks" }
  key_links:
    - { from: scripts/node20-core-receipt.js, to: scripts/node20-distribution-trust.json, via: "internal version/arch lookup with no caller override", pattern: "node20-distribution-trust" }
    - { from: test/runtime-evidence-consumers.test.js, to: release/evidence/2026.07.13-node20-core-receipt.json, via: "unconditional post-publication receipt correspondence", pattern: "readNode20Receipt" }
---

<objective>
Close provenance and consumer-bypass gaps with an independent offline trust anchor and unconditional receipt correspondence.

Purpose: Internally consistent caller files and public environment markers are not independent execution evidence.
Output: Anchored distribution validation, self-signing rejection, and consumer tests that always require real receipt facts.
</objective>

## Phase Goal

开发者可以在真实 Node.js 20 lane 运行 AgentMo core，并确保每一条 OpenClaw target mutation 路径在任何进程或文件副作用前经过不可由生产调用者绕过的 target runtime gate。

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-VERIFICATION.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-05-SUMMARY.md
@scripts/node20-core-lane.sh
@scripts/node20-core-receipt.js
@test/node20-core-runner.test.js
@test/node20-core-lane.test.js
@test/runtime-compatibility-seams.test.js
@release/evidence/2026.07.13-node20-core-receipt.json

<interfaces>
Add one versioned repository-owned trust document for the approved `20.20.2` / `arm64` / `darwin` archive. It pins archive name, archive SHA-256, checksum-manifest SHA-256, archive member, and member/executable SHA-256. The production runner selects it internally; neither CLI flags nor environment variables may replace or extend the anchor.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject self-signed distributions against a repo-owned anchor</name>
  <read_first><file>scripts/node20-core-receipt.js</file><file>scripts/node20-core-lane.sh</file><file>release/evidence/2026.07.13-node20-core-receipt.json</file><file>test/node20-core-runner.test.js</file></read_first>
  <files>scripts/node20-distribution-trust.json, scripts/node20-core-receipt.js, scripts/node20-core-lane.sh, test/node20-core-runner.test.js</files>
  <behavior>
    - Exact expected version/platform/architecture resolve to one pinned anchor; unknown or ambiguous selections fail before owned test execution.
    - Caller archive and checksum-manifest digests, archive/member names, extracted member digest, selected executable digest, and current process identity all equal the anchor and each other where applicable.
    - A matching fake archive plus fake manifest is rejected with a stable trust-anchor error and no receipt publication.
    - There is no `--trust-anchor`, environment override, network download, PATH fallback, or test-only production bypass.
  </behavior>
  <action>Add a minimal closed JSON anchor using the already observed official Node 20.20.2 darwin-arm64 distribution facts. Validate its schema, unique selector, lowercase SHA-256 fields, expected archive/member naming, and immutable selection before executing the command manifest. Keep low-level fixture tests separated from the production producer: test helpers may exercise pure digest logic, but the receipt-producing CLI must always use the repository anchor. Replace any contract test that currently proves self-signing success with a negative canary.</action>
  <verify><automated>node --test test/node20-core-runner.test.js &amp;&amp; bash -n scripts/node20-core-lane.sh</automated></verify>
  <acceptance_criteria>
    - A self-created internally consistent archive/manifest pair fails before the first owned command and leaves no receipt.
    - Tampering any pinned name or digest fails closed.
    - Existing realpath, process identity, exact command manifest, TAP count, and atomic publication tests remain green.</acceptance_criteria>
  <done>The producer can only attest the exact approved offline distribution named by repository policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove public marker shortcuts from receipt consumers</name>
  <read_first><file>test/node20-core-lane.test.js</file><file>test/runtime-compatibility-seams.test.js</file><file>scripts/node20-core-receipt.js</file><file>docs/RUNTIME_COMPATIBILITY.md</file><file>release/2026.07.13.md</file></read_first>
  <files>test/node20-core-lane.test.js, test/runtime-compatibility-seams.test.js, test/runtime-evidence-consumers.test.js, scripts/node20-core-receipt.js</files>
  <behavior>
    - Setting all public `AGENTMO_NODE20_*` variables under a real Node 20 process cannot skip receipt parsing or matrix/release correspondence checks.
    - Lane-only tests may use a marker to activate process-local assertions, but that marker is never treated as durable evidence by consumer suites.
    - Receipt/matrix/release consumer tests run only after receipt publication; they are not part of the producer's fixed Node 20 command set and therefore cannot form a circular self-attestation.
    - Consumer assertions compare exact receipt version/architecture/digests/command set and exact per-batch plus aggregate counts; broad runtime/count regexes are insufficient.
    - Missing, stale, or mismatched receipt facts fail even when marker variables are present.
  </behavior>
  <action>Move the receipt/matrix/release consumer cases out of the Node 20 producer-owned seam batch into `test/runtime-evidence-consumers.test.js`; keep runtime/gate seam tests in the real Node 20 fixed command set and update its exact expected counts. Delete the early-return branches and centralize receipt parsing/cross-check helpers where useful. Add a child-process canary that supplies the old public marker set and a missing or tampered receipt context, and require failure. Keep lane activation limited to tests that observe the selected process itself. Derive exact release/current-host/count assertions from structured receipt or command results rather than permissive patterns. This split is an evidence-ordering boundary, not a reduction in coverage: Plan 12 runs consumers after publishing the fresh receipt.</action>
  <verify><automated>node --test test/node20-core-runner.test.js test/node20-core-lane.test.js test/runtime-compatibility-seams.test.js &amp;&amp; node --test --test-name-pattern="outside the producer|missing receipt|stale receipt" test/runtime-evidence-consumers.test.js</automated></verify>
  <acceptance_criteria>
    - The historical marker-only bypass fixture fails.
    - No consumer `return` or skip path is controlled by `AGENTMO_NODE20_CORE_LANE`.
    - The producer command manifest contains no receipt/matrix/release consumer test and still has exact nonzero runtime-seam counts.
    - Exact aggregate and batch counts, version, architecture, and digests are required for tested status.</acceptance_criteria>
  <done>Environment markers can activate runtime-local tests but cannot manufacture or preserve release evidence. The full exact durable correspondence case remains intentionally RED until Plan 12 publishes a fresh receipt and reconciles matrix/release facts.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Trust Boundaries

| Boundary | Description |
| --- | --- |
| repository policy -> caller files | Caller files are untrusted until their exact identities match the independent pinned anchor. |
| producer environment -> receipt consumer | Environment is process input, not durable evidence authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-11-01 | Spoofing | Node distribution provenance | high | mitigate | Internal exact selector plus pinned archive/manifest/member digests. |
| T-11-02 | Tampering | caller archive/manifest | high | mitigate | Compare both caller artifacts to immutable repo anchor before execution. |
| T-11-03 | Elevation / Repudiation | public marker | high | mitigate | Remove consumer shortcut and require unconditional receipt correspondence. |
| T-11-04 | Information disclosure | receipt | medium | mitigate | Preserve value-blind schema; never persist paths, raw streams, or secret-bearing state. |
</threat_model>

<prohibitions>
- Do not download at runtime, read `.env`, trust a caller-supplied anchor, add a marker bypass, or persist absolute paths/raw logs.
- Do not execute OpenClaw/provider live work, touch sibling repositories, add dependencies, or promote certification claims.
- Do not stage or commit.
</prohibitions>

## Artifacts this phase produces

- Versioned offline Node distribution trust anchor.
- Anchor-bound producer and negative self-signing tests.
- Post-publication receipt consumers that always verify exact structured evidence without joining the producer command set.

## Multi-Source Coverage Audit

- VERIFICATION gaps 3 and 4: mechanism portions fully owned by this plan; refreshed durable evidence is owned by Plan 12.
- COMP-01: strengthens runtime execution provenance and evidence consumption, not target-live execution.
- D-14: neither caller environment nor caller files self-certify evidence.

<verification>Run both Task commands. Any caller override, fake self-consistent distribution success, marker-only pass, broad count/version match, or raw/path-bearing receipt leaves this plan incomplete.</verification>
<success_criteria>Only the pinned approved Node distribution can produce evidence, and only exact durable receipt correspondence can preserve tested status.</success_criteria>
<output>Create `01.2-11-SUMMARY.md`; do not commit.</output>
