---
phase: 01.2-openclaw-runtime-release-evidence
plan: "06"
subsystem: cli-runtime-boundary
tags: [node, openclaw, cli, runtime-gate, zero-input, tdd]

requires:
  - phase: 01.2-openclaw-runtime-release-evidence
    plan: "02"
    provides: lower scaffold, live run/replay, and final-spawn runtime barriers
provides:
  - syntax-only run and replay argument passes separated from post-gate materialization
  - application-level CLI runtime barriers before blueprint, runtime-plan, run-state, message, and env intake
  - isolated contract-only subprocess canaries for all three OpenClaw mutation journeys
affects: [01.2-08-release-evidence, cli-runtime-contract, openclaw-mutations]

tech-stack:
  added: []
  patterns:
    - syntax validation before current-process authorization
    - zero-argument target assertion before application loaders and materializers
    - lower runtime barriers retained as defense in depth

key-files:
  created:
    - test/fixtures/incompatible-node-runtime.js
  modified:
    - src/cli.js
    - test/cli.test.js

key-decisions:
  - "Only OpenClaw scaffold and live run/replay receive the outer CLI gate; non-live and core-only commands keep their existing admission behavior."
  - "Malformed syntax and digest-shape rejection stay ahead of runtime authorization because they consume no file or transient value bytes."
  - "The subprocess preload changes only process.versions.node in a test child and is never imported by production or release execution."

patterns-established:
  - "Parse -> authorize -> materialize/load: target mutation dispatch must preserve this order."
  - "Application parser tests pass '--' after Node preload options so application flags are delivered to cli.js unchanged."

requirements-completed: [COMP-01]

coverage:
  - id: D1
    description: "OpenClaw scaffold, live run, and live replay reject an incompatible current process before artifact, message, env, or output-root intake."
    requirement: COMP-01
    verification:
      - kind: integration
        ref: "test/cli.test.js#rejects incompatible OpenClaw mutations before artifact, message, env, or output-root intake"
        status: pass
      - kind: other
        ref: "node --test test/cli.test.js test/runtime-compatibility.test.js"
        status: pass
    human_judgment: false
  - id: D2
    description: "Run and replay parsing is syntax-only, and every outer assertion dominates its materializer and durable loader while lower seam assertions remain green."
    requirement: COMP-01
    verification:
      - kind: unit
        ref: "test/cli.test.js#keeps mutating CLI parsers syntax-only and gates dispatch before every materializer or loader"
        status: pass
      - kind: integration
        ref: "node --test test/runtime-compatibility-seams.test.js"
        status: pass
    human_judgment: false
  - id: D3
    description: "Malformed syntax remains first, and non-live/core-only commands keep prior request and artifact-admission semantics under a simulated incompatible process."
    requirement: COMP-01
    verification:
      - kind: integration
        ref: "test/cli.test.js#keeps syntax rejection and non-live/core admission behavior ahead of the target runtime gate"
        status: pass
    human_judgment: false

duration: 8m
completed: 2026-07-13
status: complete
---

# Phase 01.2 Plan 06: CLI Pre-Intake Runtime Gate Summary

**OpenClaw scaffold、live run 与 live replay 现在都遵循“纯语法解析 -> 零参数 current-process 授权 -> transient materialization / durable artifact load”，并保留下层 mutation/spawn barrier。**

## Performance

- **Duration:** 8m
- **Started:** 2026-07-13T08:41:30Z
- **Completed:** 2026-07-13T08:48:45Z
- **Tasks:** 2/2
- **Files modified:** 3 implementation/test files + 1 summary

## Accomplishments

- 将 `run` 与 `replay-run` 拆成无 `readFile`、`Buffer.from`、路径解析或 materializer 调用的 syntax-only parser，以及仅在 runtime gate 之后执行的 materializer。
- `scaffold --target openclaw` 在 blueprint admission 前拒绝；`run --live` 与 `replay-run --live` 在 message/env/path materialization 及 runtime-plan/run-state admission 前拒绝。
- 新增隔离 preload fixture 和 6 组 missing/unreadable/value-blind canary；所有请求的 scaffold/run/replay 输出根都保持不存在。
- 新增不兼容进程下的 malformed syntax、AgentMo scaffold、non-live run/replay、run-plan、report/eval/observation 回归，证明 core-only 行为未被 OpenClaw target floor 扩大拦截。
- `src/scaffold.js`、`src/run-state.js`、`src/runtime-execution.js` 的 lower barriers 未删除或放宽，focused seam suite 保持 10/10。

## Task Commits

| Task | Outcome | Commit |
| --- | --- | --- |
| Task 1 — Prove runtime rejection dominates every CLI input canary | RED contract established with isolated subprocess and source-order assertions | 未创建——仓库合同要求用户显式授权提交 |
| Task 2 — Split syntax parsing from materialization and assert before intake | GREEN implementation and focused regressions passed | 未创建——仓库合同要求用户显式授权提交 |

## TDD Gate Compliance

- **RED:** `node --test test/cli.test.js` exited 1 with 28/30 passing and 2 failing. The failures showed scaffold returned `AGENTMO_ARTIFACT_READ_FAILED` before the runtime error and the required syntax-only parser markers did not yet exist.
- **GREEN:** `node --check src/cli.js && node --test test/cli.test.js test/runtime-compatibility.test.js` passed 37/37.
- **Defense in depth:** `node --test test/runtime-compatibility-seams.test.js` passed 10/10.
- TDD commits were intentionally not created under the repository no-commit contract; command evidence records the RED/GREEN transition.

## Files Created/Modified

- `src/cli.js` — adds application-level scaffold/live-run/live-replay barriers and splits run/replay syntax parsing from post-gate materialization.
- `test/cli.test.js` — adds incompatible-process subprocess canaries, source-order assertions, absent-root checks, and non-live/core behavior regressions.
- `test/fixtures/incompatible-node-runtime.js` — contract-only preload that changes `process.versions.node` to `20.19.0` inside the child test process.
- `.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-06-SUMMARY.md` — execution evidence and remaining-risk record.

## Decisions Made

- 生产授权继续只调用既有零参数 `assertCurrentOpenClawTargetRuntime()`；没有引入 version/provider/options/bypass 输入。
- syntax-only pass 完成 option presence、互斥、target 与 digest binding shape 检查；这些检查不读取文件或创建 Buffer，因此仍可先于 runtime gate 返回稳定 request error。
- preload subprocess 在 Node options 后显式使用 `--`，以验证参数已经交付给 `cli.js` 之后的 application boundary；没有借 fixture 形成 production runtime evidence。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Prevented the Node test launcher from consuming the application `--env-file` canary**

- **Found during:** Task 2 GREEN verification.
- **Issue:** 当前 Node launcher 会把 preload 后未用 `--` 分隔的 `--env-file` 当作自身参数，导致 child 在进入 `cli.js` 前 exit 9；该结果不能检验计划定义的 application CLI boundary。
- **Fix:** 测试 helper 使用 `node --import <fixture> -- <cli> ...`，只隔离 launcher options 与 application options；production code、flag 与文档均未改变。
- **Files modified:** `test/cli.test.js`.
- **Verification:** focused GREEN passed 37/37，包含 unreadable env/message canary。
- **Commit:** 未创建——仓库合同要求用户显式授权提交。

**Total deviations:** 1 auto-fixed (1 test-harness blocker).
**Impact on plan:** 未扩大 production API 或文件范围；测试现在准确覆盖 args 已交付给 `cli.js` 后的零输入边界。

## Threat Model Verification

| Threat | Disposition | Evidence |
| --- | --- | --- |
| T-06-01 CLI mutation dispatch | Mitigated | 三条 dispatch 的 source-order contract 与 subprocess runtime rejection 均通过。 |
| T-06-02 message/env/artifact disclosure | Mitigated at application boundary | missing、directory-as-file 与 value canary 均先返回 bounded runtime code，stdout/stderr 不含路径或内容。 |
| T-06-03 alternate CLI path | Mitigated | scaffold、live run、live replay 全覆盖，lower seam suite 10/10。 |
| T-06-04 non-mutating core commands | Accepted as scoped | non-live/core admission matrix 保持原错误语义。 |

## Threat Surface Scan

- 未新增 network endpoint、auth path、artifact schema、dependency、writer 或 child-process surface。
- production 变更只把既有 file-read/materialization 延后到既有 runtime assertion 之后。
- 没有发现计划 threat model 之外的新 security surface，因此无新增 `Threat Flags`。

## Known Stubs

None — 指定 implementation/test 文件中没有 TODO、FIXME、placeholder 或空实现。

## Issues Encountered

- 真实 `node ./bin/agentmo.js ... --env-file <path>` 与 shebang 入口在当前 Node 24 上也会由 Node launcher 先处理同名 flag；这是 `cli.js` 启动前的既有 launcher/flag collision，不是本计划 application-boundary 重构可以在限定文件内消除的问题。按主执行器决定，本计划不改 flag、`bin/` 或 docs；Plan 08/re-verification 必须把该事实保留为 remaining risk，不能把 application-level gate 表述成 Node launcher 级保证。
- `npm run check` 已实际运行；focused product suites 全绿，但 repository gate 在 `test/artifact-surface-coverage.test.js` 的 exact line/surface baseline 失败。该 baseline 同时包含已完成 Plan 05 的新增 runner/receipt surfaces与本计划移动后的 `src/cli.js` line identities；依批准的 gap plan，统一 rebasing 和最终 full-suite pass 属于依赖 05–07 的 Plan 08，不在本执行器允许修改的文件范围内。

## Verification

- `node --test test/cli.test.js` — RED: exit 1, 28 pass / 2 fail.
- `node --check src/cli.js && node --test test/cli.test.js test/runtime-compatibility.test.js` — PASS 37/37.
- `node --test test/runtime-compatibility-seams.test.js` — PASS 10/10.
- `npm run check` — executed; FAILED only at the exact artifact/output surface inventory baseline pending the planned Plan 08 combined rebase.
- `git diff --check` — PASS.
- Production bypass/stub/reference scan — PASS: no runtime provider/version/bypass input and no production reference to the test preload fixture.

## User Setup Required

None — no dependency, secret, provider call, live runtime, or external service was used.

## Next Phase Readiness

- Plan 07 may proceed independently; Plan 08 must merge the exact I/O inventory shifts from Plans 05–07, rerun the full repository gate, and keep Node launcher `--env-file` collision as an explicit remaining risk unless a later approved scope changes the flag/entrypoint contract.
- No OpenClaw live/provider command ran. The passing canaries are contract evidence only and do not certify runtime execution, domain quality, production readiness, or deployment approval.

## Self-Check: PASSED

- `src/cli.js`, `test/cli.test.js`, `test/fixtures/incompatible-node-runtime.js`, and this SUMMARY all exist.
- RED/GREEN, source-order, lower-seam, value-blind, absent-root, non-live/core, syntax, and whitespace evidence was observed as recorded.
- The repository-wide inventory failure is explicitly assigned to the already-planned dependent Plan 08 and was not hidden or weakened.
- Commit verification is not applicable: no stage or commit was created under the repository contract.
- `.planning/STATE.md`, `.planning/ROADMAP.md`, and `.planning/REQUIREMENTS.md` were not modified by this executor.

---
*Phase: 01.2-openclaw-runtime-release-evidence*
*Completed: 2026-07-13*
