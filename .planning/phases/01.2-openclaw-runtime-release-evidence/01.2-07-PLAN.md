---
phase: 01.2-openclaw-runtime-release-evidence
plan: "07"
type: execute
wave: 1
depends_on: ["01.2-03"]
files_modified:
  - src/targets/openclaw.js
  - test/command-docs.test.js
  - README.md
  - docs/MVP_RUNBOOK.md
  - docs/RUNTIME_EXECUTION.md
  - docs/OPENCLAW_RUNTIME_NOTES.md
  - docs/OMX_SESSION_MIGRATION.md
autonomous: true
gap_closure: true
requirements: [COMP-01]
must_haves:
  truths:
    - "Every generated verification hint is classified and checked independently; a direct OpenClaw mutation carries its own canonical runtime preflight and cannot borrow one from another array element."
    - "The maintained caller scanner normalizes bounded shell wrappers, absolute launchers, variable launchers, and package runners, and fails closed on every unknown caller or mutation."
    - "Executable guidance under README.md and docs/**/*.md uses repository-relative paths or explicit placeholders and contains no host/user-specific absolute path."
  artifacts:
    - { path: src/targets/openclaw.js, provides: "self-contained OpenClaw mutation verification hint" }
    - { path: test/command-docs.test.js, provides: "per-hint dominance, bounded shell normalization, fail-on-unknown caller closure, and portable-path corpus gate" }
  key_links:
    - { from: src/targets/openclaw.js, to: src/runtime-compatibility.js, via: "canonical preflight embedded in the same mutation hint", pattern: "runtime-check.*openclaw" }
    - { from: test/command-docs.test.js, to: README.md, via: "recursive maintained Markdown command/path corpus", pattern: "markdownFiles" }
---

<objective>
Close the third verification gap by making generated hints independently safe and the maintained caller/document corpus fail closed across realistic launcher forms.

Purpose: Copied hints, wrapped launchers, and stale machine paths must not bypass or weaken the canonical OpenClaw runtime contract.
Output: Self-contained target hints, a bounded shell-token classifier, portable maintained guidance, and adversarial caller/path canaries.
</objective>

## Phase Goal

开发者可以在真实 Node.js 20 lane 运行 AgentMo core，并确保每一条 OpenClaw target mutation 路径在任何进程或文件副作用前经过不可由生产调用者绕过的 target runtime gate。

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-CONTEXT.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-VERIFICATION.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-REVIEW.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-03-SUMMARY.md

<interfaces>
`openClawTarget.verificationHints` is an array of independently copyable operator hints. Current `generatedCallerSurfaces()` joins that array before checking dominance, while `DIRECT_OPENCLAW_COMMAND` recognizes only line-leading bare `openclaw`/`run_openclaw`. The maintained corpus is recursively discovered from `README.md` and `docs/**/*.md`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify per-hint dominance and fail-closed launcher normalization</name>
  <read_first><file>src/targets/openclaw.js</file><file>test/command-docs.test.js</file><file>src/scaffold-files.js</file><file>src/runtime-compatibility.js</file></read_first>
  <files>test/command-docs.test.js</files>
  <behavior>
    - Each `verificationHints` element is parsed as its own executable surface; a mutation element without an earlier canonical preflight in that same element fails even when another hint contains the gate.
    - The bounded tokenizer recognizes leading assignments, `env`, `command`, bare/quoted absolute paths, `$OPENCLAW_BIN`/`${OPENCLAW_BIN}`, `run_openclaw`, and supported npm/pnpm package-runner forms without executing shell text.
    - Every normalized OpenClaw caller is classified as inspection or a closed mutation; an unknown wrapper, launcher, subcommand, mutation verb, ambiguous tokenization, or unclassified OpenClaw-shaped token fails closed.
    - Preflight dominance is checked within the same logical command sequence before each mutation, not by file-wide substring order.
  </behavior>
  <action>Replace the narrow regex/join assumptions with RED table-driven canaries and a dependency-free bounded shell tokenizer/normalizer inside the test contract. Cover `env KEY=value`, `command`, absolute, quoted-variable, bare-variable, `pnpm openclaw`, and package-exec launcher forms plus unsupported wrapper/mutation cases. Preserve recursive production-renderer and maintained-doc discovery; do not add a broad allowlist, external shell parser, or execute any discovered command. Require every new caller to be deliberately normalized and classified before the suite passes.</action>
  <verify><automated>node --test test/command-docs.test.js</automated></verify>
  <acceptance_criteria>
    - RED reproduces cross-hint gate borrowing and every wrapper category named by 01.2-VERIFICATION.
    - Unknown or ambiguous caller text fails closed rather than disappearing from the inventory.
    - The scanner remains bounded to generated owners plus README/docs executable blocks and does not inspect sibling repositories.</acceptance_criteria>
  <done>The caller inventory and dominance rules are explicit before generated/docs changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make mutation hints self-contained and maintained guidance portable</name>
  <read_first><file>test/command-docs.test.js</file><file>src/targets/openclaw.js</file><file>README.md</file><file>docs/MVP_RUNBOOK.md</file><file>docs/RUNTIME_EXECUTION.md</file><file>docs/OPENCLAW_RUNTIME_NOTES.md</file><file>docs/OMX_SESSION_MIGRATION.md</file></read_first>
  <files>src/targets/openclaw.js, README.md, docs/MVP_RUNBOOK.md, docs/RUNTIME_EXECUTION.md, docs/OPENCLAW_RUNTIME_NOTES.md, docs/OMX_SESSION_MIGRATION.md</files>
  <behavior>
    - The direct `agents add` hint contains canonical `runtime-check --target openclaw` and its mutation in one independently valid hint with fail-closed control flow.
    - README and all affected maintained docs use repo-relative commands, `$AGENTMO_REPO`, `$OPENCLAW_SOURCE_ROOT`, or angle-bracket placeholders rather than `/Users/<user>`, `/home/<user>`, or Windows user-drive paths.
    - Replacing recovery paths with variables/placeholders preserves the repository-only and sibling-project prohibitions instead of weakening them.
  </behavior>
  <action>Update the target mutation hint so its own preflight dominates its own direct OpenClaw command; do not rely on the preceding informational hint. Replace every scanner-discovered host-specific path in the listed maintained files with a portable repository variable, source-root variable, repo-relative operand, or explicit placeholder. Add a recursive Markdown gate that rejects host/user-specific POSIX and Windows absolute paths while allowing portable `/tmp` examples and explicit abstract placeholders. Keep the core Node floor distinct from the target floor and retain exact durable digest examples and evidence non-certification language.</action>
  <verify><automated>node --test test/command-docs.test.js test/targets.test.js</automated></verify>
  <acceptance_criteria>
    - Every hint and maintained caller block passes independently; moving/removing one hint cannot authorize another.
    - Wrapper/absolute/variable/package-runner canaries are classified, and unknown mutation callers fail.
    - README plus docs/**/*.md contain no host/user-specific absolute paths and remain operationally portable.</acceptance_criteria>
  <done>Generated and maintained operator surfaces share one non-borrowable, portable, fail-closed runtime contract.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Trust Boundaries

| Boundary | Description |
| --- | --- |
| generated hint -> operator shell | Independently copied instructions must retain their own authorization preflight. |
| maintained shell text -> caller classifier | Wrapper and launcher syntax is untrusted and must not evade inventory/classification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-07-01 | Elevation / Tampering | verification hints | high | mitigate | Per-element parsing and same-element preflight dominance before every mutation. |
| T-07-02 | Spoofing / Elevation | wrapped/variable launcher | high | mitigate | Bounded token normalization plus fail-on-unknown OpenClaw-shaped caller canaries. |
| T-07-03 | Repudiation | caller closure | high | mitigate | Closed inspection/mutation classifications and deliberate updates for each new surface. |
| T-07-04 | Information disclosure | maintained Markdown paths | medium | mitigate | Recursive portable-path gate and replacement with repo-relative variables/placeholders. |
</threat_model>

<prohibitions>
- Do not read `.env`, execute discovered commands, add a parser dependency, allow cross-hint/file gate borrowing, or silently ignore unknown launchers.
- Do not copy the runtime range into shell arithmetic, weaken digest bindings, or change certification semantics.
- Do not run live OpenClaw/provider commands, stage, or commit.
</prohibitions>

## Artifacts this phase produces

- Modified `openClawTarget.verificationHints` direct-mutation element; no net-new standalone production artifact.
- Added test identifiers for bounded shell tokenization, launcher normalization, per-surface dominance, closed caller classification, and portable Markdown scanning.
- Portable maintained command/recovery text across README and the five scanner-discovered Phase 01.2 docs.

## Multi-Source Coverage Audit

- GOAL: closes generated and documented OpenClaw mutation bypasses before operator effects.
- REQ COMP-01: keeps CLI, generated guidance, maintained docs, tests, and target floor aligned.
- RESEARCH: fulfills generated/docs parity, recursive corpus closure, and no duplicated authorization logic.
- CONTEXT D-14: every current caller retains an unavoidable current-process gate and value-blind guidance.

<verification>Run focused caller/target contracts. Plan 08 owns final shared inventory rebasing, repository-wide verification, and release evidence after all Wave 1 repairs complete.</verification>
<success_criteria>No generated hint can borrow authorization; wrapped/absolute/variable callers cannot evade closure; maintained command paths are portable.</success_criteria>
<output>Create `01.2-07-SUMMARY.md`; do not commit.</output>
