---
phase: 01.2-openclaw-runtime-release-evidence
plan: "02"
type: execute
wave: 2
depends_on: ["01.2-01"]
files_modified:
  - src/scaffold.js
  - src/run-state.js
  - src/runtime-execution.js
  - test/runtime-compatibility-seams.test.js
autonomous: true
requirements: [COMP-01]
must_haves:
  truths:
    - "OpenClaw scaffold, live run, and live replay reject an incompatible current runtime before any output root, index, state, target file, runtime-env resolution, or runner effect."
    - "The sole production JavaScript spawn seam rechecks the current runtime immediately before spawn, even when an orchestration-level check already ran."
    - "AgentMo-target scaffold and non-mutating/non-live Plan, run-plan, report, eval, and observation work remain allowed on core Node >=20."
  artifacts:
    - { path: test/runtime-compatibility-seams.test.js, provides: "closed JavaScript mutation inventory, anti-bypass API checks, and empty-effect negative matrix" }
    - { path: src/runtime-execution.js, provides: "final no-argument pre-spawn OpenClaw runtime barrier" }
  key_links:
    - { from: src/scaffold.js, to: src/runtime-compatibility.js, via: "OpenClaw target check before createBuildState or publication", pattern: "assertCurrentOpenClawTargetRuntime" }
    - { from: src/run-state.js, to: src/runtime-execution.js, via: "early live run/replay check plus final child-spawn check", pattern: "assertCurrentOpenClawTargetRuntime|runRuntimeCommand" }
---

<objective>
Seal the three lowest JavaScript enforcement seams for D-14 so all three JavaScript mutation journeys fail closed before their first observable effect.

Purpose: Make direct imports and alternate orchestration paths as safe as CLI usage.
Output: TDD zero-side-effect coverage and production gates for OpenClaw scaffold, live run, live replay, and final spawn.
</objective>

## Phase Goal
**As a** developer, **I want to** run AgentMo core under an actual Node 20 lane while every OpenClaw target mutation is gated before effects, **so that** core and target compatibility claims are independently trustworthy.

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-CONTEXT.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-PATTERNS.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-01-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Lock the closed JavaScript mutation inventory and empty-effect failures</name>
  <read_first><file>src/runtime-compatibility.js</file><file>src/scaffold.js</file><file>src/run-state.js</file><file>src/runtime-execution.js</file><file>test/scaffold.test.js</file><file>test/run-state.test.js</file><file>test/runtime-replay-eval.test.js</file><file>test/runtime-execution.test.js</file></read_first>
  <files>test/runtime-compatibility-seams.test.js</files>
  <behavior>
    - Under a test-only incompatible current-process simulation, OpenClaw scaffold leaves the requested root absent; live run and replay leave runner recorders and output roots empty; final runtime execution creates no marker child/file.
    - Static/API inventory proves scaffold, live run, live replay, and the sole production JavaScript spawn call all reach the canonical zero-argument assertion.
    - AgentMo scaffold and non-live/non-mutating paths remain callable under the core floor.
  </behavior>
  <action>Write RED tests for D-14 using the existing admitted artifact fixtures, absent-root checks, injectable command-runner recorders, and a harmless would-create-marker child command. A test may temporarily simulate the process runtime only inside its isolated test process; label this contract simulation explicitly and never treat it as Node 20 execution evidence. Assert ordering before `createBuildState`, runtime-env resolution, run-index preflight, filesystem publication, and spawn. Close the inventory over exactly three JavaScript mutation journeys plus the final spawn barrier and reject any version/provider/bypass option on their production APIs.</action>
  <verify><automated>node --test test/runtime-compatibility-seams.test.js</automated></verify>
  <acceptance_criteria>
    - RED failures identify missing production gates rather than fixture/admission failures.
    - Every incompatible path has an empty effect list and absent requested output/marker.
    - Tests explicitly state that current-process simulation is contract evidence only; the real Node 20 row remains untested.
  </acceptance_criteria>
  <done>The observable failure contract and complete JavaScript seam inventory are fixed before wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate scaffold, live run, live replay, and final spawn at their lowest seams</name>
  <read_first><file>test/runtime-compatibility-seams.test.js</file><file>src/runtime-compatibility.js</file><file>src/scaffold.js</file><file>src/run-state.js</file><file>src/runtime-execution.js</file></read_first>
  <files>src/scaffold.js, src/run-state.js, src/runtime-execution.js</files>
  <behavior>
    - OpenClaw scaffold asserts immediately after target resolution and before build-state/operation/file work.
    - Live run/replay assert before runtime-env or output-index preflight, and `runRuntimeCommand` asserts again before the only production spawn.
    - No production function accepts a runtime version/provider/override while dry and non-mutating core paths remain available.
  </behavior>
  <action>Import and call only `assertCurrentOpenClawTargetRuntime()` per D-14. In `scaffoldAgent`, gate only the selected OpenClaw target before `createBuildState` and publication. In live run and live replay, gate before runtime-env resolution and any run-index/output preflight. In `runRuntimeCommand`, repeat the assertion immediately before `spawn` without adding authorization inputs to its existing options. Preserve Phase 1.1 same-buffer admission, all-candidate persistability preflight, bounded child output, timeout cleanup, and non-transitive evidence semantics.</action>
  <verify><automated>node --test test/runtime-compatibility-seams.test.js test/scaffold.test.js test/run-state.test.js test/runtime-replay-eval.test.js test/runtime-execution.test.js</automated></verify>
  <acceptance_criteria>
    - All zero-effect and no-bypass tests pass.
    - Existing successful behavior passes on the current supported host.
    - No gate is placed only in CLI dispatch, after a writer/preflight, or behind an optional production flag.</acceptance_criteria>
  <done>Every JavaScript OpenClaw mutation route is protected both before orchestration effects and at the final spawn boundary.</done>
</task>
</tasks>

<threat_model>
## ASVS L1 Trust Boundaries

| Boundary | Description |
| --- | --- |
| admitted artifact -> target materializer | Valid artifacts are still untrusted for mutation until current runtime authorization passes. |
| run/replay orchestration -> filesystem/child process | Runtime plan inputs must not reach preflight, writers, or the child runner on rejection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-04 | Tampering / Elevation | alternate mutation path | high | mitigate | Closed scaffold/run/replay/spawn inventory and direct-import negative tests. |
| T-05 | Tampering / Repudiation | rejected filesystem flow | high | mitigate | Assertion ordering plus empty recorder and absent-root/marker proofs. |
| T-06 | Elevation | final child spawn | high | mitigate | No-argument assertion inside `runRuntimeCommand` immediately before the sole production spawn. |
| T-07 | Denial of service | core-only commands | low | accept | Target gate is deliberately limited to OpenClaw mutation; tests preserve non-mutating core paths. |
</threat_model>

<prohibitions>
- Do not read `.env`, add caller-supplied runtime authorization, gate only CLI dispatch, or call cleanup-after-write “zero side effects.”
- Do not gate AgentMo-target scaffold, dry/non-live work, run-plan, reports, evals, or observation on the OpenClaw range.
- Do not rewrite artifact admission, persistence, runtime result bounding, or evidence promotion rules.
- Do not run live OpenClaw/provider commands, stage, or commit.
</prohibitions>

## Artifacts this phase produces
- `test/runtime-compatibility-seams.test.js`: closed JavaScript mutation and zero-effect matrix.
- Lowest-seam gates in `src/scaffold.js`, `src/run-state.js`, and `src/runtime-execution.js`.

## Multi-Source Coverage Audit
- GOAL: all JavaScript target-mutation paths reject before effects.
- REQ COMP-01: OpenClaw scaffold/live run/replay/final spawn enforcement covered.
- RESEARCH: five-path inventory paths 1-3 and the final JavaScript spawn seam covered; shell paths continue in Plan 03.
- CONTEXT D-14: non-injectable current runtime evidence and pre-effect ordering covered.

<verification>Run focused seam and regression tests, then `npm run check` and `git diff --check`; simulated rejection does not satisfy the real Node 20 lane.</verification>
<success_criteria>All JavaScript OpenClaw mutations are gated at unavoidable seams, incompatible rejection leaves no effect, and core-only work remains usable.</success_criteria>
<output>Create `01.2-02-SUMMARY.md`; do not commit.</output>
