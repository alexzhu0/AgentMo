---
phase: 01.2-openclaw-runtime-release-evidence
plan: "03"
type: execute
wave: 3
depends_on: ["01.2-02"]
files_modified:
  - scripts/openclaw-live-smoke.sh
  - src/scaffold-files.js
  - src/targets/openclaw.js
  - test/live-smoke-script.test.js
  - test/command-docs.test.js
  - test/runtime-compatibility-seams.test.js
  - test/artifact-surface-coverage.test.js
  - README.md
  - docs/MVP_RUNBOOK.md
  - docs/RUNTIME_EXECUTION.md
  - docs/OPENCLAW_RUNTIME_NOTES.md
  - docs/OMX_SESSION_MIGRATION.md
autonomous: true
requirements: [COMP-01]
must_haves:
  truths:
    - "The live-smoke helper runs the canonical runtime-check before env value intake, date/mktemp, redirects, scaffold, agents add, or gateway start."
    - "Generated runbooks, OpenClaw verification hints, README, and recursively discovered maintained docs place the same canonical preflight before every direct mutating OpenClaw command."
    - "The command corpus treats runtime-check as a registered zero-subject command while preserving exact digest bindings for every durable-input command."
    - "One bounded renderer/corpus discovery unit owns every current generated or maintained OpenClaw caller and fails when a new caller appears without classification."
  artifacts:
    - { path: test/live-smoke-script.test.js, provides: "static ordering and Bash syntax proof for both direct shell mutation paths" }
    - { path: test/command-docs.test.js, provides: "recursive generated/maintained caller parity against production runtime and durable-subject contracts" }
  key_links:
    - { from: scripts/openclaw-live-smoke.sh, to: src/cli.js, via: "agentmo runtime-check --target openclaw before first side effect", pattern: "runtime-check.*--target openclaw" }
    - { from: src/scaffold-files.js, to: src/runtime-compatibility.js, via: "generated canonical preflight/range", pattern: "OPENCLAW_TARGET_NODE_RANGE|runtime-check" }
---

<objective>
Seal the two direct shell mutations and every maintained/generated operator caller for D-14 without duplicating runtime authorization logic.

Purpose: Prevent scripts or copied runbooks from bypassing the JavaScript mutation seams.
Output: Early shell preflight, generated/target-hint parity, recursive docs enforcement, and a closed I/O inventory.
</objective>

## Phase Goal
**As a** developer, **I want to** run AgentMo core under an actual Node 20 lane while every OpenClaw target mutation is gated before effects, **so that** core and target compatibility claims are independently trustworthy.

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-CONTEXT.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-PATTERNS.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-02-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Gate both direct shell mutation paths before their first effect</name>
  <read_first><file>scripts/openclaw-live-smoke.sh</file><file>src/cli.js</file><file>src/runtime-compatibility.js</file><file>test/live-smoke-script.test.js</file></read_first>
  <files>test/live-smoke-script.test.js, scripts/openclaw-live-smoke.sh</files>
  <behavior>
    - Source-order tests require the canonical runtime-check after argument-only validation but before env-file detection/value intake, date, mktemp, redirection, scaffold, `agents add`, and gateway start.
    - An unsupported runtime exits after only the runtime-check process; no temp/state/workspace/output/log/PID path or direct OpenClaw invocation is created.
    - `bash -n` remains green and Bash contains no duplicated version arithmetic or override variable.
  </behavior>
  <action>First write RED static/fixture tests for both shell paths, then invoke `node ./bin/agentmo.js runtime-check --target openclaw` at the earliest D-14-safe location: after argument-only validation and before any `.env` detection/value read or other effect. Keep shell failure output bounded and do not echo raw CLI stderr. Do not duplicate the supported range in Bash. Extend the Phase 1.1 surface inventory for any new process/output classification without weakening its zero pending/unclassified gate.</action>
  <verify><automated>node --test test/live-smoke-script.test.js test/artifact-surface-coverage.test.js &amp;&amp; bash -n scripts/openclaw-live-smoke.sh</automated></verify>
  <acceptance_criteria>
    - Ordering assertions cover both `agents add` and gateway start and fail if any effect moves before preflight.
    - Rejection creates no state, workspace, scaffold, run output, redirect, log, or PID artifact.
    - The script never reads `.env` during tests and never implements its own range predicate.</acceptance_criteria>
  <done>The fourth and fifth production mutation paths fail before their first shell side effect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce generated and maintained caller parity from production contracts</name>
  <read_first><file>src/runtime-compatibility.js</file><file>src/scaffold-files.js</file><file>src/targets/openclaw.js</file><file>src/artifact-subjects.js</file><file>test/command-docs.test.js</file><file>test/runtime-compatibility-seams.test.js</file><file>test/artifact-surface-coverage.test.js</file><file>README.md</file><file>docs/MVP_RUNBOOK.md</file><file>docs/RUNTIME_EXECUTION.md</file><file>docs/OPENCLAW_RUNTIME_NOTES.md</file><file>docs/OMX_SESSION_MIGRATION.md</file></read_first>
  <files>src/scaffold-files.js, src/targets/openclaw.js, test/command-docs.test.js, test/runtime-compatibility-seams.test.js, test/artifact-surface-coverage.test.js, README.md, docs/MVP_RUNBOOK.md, docs/RUNTIME_EXECUTION.md, docs/OPENCLAW_RUNTIME_NOTES.md, docs/OMX_SESSION_MIGRATION.md</files>
  <behavior>
    - Generated OpenClaw runbooks and target hints place canonical runtime-check before direct mutation and derive range text from the production constant.
    - The bounded discovery unit covers the two production renderer owners plus executable OpenClaw mutation blocks discovered under `README.md` and `docs/**/*.md`, and rejects any discovered caller lacking classification or an earlier canonical preflight.
    - Zero-subject runtime-check coexists with production-registry digest enforcement for durable AgentMo commands.
  </behavior>
  <action>Treat this wide file set as one mechanically verified synchronization unit: the bounded generated set is exactly the outputs/caller metadata owned by `src/scaffold-files.js` and `src/targets/openclaw.js`; the bounded maintained-doc set is exactly the files under `README.md` and `docs/**/*.md` whose executable blocks the production corpus scanner classifies as AgentMo/OpenClaw callers. At planning time that scanner-derived snapshot is README plus the four docs named in `<files>`; do not expand to release, examples, planning artifacts, prose-only mentions, or a manual global docs list. Write RED tests that enumerate both bounded sets and fail if a new renderer output or maintained executable caller appears unclassified. Import `OPENCLAW_TARGET_NODE_RANGE` into renderers where code generation permits, and make the single production `runtime-check --target openclaw` CLI preflight authoritative; neither docs nor Bash may own a parallel predicate. Treat `runtime-check` as registered zero-subject, continue resolving durable commands from production `DURABLE_COMMAND_SUBJECTS`, require the preflight earlier in the same executable block as every direct `openclaw agents add` or gateway mutation, preserve exact byte/digest examples, and per D-14 remove stale optional/absent-gate guidance.</action>
  <verify><automated>node --test test/command-docs.test.js test/runtime-compatibility-seams.test.js test/scaffold.test.js test/targets.test.js test/artifact-surface-coverage.test.js</automated></verify>
  <acceptance_criteria>
    - The renderer/corpus scanner reports the exact bounded generated and maintained caller set, and every member passes from production registry/constants without copied subject/range tables.
    - Every direct OpenClaw mutation in a discovered executable block has an earlier authoritative CLI preflight in that block; adding any generated/maintained caller without classification makes the tests fail.
    - Final I/O scan remains closed with zero pending/unclassified surfaces and no loss of Phase 1.1 digest binding.</acceptance_criteria>
  <done>Scripts, generated artifacts, target hints, and maintained docs cannot drift into an ungated mutation path.</done>
</task>
</tasks>

<scope_constraint>
The wide Task 2 file set is intentionally indivisible because generated instructions, target hints, current maintained command blocks, their production-constant authority, and the classifier that discovers them form one mechanically verified synchronization invariant. Splitting these representations across plans would permit an intermediate green-looking source change with stale executable guidance. Scope is bounded to the two production renderer owners and the scanner-discovered executable callers under `README.md` plus `docs/**/*.md`; tests, not a manual expanding list, define closure and fail on every new unclassified caller.
</scope_constraint>

<threat_model>
## ASVS L1 Trust Boundaries

| Boundary | Description |
| --- | --- |
| operator shell -> target state/process | Direct commands can bypass JavaScript orchestration unless preflight precedes all shell effects. |
| generator/docs -> future operator | Maintained instructions are executable capability surfaces and must carry the same authorization contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-08 | Elevation / Tampering | shell helper | high | mitigate | Canonical CLI gate before env intake/temp/redirect/direct mutation plus ordering and absent-effect tests. |
| T-09 | Tampering | generated/maintained commands | high | mitigate | Recursive corpus test requiring an earlier preflight in every mutating executable block. |
| T-10 | Information disclosure | shell diagnostics/docs | medium | mitigate | Fixed bounded guidance; no raw stderr, env values, host paths, transcripts, or payloads. |
| T-11 | Repudiation | copied contract drift | medium | mitigate | Import production constants/subject registry and reject hand-copied command authority. |
</threat_model>

<prohibitions>
- Do not read `.env`, create a temp/state/output path before runtime-check, duplicate range arithmetic in Bash, or suppress the preflight failure.
- Do not weaken exact digest command bindings or the zero pending/unclassified I/O inventory.
- Do not claim shell/static/generated/docs tests are Node 20 or live OpenClaw execution evidence.
- Do not run provider/live smoke, add AI/UI/schema work, stage, or commit.
</prohibitions>

## Artifacts this phase produces
- Earliest-effect shell preflight for `agents add` and gateway start.
- Generated OpenClaw runbook and target verification hints using the canonical contract.
- Bounded renderer/corpus caller discovery with fail-on-new-unclassified closure and closed I/O inventory tests.
- One mechanically synchronized README/docs caller set derived from the corpus scanner.

## Multi-Source Coverage Audit
- GOAL: shell/generated/documented target mutations now share the unavoidable runtime gate.
- REQ COMP-01: CLI, generated runbook, shell helper, maintained docs, and tests share one contract.
- RESEARCH: mutation inventory paths 4-5, generated caller parity, recursive command corpus, and I/O closure covered.
- CONTEXT D-14: pre-effect shell ordering, no caller override, and zero-side-effect failure covered.

<verification>Run focused corpus/shell/closure tests, `bash -n`, then `npm run check` and `git diff --check`.</verification>
<success_criteria>All five mutation paths and all maintained/generated callers use the same fail-closed target range without a shell or documentation bypass.</success_criteria>
<output>Create `01.2-03-SUMMARY.md`; do not commit.</output>
