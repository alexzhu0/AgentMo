---
phase: 01.2-openclaw-runtime-release-evidence
plan: "04"
type: execute
wave: 4
depends_on: ["01.2-03"]
files_modified:
  - package.json
  - scripts/node20-core-lane.sh
  - test/node20-core-lane.test.js
  - test/runtime-compatibility-seams.test.js
  - docs/RUNTIME_COMPATIBILITY.md
  - release/2026.07.13.md
autonomous: true
requirements: [COMP-01]
must_haves:
  truths:
    - "An actual Node 20 executable, not metadata or a mocked version, executes the owned AgentMo core suite and incompatible-OpenClaw zero-side-effect matrix before COMP-01 can pass."
    - "The release matrix separately records AgentMo core declaration, OpenClaw package declaration, official OpenClaw supported range, boundary-contract tests, current-host execution, Node 20 core execution, and target live execution/untested status."
    - "Any lane that did not execute remains `untested`; runtime evidence never promotes itself to domain quality, production approval, or broad OpenClaw certification."
  artifacts:
    - { path: scripts/node20-core-lane.sh, provides: "deterministic actual-runtime lane with an explicit owned command set and no version mock/download" }
    - { path: docs/RUNTIME_COMPATIBILITY.md, provides: "declared, official-supported, contract-tested, core-executed, target-executed/untested release matrix" }
    - { path: release/2026.07.13.md, provides: "dated bounded commands, statuses, hashes, remaining risk, and certification boundaries" }
  key_links:
    - { from: package.json, to: scripts/node20-core-lane.sh, via: "check:core:node20", pattern: "check:core:node20" }
    - { from: scripts/node20-core-lane.sh, to: test/node20-core-lane.test.js, via: "same actual Node 20 executable runs core and target-rejection proof", pattern: "node20-core-lane.test.js" }
    - { from: docs/RUNTIME_COMPATIBILITY.md, to: release/2026.07.13.md, via: "same observed lane/status vocabulary without certification promotion", pattern: "tested|failed|untested" }
---

<objective>
Execute and record the real compatibility evidence required by D-14, blocking phase completion if the actual Node 20 lane does not run.

Purpose: Replace declarations and simulations with bounded observed evidence while keeping unexecuted OpenClaw target rows honest.
Output: A deterministic Node 20 lane, executed core/negative evidence, a maintained compatibility matrix, and dated release evidence.
</objective>

## Phase Goal
**As a** developer, **I want to** run AgentMo core under an actual Node 20 lane while every OpenClaw target mutation is gated before effects, **so that** core and target compatibility claims are independently trustworthy.

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-CONTEXT.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-RESEARCH.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-VALIDATION.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-03-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build and actually run the Node 20 core and rejection lane</name>
  <read_first><file>package.json</file><file>scripts/openclaw-live-smoke.sh</file><file>test/runtime-compatibility.test.js</file><file>test/runtime-compatibility-seams.test.js</file><file>test/stage-contracts.test.js</file><file>test/artifact-admission.test.js</file><file>test/persistability.test.js</file></read_first>
  <files>package.json, scripts/node20-core-lane.sh, test/node20-core-lane.test.js, test/runtime-compatibility-seams.test.js</files>
  <behavior>
    - The lane fails before tests unless its actual executable reports major 20 from `process.versions.node`; no assignment, mock, version flag, provider, or prose substitute is accepted.
    - The same executable runs dependency-free syntax/core contracts and real OpenClaw scaffold/live run/live replay/final-spawn rejection cases with absent outputs and no child effects.
    - The Node-20-only test skips during the ordinary supported-host suite, but a fixed non-secret lane marker makes it require and record actual major 20 inside the lane; target live execution is not inferred from either lane.
  </behavior>
  <action>Write RED runtime-lane tests, add `check:core:node20`, and create a strict Bash runner that receives or resolves an actual Node executable, verifies its observed major is exactly 20, sets a fixed non-secret test-lane marker, and uses that same binary for an explicit bounded core test list plus `test/node20-core-lane.test.js` and the incompatible-target seam matrix. Outside the marked lane, the Node-20-only test must skip so `npm run check` remains the separate current-host lane; inside it, any non-20 runtime must fail. The runner must not download a runtime, rewrite process version, or silently fall back to the current host. Execute it with a real Node 20 binary; if none exists, use only an explicitly approved, checksum-verified official temporary runtime acquisition outside the runner. If actual execution cannot occur, stop Phase 01.2 as blocked and leave Node 20 `untested`—package engines, current-host tests, mock predicates, CI prose, or a locally authored workflow cannot satisfy COMP-01. Also run the supported current-host full suite separately.</action>
  <verify><automated>npm run check:core:node20 &amp;&amp; npm run check &amp;&amp; node --test test/runtime-compatibility-seams.test.js</automated></verify>
  <acceptance_criteria>
    - The recorded Node 20 observation comes from the executable that ran the owned commands.
    - Actual Node 20 rejection leaves scaffold/run/replay/final-spawn outputs and effect recorders empty.
    - Both the Node 20 lane and current-host full suite pass; otherwise COMP-01 remains incomplete.</acceptance_criteria>
  <done>AgentMo core and target-rejection behavior have actual Node 20 execution evidence rather than declaration or simulation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish the bounded compatibility and release matrix</name>
  <read_first><file>src/runtime-compatibility.js</file><file>test/runtime-compatibility-seams.test.js</file><file>docs/AGENT_BIRTH_GATE.md</file><file>docs/OPENCLAW_RUNTIME_NOTES.md</file><file>release/README.md</file><file>release/2026.07.13.md</file></read_first>
  <files>test/runtime-compatibility-seams.test.js, docs/RUNTIME_COMPATIBILITY.md, release/2026.07.13.md</files>
  <behavior>
    - Matrix contract tests require separate rows/fields for core declared `>=20`, upstream package `>=22.19.0`, official supported `>=22.19.0 <23 || >=23.11.0`, contract-tested boundaries, current-host execution, Node 20 core execution, and target-executed status.
    - Only observed commands may be `tested`; missing Node 20 or live OpenClaw execution remains `untested` and blocks only the corresponding claim.
    - Runtime evidence explicitly excludes domain quality, production approval, and wider target certification.
  </behavior>
  <action>First add RED document-contract tests for closed evidence class/status values and non-certification language. Create `docs/RUNTIME_COMPATIBILITY.md` and extend the dated release record using only actual observations: exact bounded command set, observed numeric versions, date, status, evidence reference/hash, remaining risk, and untested rows. Keep the official OpenClaw range separate from the broader upstream package declaration. Record the supported current-host lane, the required executed Node 20 lane, predicate/zero-effect contract evidence, and OpenClaw target live execution as `untested` because this phase does not run provider/live smoke. Never paste raw stdout/stderr, host paths, environment values, payloads, transcripts, or credentials. Per D-14, refuse phase completion if the real Node 20 row is not `tested`.</action>
  <verify><automated>node --test test/runtime-compatibility.test.js test/runtime-compatibility-seams.test.js test/command-docs.test.js test/artifact-surface-coverage.test.js &amp;&amp; npm run check &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>
    - Matrix rows distinguish declared, official-supported, contract-tested, core-executed, and target-executed evidence.
    - Node 20 and current-host rows match commands actually executed; live OpenClaw target remains explicitly untested.
    - Release evidence contains no secret/raw output/host path and no domain or production certification claim.</acceptance_criteria>
  <done>COMP-01 has actual bounded evidence for its required lanes and honest untested status for unexecuted target live work.</done>
</task>
</tasks>

<threat_model>
## ASVS L1 Trust Boundaries

| Boundary | Description |
| --- | --- |
| selected executable -> compatibility claim | A declared or mocked version must not impersonate the runtime that executed the test set. |
| raw command execution -> release ledger | Only bounded observed facts may cross into durable evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-12 | Spoofing / Repudiation | Node 20 lane | high | mitigate | Executable self-reports major 20 and runs the owned suite itself; no version mock/fallback. |
| T-13 | Spoofing / Elevation | release matrix | high | mitigate | Closed evidence/status vocabulary and document tests keep declarations, tests, executions, and certifications separate. |
| T-14 | Supply-chain tampering | optional temporary runtime acquisition | high | mitigate | Runner performs no download; any necessary acquisition requires explicit approval and official checksum verification, otherwise phase blocks. |
| T-15 | Information disclosure | release evidence | medium | mitigate | Bounded facts/hashes only; negative tests exclude raw logs, paths, env, payloads, transcripts, and credentials. |
</threat_model>

<prohibitions>
- Do not read `.env`, mock Node 20 execution, silently fall back to Node 24, or mark an unexecuted row tested.
- Do not substitute package engines, predicate tests, CI prose, or a workflow file for a real Node 20 run.
- Do not run OpenClaw/provider/live smoke; keep target-executed `untested` and separate from actual incompatible-runtime rejection evidence.
- Do not claim runtime smoke certifies domain quality or production readiness; do not stage or commit.
</prohibitions>

## Artifacts this phase produces
- `check:core:node20`, `scripts/node20-core-lane.sh`, and `test/node20-core-lane.test.js`.
- Actual Node 20 core plus incompatible-target zero-effect evidence.
- Current-host full-suite evidence.
- `docs/RUNTIME_COMPATIBILITY.md` and dated `release/2026.07.13.md` release matrix.

## Multi-Source Coverage Audit
- GOAL: actual Node 20 core execution and pre-effect OpenClaw target gate evidence complete across Plans 01-04.
- REQ COMP-01: core `>=20`, official target range, all five mutation paths, actual lane, and separate release ranges/statuses complete.
- RESEARCH: real runtime lane, current-host lane, upstream-vs-official separation, target-executed/untested row, and certification boundary complete.
- CONTEXT D-14: actual runtime evidence is mandatory, production bypass is closed, zero effects are observed, and unexecuted evidence cannot pass.

<verification>Actual `npm run check:core:node20`, current-host `npm run check`, focused matrix/corpus/closure tests, shell syntax from Plan 03, and `git diff --check` all pass.</verification>
<success_criteria>COMP-01 passes only after a real Node 20 process executes the owned lane; every target mutation seam is closed; the matrix remains explicit about declarations, observed tests, untested live target work, and certification limits.</success_criteria>
<output>Create `01.2-04-SUMMARY.md`; do not commit.</output>
