---
phase: 01.2-openclaw-runtime-release-evidence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/runtime-compatibility.js
  - src/cli.js
  - test/runtime-compatibility.test.js
  - test/cli.test.js
autonomous: true
requirements: [COMP-01]
must_haves:
  truths:
    - "AgentMo core remains usable on Node.js >=20 while OpenClaw target authorization uses the distinct official range >=22.19.0 <23 || >=23.11.0."
    - "The production OpenClaw runtime assertion accepts no version, provider, callback, options, environment override, or bypass flag and reads process.versions.node directly."
    - "The runtime-check CLI emits only bounded, persistable current-process evidence and cannot be used to inject a compatible version."
  artifacts:
    - { path: src/runtime-compatibility.js, provides: "immutable core/target contracts, strict pure predicate, current-process observation, and no-argument assertion", exports: [AGENTMO_CORE_NODE_RANGE, OPENCLAW_TARGET_NODE_RANGE, isOpenClawTargetNodeSupported, observeCurrentRuntime, assertCurrentOpenClawTargetRuntime] }
    - { path: test/runtime-compatibility.test.js, provides: "boundary, malformed-input, API-shape, report, and error contract" }
  key_links:
    - { from: src/cli.js, to: src/runtime-compatibility.js, via: "runtime-check --target openclaw", pattern: "observeCurrentRuntime|assertCurrentOpenClawTargetRuntime" }
    - { from: src/cli.js, to: src/persistability.js, via: "central bounded output/error ownership", pattern: "emitNonArtifactOutput|emitCliError" }
---

<objective>
Deliver the first D-14 vertical slice: a developer can inspect the actual process runtime through one canonical, bounded CLI contract while AgentMo core and OpenClaw target compatibility remain explicitly separate.

Purpose: Establish the only production runtime authority before any mutation seam consumes it.
Output: A tested dependency-free runtime policy module and `agentmo runtime-check --target openclaw [--json]`.
</objective>

## Phase Goal
**As a** developer, **I want to** run AgentMo core under an actual Node 20 lane while every OpenClaw target mutation is gated before effects, **so that** core and target compatibility claims are independently trustworthy.

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-CONTEXT.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-RESEARCH.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify the immutable runtime boundary before implementation</name>
  <read_first><file>src/runtime-env.js</file><file>src/persistability.js</file><file>test/runtime-env.test.js</file><file>test/cli.test.js</file></read_first>
  <files>test/runtime-compatibility.test.js, test/cli.test.js</files>
  <behavior>
    - The pure predicate rejects Node 20.x, 22.18.x, 23.0.x, 23.10.x, malformed, and prerelease-shaped strings; it accepts 22.19.0+, 23.11.0+, and 24+ according to the disjoint D-14 range.
    - The production assertion has arity zero and reads the current process; no production or CLI surface accepts a supplied runtime version.
    - Human and JSON reports contain only component, target, observed numeric version, canonical range, support status, and evidence class, and flow through the existing persistability/output gate.
  </behavior>
  <action>Write RED boundary-table, malformed-input, frozen-constant, exported-API-shape, current-process report, bounded-error, and CLI corpus tests. Per D-14, unit tests may pass a version string only to the pure predicate; tests must reject any production assertion/CLI parameter named or behaving as a version/provider/options/bypass input. Register `runtime-check` as a zero-subject non-mutating command rather than adding it to the durable-artifact subject table. Confirm non-mutating Plan/report/eval commands remain available on core Node >=20.</action>
  <verify><automated>node --test test/runtime-compatibility.test.js test/cli.test.js</automated></verify>
  <acceptance_criteria>
    - RED fails only because the new runtime policy and CLI route do not exist.
    - Boundary cases distinguish 22.19 from 23.0/23.10 and 23.11 exactly.
    - No expected output contains a host path, environment value, raw command output, credential material, or certification claim.
  </acceptance_criteria>
  <done>The compatibility and anti-bypass behavior is fixed before production code is added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the current-process policy and bounded runtime-check CLI</name>
  <read_first><file>test/runtime-compatibility.test.js</file><file>src/cli.js</file><file>src/runtime-env.js</file><file>src/persistability.js</file></read_first>
  <files>src/runtime-compatibility.js, src/cli.js</files>
  <behavior>
    - `assertCurrentOpenClawTargetRuntime()` succeeds only for the current process in the official supported range and otherwise throws stable code `AGENTMO_OPENCLAW_RUNTIME_UNSUPPORTED` before callers can act.
    - `agentmo runtime-check --target openclaw [--json]` observes the same process and reports the same constant without accepting alternate targets or runtime overrides.
  </behavior>
  <action>Implement a strict numeric-triplet parser and the small fixed predicate without adding semver or another dependency. Export immutable `AGENTMO_CORE_NODE_RANGE` (`>=20`) and `OPENCLAW_TARGET_NODE_RANGE` (`>=22.19.0 <23 || >=23.11.0`), a bounded observation, and a zero-parameter production assertion that directly reads `process.versions.node`. Add the exact CLI route through existing centralized output/error owners; reject unknown target/options with stable value-blind errors. Preserve Phase 1.1 exact admission and persistability behavior and do not make the pure predicate an authorization seam.</action>
  <verify><automated>node --test test/runtime-compatibility.test.js test/cli.test.js test/artifact-subjects.test.js test/persistability.test.js</automated></verify>
  <acceptance_criteria>
    - All boundary/report/CLI tests pass on the current host.
    - Source/API tests prove production mutation authorization cannot receive a runtime value or provider.
    - Existing durable command subjects and centralized output ownership remain unchanged except for the explicit zero-subject CLI registration.
  </acceptance_criteria>
  <done>One production runtime authority and one safe inspection route are ready for every later seam.</done>
</task>
</tasks>

<threat_model>
## ASVS L1 Trust Boundaries

| Boundary | Description |
| --- | --- |
| production caller -> mutation authorization | Untrusted caller arguments must not influence observed runtime identity. |
| process runtime -> CLI evidence | Runtime facts become durable/user-visible output only through the shared persistability gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-01 | Spoofing / Elevation | production assertion | high | mitigate | Zero-argument assertion reads `process.versions.node`; exported API and CLI rejection tests block injected versions/providers/options. |
| T-02 | Tampering / DoS | strict triplet parser | medium | mitigate | Closed boundary table and malformed/prerelease fail-closed tests. |
| T-03 | Information disclosure | runtime-check output/error | medium | mitigate | Existing complete-candidate persistability and bounded CLI error envelope; canary-negative tests. |
</threat_model>

<prohibitions>
- Do not read `.env`, add semver/dependencies, accept runtime overrides in production, or use package `engines.node` as OpenClaw mutation authorization.
- Do not gate non-mutating run-plan/report/eval/observation paths merely because they mention OpenClaw.
- Do not weaken exact artifact admission, persistability, non-transitive evidence, or proposal-only observation.
- Do not add AI-SPEC, UI, schema, live/provider execution, staging, or commits.
</prohibitions>

## Artifacts this phase produces
- `src/runtime-compatibility.js`: canonical core/target range and current-process assertion.
- `agentmo runtime-check --target openclaw [--json]`: bounded inspection command.
- `test/runtime-compatibility.test.js`: RED/GREEN compatibility boundary contract.

## Multi-Source Coverage Audit
- GOAL: canonical runtime authority and inspectable evidence covered here; mutation and execution evidence continue in Plans 02-04.
- REQ COMP-01: core/target range separation and production anti-injection API covered here.
- RESEARCH: recommended runtime policy/CLI slice, strict predicate, zero dependency, bounded evidence covered.
- CONTEXT D-14: direct current-process observation and exact disjoint target range covered.

<verification>Run focused tests, then `npm run check` and `git diff --check`; the real Node 20 claim remains untested until Plan 04 executes it.</verification>
<success_criteria>The CLI and policy agree on one immutable contract, the authorization API cannot be caller-spoofed, and no compatibility declaration is presented as execution evidence.</success_criteria>
<output>Create `01.2-01-SUMMARY.md`; do not commit.</output>
