---
phase: 01.2-openclaw-runtime-release-evidence
plan: "09"
type: execute
wave: 1
depends_on: ["01.2-06"]
files_modified:
  - src/cli.js
  - test/cli.test.js
autonomous: true
gap_closure: true
requirements: [COMP-01]
must_haves:
  truths:
    - "Every documented AgentMo CLI journey reaches the application before its runtime-env option is interpreted; the Node launcher cannot consume that public option first."
    - "Scaffold, live run, and live replay reject an incompatible current process with AGENTMO_OPENCLAW_RUNTIME_UNSUPPORTED before env-file, artifact, workspace, output, or target intake."
    - "The removed launcher-colliding spelling is not retained as a hidden production alias."
  artifacts:
    - { path: src/cli.js, provides: "collision-free --runtime-env-file public CLI contract" }
    - { path: test/cli.test.js, provides: "real node and shebang entrypoint pre-intake rejection canaries" }
  key_links:
    - { from: test/cli.test.js, to: bin/agentmo.js, via: "real maintained invocation without an inserted argv separator", pattern: "runtime-env-file" }
    - { from: src/cli.js, to: src/runtime-compatibility.js, via: "dispatch-time gate before option value and artifact intake", pattern: "assertCurrentProcessRuntimeCompatible" }
---

<objective>
Close the launcher-collision gap by giving AgentMo runtime environment input a public option that Node itself cannot consume.

Purpose: The application-level fail-closed gate is only meaningful when the maintained launcher actually reaches AgentMo.
Output: One unambiguous CLI option and production-shaped rejection tests for all three OpenClaw mutation journeys.
</objective>

## Phase Goal

开发者可以在真实 Node.js 20 lane 运行 AgentMo core，并确保每一条 OpenClaw target mutation 路径在任何进程或文件副作用前经过不可由生产调用者绕过的 target runtime gate。

<execution_context>
@/Users/alexzhu/.codex/gsd-core/workflows/execute-plan.md
@/Users/alexzhu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-VERIFICATION.md
@.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-06-SUMMARY.md
@src/cli.js
@bin/agentmo.js
@test/cli.test.js

<interfaces>
The sole public AgentMo spelling becomes `--runtime-env-file`. Do not preserve `--env-file` as a compatibility alias: Node 20+ owns that spelling before the application and an alias would falsely imply a reliable contract. Internal option property names may remain stable when they do not leak into user-facing help or invocation strings.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce the real launcher collision and lock the new public contract</name>
  <read_first><file>bin/agentmo.js</file><file>src/cli.js</file><file>test/cli.test.js</file><file>.planning/phases/01.2-openclaw-runtime-release-evidence/01.2-VERIFICATION.md</file></read_first>
  <files>test/cli.test.js</files>
  <behavior>
    - Tests invoke `node ./bin/agentmo.js ... --runtime-env-file ...` without placing `--` before the script path or otherwise shielding arguments from Node.
    - At least one production-shaped test executes the package shebang entrypoint directly.
    - Under an incompatible current-process fixture, scaffold, live run, and live replay all return the stable AgentMo runtime error before a missing or unreadable env canary and before every requested output/workspace root exists.
    - `--env-file` is rejected as unsupported by the application contract and is absent from public help; tests do not accept a hidden legacy alias.
  </behavior>
  <action>Add RED tests around the actual binary entrypoint. Reuse the existing value-blind incompatible-runtime preload technique, but remove the test-only argv separator that concealed Node's launcher parsing. Cover missing and unreadable runtime-env canaries and assert request roots remain absent. Keep assertions on error code/class and side-effect absence; do not inspect `.env`, raw provider output, or sibling repositories.</action>
  <verify><automated>node --test --test-name-pattern="runtime-env-file|real launcher|shebang|pre-intake" test/cli.test.js</automated></verify>
  <acceptance_criteria>
    - RED demonstrates the old public spelling cannot satisfy the maintained invocation.
    - The new tests fail until all three journeys use the collision-free spelling and preserve stable fail-closed ordering.
    - No helper inserts an argv separator ahead of `bin/agentmo.js`.</acceptance_criteria>
  <done>The test suite models the public Node/shebang launcher instead of an artificial protected argv shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rename the public runtime environment option without a compatibility trap</name>
  <read_first><file>src/cli.js</file><file>test/cli.test.js</file><file>src/runtime-compatibility.js</file></read_first>
  <files>src/cli.js, test/cli.test.js</files>
  <behavior>
    - All relevant parsers, option-value diagnostics, safe option registries, and CLI help expose only `--runtime-env-file`.
    - Runtime assertion remains before option value validation and before artifact/message/env/output intake.
    - Compatible runtime behavior and value-blind env metadata behavior remain unchanged except for the public spelling.
  </behavior>
  <action>Replace the public option spelling at every AgentMo CLI parse/help/diagnostic surface and update its tests. Do not add a wrapper launcher, NODE_OPTIONS requirement, delimiter convention, or legacy alias. Preserve internal runtime-env resolution semantics and stable error codes. Search production/help/tests for residual AgentMo CLI `--env-file` usage, while leaving unrelated Bash-script-local flags alone when they are not passed through the Node launcher.</action>
  <verify><automated>node --test test/cli.test.js &amp;&amp; node ./bin/agentmo.js --help | rg --fixed-strings -- "--runtime-env-file" &amp;&amp; ! node ./bin/agentmo.js --help | rg --fixed-strings -- "--env-file"</automated></verify>
  <acceptance_criteria>
    - Public help contains `--runtime-env-file` and no standalone `--env-file` AgentMo option.
    - Full CLI tests pass, including real launcher and shebang negative paths.
    - Compatible live request planning still reports only value-blind env metadata.</acceptance_criteria>
  <done>The public launcher contract is unambiguous and the pre-intake gate is reachable in maintained invocations.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Trust Boundaries

| Boundary | Description |
| --- | --- |
| Node launcher -> AgentMo argv parser | The host runtime must not claim an application-owned option before AgentMo can enforce its gate. |
| AgentMo dispatch -> mutable input | Runtime rejection must dominate all env, artifact, filesystem, output, and target work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-09-01 | Elevation / Tampering | launcher argv | high | mitigate | Unique public flag plus real node/shebang tests without test-only delimiter shielding. |
| T-09-02 | Information disclosure | env intake | high | mitigate | Incompatible runtime is rejected before path readability or value parsing; assertions stay value-blind. |
</threat_model>

<prohibitions>
- Do not read `.env`, add a wrapper, require caller delimiters, retain `--env-file` as an AgentMo alias, or change non-OpenClaw schemas.
- Do not touch sibling repositories, dependencies, provider/live execution, ROADMAP/STATE, or release claims in this plan.
- Do not stage or commit.
</prohibitions>

## Artifacts this phase produces

- Modified `src/cli.js` with one collision-free public runtime environment option.
- Modified `test/cli.test.js` with production-shaped Node/shebang pre-intake canaries.

## Multi-Source Coverage Audit

- VERIFICATION gap 1: fully owned by this plan.
- COMP-01: strengthens the application entrypoint proof; it does not claim target-live or domain quality.
- D-14: current-process evidence remains non-injectable; test preload is a negative fixture, not a production override.

<verification>Run both Task commands and inspect a production help invocation. Any required delimiter, legacy alias, launcher-native error, or created request root leaves this plan incomplete.</verification>
<success_criteria>All three mutation journeys reach AgentMo and reject an incompatible current process before intake through the real maintained launch forms.</success_criteria>
<output>Create `01.2-09-SUMMARY.md`; do not commit.</output>
