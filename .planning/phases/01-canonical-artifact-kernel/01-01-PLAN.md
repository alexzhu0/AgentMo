---
phase: 01-canonical-artifact-kernel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/blueprint.js
  - src/blueprint-draft.js
  - src/report.js
  - src/control-snapshot.js
  - src/build-state.js
  - src/cli.js
  - src/scaffold-files.js
  - src/birth-report.js
  - src/handoff.js
  - examples/win9.agentmo.json
  - examples/support-triage.agentmo.json
  - test/blueprint.test.js
  - test/blueprint-draft.test.js
  - test/report.test.js
  - test/control-snapshot.test.js
  - test/build-state.test.js
  - test/cli.test.js
  - test/scaffold.test.js
  - test/targets.test.js
  - test/stage-contracts.test.js
  - test/canonical-identity.test.js
autonomous: true
requirements: [CORE-01, CORE-03]
must_haves:
  truths:
    - "新生成的 blueprint、report、build-state、CLI help 与 scaffold 只使用 AgentMo/agentmo_* identity。"
    - "机器输出与 human 输出只把 Discover、Plan、Produce 暴露为顶层生命周期。"
    - "既有状态词只作为 Produce maturity 出现，不形成第二套 lifecycle。"
  prohibitions:
    - statement: "不得把 active examples 或新 emitter 保留为 agentmother_* identity。"
      status: resolved
      verification: "canonical identity suite 对 current examples 与生成物做字段级断言。"
    - statement: "不得重命名或删除已定义为 Produce 内部 gate 的 birth/delivery/observe artifact。"
      status: resolved
      verification: "targeted report/control/scaffold tests 验证 gate 归属而不是第四阶段。"
  artifacts:
    - path: src/blueprint.js
      provides: "Canonical AgentMo blueprint identity and three-stage validator contract"
    - path: src/report.js
      provides: "AgentMo report with Produce maturity instead of parallel lifecycle"
    - path: test/canonical-identity.test.js
      provides: "Bounded current-output identity regression matrix"
  key_links:
    - from: src/blueprint-draft.js
      to: src/blueprint.js
      via: "shared agentmo_version schema constant and validator"
    - from: src/report.js
      to: src/control-snapshot.js
      via: "produce_maturity field and three-stage pipeline summary"
---

<objective>
把所有当前机器 emitter 与用户可见输出切换到 canonical AgentMo identity，并消除与 Discover→Plan→Produce 并列的 lifecycle。

Purpose: 为后续显式 legacy migration 提供唯一、稳定的新格式目标。
Output: canonical schema/constants、AgentMo-only emitters、Produce maturity contract 与回归测试。
</objective>

## Phase Goal

**As a** AgentMo 开发者，**I want to** 只生成 AgentMo 身份和三阶段工件，**so that** legacy 兼容不会继续污染新输出或创造第四阶段。

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 canonical blueprint identity 与 emitter contract</name>
  <read_first>
    package.json
    src/blueprint.js
    src/blueprint-draft.js
    examples/win9.agentmo.json
    examples/support-triage.agentmo.json
    test/blueprint.test.js
    test/blueprint-draft.test.js
    test/stage-contracts.test.js
  </read_first>
  <files>package.json, src/blueprint.js, src/blueprint-draft.js, examples/win9.agentmo.json, examples/support-triage.agentmo.json, test/blueprint.test.js, test/blueprint-draft.test.js, test/targets.test.js, test/stage-contracts.test.js, test/canonical-identity.test.js</files>
  <behavior>
    - Canonical blueprint requires `agentmo_version: "0.1"` and never emits `agentmother_version`.
    - Pipeline phase keys are exactly `discover`, `plan`, `produce`.
    - Missing runtime_profiles warning names AgentMo only.
    - Active examples validate as canonical AgentMo inputs.
  </behavior>
  <action>Per D-07/D-08, add `BLUEPRINT_SCHEMA_VERSION` and canonical identity constants in `src/blueprint.js`, make `agentmo_version` the only canonical required field, update validation messages and `src/blueprint-draft.js`, normalize both active examples, and update package description. Keep legacy recognition out of the canonical validator; Plan 03 will inspect legacy identity before calling it. Add field-level tests that exclude bounded migration fixtures rather than using an unscoped repository grep.</action>
  <verify><automated>node --test test/blueprint.test.js test/blueprint-draft.test.js test/targets.test.js test/stage-contracts.test.js test/canonical-identity.test.js && npm run check && git diff --check</automated></verify>
  <acceptance_criteria>
    - `src/blueprint.js` exports `BLUEPRINT_SCHEMA_VERSION` and validates `agentmo_version`.
    - Both active example JSON files contain `agentmo_version` and no legacy identity key or current AgentMother prose.
    - Targeted tests exit 0 and identify the three canonical pipeline keys exactly.
  </acceptance_criteria>
  <done>Canonical blueprints and drafts emit and validate only AgentMo identity while preserving exactly three top-level stages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 统一 report、control、build-state、CLI 与 scaffold 输出</name>
  <read_first>
    src/report.js
    src/control-snapshot.js
    src/build-state.js
    src/cli.js
    src/scaffold-files.js
    src/birth-report.js
    src/handoff.js
    test/report.test.js
    test/control-snapshot.test.js
    test/build-state.test.js
    test/cli.test.js
    test/scaffold.test.js
  </read_first>
  <files>src/report.js, src/control-snapshot.js, src/build-state.js, src/cli.js, src/scaffold-files.js, src/birth-report.js, src/handoff.js, test/report.test.js, test/control-snapshot.test.js, test/build-state.test.js, test/cli.test.js, test/scaffold.test.js, test/canonical-identity.test.js</files>
  <behavior>
    - Report kind/header are `agentmo_report` / `AgentMo report`.
    - Report/control expose `produce_maturity` and do not expose a parallel lifecycle field.
    - Build-state source uses `agentmoVersion` from canonical blueprint.
    - CLI help and every generated scaffold file contain no current AgentMother identity.
  </behavior>
  <action>Per D-08/D-12, rename report builder/formatter to AgentMo terminology, replace lifecycle output with `produce_maturity` carrying existing status semantics, propagate it through control snapshot, and update build-state source metadata. Update all internal imports. Rewrite CLI banner/help, scaffold/readme/runbook/runtime-contract/AGENTS/IDENTITY/memory/version-ledger text, plus birth/handoff guidance, so birth/delivery/observe remain explicitly Produce-internal gates or proposals. Update precise JSON and human-output tests.</action>
  <verify><automated>node --test test/report.test.js test/control-snapshot.test.js test/build-state.test.js test/cli.test.js test/scaffold.test.js test/canonical-identity.test.js && npm run check && git diff --check</automated></verify>
  <acceptance_criteria>
    - JSON report/control snapshots contain the three-stage pipeline and `produce_maturity`, with no `lifecycle.stage`.
    - CLI help begins with `AgentMo CLI` and validate/report descriptions use AgentMo only.
    - Generated base and OpenClaw scaffold text contains no current `AgentMother` or `agentmother` token.
  </acceptance_criteria>
  <done>All current machine and user-visible emitters share the canonical identity and three-stage contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Legacy input → canonical validator | Legacy data must not be silently accepted as a canonical blueprint. |
| Blueprint → generated output | Current emitters must not reintroduce old product identity or a fourth stage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Spoofing | canonical identity | high | mitigate | Single canonical schema constant plus field-level emitter/validator tests. |
| T-01-02 | Tampering | lifecycle representation | high | mitigate | Exact pipeline-key assertions and removal of parallel lifecycle output. |
| T-01-03 | Information Disclosure | generated help/scaffold | high | mitigate | Bounded generated-file scan with explicit legacy-fixture exclusions. |
</threat_model>

## Artifacts this phase produces

- `BLUEPRINT_SCHEMA_VERSION`, canonical blueprint identity constants.
- `agentmo_version`, `agentmo_report`, `produce_maturity`, `agentmoVersion` fields.
- AgentMo-named report builder/formatter exports.
- `test/canonical-identity.test.js`.

<verification>
Run targeted identity/output tests, then `npm run check` and `git diff --check`.
</verification>

<success_criteria>
Every active machine emitter, CLI help surface and generated scaffold uses AgentMo identity and only the three approved top-level stages.
</success_criteria>

<output>Create `.planning/phases/01-canonical-artifact-kernel/01-01-SUMMARY.md` when done.</output>
