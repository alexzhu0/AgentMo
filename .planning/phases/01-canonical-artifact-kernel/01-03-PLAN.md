---
phase: 01-canonical-artifact-kernel
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/artifact-registry.js
  - src/artifact-migration.js
  - src/blueprint.js
  - src/control-snapshot.js
  - src/birth-report.js
  - src/cli.js
  - src/secret-redaction.js
  - src/evidence-audit.js
  - test/artifact-migration.test.js
  - test/blueprint.test.js
  - test/control-snapshot.test.js
  - test/birth-report.test.js
  - test/delivery-report.test.js
  - test/cli.test.js
  - test/fixtures/migration/legacy-blueprint.json
  - test/fixtures/migration/legacy-report.json
  - test/fixtures/migration/legacy-build-state.json
  - test/fixtures/migration/canonical-blueprint.json
  - test/fixtures/migration/hostile-secret.json
autonomous: true
requirements: [CORE-02]
must_haves:
  truths:
    - "普通 loader 对支持的 legacy identity 返回精确 migration-required 诊断且零写入。"
    - "agentmo migrate 无 --out 时为 deterministic、value-blind、whole-batch preview。"
    - "三类已知 legacy family 可转换为 canonical identity；未知、冲突或 hostile input 整批拒绝。"
  prohibitions:
    - statement: "不得自动迁移、猜测未知版本或把 legacy raw content 写入状态/诊断。"
      status: resolved
      verification: "loader/preview negative matrix asserts exact errors and zero derived files."
  artifacts:
    - path: src/artifact-registry.js
      provides: "Closed legacy family recognizers and canonical transforms"
    - path: src/artifact-migration.js
      provides: "Read-only migration planner and deterministic receipt model"
    - path: test/artifact-migration.test.js
      provides: "Legacy/hostile/batch migration contract matrix"
  key_links:
    - from: src/blueprint.js
      to: src/artifact-registry.js
      via: "legacy inspection before canonical blueprint validation"
    - from: src/cli.js
      to: src/artifact-migration.js
      via: "migrate command default preview"
---

<objective>
建立 closed legacy registry、ordinary-loader migration gate 和默认只读 migration preview。

Purpose: 让 legacy input 只能通过显式、可审阅、value-blind 的迁移入口进入 canonical AgentMo。
Output: registry、preview planner、receipt model、CLI preview 和 hostile/batch tests。
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 closed legacy registry 与 ordinary-loader gate</name>
  <read_first>src/blueprint.js, src/report.js, src/build-state.js, src/control-snapshot.js, src/birth-report.js, src/secret-redaction.js, src/evidence-audit.js, test/blueprint.test.js, test/control-snapshot.test.js, test/birth-report.test.js, test/delivery-report.test.js</read_first>
  <files>src/artifact-registry.js, src/blueprint.js, src/control-snapshot.js, src/birth-report.js, src/secret-redaction.js, src/evidence-audit.js, test/artifact-migration.test.js, test/blueprint.test.js, test/control-snapshot.test.js, test/birth-report.test.js, test/delivery-report.test.js, test/fixtures/migration/legacy-blueprint.json, test/fixtures/migration/legacy-report.json, test/fixtures/migration/legacy-build-state.json, test/fixtures/migration/canonical-blueprint.json, test/fixtures/migration/hostile-secret.json</files>
  <behavior>
    - Registry recognizes only legacy blueprint v0.1, report v0.1 and build-state v1 legacy metadata.
    - Blueprint and applicable build-state loaders throw `AGENTMO_MIGRATION_REQUIRED` before canonical validation.
    - Report legacy family is explicitly N/A for ordinary loading and remains migrate-only.
    - Unknown/conflicting identity and hostile object keys/values fail value-blind.
  </behavior>
  <action>Per D-01/D-02/D-06/D-10/D-11, implement `src/artifact-registry.js` with closed own-property recognizers, stable rule IDs and canonical transforms. Reject old+new key conflicts, unknown versions and multi-family matches. Add migration-specific recursive audit for hostile values and object keys without expanding to all current writers. Wire `loadBlueprint`, `loadBuildState` and build-state subject loaders to inspect legacy identity first and preserve structured `AGENTMO_MIGRATION_REQUIRED`; ensure optional build-state CLI paths do not swallow this code. Record report as migrate-only N/A. Tests must assert source bytes/directories unchanged and zero derived output.</action>
  <verify><automated>node --test test/artifact-migration.test.js test/blueprint.test.js test/control-snapshot.test.js test/birth-report.test.js test/delivery-report.test.js && npm run check && git diff --check</automated></verify>
  <acceptance_criteria>
    - Registry exports exactly three legacy families with stable rule IDs and output basenames.
    - Applicable ordinary loaders return exact code plus bounded `agentmo migrate` guidance.
    - Unknown, conflicting, secret-shaped and raw-content inputs expose no offending value or host-sensitive path.
  </acceptance_criteria>
  <done>Legacy identity detection is closed, actionable and side-effect free at ordinary load boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 deterministic migration preview 与 receipt model</name>
  <read_first>src/cli.js, bin/agentmo.js, src/artifact-registry.js, src/secret-redaction.js, src/evidence-audit.js, test/cli.test.js, test/artifact-migration.test.js</read_first>
  <files>src/artifact-migration.js, src/cli.js, test/artifact-migration.test.js, test/cli.test.js</files>
  <behavior>
    - `agentmo migrate input...` without `--out` performs no filesystem writes.
    - Whole batch is single-read, size-bounded and fully validated before any apply eligibility.
    - Canonical input is an `already_canonical` no-op.
    - Receipt schema is `agentmo.migration-receipt.v1` and excludes raw content, basename and absolute path.
  </behavior>
  <action>Per D-03/D-05/D-09/D-10/D-11, implement `planArtifactMigration` and deterministic serialization/digests in `src/artifact-migration.js`. Parse one or more CLI inputs; default to preview in human/JSON formats. Use stable ordinals and registry-owned output basenames, compute input/output byte digests, rule IDs, plan digest and value-blind warnings. Reject collisions and unsafe size/content before any apply step. Do not implement general artifact admission or current-writer persistability.</action>
  <verify><automated>node --test test/artifact-migration.test.js test/cli.test.js && npm run check && git diff --check</automated></verify>
  <acceptance_criteria>
    - Preview of the same inputs is byte-deterministic and writes no file or directory.
    - Mixed valid/invalid batch returns per-item value-blind status and an overall non-applicable plan.
    - Receipt model contains only identities, versions, digests, rules, ordinals, results and redacted warnings.
  </acceptance_criteria>
  <done>Developers can inspect a complete migration plan without mutating source or destination state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Untrusted JSON → registry | Attacker controls keys, values, versions and family conflicts. |
| Registry → diagnostics | Diagnostics must not echo raw content, basenames or host paths. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01 | Spoofing | legacy identity | high | mitigate | Own-property closed recognizers and conflict rejection. |
| T-03-02 | Information Disclosure | diagnostics/receipt | critical | mitigate | Recursive key/value audit and registry-owned subjects/basenames. |
| T-03-03 | Tampering | batch plan | high | mitigate | Whole-batch validation and deterministic plan digest before apply eligibility. |
</threat_model>

## Artifacts this phase produces
- `LEGACY_ARTIFACT_REGISTRY`, registry family/rule records.
- `AgentMoMigrationRequiredError`, error code `AGENTMO_MIGRATION_REQUIRED`.
- `planArtifactMigration`, `buildMigrationReceipt`, deterministic migration plan/receipt fields.
- CLI command `migrate` preview surface.
- `test/artifact-migration.test.js` and bounded legacy fixtures.

<verification>Run migration/loader/CLI suites, then `npm run check` and `git diff --check`.</verification>
<success_criteria>All supported legacy families have a single explicit preview path; ordinary loading remains fail-closed and side-effect free.</success_criteria>
<output>Create `.planning/phases/01-canonical-artifact-kernel/01-03-SUMMARY.md` when done.</output>
